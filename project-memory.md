## Hard Constraints
- Application must run on web and Android (APK), no iOS support
- Data is now stored in cloud (Aliyun Supabase compatible service) with local synchronization; student data is isolated via RLS
- Spelling dictation mode must accept multiple synonymous answers separated by parentheses, slashes, or equal signs
- Spelling dictation mode must automatically recognize singular and plural forms of terms as valid answers (applies to the last word of multi-word phrases)
- Spelling dictation mode must allow -ise suffix and its derivative -isation suffix to match -ize and -ization respectively
- All question types must mask answer terms (full name, singular/plural variants, aliases) in hints with equal-length underscores (preserving spaces and punctuation); flashcards and Wordle excluded (crossword's English hints also mask answer terms; Chinese hints are pure Chinese and need no masking)
- Crossword: Auto-advance after typing a letter is ENABLED with optimized logic — if a grid cell belongs to only one word, auto-advance in that word's direction (right for horizontal, down for vertical); if shared by two words (cross cell), first follow last input position (same row → horizontal, same column → vertical), else if the cell is exactly one word's first letter then follow that word's direction, else (both first letters, or both non-first letters with no directional cue) stop. Backspace auto-deletes and moves left/up based on last delete position (same row → left, same column → up; neither → stop). Do not skip filled cross cells during input. Navigation also via direction keys (desktop) / grid tapping (mobile).
- Public GitHub repository must exclude teacher IP: *.xlsx (Excel vocabulary source files), app/public/vocab-data.json (generated built-in vocabulary), reset-tool/, teacher-private-key.json, *.apk (Android installation packages), A1/A2 Paper*.pdf (PPT 打印版 PDF), pdf-text/, ppt-text/ (PPT/PDF 提取文本), pdf-libs/ (vendored pypdf 库), unit-tags-backup/ (旧分类文档备份)；这些文件留在本地，由 .gitignore 忽略，其中词库数据可经 app/scripts/generate-vocab.mjs 重新生成
- Web deployment requires local build (npm run build) then manual upload of dist folder; do NOT use CI/CD auto-build (Netlify/Vercel/GitHub Pages) as they would lack vocabulary data
- Dictionary feature must be excluded from check-in detection and mastery level calculation (pure lookup tool, no data writing)
- Teacher-only features (including vocabulary management page and ImportPanel) must be hidden from student version; controlled by cloud-based teacher role (isTeacher) instead of local IS_ADMIN flag
- Backup page is only visible to offline users (those who skipped login); login users (students and teachers) cannot access it
- Android hardware/gesture back button behavior: Navigates to homepage on non-home screens; exits app only on homepage
- Narrow screen (≤720px) hamburger menu auto-collapses when clicking or dragging outside the menu
- Profile page layout: Logout card positioned above password change card; password change card is collapsed by default and expands on click
- Web version back button is displayed in content area top (independent row), only visible on non-home pages, and does not affect title bar layout
- VersionName updated to 1.3.3, versionCode 9
- 学生信息安全（2026-08-25 讨论定稿）：学生端 RLS 完全隔离（只能读写自己的 student_data / quiz_submissions）；姓名权威源 = auth.users.user_metadata.name，student_data.data.name 是冗余（学生每次登录上传自动覆盖同步），quiz_submissions.name 是交卷快照（不追溯）。改名方案（仅 9~20 名学生规模，采用途径 2 老师代改）：在阿里云 Supabase 控制台 Authentication→Users 编辑该用户 Raw User Metadata 的 name（界面不支持则 SQL：update auth.users set raw_user_meta_data = raw_user_meta_data || '{"name":...}'::jsonb where email=...），无需动 student_data（等学生重新登录上传自动同步）；历史答卷姓名不追溯。注册改英文名（2026-08-26 已改 placeholder）：IdentityGate.tsx 注册页姓名 placeholder 已改为「填写英文名+姓氏拼音（便于老师核验）」，label 仍为「姓名」（可选再加 /^[A-Za-z][A-Za-z .'-]*$/ 校验）。邮箱脱敏（2026-08-26 已实施）：maskEmail() 实现在 app/src/lib/shuffle.ts，规则=用户名保留首尾字符、域名完整保留（如 z******n@dtd-edu.cn），已替换 TeacherCheckPanel.tsx / ClassManager.tsx（表格邮箱列+分组预览兜底）/ QuizManager.tsx（成绩邮箱列+答卷标题兜底）；数据库与排序逻辑不变。
- 词库上云后，词条内容（term/释义/paper/category/unit/答案容错等）的增删改统一走云端（教师账号在 VocabManager 在线改 + 发布到 vocab_releases）；本机数据源（unit-mapping.json / answer-aliases.json / *.xlsx）只用于代码/结构改动（改完也需重新发布到云端）。若用户指令超出「代码/结构」范围、可能造成本机数据源与云端词库不一致（两边分叉），暂缓执行并提示用户去云端改，而不是直接在本机改数据源。

## Engineering Conventions
- Term entries with parentheses, slashes, or equal signs are treated as multiple valid answers
- Scholar names are judged by last name;合著者 and institutions require full name or accepted abbreviation
- Normalization applied: ignore case, spaces, hyphens, punctuation, and diacritics for answer comparison; -isation normalized to -ization; -ise normalized to -ize
- Per-item answer aliases (同义/等价写法、单复数放宽、特殊姓氏) live in app/src/lib/answer-aliases.json (termAliases / scholarAliases / surnameOverrides)，由 answers.ts import；改单个词条答案容错直接编辑此 JSON 后 rebuild
- For three co-authors: first and second author last names separated by English comma, second and third separated by &
- Crossword grid uses CSS Grid layout with responsive sizing (adapts to screen width, minimum 16px cell size)
- Crossword on mobile uses hidden input to trigger soft keyboard for character entry
- Git repository is at project root (...\Vocabulary Project), not nested in app folder
- git 未配置全局代理（保持现状，不要改动 git config）；每次 `git push`（及 fetch/pull 连 GitHub）都临时加代理参数 + openssl 后端（schannel 过 HTTP 代理会 SSL 握手失败）：`git -c http.proxy=http://127.0.0.1:7897 -c https.proxy=http://127.0.0.1:7897 -c http.sslBackend=openssl push`
- Navigation bar: On screens ≤720px (mobile portrait), tabs are collapsed into a hamburger menu; on larger screens, tabs remain visible
- Crossword hints are randomly displayed in Chinese or English (uses non-empty language if one is missing) and remain consistent during puzzle session
- Crossword layout: .cw-layout uses single column with crossword grid above hint column, regardless of screen size (removed大屏 ≥860px two-column并排规则)
- Crossword counts as formal practice: records study time, mastery (weight +10/−20, between matching and spelling), check-in question count, and wrong book; settlement happens once when clicking the "结算" (formerly "显示答案") button, judging all clues at once by the student's current fills (empty/partial cells count as wrong)
- Crossword "返回选择"/"重新生成" prompt a confirmation dialog if the puzzle hasn't been settled yet
- Crossword answers use singular forms only; slash/parenthesis-separated aliases are still randomly chosen among alternatives (grid length hints at the answer), but singular/plural variants collapse to the singular form
- Dictionary feature supports Chinese-English mixed search, covering term original text, aliases, Chinese translations, scholar names, aliases, last names, and theoretical schools; scholar entries highlight last names (large bold + accent color)
- Dictionary entries display Paper tags (e.g., Paper 1, Paper 2) instead of "学者" or "术语" labels
- Dictionary uses single-expand mode: only one entry detail is expanded at a time; clicking a new entry auto-collapses the previous one
- Dictionary search is separated into "术语" and "学者" tabs (no "全部" option), defaulting to "术语"
- Scholar entry for bell hooks is formatted with "bell hooks" as the main name and her real name displayed as the full name in other single-author entries
- When importing Excel files with scholar names, the system detects "suspicious" names (those with parenthetical real names or all-lowercase pen names) and prompts the user to specify the surname for spelling dictation judgment
- Teacher backend is a comprehensive admin page with two tabs: "词库导入" (ImportPanel) and "打卡核验"
- Student data is stored in public.student_data table with user_id (Supabase auth uid), email, data (JSONB containing check-in, progress, wrongBook), and updated_at fields
- Local learning data (progress/checkin/wrongBook) is namespaced per user account: login calls setDataScope('user:<id>') to switch the localStorage key namespace; logout switches back to 'guest'. Offline (skipped-login) users use the original guest keys. Vocabulary/contexts/surname-overrides remain global (shared across accounts)
- Check-in celebration "already shown today" flag is per-account: logged-in users use 'socio_vocab_celebrated:user:<id>', offline guests use the original 'socio_vocab_celebrated' key
- Row Level Security (RLS) applied to student_data: students can only read/write their own rows; teachers (in public.teacher_roles table) can read all student rows
- 角色状态随登出一并复位：signOut 中除 setAuthUser(null) 外，必须同时 setIsTeacher(false) 与 setIsDeveloper(false)（曾因缺 setIsDeveloper(false) 导致开发者退出后、刷新前其他账户在 AppBody 残留 isDeveloper=true 仍可进 DevPanel，bug 已修复）
- Teacher roles are stored in public.teacher_roles table (read-only to all, no write access via REST API); teacher accounts are manually added via SQL
- Forgot password handling: Students contact teacher, who manually resets password via Aliyun Supabase Authentication console; students can change password to custom one after logging in with temporary password
- 密码重置方案已定稿（2026-08-27 确认）：维持「控制台手动重置」不变，**暂不引入 Edge Function**。原因：前端只有 anon key 无法调用 admin API（改密码必须 service_role/admin 权限，密钥绝不能进前端）；若走教师后台重置需部署 Edge Function（持 service_role + 函数内按 user_roles 校验 teacher 防滥用），当前全学生约 20 人、重置为低频零星操作，性价比低。待学生规模变大、重置频繁时再评估 Edge Function 方案（备选：supabase.auth.resetPasswordForEmail 需邮件服务，当前未配置故不可用）
- Offline backup is simplified to pure data export/import without device fingerprint verification or identity checks
- Profile page includes login information display, logout function, and password change function (collapsible)
- Login page displays "Forgot password? Contact teacher to reset to temporary password, then change in 'My Account' after login"
- Vocabulary items have a `unit?: string[]` field for topic-level classification (array to allow multi-belonging), following official Cambridge 9699 syllabus structure: Paper 1 = 11 units (社会理论, 社会化和社会控制, 社会认同基础, 阶级认同, 性别认同, 族裔认同, 年龄认同, 研究方法, 研究设计, 方法论取向, 研究议题); Paper 2 = 5 units (家庭功能, 家庭多样性, 社会政策与家庭变迁, 性别平等, 年龄角色); Paper 3 = 7 units (教育功能, 课程影响, 智力与学业成就, 社会阶层与学业成就, 族群与学业成就, 教育与不平等, 性别与学业成就); Paper 4 Globalisation = 4 units (理论视角与核心概念, 全球犯罪与不平等, 全球化移民, 人权发展与全球治理); Paper 4 Media = 2 units (媒体所有权与控制, 媒体再现与效果). Terms may belong to multiple units.
- Unit mapping 唯一 source of truth 是 app/src/lib/unit-mapping.json (keyed by 原始主题 category → type → term → unit[]，直接人工维护；改标签直接编辑此文件后跑 app/scripts/generate-vocab.mjs 重新烘焙)。已删除 app/scripts/build-unit-mapping.mjs 与 paper1_classification.md / paper234_classification.md（旧分类文档，收敛前备份在项目根 unit-tags-backup/）。unit order & paper/sub lookup 在 app/src/lib/unitMapping.ts (UNIT_ORDER, CATEGORY_TO_PAPER, unitsForRaw, attachUnits, unitListFor)。
- Frontend filter is three-tier 考卷→主题→单元: filterByPaperCat(items, paper, cat, unit) in CategoryFilter.tsx, wired into all 7 practice components (MultipleChoice/Spelling/Matching/Flashcards/Wordle/Crossword/ProgressView). Paper 4 only shows 单元 after selecting 主题 (Globalisation/Media).
- The Family scholar entry "Resource unknown" (placeholder/statistics dump, no real name) is intentionally left without a unit.
- Paper 1 的「社会理论」单元（24 条：18 术语 + 6 学者）放在 Paper 1 最前，是从「社会化和社会控制」兼出的（保留原归属，多归属），包含宏大理论视角/流派及其标志概念（Marxism/Functionalism/Interactionism/Structuralism/Conflict theory + 相关核心概念）；学者为 Talcott Parsons/Louis Althusser/Paul Willis/George Herbert Mead/Charles Cooley/Robert Merton。C 类（Social Exchange Theory、Social Learning Theory、Free will、Nature、Nurture）留在「社会化和社会控制」不动。
- Paper 1 原「社会认同」单元（146 条）已拆成 5 个单元：「社会认同基础」(22，含 Achieved/Ascribed status、Discrimination/Prejudice/Stereotype/Intersectionality、Globalisation、Life expectancy/Longevity/Literacy rate 等通用概念)、「阶级认同」(60)、「性别认同」(35)、「族裔认同」(24)、「年龄认同」(8)。多归属：Accent/Dialect/Cultural deprivation(identity 版) 兼阶级+族裔；Heidi Safia Mirza 兼性别+族裔；Manipulation/Verbal appellation 兼社会化+性别；Collectivism/Individualism 归阶级；Slavery/Inter-generational 只归阶级。
- 后现代：Postmodernism/Meta-narrative/Lyotard 兼入「社会理论」+「社会化和社会控制」（保留原单元，形成三单元「兼」）；Lyotard 保留「方法论取向」不移出（Paper 4 的 Lyotard/Baudrillard 不动）；Popular culture 移到「阶级认同」（对应 high culture，是阶级身份元素）；Blurring/Consumerism/"Pick and Mix" 留在「社会认同基础」。「兼」=多单元概念，不限于双单元。
- Supabase RPC 函数约定：函数参数名 = 前端 `supabase.rpc` 传的 JSON 键名（无前缀，PostgREST 按参数名匹配）；函数体引用参数用 `v_` 局部变量或 `$n` 位置参数，避免与目标表列名同名（报 `column reference ... is ambiguous`）；`ON CONFLICT` 用 `ON CONFLICT ON CONSTRAINT <约束名>` 而非裸列名；改参数名须先 `DROP FUNCTION`（`CREATE OR REPLACE` 不能改参数名，报 42P13），DROP 后需重新 GRANT。
- 发布收尾（2026-09-05 定，**2026-09-13 修订为「一次构建」**）：**默认一条命令 `npm run ship`**（= `npm run release` + `npx wrangler deploy`）。完整顺序：① `git commit + push` GitHub（提交信息中文经 PowerShell 传参会 GBK/UTF-8 双重乱码，务必用纯 ASCII/英文 message）；② **先手动改 `app/android/app/build.gradle`：`versionCode` +1、`versionName` 递增**（⚠ 漏过两次，务必在打包前改）；③ `$env:CODEBUDDY_SAFE_DELETE_ENABLED='0'` 后 `npm run ship` → build（**全程只构建一次**）→ `cap sync` → `gradlew assembleRelease` → lark-cli 自动发飞书 APK → **再部署同一份 dist 到 Cloudflare**。**关键约定：部署必须在 `npm run release` 之后，且之后不得再 build** —— `npx wrangler deploy` 只上传 `app/dist`（`wrangler.toml` 的 `assets.directory`），**不做任何构建**；若"先部署再跑 release"或"跑两次 build"，`vite.config.ts` 里的 `BUILD_VERSION = String(Date.now())` 会产生两个不同版本号，于是「APK 内嵌 version.json ≠ 线上 version.json」（2026-09-12 实测踩到：两个号相差 6 分钟、内容其实相同）。分步走时的顺序固定为：commit → 改版本号 → `npm run release` → `npx wrangler deploy`。每轮改动用户确认后按此收尾。
- **打包必读：OneDrive 占位文件会让 Gradle 打包失败（2026-09-11 实测，会复发）**：本项目位于 OneDrive 同步目录内，OneDrive 会把 `app/node_modules/**`、`app/android/**/build/**` 下的文件转成**云占位文件（reparse point，属性含 PINNED|REPARSE_POINT）**，Java/Gradle 做输入快照时报 `Cannot snapshot <文件>: not a regular file`（另一种表现是 `Unable to delete directory`）。**关键：暂停 OneDrive 不会把已占位的文件还原**，必须让 Gradle 重新生成那些目录。稳妥顺序：① 暂停 OneDrive 同步 → ② `cd app/android` 后 `.\gradlew.bat --stop` → ③ `.\gradlew.bat :app:clean`（个别目录仍失败时，可把被占位的 `build` 改名成 `build.bak-*` 让 Gradle 重建；注意 `android/app/build` 可能因 OneDrive 占句柄而改名被拒，此时用 `:app:clean`）→ ④ `$env:CODEBUDDY_SAFE_DELETE_ENABLED='0'` 后 `npm run release`。根治办法：把项目移出 OneDrive，或排除 `app/node_modules`、`app/android` 的同步。（改名残留的 `build.bak-*` 在 .gitignore 覆盖范围内、不影响仓库，但 IDE safe delete 会因超过 500 文件阈值拒绝删除，需手动清理。）**2026-09-15 实测补充（失败点其实不止 gradle）**：这次 build 一上来就挂在 `[plugin vite:prepare-out-dir]` —— IDE 安全删除层把删除改成"移入回收站"，而 OneDrive 路径下回收站操作直接报 `trash` operation aborted（`app/dist/skill`）。**解法仍是第③步的 `$env:CODEBUDDY_SAFE_DELETE_ENABLED='0'`**：设上之后 `npm run ship` 一条命令跑通（build → cap sync → gradlew assembleRelease BUILD SUCCESSFUL → 飞书发 APK → wrangler deploy）。**并且不必预先 clean**：本次 `gradlew :app:clean` 与把 `android/app/build` 改名都被安全删除层拒绝（访问被拒），但 ship 照样成功（Gradle 增量处理，78 executed / 126 up-to-date）。另：改版本号后要照惯例补一个 `chore: bump android versionCode N / versionName X.Y.Z` 提交推送，否则仓库与实际发布不符。
- 本机 psql 可直接跑云端迁移/查询（2026-09-05 用户提醒）：连接串 `postgresql://postgres@spb-olltk79n0rjrawe5.supabase.opentrust.net:5432/postgres`（凭 `%USERPROFILE%\.pgpass`）；套路 = `$env:PGPASSFILE="$env:USERPROFILE\.pgpass"; psql "<conn>" -w -v ON_ERROR_STOP=1 -f 文件.sql`。批量导 JSON 可先由脚本生成幂等 INSERT SQL 再 psql -f（题库导入即此方式，脚本 app/scripts/export-bank-import.mjs）。⚠️ **2026-09-15 又踩一次**：本机 psql 装在 `C:\Program Files\PostgreSQL\17\bin\psql.exe`（**已在 PATH**，不是路径问题）——**真正原因是漏了 `PGPASSFILE` 与 `-w`，psql 停在密码提示上，终端表现为"命令无响应"** ✗。教训：调 psql 前先照抄上面这行套路；只读自查也别省 `-w`。
- **自建反向代理（如 Worker 的 `/sb/*`）两条铁律（2026-09-13 实测踩到，详见 KNOWN_ISSUES 第 4 条）**：① **CORS 必须回显** `Origin` 与 `Access-Control-Request-Headers`，不要写死允许头——`supabase-js` 等客户端库会随版本带新头（如 `x-supabase-api-version`），写死即预检失败，而客户端只看到一个无信息量的 `Failed to fetch`；⚠️ **curl 不走预检，所以"curl 测通"不能证明浏览器/WebView 可用**，必须用真实浏览器或设备验证。② **带鉴权的代理响应必须显式禁缓存**（`Cache-Control: no-store, private` + `CDN-Cache-Control: no-store`）——Cloudflare 边缘缓存**按 URL 分键、不看 `Authorization`**，一旦缓存了 RLS 作用域内的查询结果就会**跨用户串数据**。
- **会话恢复的时序约定（2026-09-13 踩坑修复）**：主站 `Shell()` 的判定是「未登录 → 渲染 `IdentityGate`」，为此 `store.tsx` 引入了 `authReady`（未判定完成时显示启动加载态）。**`authReady` 必须在 `establishAuth()` await 完成之后才置位**：`establishAuth` 内部先 `await pullCloudData()` 合并云端数据、之后才 `setAuthUser()`，若提前置位就会出现 `authReady=true` 而 `authUser=null` 的窗口 → Shell 渲染登录页（表现为"从 skill 子站回主站时闪出/停留在登录界面"，在需要走同源代理回退的设备上尤其明显，因为云端合并更慢）。
- 移动端系统栏与安全区（2026-09-11 定稿，详见项目根 `移动端系统栏与安全区方案.md`）：应用整体 edge-to-edge，**Capacitor 内置 SystemBars 的自动 insets 处理必须关闭**（`app/capacitor.config.ts` → `plugins.SystemBars.insetsHandling='disable'`）；顶部避让由 `MainActivity` 按 URL 判定（含 `/skill/`=知识库子站 → 顶栏自绘深蓝、状态栏图标浅色；其他=主站 → WebView 避让状态栏、状态栏区域由 windowBackground 填 surface 色），底部一律不消费（内容铺到屏幕底边、手势条悬浮）；只有交互控件（问答输入框、抽屉底部按钮、移动导航列表、版本横幅）留安全区。**改任何移动端顶部/底部布局前先读这份文档**。
- 知识库（教材 AI 问答）数据分层（2026-09-11，详见项目根 `知识库AI问答方案.md`）：`skill_content`（蒸馏教材章 + 真题评分视角）=结构/导航层；`skill_pages`（教材 OCR 原文按页，1015 页）=细节层（按需取页）；`skill_page_index`（页级关键词 + Unit→页码 + 章→页码 + 中英术语桥）=常驻索引层；`skill_scaffolds`（按章答题脚手架，17 章）=教学口径层。四表均 RLS 仅登录可读。教材换版流程：`skill-pdf-pages.py` 重新生成 → `skill-pages-import.mjs` 生成 SQL → psql 导入；改动检索后跑 `skill-coverage-check.mjs` 体检。
- **实时多人在线（课堂活动）能力边界（2026-09-11 实测）**：详见项目根 `实时多人在线功能规划.md`。要点：当前托管实例的 Realtime **只有 Presence 与 Postgres Changes 可用，Broadcast 坏**（一发广播就 `socket closed: 1011`，`realtime.messages` 从未写入过一行，补 RLS 策略后仍失败）；**Postgres Changes 的开关不在控制台**（Database→Replication 显示 "coming soon"），要用 SQL `alter publication supabase_realtime add table public.xxx` 手动加表（`supabase_realtime` 发布默认为空，所以"订阅了却没反应"是正常的）；架构结论 = **把「实时」当「数据库变更的通知」**（表 + RLS 控权 + RPC 判定 + 信号后按需拉取 + 断线拉快照），而不是消息总线。

- **本地构建注意（2026-09-13 实测，会复发）**：`npx wrangler dev` 运行中（或 OneDrive 占句柄）时跑 `npm run build`，会在清空 `dist/` 时报 `[safe-delete] 操作失败：... trash operation: Unknown { description: "Some operations were aborted" }`，构建中断、`dist` 可能半残（页面/接口随之报错）。**处置：先停掉 8787 上的 wrangler dev，再用 `$env:CODEBUDDY_SAFE_DELETE_ENABLED='0'` 构建**，然后重启 dev。与发布流程中"release 前同样要关 safe-delete"属同一条约定。

- **Worker 路由命名空间约定（2026-09-13 定，主站 AI 基础设施起点）**：`app/worker.ts` 按前缀分域，**新增能力必须归入既有前缀、不得另起**：① `/skill-api/*` = **教材知识站（子站，学生向）**：`ask`（问答，多级模型链 + `ai_gate` 门禁、teacher/developer 豁免）、`terms`（中文问句→英文检索词，走 Workers AI 免费档）；② **`/app-api/*` = 主站（**登录即可**，2026-09-13 启用；2026-09-15 由「强制 teacher/developer」改为与子站同口径：必须登录 + 沿用 `ai_gate` 门禁，教师/开发者始终放行 —— 因为主站后续面向学生的 AI 功能（作文批改等）也要接入同一批模型）**：端点 `ai/transcribe`（视觉转写，OCR 阅卷）与 `ai/complete`（文本补全，三档路由），与子站共用同一批模型，但档位策略、护栏、文案各自独立；③ `/sb/*` = Supabase 同源代理（见 KNOWN_ISSUES 第 4 条）；④ `/wb/*` = World Bank 代理。**模型 key 位置**：线上经 `wrangler secret put` 注入（`wrangler.toml` 的 `[vars]` 只有 Supabase 两项），**本地开发/实验读 `app/.dev.vars`**（`.gitignore` 已覆盖 `.dev.vars*`；模板 `app/.dev.vars.example` 可提交）。**魔搭额度不分池**：子站问答与主站阅卷共用同一个 ModelScope 账号的每日 ~250 魔粒（2026-09-13 决策，理由：子站=学生复习期、主站阅卷=教师批卷期，时间错开）。**落地时机**：主站 AI 基础层（抽 `worker/ai/*` 共享模块：provider 泛化到视觉消息、模型目录、鉴权分级、额度记账、每功能开关、审计头）+ `worker/routes/*` 分文件，与 OCR 入口**一次性搭建**，不提前重构。

- **ManageBac 分数同步：短码约定与匹配链路（2026-09-15 定，教师确认）**：目标 = 把「试卷成绩」（`grouper_runs`）一键写进 ManageBac 成绩册，替代现有「复制成绩（邮箱⇥分数）→ 油猴脚本粘贴」流程（该流程仍在，零凭证可继续用）。**短码**（我方生成、创建记录时固定不变）：`[A1-0712]` / `[A2-0712]` = **年级位 + 创建日月日**；同日冲突加 `b/c`；年级位沿用 `PaperResults.tsx` 的 `inferClassId` 规则（AS/A1 → A1；A2/P3/P4 → A2），创建时可手改，无法唯一推断时教师选。**为什么不含 paper 号**（教师 2026-09-15 判断）：短期不会同一天布置不同 paper 的作业，混搭总复习也不会分两个 task 建；而年级是真实维度。**匹配层级**：① 已存绑定的 `mb_task_id`（URL 数字 id，改名不变）② 在**该班级** task 列表内按 `[短码]` 精确匹配（班级范围内匹配 ⇒ 同作业发两班、两 task 同名也不歧义）③ 无兜底 —— 匹配不到即**报错停下、绝不猜、不写**。**明确不做**「列出 task 让教师点选 + 顺手补短码改名」的反向兜底（教师判定该情形几乎不存在）；真正的安全网 = **写前差异预览**（显示即将写入的 task 名与逐行分数，确认后才写）。**学生定位**：ManageBac 成绩册行内**无稳定学生 id** —— `input#grade-score-<n>` 是成绩记录 id（随 task 变），行内 `<a>` 路径里的数字与「学生个人信息主页 URL」对不上（教师编油猴脚本时已核对）；故沿用**名单 xlsx 桥接**：导入 ManageBac 导出的班级名单（姓名 + 邮箱）→ 建 `邮箱 → ManageBac 显示名` 映射 → 与站内邮箱（学生须用**学校邮箱**注册）对上 → 按显示名定位行；**不强制每学期重导**，但发现有人匹配不到时提示重导。**班级绑定**：`classes` 增 `mb_class_url`（教师贴 `https://<school>.managebac.cn/teacher/classes/<id>/gradebook/core_tasks` 形态 URL）+ `mb_class_id`（前端当场解析校验）。**界面事实（教师实测 2026-09-15）**：全部任务视图**默认列 current term、有下拉可切 term、不分页、表格横向滑动** ⇒ 抓取只需一次 DOM 解析（横滑是真 overflow 还是虚拟渲染待实测）。**前端 xlsx 能力现成**：`xlsx@0.18.5` + `app/src/lib/excelImport.ts`（`XLSX.read` / `sheet_to_json`），名单导入无需新依赖。**迁移草稿**：`db-migration-mb-sync.sql`（`grouper_runs.mb_short_code` / `classes.mb_class_url+mb_class_id` / `mb_rosters` / `mb_task_links`，均仅 teacher/developer 可读写）；**方案文档**：`分数同步到ManageBac方案.md`。⚠️ **硬约束（教师 2026-09-15 两次强调）**：现有「复制成绩（邮箱⇥分数）→ 油猴脚本在 ManageBac 页面粘贴」的手动兜底流程**必须原样保留** —— `PaperResults.tsx` 与 `QuizManager.tsx` 的「复制成绩（ManageBac）」按钮、以及它输出的 `邮箱<Tab>分数` 格式**不得移除、不得改格式**（该流程零凭证、零服务器，是浏览器自动化不可用时的唯一退路）；新同步功能只能是**新增**，不能替换它。**2026-09-15 已执行两批迁移**（本机 psql 直连）：`grouper_runs.mb_short_code`、`quizzes.mb_short_code`、`classes.mb_class_url/mb_class_id`、`student_data.mb_exempt`、`mb_rosters`、`mb_task_links`（**双源**：`run_id` 与 `quiz_id` 二选一 + CHECK）。**随堂测验/作业同款接入**（教师要求）：测验成绩页也有同一张 ManageBac 区块；短码年级位由 `quizzes.papers` 推（P1/P2 → A1、P3/P4 → A2），这份卷没选 paper 时由界面下拉选。**「不登分」例外学生**：AS 班有一名学生不进 ManageBac 名单（业余自学、不需要登分 ⇒ 教师明确说这是正常现象，不是名单导出缺人）⇒ 在「班级管理 → 学生分班」取消勾选「ManageBac 登分」，此后不计入名单缺口、同步时跳过她。**前端已实现（2026-09-15）**：短码自动/一键生成 + 带方括号复制、班级绑定 URL 当场解析校验、名单 xlsx 导入（自动认姓名/邮箱列 + 与站内邮箱比对提示缺口）；**尚未实现**：第 4 步「抓班级 task 列表 → 按短码精确匹配 → 绑定 → 差异预览 → 写入 → 回读校验」（需 Browser Run）。⚠️ **papers 格式坑（2026-09-15 教师实测报障）**：`quizzes.papers` 存的是 **`'Paper 3'`（不是 `'P3'`）** —— 见 QuizManager 的 `papers: draft.paper === 'all' ? [] : [draft.paper]`，而 draft.paper 取值就是 `'Paper N'`；`classes.papers` 目前**存的是空数组**。因此短码年级位解析必须**取字符串里的数字**（兼容 `3` / `P3` / `Paper 3`），早期实现只 `replace(/^P/,'')` → 把 `'Paper 3'` 变成 `'APER 3'` → `Number()` NaN → 判定推不出 → 兜底成 A1 ✗（症状：A2 的作业生成出 `A1-0915`）。已修 `mbSync.gradeFromPapers`；并新增「修正」按钮（`ensureRunShortCode` / `ensureQuizShortCode` 的 `force` 参数）用于重算已生成的错码 —— **重算后必须同步改 ManageBac 的 task 名**（匹配只看名字里那个短码）。

- **ManageBac 写什么值：两侧口径不同（2026-09-16 教师定）**：**试卷成绩（`grouper_runs`）写折百分**（按本卷分数线算出的百分制：`PaperResults` 的 `conv`，即 `bandLinear(raw, full_raw, rows)` 再取整）；**随堂测验 / 作业（`quizzes`）写原始分**（最终分 `grading.final_score ?? score`，含迟交罚分与订正加分）。两侧预览表都有「写」勾选列与「写入值」列，写明写的是什么。**没设分数线的卷子算不出折百分 ⇒ 那些行不给勾选、也不会被提交**（若放任勾选，值会是空串而被 Worker 的 `filter(u => u.row && u.score)` 静默丢掉）。另：两页各自独立一套「读取差异 → 勾选 → 写入 → 回读确认」，共用 `cloud.ts` 的 `fetchMbMarks` / `writeMbMarks`。
- **ManageBac 成绩册写入必须用「真实输入」，且回读要轮询（2026-09-16 两次失败后定位）**：① **现象**：`POST /app-api/mb/write` 返回 200、**当场回读也确实读到新值**，但 ManageBac 上分数没变 —— 是教师用**油猴脚本**（仍保留的退路）写进的 20/18/17 才对照出「程序根本没写进去」。② **根因**：成绩册是**前端异步渲染**（读 task 列表都得轮询等它出来），分数框是框架的**受控组件**；直接 `el.value = x` 只改了 DOM 显示值（所以当场回读能看到新值、**极易误判成功**），框架内部状态没变，提交给服务端的仍是旧值 ⇒ 「看着写上了、实际没有」。③ **正解**：走 CDP **真实输入** —— 页面内只做**定位**（`locateExpr` 返回目标 input 的 id，必要时补一个临时 id），再 `Runtime.evaluate` 聚焦 + `select()` 全选，然后 `cdp.send('Input.insertText', { text })` 插入，最后补派发 `change` + `blur`。它触发浏览器原生 `input` 事件，框架 `onChange` 正是监听它，等价于人工选中后打字。④ **回读必须轮询**（1.5s × 最多 8 轮，全部命中期望值就提前结束）：原来固定 `sleep(4000)` 就下结论，会把"还在保存"报成失败。⑤ **UI 口径要分清三种结果**：`ok:false`（没能写入：定位不到行/没有分数框）、`ok && !saved`（已提交但未回读到）、已确认 —— 混成一句"未确认落库"教师会读成失败（这次就是这么误判的）；另外**定位失败的行不要用空的 `actual` 覆盖表格显示**，否则会把原有分数抹成空白（已修）。
- **模型名必须由后端单一提供，前端不得写死（2026-09-17 教师发现）**：教师注意到子站问答「快速档」尾缀仍显示 `Qwen3-235B` —— 因为 `skill-site/src/AskView.tsx` 里**硬编码**了两份模型名：`TIERS` 的提示文案与 `MODEL_NAME`（响应头代号 → 展示名）。后端换了模型，这两处不会跟着变。**根治办法（已落地）**：① `worker.ts` 新增 `GET /skill-api/models` 返回 `AI_TIER_CATALOG`（直接引用 `MS_MAIN`/`MS_THINK`/`OR_MODEL`/`CHAT_MODEL` 常量，**模型名的唯一真源**，纯只读不需登录、回 CORS）；② AI 响应头新增 `X-AI-Model-Id`（**真实模型 id**）并加进 `Access-Control-Expose-Headers`，`ask.ts` 把它带回前端；③ 前端档位改成**下拉菜单**、选项文本为「模型短名（档位）」如 `Qwen3.5-397B-A17B（深度）`（模型名很长，横排会把界面挤乱；`shortModel()` 去掉 `Qwen/` 等供应商前缀），尾缀优先显示真实模型 id、旧部署无该头时退化为代号；④ 前端保留 `TIER_FALLBACK` 内置清单，只在拉不到目录时兜底。**教训**：凡「后端会变的值」都不要在前端写第二份 —— 这类不一致不会报错，只会安静地显示错信息。
- **换魔搭模型必须同时算「魔粒预算」（2026-09-17 教师提醒）**：每日免费额度约 **250 魔粒**，按「基础算力 + 模型规模」扣减 —— **主流档 1 魔粒/次、旗舰档 2 魔粒/次**（`worker.ts` 旧注释的口径）⇒ **换成旗舰档会让每日可用次数直接减半**。教训：2026-09-17 为修「模型下架」临时挑 id 时，把思考档换成了第三方旗舰 `ZhipuAI/GLM-5.2`，没意识到档位问题时成本已翻倍；现已改回同门 `Qwen/Qwen3.5-397B-A17B`。**选型口径：高频档（子站问答主力 `MS_MAIN`）优先选 1 魔粒档的小号同门模型；低频档（思考 `MS_THINK`、旗舰 `MS_V4`）才允许用大号；`worker/ai/text.ts` 的 `MS_MODEL` 只是「免费档都失败后的兜底」，平时不烧魔粒。** 注意魔搭**没有公开的费率表**（官方文档页是前端渲染的空壳，第三方教程还停留在「每天 2000 次」的旧口径，且提到不同模型次数不同 —— Qwen3-Coder 仅 500 次），准确档位只能看账号控制台的魔粒说明/余额流水。
- **线上 secret 会丢，症状是「Worker 缺少 BROWSER_ACCOUNT_ID / BROWSER_API_TOKEN」（2026-09-18 修复）**：ManageBac 同步走 Browser Rendering 的 **REST API**（`POST /accounts/<id>/browser-rendering/devtools/browser` + CDP over WebSocket；**binding 路径在本账号会 500**，见 `worker/mbApi.ts` 顶部注释），因此**必须有两个 secret**：`BROWSER_ACCOUNT_ID` + `BROWSER_API_TOKEN`（旧名 `CF_ACCOUNT_ID`/`CF_API_TOKEN` 也认，但 `CF_` 前缀在线上似乎不被注入，**务必用 `BROWSER_*`**）。2026-09-18 教师报「同步管线又断了」，根因就是这两个 secret 在线上丢失（本地 `.dev.vars` 里一直有）。**修复**：`wrangler secret put` 推上去即可，**立即生效、无需重新部署**。**验证 token 真的可用**（可能过期或权限不足）：直接 `POST https://api.cloudflare.com/client/v4/accounts/<id>/browser-rendering/devtools/browser?keep_alive=120000`，成功返回 **HTTP 200 + `{"sessionId":"...","webSocketDebuggerUrl":"wss://..."}`** —— 注意这两个是**顶层字段、没有 `result` 包装**（按 `result.sessionId` 取值会取到 null）；本机测试要**走代理**（`--proxy http://127.0.0.1:7897`），用完 `DELETE` 同路径 + `/<sessionId>` 清理。token 需具备 `Browser Rendering - Edit` 权限。
- **视觉通道选型的三个坑（2026-09-18 实测后定案，比"记 id"更重要）**：① **不要按名字找视觉模型** —— 上一轮只挑名字带 `VL` 的（`InternVL3_5-241B` / `ERNIE-4.5-VL-28B`），两个都「HTTP 200 空响应」等于没后端；而 `Qwen/Qwen3.5-*`、`Qwen/Qwen3.8-*` **本身就是原生多模态**（名字不带 VL 却能识图）。实测（`app/_ocrlab_out/vision-quality-probe.mjs`，手写图 15 个关键词）：`Qwen3.5-122B-A10B` 15/15、0.99s；`Qwen3.8-27B` 15/15、1.5s。② **CF Workers AI 的 `mistral-small-3.1-24b` 能出结果但手写识别很差**（教师实测：blackboard→textbook、IQ→2a）——它只能当"外部通道全挂时保命"的最后一档，**不能当主力**（曾因误以为魔搭视觉全灭而把它排第一，导致这个质量问题）。③ **OpenRouter 免费档有雷，不能只看 pricing=0**：`qwen/qwen3.8-27b:free`、`google/gemma-4-31b-it:free`、`google/gemma-4-26b-a4b-it:free` 调用一律 **429**；`thinkingmachines/inkling:free` 返回 **403「only available on agentic harnesses」**（不允许 API 调用）。实测能用的免费视觉是 `inclusionai/ling-3.0-flash-vl:free` 与 `dots-studio/dots-3-note-preview:free`（均 15/15、约 6s）。**现行链路**：① OpenRouter `ling-3.0-flash-vl:free`（免费、不耗魔粒，故第一）→ ② 魔搭 `Qwen/Qwen3.5-122B-A10B`（最快 0.99s，但**耗魔粒，全账号 ~250 魔粒/日不分池**，故排在免费通道之后）→ ③ CF Workers AI（免费自有通道，质量差，末档）。**换模型前一律先用 ms-models.mjs（列表）+ ms-vision-check.mjs / vision-quality-probe.mjs（真实图片）实测，不要照抄记忆里的 id 或按名字推断。**
- **魔搭模型 id 会过期，且失败信息必须完整透出（2026-09-17）**：教师报「OCR 识别报错，看着像 rate limit」——**不是限流**。真实原因：魔搭下架了整个 `Qwen/Qwen3-*` 系列，OCR 的 `Qwen/Qwen3-VL-235B-A22B-Instruct` / `Qwen/Qwen3-VL-8B-Instruct` 与**子站问答的文本主力 `Qwen/Qwen3-235B-A22B`** 全部返回 `400 Model id : ... , has no provider supported`（账号 `GET /v1/models` 里已无这些 id，换代成了 Qwen3.5）。**影响面不只 OCR**：子站 `/skill-api/ask`、主站 `/app-api/ai/complete` 同时全挂。**鉴别方法**（这次用的）：假 key → 401（鉴权机制正常）；不存在的模型 → `Invalid model id`（与我们的报错不同 ⇒ 模型 id 本身合法）；我们的 key + 旧 id → `no provider supported` ⇒ **key 有效、账号正常、只是该模型没有 provider**。**已换为实测可调用的 id**：视觉 `OpenGVLab/InternVL3_5-241B-A28B`（降级 `PaddlePaddle/ERNIE-4.5-VL-28B-A3B-PT`）、文本 `Qwen/Qwen3.5-122B-A10B`（主力）、`ZhipuAI/GLM-5.2`（思考档）、`deepseek-ai/DeepSeek-V4.1-Flash`（旗舰档）。**免费池本身也会抖动**：同一批测试里出现「HTTP 200 但 `choices` 为 null」的空响应（`GLM-5.2` 先后两次结果不同）⇒ 多级降级链必须保留。**工具**：新增 `app/scripts/ms-models.mjs`（从源码正则提取 `MS_*` id → 列账号可见模型 → 逐个 1-token 探测；`--list` 不耗额度）—— 以后再遇到 AI 全线报错，先跑它。**补充**：`app/scripts/ms-vision-check.mjs` 自带极小 PNG 编码器（只用 node 内置 zlib，不依赖图片素材），画三色块图问模型「几个矩形、什么颜色」，专门验证**图片通路**（因为纯文本探测时视觉模型会回「200 但内容为空」，分不清是抖动还是模型不收纯文本）。**实测结论**：魔搭账号里仅有的两个可见视觉模型 `OpenGVLab/InternVL3_5-241B-A28B` 与 `PaddlePaddle/ERNIE-4.5-VL-28B-A3B-PT` **连图片也拿不到响应** —— 用 240x120 自造图复测，两者都返回 `choices:null` 且 **`prompt_tokens:0`**，说明**请求根本没被模型处理**（不是免费池抖动，而是这两个 id 在 API-Inference 上没有后端，只能「在线体验」用）；好处是**它们不计魔粒**，所以探测它们几乎零成本。**注意 `SupportApiInference` 字段不可作判据**：连能正常调用的 `Qwen/Qwen3.5-122B-A10B` 也标 `false`。⇒ 故在 `worker/ai/vision.ts` 加了 **`@cf/mistralai/mistral-small-3.1-24b-instruct`（Cloudflare Workers AI，完全不依赖魔搭、不耗魔粒）并把它提到第一档**，魔搭两档降为后备（恢复后自动接回）。Worker AI 同步返回字段是 `response`（不是 `choices`），且传图形制不统一（`image_url`+data URL 与 `image`+字节数组两种都试）。**新增自检能力**：`POST /app-api/ai/vision-check` 用 `worker/ai/probeImage.ts` 里的内置小图（240x120 三色块，832 字节 base64）逐个通道问「几个矩形、什么颜色」，OCR 面板上有「检测识别通道」按钮 —— 通道再挂时一眼定位，不必用答卷去试。**查魔粒档位的方法（教师 2026-09-17 提供）**：在 `modelscope.cn/models?filter=inference_type&page=1&tabKey=task` 点进具体模型页 → 看**代码范例**里显示的预计扣减（该模块是前端渲染，WebFetch 抓不到）。已确认 `Qwen/Qwen3.5-122B-A10B` = **1 魔粒/次**，可作高频主力。**另一个独立教训**：前端 `OcrMarkPanel` 只显示 `p.error.slice(0, 40)`，把「模型已下架」截成了看不懂的半截话，教师因此误判成 rate limit 并白查；现已改为完整错误上浮到页面顶部（`whiteSpace: pre-wrap` 可换行），Worker 侧 `detail` 从 300 放宽到 800，并新增 `describeFailure()` 把魔搭的 `{"error":{"message":...}}` 解析成人话（`has no provider supported` 会附带「该模型已下架，需换 id」的提示）。
- **OCR 面板的两个坑（2026-09-15 教师实测后修复）**：① **词库匹配绝不能在渲染里现算** —— `OcrMarkPanel` 原在渲染体内对每页调 `matchTranscript(全文, table)`，`totals` 里又算一遍，于是「改备注每敲一个字」「鼠标划过标记（`onMouseMove` → `setTip` 高频 setState）」「识别完一页」等**任何重渲染都会把所有页的全文重跑一遍容错匹配** → 表现为"载入慢 / 改备注卡一会儿"（**与记录条数无关**：排查时表里只有 2 条，是纯计算问题）。修法：`Page.hits` 随文本算一次（`transcribe` 成功时、`loadRecordIntoPanel` 载入时），渲染与 `totals` 只读缓存；`useEffect([table])` 仅在词库变化时补算。② **图片解码失败必须报错，不能静默回退** —— `downscale` 原来 `img.onerror = () => resolve(dataUrl)`，把 Chrome/Edge **根本画不出**的 iPhone **HEIC/HEIF** 原图一路带回 `<img>` → 界面只是一块空白、毫无提示；现改为返回 `null`，由 `addFiles` 提示教师把相机格式改为「兼容性最高」(JPEG) 或先转 JPEG。③ 顺带：`ocr_pages` 列表查询**不要 select text** —— 已加 `text_len`（触发器维护，迁移 `db-migration-ocr-textlen.sql`），列表只取元信息，正文按 id 单取（`getOcrRecordText`）。④ **全屏覆盖层要用 `createPortal` 挂到 `document.body`** —— OCR 的「查看原图」覆盖层原在面板内部渲染（`position: fixed` + `z-index: 70`），被教师反馈"图片显示不出"：CSS 与 `index.html` 都干净、无 CSP、祖先也无 `transform`，但仍改挂 `body` 并把 z-index 提到 120（与全局模态同档）—— **别让全屏层受祖先层叠/裁剪上下文影响**。同时在覆盖层条栏常显「尺寸 · 数据量」、失败时显示 `src` 开头，**让 UI 自己报出是数据问题还是显示问题**（教师屏幕看不到时，这行字就是唯一证据）。⑤ 教师口径澄清：图片**只在缩略图与覆盖层**里出现（结果区自 2026-09-15 起"文本独占整宽"，不再并排）。
- **横向滚动表格的冻结列投影（2026-09-15）**：`index.css` 的 `.check-table:first-child::after` 默认 `opacity:0`，**只有真正横滑时**（`.is-scrolled`）才显示 —— 因为 `scroll` 事件**不冒泡**，逐表挂监听要改 9 处，故改为 `lib/tableFreeze.ts` 在 `document` 上用**捕获阶段**监听一次、给命中的 `.check-table` 打/摘类（`App.tsx` 挂载时安装）。新表格只要用 `.check-table` 就自动享受。

- **答案判定口径：aliases 与 surnameOverrides 的真实关系（2026-09-12 查清，勿再误判）**：① **术语侧**：词条内嵌 `item.aliases`（教师后台「额外可接受答案」编辑、随词库发布上云）优先，缺失时才回退静态 `answer-aliases.json` 的 `termAliases`（`answers.ts` 的 `termAliasList`）。② **学者侧（2026-09-12 起已改为「叠加」生效）**：`getAcceptableKeys()` 的学者分支 = **自动规则（基础）+ `item.aliases`（叠加追加）**。基础链仍是 if/else-if 互斥：静态 `scholarAliases` → `surnameOverrides` → `et al.`→第一作者姓氏+et al / `&`→各作者姓氏 / 单人姓名→完整姓名+末尾姓氏 / 机构→只认完整原文；随后**再追加** `item.aliases`（教师后台「额外可接受答案」手填、随发布上云），**不取代自动规则**（`quiz.ts` 的 `isAnswerCorrect` 走同一路径）。选「叠加」而非「接管」的理由：① 零回归——只增不减，教师编辑不会静默丢掉默认判定；② 云端已有的 42 条旧别名在叠加语义下无害（见④）。**固有限制：叠加语义下教师「删掉」静态表已有的写法不会让其失效**（删掉的只是叠加项）；要纠正自动规则本身（如非常规笔名不该认最后一个词）仍走 `surnameOverrides`；若将来需要「只按列出的写法判定」，需加 per-item 开关（尚未做）。配套（`answers.ts`）：`collectMaskForms` 脱敏与 `getSearchableForms` 检索已同步纳入 `item.aliases`，否则教师新补的写法会在释义提示里把答案露出来、或词典搜不到；`isSuspiciousScholarName` 未改（只在导入 xlsx 时调用、那时拿不到云端 aliases，且它决定的是「是否指认姓氏」）。另：**学者的「完整原文」永远算对**——学者分支在自动规则之前统一 `push(item.term)`（2026-09-12 修）；此前静态别名表只收简称（如 `Michael (Dunlop) Young` → `["Michael Young","Young"]`），导致"照着词库抄全名反而判错"。术语侧本来就把 `term` 包在 `termAliasList` 里，无此问题。③ `surnameOverrides`（合并优先级 内置 < 云端发布 < 本机）是 2026-08-21 (`6c95f26`) 引入的**过渡机制**，其两条实际用例已在 2026-08-23「答案容错收敛到 answer-aliases.json」(`bb62eb6`) 时**改写进静态表**（`Michael (Dunlop) Young` → `["Michael Young","Young"]`；`(Gloria Jean Watkins) bell hooks` → 进该 JSON 的 `surnameOverrides`）。故「本机/云端都没有覆盖条目」是**正常状态**，不是 bug：`vocab_releases.surname_overrides` 为 `{}` 与之自洽（v93 的 data/unit_order 哈希与 v92 完全相同，属空壳版本、无副作用）。④ 注意：云端 238 位学者中**已有 42 位带 `item.aliases`**（形如 `Vauclair et al.`、`Baumberg Bell & Gaffney`），原先因判分不读而是**僵尸数据**，2026-09-12 起随②的「叠加」语义生效。落库时间可查：v1 有 7 条（=静态表那 7 条）、v6 有 9 条、**v27（2026-08-26 05:31）由 9 条跳到 41 条、v28 到 42 条**；仓库里找不到对应生成代码（`attachAliases` 自 `b0d29e4` 起就只写静态表那 7 条，git 全历史亦无痕迹），`note`/`published_by` 均为空 → 大概率是当时的一次性脚本或临时工具留下。逐条比对：**绝大多数与实时自动规则等价**（合著 `Baumberg Bell & Gaffney` 与实时 `coAuthorSurnamesKey` 的 `Baumberg, Bell & Gaffney` 去标点后相同；`Vauclair et al.` 与 `firstSurnameEtAl` 相同；机构与静态表相同），**只有 `Japan's 6th national survey on household changes` → `Japan`、`Supreme People's Court of China` → `China` 这类"长名称取简称"与实时规则不等价**。结论：**2026-09-12 按「叠加」语义落地后，这 42 条里只有上述两类「长名称取简称」产生实际行为变化**（教师确认系本人手改且要保留：答 `China` / `Japan` 应答对），其余 40 条与自动规则等价、行为不变；因用的是「叠加」而非「接管」，这批旧别名也不会冻结自动规则（自动规则照常对所有名字生效）。⑤ 现状取值：v93 = 901 条（238 学者 / 663 术语），其中 42 学者 / 66 术语带 aliases；v93 与 v92 的 data/unit_order 哈希完全相同。

## Lessons Learned
- **✅ Browser Run 可用路线已确认（2026-09-15，修正上一条结论）**：**不依赖 Workers binding** —— 用 API Token 走 REST `POST /accounts/<id>/browser-rendering/devtools/browser?keep_alive=…` 创建会话（约 2 秒），**响应里直接带页面级 `webSocketDebuggerUrl`**（`wss://live.browser.run/...?jwt=…`；⚠️ `json/list` 实测返回 `{"success":false,"errors":[{"message":"Gone"}]}`，**不要依赖它**，用 `json/new` 的响应字段即可），随后 CDP over WebSocket 导航 / 执行 JS / 截图，最后 `DELETE` 关闭会话。**实测一整轮（建会话+开页+CDP 读结构+截图+关闭）仅消耗 8.4 秒浏览器时长** → 免费版 10 分钟/天 ⇒ 每天约 70 次，完全够用。脚本：`app/scripts/cf-browser-cdp.mjs`（读 `.dev.vars` 的 `CF_ACCOUNT_ID`/`CF_API_TOKEN`，**不打印 token**；`--url` 指定页面）；另 `app/scripts/cf-browser-rest.mjs` 为 Quick Actions 直连探针（截图/HTML/PDF ✓，但**无状态、做不了交互** ✗）。**免费版限额（官方 limits 页）**：浏览器时长 **10 分钟/天**、并发 3、新建实例 1/20 秒、不活动超时 60 秒（`keep_alive` 最长 10 分钟）、Quick Actions 1 次/10 秒。⚠️ **超额行为（2026-09-15 查官方 limits 页确认，勿再反复怀疑）**：超出每日 10 分钟后**返回 `429: Browser time limit exceeded for today`（拒绝服务，持续到下一个 UTC 日切重置）**，**不会自动计费、也不会自动升级套餐** —— 只有手动升到 Workers Paid 才解除（且升级后需 **redeploy** Worker 才生效）。另：官方 FAQ 指出"用量偏高最常见原因是没显式关闭浏览器会话"（未 close 会一直烧到超时 60 秒 / keep_alive 最长 10 分钟）⇒ 本项目脚本必须始终显式关闭（现有 `cf-*`/`mb-*` 脚本都是 `DELETE` 会话 ✓），并处理 `Retry-After` ✓。**结论：可以放心用；真正会"花钱"的只有手动升级。****结论：「前端一键同步分数」的技术前提已满足** —— 交互式会话可用；登录态可用 CDP 注入 cookie，并配合官方 `features/live-view` 与 `features/human-in-the-loop` 让教师**手动登录一次**，从而**无需把密码交给服务端**。
- **Browser Run 实测结论（2026-09-15）：REST 可用、Worker binding 当前不可用**：用 API Token（权限 `Browser Rendering - Edit`）直连 REST `POST /accounts/<id>/browser-rendering/{content|screenshot}` **完全正常**（content 200/2.8s 返回真实 HTML；screenshot 200/1.6s 返回 22KB PNG）→ 由此证明 **Free 套餐可用、无需控制台启用**，binding 的失败与套餐无关。而 Worker 侧 `[browser] binding`（本地 `wrangler dev` + `remote = true`）**创建会话稳定 500、`quickAction` 挂起** ✗ → 剩余嫌疑：① wrangler 版本偏旧（本机 4.126，官方已 4.131）② Wrangler OAuth token 权限/scope（`whoami` 仅列 user read / offline_access / account read）③ 本地 remote-binding 代理兼容问题（生产内网里可能正常）。探针脚本：`app/scripts/cf-browser-rest.mjs`（读 `.dev.vars` 的 `CF_ACCOUNT_ID`/`CF_API_TOKEN`，**不打印 token**）。⚠️ **Quick Actions 是无状态的**（截图 / PDF / HTML / markdown ✓），**做不了"登录 + 填分"** ✗ → 交互式自动化必须走 **Browser Sessions**（binding 或 CDP ✓），这也是「前端一键同步分数」的技术前提。
- **Cloudflare Browser Rendering（Browser Run）在本项目环境当前不可用（2026-09-15 实测，待排查）**：已接入 `[browser] binding = "BROWSER"` + `compatibility_flags = ["nodejs_compat"]` + 令牌门禁实验路由 `app/worker/labBrowser.ts`（`/app-api/lab/*`；仅本地 `.dev.vars` 设 `LAB_TOKEN` 时生效，**生产不设该 secret 即等于 404**）。排查结论：① 本地 `wrangler dev` **必须给 binding 加 `remote = true`**，否则以 local 模式注册、请求挂住 120s 无响应（日志里 `env.BROWSER … local` 即此症状）；② 加 `remote` 后能连到 CF，但**创建浏览器会话稳定 500**（`Unable to create new browser: code: 500: internal error; reference = …`，三次一致、各约 21s），`quickAction('screenshot')` 亦挂起 → 判断为**账号侧未启用 / 套餐不含 Browser Rendering**（或 OAuth token 缺对应 scope：`wrangler whoami` 仅列 user read / offline_access / account read）。待教师查控制台 Browser Run 页是否需 Enable/升级、Workers 是否 Free。**副作用**：引入 `@cloudflare/puppeteer` 后 worker 打包体积 27KB → 719KB（若最终不走这条路，应撤掉该依赖与 lab 路由）。**判断**：现有 Tampermonkey「复制→去 ManageBac 页面粘贴」流程本身是**零凭证、零服务器**的最优解，Browser Run 的增量价值仅"任意设备（含手机）一键"；若只需电脑端点一下，改 Tampermonkey（脚本直接从本站在线拉分数、免复制粘贴）成本更低且无配额依赖。
- **「向校外开放」的前置清单（2026-09-15 讨论，未立项；勿只做 Turnstile）**：若要把站点开放给校外用户，**只加 Turnstile 不足以防护**——它只挡自动注册，挡不住"人工注册后用脚本刷 AI"。必须成对做：① **注册闸门**（Turnstile + 学校邮箱域名白名单/邀请码）；② **每人每日 AI 配额**（Supabase 按 `user_id` 记账，覆盖 `/app-api/*` 与 `/skill-api/*` 两个入口）——当前只有**一个共享的魔搭 250 魔粒/日池**，单个账号即可烧光。可选③：把 `ai_gate` 由"全局暂停"扩展为按功能开关。前置动作：Turnstile 需先在 CF 控制台建站点取 site key（教师侧操作）。
- **PowerShell → psql 的内联中文会变 GBK（2026-09-14 实测）**：`psql -c "...'自检'..."` 中的中文字面量经 PowerShell 传递后被编码成 GBK 字节，Postgres 报 `ERROR: invalid byte sequence for encoding "UTF8": 0xd7 0xd4`。**自查/一次性 SQL 只用 ASCII 字面量**；需要中文时走 `psql -f 文件.sql`（UTF-8 文件本身没问题，如 `db-migration-*.sql`）。同理：`git commit -m` 的中文 message 也会乱码，一律用英文（已有约定）。
- **校验线上产物别用 PowerShell 内联字符串比对（2026-09-14 两次误报）**：用 `curl ... | Out-String` 后 `$body.Contains("xxx")` 判断线上 JS/CSS 是否含新代码，**会误报 False**（大文件 + 中文/编码处理不可靠）。正解：`curl -o 临时文件` 落盘 → 再用 `Get-Content -Raw -Encoding UTF8` 或比字节数（线上与本地字节数完全相同即同一产物）。
- **OCR 阅卷面板的"图文重叠"布局坑（2026-09-14 实测，会复发）**：图片放进 CSS Grid 单元格时，**限宽规则必须命中图片本身那一层**。我最初把规则写成 `.ocrm-page > img`（直接子元素），但图片实际是**孙元素**（在 `.ocrm-result` 里）→ 规则不生效 → 图片按**固有宽度（长边 1800px）**渲染，溢出它所在的 34% 栅格并**直接盖住右侧文字**（教师实测反馈"文字与原图重叠、可读性差"）。配套加固：`.ocrm-text` 需 `min-width: 0`（grid/flex 子项默认 `min-width: auto`，长文本会撑列溢出）+ `overflow-wrap: anywhere`（OCR 可能产出无空格长串）。**教训：写"图 + 文并排"布局时，先确认选择器命中的是不是真实元素层级。**
- **主站 AI 入口（OCR 辅助阅卷）两个前端踩坑（2026-09-14 实测，会复发）**：① **`<video>.srcObject` 必须在 React 提交之后才赋**——首次打开相机时 `<video>` 是随状态更新才挂载的，若在 `openCam` 里用 `setTimeout(0)` 赋值，那一刻 `ref.current` 仍是 null，表现为「**授权成功、不报错、画面全黑**」；正解 = 放进依赖 `camOn/camId` 的 `useEffect` 里赋值，并对 `play()` 被自动播放策略拦截做兜底（点画面播放）+ 2 秒后按 `videoWidth === 0` 给自检提示。② **`<input capture="environment">` 桌面浏览器按规范忽略**（Windows/macOS 只弹文件框，Android/iOS 才唤起系统相机）→ 必须按 UA 分流：移动端走系统相机、桌面走应用内 `getUserMedia`，否则按钮文案与行为不符。③ 附带事实：Windows 桌面下 `getUserMedia` 需用 Edge/Chrome **直接**打开页面（IDE 内置预览的 webview 通常不放行相机权限，且不弹询问），但 **`127.0.0.1`/`localhost` 本身属安全上下文、不是黑屏原因**。④ 同一类坑：**剪贴板写权限在 IDE 内置预览的 webview 里被拒** —— 表现为点「复制短码 / 本地脚本同步」时报 `Failed to execute 'writeText' on 'Clipboard': Write permission denied.`（2026-09-15 教师实测）。**根因不是权限缺失而是实现错误**：`copyText` 原先是 `await navigator.clipboard.writeText(text); return;`，抛错即中断 → 后面的 `execCommand` 回退永远走不到。正解 = `app/src/lib/clipboard.ts` 改成多级回退（Clipboard API → `execCommand('copy')` → 返回 `'failed'`），调用方在 `'failed'` 时展示**可手动 Ctrl+C 的只读 textarea**（`PaperResults.tsx` / `QuizManager.tsx` 的 `manualCopy`）。**结论：`navigator.clipboard` 存在 ≠ 可用，凡用它必须 try/catch 再回退。**
- 教材蒸馏粒度注定丢细节（2026-09-11 实测）：book-to-skill 默认按"章"蒸馏，681 页 9 章 → 每章仅 5–7KB，具体研究案例（如 Ward 2015 的 Boiz/Geeks）被压成术语清单，AI 只能答"知识库未覆盖"。正解不是把蒸馏调细，而是**引原文**（页级 OCR 提取 + 关键词索引 + 按需取页），蒸馏产物退居结构/导航层。
- Capacitor 8 会自动处理安全区、并静默干扰自绘方案（2026-09-11 实测）：内置 `SystemBars` 插件（Bridge.java 自动注册）在「WebView 版本 < 140 或页面缺 `viewport-fit=cover`」时，会给 WebView **父容器**设 `padding-bottom = 导航栏高度`，表现为底部一条纯色带（露 windowBackground），从 CSS 侧极难察觉；本项目处理 = `insetsHandling='disable'` 后完全自绘。
- 跨 Web/原生的初始化不要放在 JS 里（2026-09-11 踩坑）：曾用 JS 启动时调自定义插件控制 WebView 顶部避让，调用发生在 bridge 就绪前被 `catch` 静默吞掉，表现为"设置不生效"。改为原生按 URL 判定，并在 `onCreate`/`onPostCreate`/`onResume`/`onPageCommitVisible`/`onConfigurationChanged` 五个时机应用（insets 监听 + 主动读取双保险）。
- 系统栏高度不能用资源取：`android.R.dimen/status_bar_height` 在部分 ROM（小米 HyperOS 实测）不可靠 → 用真实 `WindowInsets.getInsets(systemBars|displayCutout).top`；另 `StatusBar.setBackgroundColor` 在 Android 15+ 已废弃（强制 edge-to-edge），只能作老系统兜底。
- 阿里云 Supabase 兼容版（AnalyticDB/Greenplum 内核）控制台 Authentication→Users 只能查看 Raw User Metadata JSON、**不能编辑**；改 auth.users 的 user_metadata 只能走 SQL Editor
- 阿里云 ADB 的 SQL Editor **不支持**标准 Postgres 的 `UPDATE ... FROM (VALUES ...) AS v(...)` 语法（报 42601）；批量改名用「临时表 + insert values + UPDATE...FROM 临时表 JOIN」写法（2026-08-25 已实测可行）
- 阿里云 AnalyticDB PostgreSQL 版 Supabase 免费版虽标"禁生产"，但对本地数据为主的应用风险可控（数据不丢失，仅需迁移）
- 腾讯云 CloudBase 个人版（19.9元/月）比阿里云 Supabase 付费版（44.2元/月）更符合自费教育项目预算
- 创建阿里云 Supabase 项目需先配置专有网络（VPC）和交换机，二者免费但需与项目同地域及可用区
- 阿里云 Supabase 兼容版默认 enables email auto-verification (no need for email confirmation); Auth and REST API are fully functional
- koa-connect wrapper caused ctx leaks when migrating Express middleware to Koa; native Koa rewrite is required
- Cloudflare Pages automatic deployment fails due to missing vocab-data.json (excluded from Git); manual dist upload is necessary
- 阿里云 Supabase 兼容版 supports Edge Functions (functions/v1 endpoint exists), enabling potential server-side logic in future
- Database memory usage (around 80%) is normal due to PostgreSQL's cache mechanism; student login load is minimal and within free tier capacity
- "Instance access to public network" is not required for current Auth functionality (email auto-verified, password reset via console); only needed for email/SMS services
- Elastic Network Interface (ENI) is an infrastructure detail and does not require manual configuration
- PostgREST RPC 参数名三连坑（2026-08-27 交卷函数 submit_quiz_submission 实测）：① 参数名带 p_ 前缀 → 前端 supabase.rpc 传无前缀键名报 "Could not find the function ... in the schema cache"（PostgREST 按参数名匹配前端键名）；② 去前缀后参数名与表列名同名 → INSERT 报 "column reference ... is ambiguous"（VALUES 与 ON CONFLICT 裸列名均会歧义）；③ CREATE OR REPLACE 改参数名 → 42P13。最终写法：参数名=前端键名（无前缀）+ 函数体用 $1..$n 位置参数 + ON CONFLICT ON CONSTRAINT。
- RPC 参数个数变更是更隐蔽的第四坑（2026-08-30 实测）：`CREATE OR REPLACE` 新增参数（如加 `total_points integer default null`）**不替换旧函数，而是创建新重载**，旧签名函数原样保留，且**不报错**（带默认值可建重载）。PostgREST 按参数个数匹配 → 前端仍传旧参数个数时命中旧函数，新逻辑"静默不生效"，表现为"函数回退"。教训：改动 RPC 参数列表必须 `DROP FUNCTION` 旧签名（DROP 后重新 GRANT，EXECUTE 默认 PUBLIC 有，通常无需额外 GRANT）+ 前端 `supabase.rpc` 传参个数与函数签名严格一致；排查"函数像被回退"时先查 `pg_proc` 是否有多个重载（`select oid::regprocedure from pg_proc where proname=...`）。
- Android 打包流程（2026-08-31 实测修订）：① CodeBuddy 环境的安全删除 shim 会拦截 `cap sync`/`cap build` 的批量删除（回收站在此系统不可用报错），需前置 `$env:CODEBUDDY_SAFE_DELETE_ENABLED='0'` 绕过；② Capacitor 8 的 `cap build android --release` 语法已废弃（报 unknown option），新语法 `--androidreleasetype APK`；③ 因 build.gradle 已配置 release signingConfig（debug.keystore），gradle 直接产出**已签名** `app-release.apk`，`cap build` 自己再签名会因 jarsigner 处理不了中文路径（OneDrive\文档）报 unsigned 缺失——**最终改用 `gradlew.bat assembleRelease` 直接构建**，绕开 cap 签名；④ 版本号在 `android/app/build.gradle`（versionCode 手写 +1，versionName），`npm run icons` 用 `assets/` 源图生成 Android 图标（已并入 `npm run release` 默认流程，release 脚本 = icons + build + cap sync + gradlew assembleRelease + feishu:send-apk）。
- 流派/理论关系设计（设计决策与进度见项目根 `流派设计决策.md`，2026-08-30 建立）：理论流派字段 `theory`/`theories` 已实现（VocabManager 单条多选编辑 + 批量追加/移除 + 按流派筛选 + 流派管理卡片重命名/删除 + 未发布脏标记 + 发布确认）；数据清洗进行中（100% 手动，参考 `theory-diagnostic-report.md` 归并建议 + `theory-cleanup-list.md` 优先处理清单，均含业务数据已 gitignore）；「概念网络/反义关系」练习（contrast 存词条 id、概念反义选择/流派对立配对/立场判断三题型）已定稿但**暂缓**，等流派整理完善后再做；Mindmap 教学材料读取（图片/.mm/.xmind/.opml 可读，MindManager .mmap 不可读）讨论中，用户 quote 该文件时可继续。
- UI 设计（设计决策与进度见项目根 `UI设计.md`，2026-08-30 建立，已 gitignore）：**两级导航已实现**（2026-08-31，练习/趣味/后台分组手风琴 + 窄屏汉堡 + 点击外部自动收回 + 展开箭头加大）、**社会数据看板已基本完成**（2026-08-31，World Bank 实时 + 快照、国家对比/历史趋势、折线悬停数值、APK 直连 Worker 代理、fetch 超时+快照先行）；夜间模式（CSS 变量驱动，推荐 prefers-color-scheme，排查出 Wordle absent 硬编码 #555 等 4 处风险点，未定稿）等所有 UI 想法统一汇总于此文件，以后 quote 该文件可继续讨论。
- 排行榜功能（暂缓，仅探讨未实现）：技术上可行——用「视图（view）」只暴露姓名+连续打卡天数等少数列，给视图单独设 RLS「所有已登录用户可读」，原 student_data 表仍保持学生只能读自己，隔离不破坏。但核心难点是防作弊：当前"学生本地计算后整坨 JSON 上传"的架构下无法真正防作弊，排行榜会把该弱点放大，故定位为"激励工具"而非"严谨排名"
- 防作弊三档方案（探讨结论，未实现）：①数据库触发器做「合理性约束」（连续天数单次增不超过1、日期不能是未来、单日题数设上限）——性价比最高，能挡住"直接改数据"类粗暴作弊，但前置条件是先把关键字段（如 bestStreak）从 data JSON 拆成独立列才能加约束；②服务端权威计数（学生只上报"做了一道题"事件，连续天数由服务端累计，学生无"改汇总值"入口）——更彻底但改动大，仍防不住"脚本每天自动刷"；③服务器时间戳（用数据库 now() 作唯一权威时间，不接受客户端传来的日期）——防"回填过去补签"。结论：若做排行榜，先上第一档即可
- 词库上云与同步方案（已定稿，P0-P3 已实现，见项目根 `词库上云与同步方案.md`）：教师发布词库到 `vocab_releases` 表 + 学生启动/回首页查版本静默拉取 + 首页 banner + 稳定 id（type+term+paper+category+unit 双哈希，unit 参与区分同名 Cultural deprivation）+ 词条管理 CRUD（VocabManager）+ aliases 答案容错（VocabItem.aliases，判定优先读内嵌 aliases）。P4（自动拆别名）暂缓。Syllabus 层级（科目/动态 Category/Unit）长期规划、暂缓。
- 用户分类管理方案（已定稿，P0/P1 已实现，见项目根 `用户分类管理方案.md`）：账号从「教师/学生」二分类演进为四分类（developer/teacher/student/guest 注册游客）+ 多归属（user_roles 表一行一角色替代 teacher_roles，isTeacher → roles.includes('teacher')）；班级功能（classes 表 + student_data.class_id）；统计默认排除 developer 角色。背景：教师这学年教两个年级（A1 学 Paper1-2、A2 学 Paper3-4）。P0（developer 角色 + 统计排除）与 P1（classes 表 + class_id + 教师后台按班级筛选）已实现并验证；P2（四分类角色完整迁移 + guest 注册游客）、P3（多教师多科目关联）待做。已排除账号：chenzh@dtd-edu.cn=teacher+developer、abc@example.com=student+developer、test.student@example.com=developer。（⚠ 2026-09-13：该文档**已从公开仓库移除**（`git rm --cached` + `.gitignore`），因为它新增了「未来收费化 / 权益分层」内部规划；此后只存本机 + OneDrive，改它不走 git。）
- 班级管理界面（已实现，2026-08-23 由用户在其他 agent 完成）：前端 app/src/components/ClassManager.tsx（教师后台 AdminPanel 新增「班级管理」tab，四个 tab：打卡核验/班级管理/词条管理/批量导入），支持建班/重命名/删除班级（删除前自动清空该班学生 class_id，配 confirm）、给每个学生下拉指定班级（含置空=未分班/撤销分班）、按班级分组预览，学生列表排除 developer 测试账号。后端 RLS 在项目根 db-migration-class-assign.sql：classes 已登录可读 + 教师 for all 可写；student_data 新增 "student_data_assign_class" 策略允许教师 for update 更新 class_id（含置空）。「打卡核验」已支持按班级筛选。
- 随堂测验/作业功能（已实现，2026-08-25，方案见项目根 `随堂测验和作业功能.md`）：教师创建「随堂测验（quiz，严格限时）」或「作业（homework，放宽时限、可保存退出继续）」→ 生成 4 位密码写入 quizzes 表；学生首页输密码（校验 open_at 统一开考 / due_at 截止与结果公布）→ 锁导航 + 限时 + 乱序 → 交卷 → 教师后台看成绩与答卷。核心：新增 quizzes / quiz_submissions 两张 Supabase 云表，题目快照固定存 quizzes.questions（所有学生同一份题）；题库稳定 id（itemId）判分，选择/匹配按 answerIndex 下标比对，拼写复用 isCorrectAnswer（含 aliases/归一化容错）。题目按勾选题型「均匀随机分配」（轮转序列 + 洗牌），干扰项优先同 paper，再补其他 paper，且术语/学者类型筛选（type_filter，经 db-migration-quiz-edit.sql 增加）保证干扰项不混类。防作弊：「硬拦截」= 统一开考 / 限时自动交卷 / 每人一份 unique(quiz_id,user_id) / 学生端按 order_seed 确定性乱序题目 / 应用内锁导航（App.tsx inQuiz 拦截 goto 与 Android 返回键）；「记录」= 切屏失焦 leave_count/leave_seconds（visibilitychange/blur/focus，教师可见）。作业模式 saveAndExit 存 in_progress 草稿（含 order_seed 与 answers），回来凭密码恢复同一乱序继续。判分在前端 gradeQuiz 后连同 answers 整包 upsert 上传（onConflict quiz_id,user_id）。学生端可看「历史结果」（due_at 未到则显示「结果尚未公布」）。新增文件：app/src/lib/quiz.ts、components/QuizManager.tsx（教师，AdminPanel 新增「随堂测验/作业」tab，现共五 tab：打卡核验/班级管理/随堂测验/词条管理/批量导入）、components/QuizTaker.tsx（学生，Home 入口）；cloud.ts 增 createQuiz/updateQuiz/deleteQuiz/listQuizzes/getQuizByCode/upsertSubmission/getMySubmission/listMySubmissions/listQuizSubmissions。SQL：db-migration-quiz.sql（主表+RLS）、db-migration-quiz-edit.sql（type_filter 增量）、db-migration-quiz-rls-fix.sql（修复：原 submissions 只教师可读，导致学生/开发者 upsert 报 "new row violates RLS"，改为学生可读自己的 + 教师读全部）。语境填空（P3）等 ContextPassage 练习组件完成后再接入。
- 开发者指定/撤销教师（待做，开学前做）：给 developer 角色开「写 user_roles」的 RLS 策略（现在 user_roles 只有读策略，角色全靠 SQL 手动 insert）。开发者后台加「指定教师」功能：从「已登录过的账号列表」里选一个设为 teacher（前端拿不到 Supabase uid，靠按邮箱查需要管理员权限，故从已登录账号列表选，同事必先注册登录一次才会出现）。同时支持撤销：指定教师=加一行 teacher 角色、撤销=删该行。写操作配确认弹窗防误点。
- 单元分类编辑（已实现，2026-08-26）：unitOrder 从硬编码 UNIT_ORDER 升级为「可编辑 + 云端同步」。unitMapping.ts 增 loadUnitOrder/saveUnitOrder（localStorage，默认 fallback UNIT_ORDER）+ subsForPaper，unitsForPaper/unitOrderFor/unitListFor 加可选 order 参数；store.tsx 增 unitOrder 状态 + addUnit/removeUnit/moveUnit/renameUnit（删除/重命名时同步清理/改名词条 unit）；VocabManager 增「单元管理」折叠卡片（选 paper，Paper 4 选主题 → 增删/上移下移/重命名）；随「发布词库」同步（vocab_releases.unit_order 字段，db-migration-unit-order.sql）。attachUnits 改为「只补缺不覆盖」，修复用户手动改选/重命名单元在刷新后被 unit-mapping.json 重算回旧值的 bug。CategoryFilter/QuizManager 单元下拉改用动态 unitOrder。
- 批量迁移单元（已实现，2026-08-26）：VocabManager 增「批量迁移单元」多选模式（勾选/全选词条 → 下拉选目标单元 → 整体替换 unit），多选模式下禁用编辑/删除单个词条。
- 匹配题改为「配对块」（已实现，2026-08-25）：随堂测验/作业的 matching 从「单题 4 选 1 释义」改为真正的配对——一道题含 6 对术语↔释义（QuizQuestion.pairs），左侧术语列+右侧打乱释义列点选配对；判分按正确配对数计分（每对 1 分），question_count 语义改为「总评分点数」（拼写/选择每题 1 分、匹配块每对 1 分）。quiz.ts 增 buildMatchingBlock / PAIRS_PER_BLOCK=6 / totalPoints / matchingCorrectCount，buildQuizQuestions 每 6 个 matching 词条组一块（尾部不足 2 个转 choice）。
- 通知/打卡提醒（未实现）：APK 用 @capacitor/local-notifications 本地通知精确调度（无需服务器，App 关闭也能触发）；网页 Notification API 降级（仅页面打开可靠）。需新增提醒设置入口（开关+时间，存本地+云端），到点复用 isDayChecked/todayKey 检查未达标则弹提醒。
- 教师公告推送 FCM（未实现，较复杂，第二阶段）：FCM 消息免费；教师发公告 → Supabase announcements 表 → Edge Function 监听 insert → FCM HTTP v1 API 推送学生设备。需 Firebase 项目 + @capacitor/push-notifications token 注册 + token 生命周期管理。
- 语境填空（待开发，2026-08-31 整理出第一批题库）：ContextPassage 框架已在（`types.ts` 定义 `{{术语}}` 标记挖空、`storage.ts` 有 loadContexts/saveContexts、随堂测验方案 P3 待接入）。第一批兜底题库已整理到项目根 `语境填空题目池.md`（已 gitignore，含答案；288 道原始题去重后约 130 道可挖空，57 道原配答案、约 90 道补答案、约 20 道🔶存疑待教师确认）。「输入范文自动挖空」设计要点（用户 2026-08-31 补充，此前在 trae 讨论过）：**复用 `app/src/lib/answers.ts` 的 `maskAnswer`/`collectMaskForms` 机制**——自动挖空 = maskAnswer 的反向应用：遍历词库词条，在范文文本中匹配词条的所有"答案表面形式"（term/aliases/单复数变体/学者姓氏，`collectMaskForms` 已实现），把命中处替换为 `{{术语}}` 标记，同时输出挖空位置对应的答案词条列表。即自动挖空与"释义里词条本身泄露时脱敏"共用同一套词条匹配逻辑。**另要求（2026-08-31 追加）：自动挖空必须有手动约束与确认环节**——① 避免**重复挖词**（同一术语在文本中多次出现时控制挖空次数/位置，防止题目失衡）；② 避免**断章取义**（挖空切分不能破坏语义上下文，自动匹配的候选需经教师确认/调整后再入库）。**又一点（2026-08-31）：语境题型与词库覆盖率错配的处理**——题目池不可能覆盖词库全部条目，教师创建测验/作业勾选「语境」题型但所选词条无对应语境题时，创建阶段做覆盖率校验并弹提示窗，提供三出口：重新生成 / 重选单词 / 取消勾选语境题型（已记入 `随堂测验和作业功能.md` 决策九）。**形态设计（2026-08-31）：统称「语境题」（非仅挖空）**，三种形态共享同一批 ContextPassage（原文+目标词），仅作答/判定不同：① 选词填空（挖空+选项，类选择题）；② 拼写填空（挖空+打字，复用 isCorrectAnswer 容错）；③ 原文点击选词（不挖空，给描述，学生在原文点击最符合的词，需存目标词位置判定）。教师一次挖空/标注可复用三种形态出题（已记入方案文档决策十）。
