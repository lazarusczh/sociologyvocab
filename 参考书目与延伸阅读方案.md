# 参考书目与延伸阅读方案：把教科书引用变成可点的外链

> 目标读者：教师（你自己）
> 定位：从教科书蒸馏 Skill 中抽取被引用的专著/论文，**离线**补全元数据，生成静态书目页，并在词条详情里挂「延伸阅读」。
> 状态：**方案阶段，未动工**（2026-09-08）
> 最后更新：2026-09-08

---

## 〇、一句话结论（TL;DR）

- **「网络拉取」放在离线脚本里做一次性补全，运行时零 API 调用**。页面只渲染静态 JSON + 拼好的 `<a target="_blank">`。
- **两级降级保证永不失效**：已核实条目显示完整书目 + DOI 直链；未核实条目只显示教材原文引用 + Scholar/Open Library 搜索链接（纯拼 URL，永远可点）。
- **摘要卡片 + 外链并行**：外链解决"去哪找"，摘要卡片解决"讲了什么"，后者才是学生真正读到的东西。
- **与词条双向关联**：学者类词条按姓氏自动匹配著作（命中成本低、准确率高）；术语类词条靠共现自动关联 + 手工确认。
- **入口**：「资料」组新增第 5 个 pill「参考书目」，主站、免登录（书目元数据是事实性信息，不同于教材正文）。

---

## 一、已确认的现实约束

1. **蒸馏 Skill 里没有参考文献表。** `9699textbook1` 的 9 个章节 md 是骨架式笔记（每章约 60 行）。项目内无教材 PDF、无 OCR 中间产物。
2. **实测抽取结果（2026-09-08，脚本已跑通）**：共 **64 条**，其中 **9 条带书名**（high confidence）、**55 条仅「学者 + 年份」**。

   ```
   按章节: ch04 家庭 17 | ch01 9 | ch08 全球化 9 | ch05 教育 8 | ch03 方法 7
           ch02 6 | ch06 媒体 5 | ch07 宗教 5 | ch09 3
   ```

   带书名的 9 条（质量很高，全部命中正确）：

   | 引用 | 章节 |
   |---|---|
   | Durkheim《Suicide》(1897) | ch02/03/07/09（跨 4 章，已自动合并） |
   | Durkheim《Rules of Sociological Method》(1895) | ch03 |
   | Jack Douglas《The Social Meanings of Suicide》(1967) | ch03 |
   | Atkinson《Discovering Suicide》(1978) | ch03 |
   | Dobash & Dobash《Violence Against Wives》(1979) | ch04 |
   | Bowles & Gintis《Schooling in Capitalist America》(1976) | ch05 |
   | Paul Willis《Learning to Labour》(1977) | ch05 |
   | Dahl《Who Governs?》(1961) | ch06 |
   | Glasgow Media Group《Bad News》(1976) | ch06 |

3. **已知缺口：无年份的旗舰研究抽不到。** 教材对最核心的研究常只写人名、或用全角括号，例：`**Stan Cohen（Mods & Rockers）**`（道德恐慌，ch06 核心考点）、`**de Beauvoir**`、`**Armstrong**`。这类恰恰是考纲必考项，**必须人工补录**（见 §五 阶段 C′）。
3. **国内网络可达性有前车之鉴**：`DataBoard.tsx` 已有"无法访问 api.worldbank.org（需科学上网）"兜底。文献 API 同样会慢、会超时。
4. **外链 ≠ 可读**：社会学经典多数在付费墙后。

---

## 二、总体架构：为什么不做运行时实时 API

| | 运行时实时搜索 | **构建期预生成（选它）** |
|---|---|---|
| 数据性质 | 教科书引用是**固定集合**，实时搜索零收益 | 一次性补全即可 |
| 稳定性 | 依赖国内到 OpenAlex/Crossref 的链路 | 产物是本地 JSON，永远可加载 |
| 延迟 | 每页几十个请求 | 0 请求 |
| 准确率 | 模糊搜索会错配（`Durkheim (1961)` 是英译本年份，几十个版本） | 静态表 + 人工校对 |
| 一致性 | — | 与 `pastpaper-topics.json`、`social-data.json` 同一套路（项目已验证） |

**核心洞察：外链根本不需要 API。** `https://doi.org/10.xxxx`、`scholar.google.com/scholar?q=...`、`openlibrary.org/search?q=...` 全是拼字符串，零请求、零失败。

所以"借用公开网络"的价值只体现在**离线补全阶段**（拿到 DOI、ISBN、封面、OA 状态），拿到之后就固化进 JSON。

---

## 三、数据模型

产物：`app/public/textbook-refs.json`

```ts
interface TextbookRef {
  id: string;              // 'bowles-gintis-1976'（slug，幂等）
  chapters: string[];      // 归属章节 ['ch05']（跨章引用可多个）
  citationRaw: string;     // 教材原文："Bowles & Gintis《Schooling in Capitalist America》(1976)"
  context?: string;        // 教材中的上下文片段（供校对 + 卡片副标题）

  // —— 以下为补全字段，可缺省 ——
  authors?: string[];      // ['Samuel Bowles', 'Herbert Gintis']
  year?: number;           // 1976
  title?: string;          // 标准书名/篇名
  type?: 'book' | 'article' | 'chapter' | 'report';
  publisher?: string;
  doi?: string;
  isbn?: string;
  coverUrl?: string;       // Open Library 封面
  openAccess?: boolean;    // OpenAlex is_oa
  oaUrl?: string;          // 开放获取全文（仅 CC 授权才填）

  // —— 状态与内容 ——
  verified: boolean;       // 是否已核实（决定 UI 降级层级）
  summary?: string;        // 摘要卡片：这个研究讲了什么（中文）
  why?: string;            // 一句话：考纲里它用来论证什么
  relatedTerms?: string[]; // 手工确认的关联词条（term 字符串）
  autoTerms?: string[];    // 共现自动关联，UI 上弱化/折叠显示
  customLinks?: { label: string; url: string }[]; // 教师自定义链接（见 §四）
}
```

---

## 四、外链策略与链接源

### 4.1 系统内置（可程序化构造，稳定）

| 链接 | 构造方式 | 说明 |
|---|---|---|
| **原文 / DOI** | `https://doi.org/{doi}` | 仅 `verified` 且有 doi 时显示；可能跳付费墙 |
| **Google Scholar** | `scholar.google.com/scholar?q={title+authors}` | 永远可构造，用于找免费版/引用 |
| **Open Library** | `openlibrary.org/search?q={title+authors}` | 专著为主，可借阅/预览 |
| **Internet Archive** | `archive.org/search?query=...` | 有全文借阅的老书 |
| **WorldCat** | `worldcat.org/search?q=...` | 查就近图书馆馆藏 |
| **开放获取全文** | 直接 `oaUrl` | 仅当 `openAccess===true` 且 CC 授权 |

### 4.2 关于 z-library 一类来源：走「教师自定义链接」，不内置

教师已表示合规包袱不大、可接受 dodgy sources。**方案不否定这一点，但工程上不内置**，理由纯粹是维护性的：

- **域名漂移**：这类站没有固定域名，也没有官方 API/URL 规范，硬编码进代码等于每次换域名都要改代码 + 重新发版（走一遍三步走发布流程）。
- **仿冒与钓鱼**：搜索引擎前排多为仿冒站，学生点击有账号与设备风险。
- **无法校验**：内置链接无法程序化判断存活，坏了也不知道。

取而代之，数据模型里留 **`customLinks: [{label, url}]`**，前端忠实渲染成一个带 ⚙ 标记的按钮。教师可以在 JSON 里给任意条目加**任何**链接——z-library 镜像、学校图书馆数据库、教师自扫讲义 PDF、Guardian/BBC 相关报道，全部适用。

> 这个设计其实比内置更有用：责任边界清晰（链接由教师决定），且不受发版周期约束（改 JSON 重新 build 即可，甚至可考虑后续做成教师端可编辑）。

### 4.3 前端渲染约定

```tsx
<a href={url} target="_blank" rel="noopener noreferrer">原文</a>
```

> **待实测**：APK（Capacitor）端 `target="_blank"` 的行为——可能在 webview 内打开而非系统浏览器。若体验不佳，改用 `Capacitor.isNativePlatform() && window.open(url, '_system')` 分支。

---

## 五、生成管线（三个脚本阶段）

### 阶段 A：抽取 — `app/scripts/extract-textbook-refs.mjs`（新）

扫描 9 个章节 md，正则抓取：

- `Author (year)` / `Author (year/year)` / `A & B (year)`
- `《Title》(year)` 与 `*Title*`（书名号/斜体）
- 纯人名引用（`Durkheim`、`Goffman`）——标记为无年份候选

输出 `app/data/refs-candidates.json`（**中间产物提交进 git，便于人工校对与版本 diff**），每条带 `chapters` + `context`（命中行原文）。

**数据来源（已定）**：读本机 Skill 目录 `C:\Users\rebir\.agents\skills\9699textbook1\chapters\*.md`，支持 `--src <dir>` 覆盖。

理由：书目是**新增派生数据**，教材内容极稳定，无需走云端；`skill_content` 的 RLS 匿名读返回空，走云端反而要额外处理 key。本机 Skill 已是"云端内容的成品"，直接读最简单可靠。

### 阶段 B：补全 — `app/scripts/enrich-textbook-refs.mjs`（新）

Node 端串行调用（无 CORS 问题）：

| API | 用途 | 限额 |
|---|---|---|
| **OpenAlex** `api.openalex.org/works?search=` | 论文 + 部分专著，DOI、`is_oa`、`best_oa_location` | 免费无 key 可基础查询，10 万次/天、10 req/s；加 `mailto` 进 polite pool |
| **Crossref** `api.crossref.org/works?query.bibliographic=` | 论文 DOI 权威 | 免费，加 `mailto` 走 polite |
| **Open Library** `openlibrary.org/search.json` | 专著 ISBN、封面、可借阅状态 | 免费 |

- 本地缓存 `.cache/refs/`（存原始响应，避免重复请求、便于排查）
- 串行 + 请求间隔（避开 10 req/s 突发限制）+ 重试
- **自动置信度打分**：作者姓氏匹配 **且** 年份 ±1 **且** 标题相似度达标 → `verified = true`；否则 `verified = false`
- 幂等：重跑只补空缺字段，不覆盖人工校对过的值（用 `verified`/人工标记保护）

### 阶段 C′：人工补录 — `app/data/refs-manual.json`（新，不可回避）

自动抽取覆盖不到**无年份的旗舰研究**（`Stan Cohen（Mods & Rockers）` 等，见 §一 第 3 条）。用手工文件补上，脚本 `--merge` 合并：

```json
{ "refs": [
  { "id": "cohen-moral-panic",
    "chapters": ["ch06-media"],
    "citationRaw": "Stan Cohen《Folk Devils and Moral Panics》(1972)",
    "authors": ["Stan Cohen"], "year": 1972,
    "title": "Folk Devils and Moral Panics", "type": "book",
    "verified": true,
    "summary": "Mods & Rockers 事件…", "why": "道德恐慌 + 越轨放大螺旋的唯一出处" }
]}
```

- 人工条目标 `source: 'manual'`，**重跑脚本不覆盖**（`mergeInto` 只补空字段），人工成果可长期累积
- 建议首批补 **10–20 条**：Cohen 道德恐慌、Goffman 拟剧论、Weber 新教伦理、Oakley 家务劳动、Young & Willmott 对称家庭、Gillborn & Youdell、Sullivan 文化资本、Wallerstein 世界体系、Bourdieu 再生产、Frank 依附理论…

### 阶段 C：校对 — 人工（不可回避，1–2 小时）

- 过一遍 `refs-candidates.json`，删掉误抽（如"1976"其实是年份数据不是引用）
- 对 `verified: false` 的高价值条目手动补 `title` / `doi`
- 补 `summary` / `why` / `relatedTerms`（我可基于 Skill 章节内容起草，教师润色）

---

## 六、页面设计：`TextbookRefs.tsx`（新）

### 6.1 注册点（`src/App.tsx`）

| 位置 | 行号 | 改动 |
|---|---|---|
| `View` 联合类型 | 29–49 | 末尾加 `\| 'refs'` |
| `NAV_PILLS` 资料组 | 80–85 | 加 `{ key: 'refs', label: '参考书目' }` |
| `<main>` 条件渲染 | 508–527 | 523–525 附近加 `{view === 'refs' && <TextbookRefs />}` |

### 6.2 布局

沿用现有 CSS 习惯（`.card` / `.collapse-head` / `.badge` / `.tag-filter`，见 `index.css:211/566/228/1029`）：

```
[搜索框]  [章节筛选 pills]  [☑ 仅显示已核实]

▸ Chapter 5: Education                        (8)
    ┌────────────────────────────────────────────┐
    │ Bowles & Gintis《Schooling in Capitalist America》(1976)  ← 教材原文
    │ Samuel Bowles, Herbert Gintis · 1976 · Basic Books   ← 标准书目（verified）
    │ [摘要卡片] correspondence theory：学校结构对应 workplace…   ← summary
    │ 考纲用法：论证 "myth of meritocracy"                    ← why
    │ 关联词条：meritocracy · hidden curriculum · social reproduction
    │ [原文] [Google Scholar] [Open Library] [WorldCat] [⚙]
    └────────────────────────────────────────────┘
```

**未核实条目**降级为一行 + 两个搜索按钮，**不显示**标准书目行与 DOI 按钮：

```
    │ Sullivan (2001)                                   ← 仅原文
    │ [Google Scholar] [Open Library]
```

组件加载 `fetch('/textbook-refs.json', { cache: 'no-store' })`，沿用 `DataBoard.tsx:108` 的 `fetchWithTimeout` 范式 + 逐项 catch。

---

## 七、延伸阅读：与词条的双向关联

### 7.1 关联建立（两条路径，成本都很低）

| 词条类型 | 匹配方式 | 说明 |
|---|---|---|
| **学者**（`type: 'scholar'`，如 `Bourdieu`、`Willis`） | **自动**：词条 term（或姓氏）出现在 `ref.authors` 中 → 关联 | 天然契合，准确率极高，几乎零人工成本 |
| **术语**（`type: 'term'`，如 `meritocracy`） | **共现自动**：抽取时记录上下文句，句中整词命中 vocab term → 写入 `autoTerms`；教师确认后提升为 `relatedTerms` | 教材提到 Bowles & Gintis 的句子往往也提到 meritocracy，信号质量不错 |

### 7.2 词条侧渲染（`src/components/Dictionary.tsx`）

插入点：`ResultRow()` 的 `dict-detail` 区块内，**第 161 行之后、162 行 `</div>` 之前**（已在 `stopPropagation` 作用域内，点击不会触发行折叠；视觉上排在释义 149 / 备注 150 / 标签 155–161 之后）。

```tsx
{reading && reading.length > 0 && (
  <div className="card" style={{ marginTop: '0.5rem', padding: '0.8rem' }}>
    <div className="muted" style={{ marginBottom: '.4rem' }}>延伸阅读</div>
    {reading.map(r => (
      <div key={r.id} className="row">
        <span>{r.title ?? r.citationRaw}</span>
        <span className="spacer" />
        <a href={r.doi ? `https://doi.org/${r.doi}` : scholarUrl(r)}
           target="_blank" rel="noopener noreferrer">查看</a>
      </div>
    ))}
  </div>
)}
```

> 内联 `style` 补 padding 是项目既有写法（见 `ConceptMapViewer.tsx:198`、`CategoryFilter.tsx:70`）。

### 7.3 反向关联

书目卡片底部显示「关联词条」badge，点击跳到词典并定位该词条（若现有 `Dictionary` 支持外部传入搜索词则直接复用，否则 v2 再做）。**v1 建议只做静态展示，不做跳转**，避免动到主链路。

---

## 八、摘要卡片

**内容来源**：Skill 章节 md 里的 `## Worked Example` 段落（如 ch05 的 Willis《Learning to Labour》"lads" 研究）+ 正文中对研究发现的陈述句。

**生产方式（半自动）**：

1. 脚本抽取候选研究段落 → 我基于原文起草中文 `summary`（80–150 字）+ `why`（一句话考纲用法）
2. 教师校对润色
3. 写入 JSON

**v1 范围**：只对 **top 20–30 个旗舰研究**做摘要（Willis、Bowles & Gintis、Durkheim 自杀研究、Cohen 道德恐慌、Oakley、Duncombe & Marsden、Gillborn & Youdell、Young & Willmott…）。其余条目 v1 只有书目 + 外链，后续按需补。

> 这正是解决"外链 ≠ 可读"的关键：摘要卡片是学生**不点任何链接**就能读到的东西，外链只是给学有余力者的补充。

---

## 九、改动清单

| 文件 | 类型 | 说明 |
|---|---|---|
| `app/scripts/extract-textbook-refs.mjs` | 新增 | 阶段 A 抽取 |
| `app/scripts/enrich-textbook-refs.mjs` | 新增 | 阶段 B 补全（含缓存/限流/重试） |
| `app/data/refs-candidates.json` | 新增 | 中间产物（提交，便于校对 diff） |
| `app/public/textbook-refs.json` | 新增 | 最终产物（页面加载） |
| `app/src/components/TextbookRefs.tsx` | 新增 | 书目页（约 200 行） |
| `app/src/App.tsx` | 改 3 处 | `View`(29–49) / `NAV_PILLS`(80–85) / `<main>`(508–527) |
| `app/src/components/Dictionary.tsx` | 改 1 处 | 161 行后插入延伸阅读卡 |
| `app/src/index.css` | 改 | 新增 `.ref-*` 少量样式 |

**不改动**：`worker.ts`（无新后端路由）、`skill-site/`（子站不动）、数据库（无新表）、`android/`（随常规发版即可）。

> `.gitignore` 未忽略 `public/*.json`；`textbook-refs.json` 内容均为公开书目元数据，**不含教材正文**，可安全提交。

---

## 十、落地步骤与工作量

| # | 步骤 | 产出 | 估时 |
|---|---|---|---|
| 1 | 写抽取脚本，跑出候选清单 | `refs-candidates.json` | 1h |
| 2 | **教师看真实数量与质量**，决策是否调整抽取规则 | — | 0.5h |
| 3 | 写补全脚本，跑 OpenAlex/Crossref/Open Library | 补全后的 JSON | 2h |
| 4 | 人工校对 + 补 `verified` | — | 1–2h |
| 5 | 起草 top 20–30 条摘要卡片 + `why` | — | 1h |
| 6 | 词条关联：学者自动匹配 + 术语共现规则 | — | 1h |
| 7 | 写 `TextbookRefs.tsx` + CSS + 三处注册 | 页面可访问 | 3h |
| 8 | 词条详情插入延伸阅读卡 | — | 1h |
| 9 | 冒烟（Web + APK 外链行为实测） | — | 1h |

**合计约 1.5–2 个工作日**，且**每一步都可独立验证、随时可停**。

---

## 十一、风险与开放问题

| 风险 | 缓解 |
|---|---|
| 抽取误报/漏报 | 中间产物可审阅；`context` 字段保留原文便于判断 |
| API 补全错配 | `verified` 双层级；错配条目退化为搜索链接，无害 |
| 国内访问 OpenAlex 慢 | 只在离线脚本里跑一次，慢也无所谓；大不了手工填 |
| APK 外链在 webview 内打开 | §4.3 已列待实测项，必要时加 `window.open(url,'_system')` 分支 |
| 书目页是否免登录 | 建议**免登录**（元数据属事实性信息）。若顾虑"本书引用了 X"的编排，可退而挂 `authOnly: true` 与教材 AI 一致 —— **待教师定夺** |
| 摘要卡片的版权 | 摘要是基于教材的**转述**（非原文复制），且由教师把关；措辞应改写而非照搬 |

### 已决策（2026-09-08）

1. **入口 = 主站「资料」组新增 pill，不进教材知识库子站**，不设 `authOnly`（免登录）。
   理由：书目元数据（作者/书名/年份/DOI）是事实性信息，与教材正文的版权性质不同；且放在主站学生随手可查，才有使用频率。
2. **抽取数据源 = 本机 Skill 目录**（`--src` 可覆盖），不走 Supabase。
