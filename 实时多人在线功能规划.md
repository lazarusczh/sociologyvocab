# 实时多人在线功能规划

> 用途：为「课堂实时活动」这类需求提供**经实测验证**的后端能力边界与可选活动清单。
> 排查日期：2026-09-11。结论均基于对当前托管实例的实测（不是文档推测）。
> 最近更新：2026-09-22（新增第七节「第二个落地活动：课堂拼写竞赛」，并更正待办中的失效记录）。

---

## 一、结论摘要

**能做**：以「数据库表 + Postgres Changes 实时推送 + RLS 控权 + RPC 判定」为核心的一切课堂活动——全班作答、实时看板、投票、抢答、分组、共创、进度管控。

**做不到**：需要毫秒级、高频、无落库的即时通信——实时协作画布、光标同步、节奏类竞速、音视频。

一句话：**把「实时」当作「数据库变更的通知」，而不是「消息总线」。**

**已定稿的第一个活动**：课堂猜词（随机抽一名学生上讲台描述、其余学生在自己设备抢答）——完整设计见**第六节**。

**已定稿的第二个活动**：课堂拼写竞赛（Spelling Bee——全班同题同轮默写、答错出局、剩 3 人抢答定胜负）——完整设计见**第七节**。

---

## 二、后端实时能力现状（实测）

### 能力矩阵

| 能力 | 结论 | 证据 |
|---|---|---|
| 连接 / 订阅频道 | ✅ 可用 | `STATUS = SUBSCRIBED` |
| Presence（在线状态） | ✅ 可用 | 双客户端实测：A `track()` → B 收到 `join` + `sync`，且能读到内容 |
| **Postgres Changes** | ✅ **可用** | 端到端实测：订阅 `public.realtime_probe` → 插入一行 → **收到变更** |
| Broadcast（发消息） | ❌ 不可用 | 一发送即 `socket closed: 1011`（服务端内部错误），约 30ms 内断连并无限重连 |

### 关键发现

1. **Broadcast 坏在服务端，不是配置问题**
   - `realtime.messages` 表**历史上一条记录都没有**（0 行）——广播从未成功落库
   - 已确认该表 RLS 策略齐全（`realtime can send` / `realtime can receive`）、今天的分区存在
   - **补上策略后重测仍 1011** → 判定为托管方 Realtime 服务实现残缺

2. **Postgres Changes 的开关在 SQL 里，不在控制台**
   - 托管控制台 Database → Replication 页显示 **"coming soon"**（他们没做这个 UI）
   - 但底层能力完好，**一句 SQL 即可启用**：`alter publication supabase_realtime add table public.xxx;`
   - 注意：`supabase_realtime` 发布默认**一张表都没有**，所以「订阅了却什么都没发生」是正常的，不是坏了

3. **服务端是旧版单租户架构**
   - `realtime` schema 下只有 `messages` / `subscription` / `schema_migrations`，**没有** `tenants` / `extensions`
   - 迁移版本停在 `20250905041441`（2025-09-05），明显早于客户端库
   - 这很可能是 Broadcast 异常（1011）的根源：旧服务端遇到新客户端发的广播帧处理不了

4. **psql 可直连云端库**（凭 `%USERPROFILE%\.pgpass`）
   - 控制台没有的开关都能自己做：
     `postgresql://postgres@spb-olltk79n0rjrawe5.supabase.opentrust.net:5432/postgres`

---

## 三、技术约束

| 维度 | 现状 | 含义 |
|---|---|---|
| 延迟量级 | Postgres Changes ≈ 几十 ~ 几百 ms | 课堂交互完全够用；但没有「毫秒级手感」 |
| 权威性 | 数据库写入顺序 / 时间戳 | 抢答判定**天然公平**（服务端裁定，不靠客户端时钟） |
| 持久化 | 一切落库 | 掉线 / 刷新后**拉一次快照即可对齐**，结果不会丢 |
| 控权 | RLS | 按 `class_id` / `session_id` 隔离，比 Broadcast「知道频道名就能进」更严 |
| 推送量 | 每次变更一条推送 | 别把高频写入表加入发布，也别推大 payload |
| 断线 | 移动浏览器会冻结后台页面 | 必须有「回到前台重连 + 拉快照」（与版本提醒是同一类问题） |

---

## 四、推荐架构

### 三条铁律

1. **权威在服务端**：一切判定（对错、得分、抢答顺序）走 RPC，前端只发意图。沿用现有 `submit_quiz_submission` 的做法。
2. **推送只做「信号」**：变更事件里只放轻量字段（`session_id` / `type` / 必要 id），客户端收到后**按需拉权威数据**。这样复杂 RLS 过滤也不受 Realtime filter 能力的限制。
3. **一切落库**：实时层只负责「通知」，活动结果、进度、答案全部写表。

### 建议的表（命名可调）

```
live_sessions      一次课堂活动（code / title / host / class_id / state / 当前题号）
live_participants  参与与在线（session_id / user_id / joined_at / last_seen）
live_events        教师指令与广播事件（session_id / type / payload / created_at）
live_answers       学生作答与进度（session_id / user_id / q_index / answer / is_correct / created_at）
```

### 数据流

```
学生加入 → insert live_participants  → 教师端实时看到名单
教师开题 → insert live_events(type='start', q=3) → 全班实时收到 → 各自拉题
学生作答 → RPC 判定 + insert live_answers → 教师端实时看板（正确率 / 未答名单）
```

### 启用步骤（SQL 模板，幂等）

```sql
do $$
begin
  if not exists (
    select 1 from pg_publication_tables
    where pubname = 'supabase_realtime' and schemaname = 'public' and tablename = 'live_events'
  ) then
    alter publication supabase_realtime add table public.live_events;
  end if;
end $$;
```

### 客户端订阅模板

```ts
// 收到「信号」→ 按需拉权威数据
supabase.channel(`live:${sessionId}`)
  .on('postgres_changes',
      { event: 'INSERT', schema: 'public', table: 'live_events', filter: `session_id=eq.${sessionId}` },
      ({ new: row }) => applyEvent(row))
  .on('postgres_changes',
      { event: '*', schema: 'public', table: 'live_answers', filter: `session_id=eq.${sessionId}` },
      () => refreshBoard())
  .subscribe();
```

在线名单另挂一个 Presence 频道（同一客户端可同时挂多个频道）。

### 已知限制（写代码前必须知道）

- 表**必须有主键**，否则无法订阅
- **DELETE 事件默认只带主键**（要拿到完整旧行须设 `replica identity full`，代价更高）
- `filter` 只支持**单列 + 有限运算符**（`eq/neq/lt/lte/gt/gte/in`）；复杂过滤请用「信号 + 拉取」模式
- 订阅**需要该表对订阅者有 SELECT 权限（过 RLS）**，否则收不到

---

## 五、以当前条件可以做的活动类型

### A. 在线状态类（只依赖 Presence，最轻）

| 活动 | 说明 |
|---|---|
| 课堂签到 / 在线名单 | `presenceState()` 直接给出，零额外成本 |
| 举手 / 「我需要帮助」 | 学生 `track({ help: true })`，教师端实时亮灯 |
| 分组名单 | 每组一个频道，成员状态自动可见 |

### B. 全班作答类（Postgres Changes + RPC，最实用）

| 活动 | 说明 |
|---|---|
| **随堂小测实时版** | 教师开题 → 学生作答 → 教师端实时看正确率、选项分布、未答名单 |
| **投票 / 表决** | 实时柱状图，结果落库可追溯 |
| **匿名提问墙** | 学生提交问题 → 教师端实时列表，可加「赞」排序 |
| **限时练习闯关** | 按正确数 + 用时实时排行 |

### C. 竞赛 / 游戏类（数据库时间戳做权威，公平）

| 活动 | 说明 |
|---|---|
| **抢答** | 谁先写进库谁赢（服务端时间戳裁定），比 Broadcast 更公平 |
| **词库对战** | 一对一或分组，判定与计分全在 RPC |
| **拼写 / 接龙竞速** | 复用现有 `spelling` / `chain` 题型，加实时排行 |
| **限时排位** | 同一套题，实时显示全班进度与名次 |

### D. 协作 / 共创类（提交式协作，非同时编辑）

| 活动 | 说明 |
|---|---|
| **全班共建概念图** | 每人提交 1–2 条关系 → 实时合进 `vis-network` 图 |
| 集体提问池 / 头脑风暴 | 提交即可见，可投票收敛 |
| 小组互评 | 各组产出实时可见，互相打分 |
| 共写范文骨架 | 分段提交，实时拼装 |

### E. 教师控制台类

| 活动 | 说明 |
|---|---|
| 实时进度看板 | 谁做到第几题、卡在哪、错在哪 |
| 逐题解锁 / 控节奏 | 教师用 `live_events` 放行 |
| 即时统计反馈 | 讲评时直接放分布图 |

---

## 六、首个落地活动：课堂猜词（描述抢答）

> 形态：随机抽一名学生上讲台，**只能用语言描述、不能说出术语本身**，其余学生在自己设备上输入答案抢答。
> 相对旧的线下做法（随机转盘 + 口头抢答），线上的价值在于 **回答端全员参与 + 自动判定 + 名次计分**。

### 为什么它天然适配当前能力

| 需求 | 用什么 |
|---|---|
| 随机抽人（本场不重复，抽完重置） | 班级名单 − 已抽过者；Presence 判断在线，避开离线者 |
| **术语对全班保密** | 术语放**教师独占**的 `live_round_secrets`（RLS 仅教师可读） |
| 谁在描述 / 谁答对了 | Postgres Changes 实时推送 |
| 答对判定（容错） | 服务端 RPC 查 `vocab_answer_forms`（复用现有 aliases 口径） |
| 名次公平 | 数据库 `created_at` 时间戳 |

### 关键约束：术语字段绝不能放在全班可读的表里

学生客户端**本地就有整套词库**，所以只要 `term_id` 能被学生查到，就等于把答案给了他们。
而学生**必须**能读 `live_rounds`（否则不知道谁在描述）→ 因此：

- ✅ 术语单独放 `live_round_secrets`，**RLS 设为仅教师可读**（描述者在讲台看教师屏，不需要读库）
- ❌ **绝不把术语写进 `live_rounds`**

### 屏幕分工（教师端用扩展投屏，不用镜像）

| 屏幕 | 内容 |
|---|---|
| **控制窗口**（笔记本屏） | 本轮术语（大字）·「换一个」·「开始计时」· 谁答对第几名 · 上一轮结果 · 本场还剩 X 人未抽过 |
| **投屏窗口**（投影屏） | 谁在描述 · 倒计时 · 已提交 / 已答对人数 · 实时前三名 —— **不含术语、不含答案** |
| **学生端**（未被抽中） | 「谁在描述」+ 倒计时 + 输入框 + 已答人数；**永不显示标准答案** |
| **学生端**（被抽中） | 全屏「你被抽中了！请上台描述」；本轮结束显示自己得分 |

实现：控制窗口点「打开投屏窗口」→ `window.open()` → 拖到投影屏 → F11 全屏；两个窗口用 `BroadcastChannel` 同步状态（同源、零成本）。

### 状态机

```
idle       等待开始
drawing    抽人（池 = 本场未抽过者 − 已离线者）
ready      已抽中 X（X 端弹「请上台」）；教师端显示术语，可「换一个」
describing 教师点「开始计时」→ 服务端写 deadline_at = now() + 时长；回答框开放
solved     出现正确答案 → 按提交顺序排名次给分 → 本轮结束
timeout    时限内无人答对 → 本轮无分，教师端明示
```

- 抽完所有人后池子自动重置（小班一节课会走好几个周期 → 每人当描述者的次数天然平均）
- 教师端必须有「**提前结束本轮**」（学生答得快 / 无人能答时用）
- 计时权威：`deadline_at` 由**服务端写**，客户端只显示剩余秒数（防时钟偏差与篡改）

### 表设计

```
live_sessions       一场课（class_id / host_id / title / state / scoring jsonb / created_at / closed_at）
live_rounds         每轮（session_id / round_no / describer_id / state / started_at / deadline_at / solved_at）
live_round_secrets  该轮术语（round_id / term_id）              ← RLS：仅教师可读
live_guesses        抢答记录（round_id / user_id / text / is_correct / created_at）
live_scores         本场累计（session_id / user_id / points / described_count / correct_count / best_rank）
```

抽取池不建表：`select distinct describer_id from live_rounds where session_id = …` 即「已抽过的人」。

### 计分（小班口径：人人有份、快者更多）

**分数 = 基础分（答对即有） + 名次加成**

| 参数 | 默认 | 说明 |
|---|---|---|
| 基础分（只要答对） | **1** | 设 0 = 退回「只有前三名有分」 |
| 第 1 名加成 | **+2** | 合计 3 分 |
| 第 2 名加成 | **+1** | 合计 2 分 |
| 描述者（被猜对才给） | **2** | 固定 |

落到实际班型（本校社会学为小班教学）：

| 班型 | 每轮给分 |
|---|---|
| 5 人（1 描述 + 4 人答） | 3 / 2 / 1 / 1 —— **4 人全有分** |
| 12 人（1 描述 + 11 人答） | 3 / 2 / 1 ×9 —— **答对的几乎人人有分** |

- 参数**在教师端可配置**，一场开始前设好、场中不改（避免榜单口径不一致）
- 两个班各自成榜、不跨班比较（每轮产出总量不同）
- 名次定义：**第 1 / 2 / 3 条「正确」提交**（按服务端 `created_at`）
- 答错**不扣分**、可继续猜（保证学困生也能拿到基础分，不至于整场挂零）

### 容错判定（复用现有 aliases 机制）

现有可接受写法是前端 `answers.ts` 现算的，数据库里没有 → 服务端判不了。因此需**物化一张表**：

```
vocab_answer_forms(term_id, form_normalized)   ← getAcceptableForms 的产物，随词库发布更新
```

RPC 判定即为：

```sql
exists (select 1 from vocab_answer_forms
        where term_id = … and form_normalized = normalize(p_text))
```

好处：前端练习与课堂练习**用同一份 forms**，不会出现「练习判对、课堂判错」。

> ⚠️ **前置改动**：`surnameOverrides`（教师手填的「非常规学者名」覆盖表，如 `bell hooks`）目前**只存在本机 localStorage**，
> 服务端判定拿不到它。**已决定：把它纳入发布内容一起上云**，保证服务端与前端判定完全一致。

### 其它口径

- **时限**：默认 60 秒、可调（小班建议 30–45 秒，节奏更紧凑）；**由教师手动启动**
- **场次**：一节课一场；结束后可存档查看最终榜
- **不接打卡、不接练级**：打卡口径是「课后自主学习」，上课打卡会破坏这个激励；练级体系尚未落地。计分只在本场内排名
- **防抄答案**：`live_guesses` 的 SELECT 只允许本人 + 教师；需要向全班显示「已答对 M 人」时，走一个**只返回计数的 RPC / 视图**

### 边界情况

| 情况 | 处理 |
|---|---|
| 抽中者离线 / 掉线 | Presence 判断，教师端提示「已离线，重抽」 |
| 重复提交 | **只认第一条**（无论对错） |
| 时限内无人答对 | 本轮无分，教师端明示 |
| 描述者说出了术语本身 | 教师可点「违规，本轮作废」（可选功能） |
| 网络抖动 | 断线重连后**拉快照**（本轮状态 + 自己的提交），不依赖增量 |

---

## 七、第二个落地活动：课堂拼写竞赛（Spelling Bee）

> **形态**：教师按轮下发**同一道**默写题，全班同时作答；答错 / 超时出局，存活者进入下一轮。
> **定稿日期**：2026-09-22（全部口径已拍板，未实现）。与第六节共用 `live_*` 基建，差别只在「本题是否存在唯一胜者」。

### 层级与回合流程

```
课（session）→ 回合（结束后全员重置）→ 轮（一轮 = 一题）

淘汰段：多人同场，题目按难度递增
  答对者存活进入下一轮；答错 / 限时内未提交 → 出局
  ↓ 剩 3 人（此时前三名已锁定）
抢答段：一题定胜负，第一个答对者胜出 → 回合结束
  答错即出局；题目对其余人继续开放；3 人全错 → 三人并列第 1
  ↓
第 1 名 = 抢答成功者；第 2、3 名 = 抢答段其余两人（并列第 2）
```

- 一节课若干回合，回合之间**全员重置**（回到池子）；整节课排名 = 各回合前三名积分之和
- 重新集结**回合**（而非"一场淘汰赛打到底"）是刻意的：小班里一场打到只剩 1 人会过长，且早出局者长时间无事可做
- **抢答段与淘汰段共用同一套提交 RPC 与判定**，只多一个「本题是否有唯一胜者」标志

### 题干：固定英文脱敏释义

- 用 `maskAnswer(item, item.definition)`，**不用中文**
- 两条理由：① 词典页检索源 `getSearchableForms`（`answers.ts`）只索引 term / aliases / chinese，**不索引 definition** ⇒ 英文题干天然绕开「输入中文反查英文」这条最方便的作弊路径；② 传统 spelling bee 就是纯英文流程
- **防作弊配套**：锁导航 + 禁用词典入口 + 不接练级与掌握度。注意这只能挡住随手一查 —— 词条整包在客户端，DevTools 可读到 definition 原文，这是无法靠前端防住的固有面
- **公布答案的时机**：本轮 deadline 之前绝不公布；由**服务端**在轮次结束后才放开可见性，不能交给客户端决定
- 设备前提：本校学生上课自带电脑（教师 2026-09-22 确认），拼写靠键盘，不存在移动端公平性问题

### 难度递增（可操作化）

- 代理 = **最短可接受写法的归一化字母数**（不是 term 字面）
  - 依据：学生写出任一可接受形式即算对，所以难度取决于**最容易的那个形式**
  - 必须避免的口径错误：`Intelligence Quotient (IQ)` 按 term 是 22 字母、按可接受写法是 `IQ`（2 字母），方向完全相反
- 完整口径 = `term + aliases + 单复数变体`，再过 `normalizeKey`（大小写 / 标点 / 重音 / `&`→and / `-isation`→`-ization` / `-ise`→`-ize`）
- 学者**不分池**（与术语同池出题）；长短语（如 `Commodification of international education`）**不排除**，自然落到最高档，正好作为后期淘汰率的来源
- **不引入错题记录**：与「课堂活动不喂练习口径」冲突（竞赛自身无法积累）、冷启动、反馈循环、以及个人 vs 全班口径未定

实测分档（云端 v96，663 条术语，完整口径）：

| 最短可接受写法字母数 | 条数 |
|---|---|
| ≤ 5 | 30 |
| 6–8 | 88 |
| 9–11 | 164 |
| 12–14 | 142 |
| ≥ 15 | 239 |

参考数据：aliases 覆盖率 68/663（10%）；术语平均 2.21 个可接受写法（最多 8）；单复数/归一化能额外缩短的仅 9 条。含 `/`、`(`、`=` 的 21 条 term **全部配有干净 aliases**（如 `Multiculturalism (= Cultural diversity)` → `Multiculturalism | Cultural diversity`），可正常出题。

### 出局口径与回合终止

- **限时内未提交 = 出局**；判定留 **1 秒宽限**（RPC 参数 `p_grace_seconds`，默认 1）—— 网络抖动同样影响「准时到达」，与抢答窗同一个理由。宽限只影响「这次提交收不收」，不改变展示给学生的限时
- **答错后不能再提交**（只认第一条，与第六节一致）
- 抢答段答错即出局，但题目对其余人继续开放；**3 人全错 → 三人并列第 1**（他们共同走到决赛段、无人战胜任何人）
- 回合终止：抢答段产生胜者即结束。**不需要「轮数上限 + 人数阈值」的复合条件** —— 名次在「剩 3 人」时已锁定，即便保留硬上限，最坏也只是 3 人并列第 1
- **轮数上限：先不做，但留口子**（教师 2026-09-22 定）—— 做成「新建课堂活动时教师可手动填写的数字」（默认留空 = 不限制）。等课堂实践攒够数据后，统计「通常几轮筛到只剩 3 人」，再回填默认值。参考量级（暂不采用）：题干中位 124 字符 → 约 40–50 秒/轮 → 一个回合 6–8 分钟 → 一节 40 分钟课实际跑 3–4 个回合

### 服务端判定（与前端同口径）

物化 `getAcceptableKeys(item)` 的产物，即**归一化之后的键**（不是 `getAcceptableForms` 的原始写法）：

```sql
vocab_answer_forms(term_id, form_normalized)
-- RPC 判定即：
exists (select 1 from vocab_answer_forms where term_id = ? and form_normalized = public.normalize_answer(input))
```

- **SQL 里不写任何英语形态学规则**：单复数、英式美式、标点重音全部在物化时一次算好；服务端只需对输入跑同一个 `normalizeKey`。这样服务端与前端永久同一口径，「练习判对、竞赛判错」从结构上不可能发生
- **沿用现有放宽**：英式 / 美式拼法互认（`globalisation` 与 `globalization` 同键；词库 38 条含 `-is(e|ation)`）。双写辅音的英美差异（`labelling` / `labeling`）**不互认**，保持现状 —— 对拼写竞赛来说这反而合理
- **抢答段的胜负判定：时间窗并列，不做抢占**（教师 2026-09-22 定）
  - 不做「谁先写入谁赢」的原子抢占：网络抖动会让实际同时答对的两人被硬分出先后（同班同网但设备/链路不同，往返差几百毫秒很常见）
  - 改为**按服务端时间戳的并列时间窗**：以本轮**首个答对者的 `created_at`** 为基准，**基准 + W 毫秒内**提交的正确答案一律算并列第 1
  - **提交永不抢占**：提交 RPC 只做「是否在 deadline 内 + 判定对错 + 写行 + 更新计数」，即使已有人答对也照常接受；胜负在**结算**时聚合得出
  - 结算时机：教师端收到「本轮有人答对」的推送后，等 W 毫秒自动调用一次「结算本轮」RPC（把窗口内的答对者写进 `solved_users`、关闭该轮）；教师也可手动提前结算
  - **W 默认 1000 ms，可配**。W 越大越宽容，代价是抢答段更容易出现并列
  - ⚠ **时间戳必须由服务端 `now()` 生成**：若接受客户端传来的时间，学生改本机时钟即可霸占第 1 名

### 计分

- 每回合前三名得分，**沿用第六节的参数表**（基础分 1 + 名次加成：第 1 名 +2、第 2 名 +1）⇒ 第 1 名 **3** 分 / 第 2 名 **2** 分 / 第 3 名 **1** 分，跨回合累加成整节课排名；参数教师端可配，一场开始前设好、场中不改，与第六节同一原则
- **同一回合内口径会有一次刻意切换**：淘汰段按「正确」决定名次，抢答段第 1 名按「速度」（服务端 `created_at`，且带 W 毫秒并列窗）决定。这是抢答机制的固有语义，不是口径不一致，但需在文档与 UI 里写明
- **并列名次占用顺位**（标准排名法）：抢答段 2 人并列第 1 ⇒ 剩下的那人是**第 3 名**（1、1、3），不是第 2 名

**抢答段的三条收束情形**（已实现）：

| 情形 | 名次与积分 |
|---|---|
| 1 人在窗口内答对 | 该人第 1（3 分）；另外两人并列第 2（各 2 分） |
| 2 人并列答对（同一窗口内） | 两人并列第 1（各 3 分）；剩下的人第 3（1 分）—— 占顺位 |
| 3 人全错（无人答对） | 三人并列第 1（各 3 分） |

> ⚠ 第 1 行与第 3 行对比略反直觉：**全错反而拿 3 分、部分答错只拿 2 分**。这是教师 2026-09-22 明确选定的口径（理由：他们共同走到决赛段、无人战胜任何人）；若日后觉得别扭，可改为「无人答对 ⇒ 并列第 2」或「并列第 1 但记 0 分」。

### 三端界面（教师 2026-09-22 提出投屏需求）

与第六节同一套分工（控制窗口 / 投屏窗口 / 学生端），本活动特有的内容：

| 屏幕 | 内容 |
|---|---|
| **控制窗口**（笔记本） | 本轮术语原文 · 开始 / 结束本轮 · 提前结束回合 · 轮数上限输入框 · 每人的提交明细（含答错内容与耗时，供讲评） |
| **投屏窗口**（投影屏） | ① **存活表**：全班每人一格（存活 / 本轮出局 / 已淘汰）—— 人数 ≤ 20，用网格卡片比表格更有节目感；② **总积分榜**（跨回合累加，回合间隙放大展示）；③ 第几轮 / 回合 · 剩余人数 · 倒计时。**不含术语、不含答案、不含任何人写了什么** |
| **学生端（存活）** | 题干 + 输入框 + 倒计时 + 自己是否已提交 |
| **学生端（已出局）** | 「本轮已出局，等待下一回合」+ 总积分榜（自己名次高亮）。**本轮结束前不显示答案**；结束后显示本轮正确答案（讲评价值） |

- 投屏窗口与教师控制窗口的同步，复用第六节的 `BroadcastChannel`（同源、零成本）
- 投屏表的数据来源 = 存活状态行（`live_participants` 一类的「回合存亡」）+ 跨回合累计的 `live_scores`；**这两张表要加进 `supabase_realtime` 发布**（只推信号，客户端收到后拉权威数据）
- 投屏只显示**姓名 / 状态 / 积分**，不显示作答内容

**已定口径（教师 2026-09-22）**：

1. **淘汰状态到点更新**，不实时 —— 本轮 deadline 到后统一揭晓谁出局，避免全班围观「谁先出局」干扰作答
2. 投屏**不显示**谁写错了、写的是什么；讲评留在控制窗口
3. 学生端**不显示别人的名次**（大屏已有）

### 与第一个活动的关系

- 复用 `live_sessions` / `live_participants` / `live_events` / `live_scores`
- 新增表（命名待定）：轮次题干与期限 / **轮次术语（RLS 仅教师可读）** / 提交记录 / 回合存亡
- 术语绝不能落在学生可读的表里 —— 与第六节同一条铁律

### 配套的存量改造（独立于 `live_*` 基建）

现有拼写题是**中文优先**，与竞赛口径不一致，需同步改造：

| 位置 | 现状 | 改为 |
|---|---|---|
| `app/src/lib/quiz.ts`（随堂测验 / 作业的拼写题） | `prompt: item.chinese \|\| maskedDef`，中文优先 | **随机 / 对半开**：有中文的词条一半中文、一半英文脱敏释义；无中文者（239 条学者）只能英文 |
| `app/src/components/Spelling.tsx`（自由练习） | 中文大标题 + 英文释义小字提示，**两条同时给** | 只给随机选中的那一种 |

- **必须两处同步改**，否则会出现「练习随机、作业中文优先」的口径分叉
- 实际改动落在 4 个文件：`quiz.ts`（题干随机）、`answers.ts`（新增 `pickSpellingPrompt` 统一口径）、`Spelling.tsx`（题干随机 + 只显示选中那一种）、`QuizTaker.tsx` 的 `SpellingAnswer`（原本在题干下方**把中文与释义两条都列出**，等于双份提示，须删）
- **订正练习（`CorrectionPractice.tsx`）刻意保留双提示**，不在本次改造范围 —— 订正的目的是补记忆，给足线索（教师 2026-09-22 确认保留不动）
- **旧作业不受影响**：`buildQuestion` 产出的是题目快照，题干与 `promptLabel` 在创建时已固化，改动只对之后新建的作业 / 测验生效
- **这不是纯展示改动**：一半题目变成读英文定义（术语类中位 124 字符），正确率会实打实下降；而自由练习与作业是喂 `recordItem` / 掌握度的（课堂活动才不喂），练级体系的 XP 获取曲线会因此被整体压平，需一并考虑

---

## 八、做不到 / 不建议

| 类型 | 原因 |
|---|---|
| 实时协作画布、光标同步 | 需要 Broadcast 级低延迟高频通道 + 客户端预测（且 Broadcast 在本实例已坏） |
| 节奏类 / 毫秒竞速游戏 | 受 Postgres Changes 延迟限制，手感不成立 |
| 音视频 | 完全不在一个量级（需 WebRTC 或第三方） |
| 离线多人 | 必须在线；离线只能单人练习 |
| 用 Postgres Changes 推大 payload | 每次变更都会推送，大对象会放大带宽与解析成本 |

---

## 九、待办与报障线索

**前置改动（做活动之前要先完成的）**

- [x] `surnameOverrides` **上云**（**已完成**）：`db-migration-vocab-surname-overrides.sql` 给 `vocab_releases` 加了 `surname_overrides` 列，`cloud.ts` 已读写，`answers.ts` 按 `builtin < 云端发布 < 本机` 三层合并。本文档此前把它列为待办，属**滞后记录**
- [x] 物化 `vocab_answer_forms`（**已完成 2026-09-22**，见本节附）：表 + `normalize_answer()` 已建并执行；`replaceAnswerForms()` 已接入发布流程（另有「重建判定表」按钮）；对拍体检 5508/5508 一致
- [ ] 建 `db-migration-live-*.sql`：按第四节「启用步骤」写幂等 SQL，并把 `live_rounds` / `live_guesses` / `live_scores` 加进 `supabase_realtime` 发布

**存量改造（与 `live_*` 基建无关，可独立进行）**

- [ ] 拼写题题干由「中文优先」改为「随机 / 对半开」：`quiz.ts`（测验 / 作业）与 `Spelling.tsx`（自由练习）**两处同步改**；旧作业是题目快照、不受影响。详见第七节末「配套的存量改造」

**活动一（课堂猜词）实施**

- [ ] 表 + RLS（`class_id` / `session_id` 隔离；**`live_round_secrets` 仅教师可读**）
- [ ] RPC：抽人 / 换词 / 开始计时 / 提交判定 / 结束本轮 / 结算
- [ ] 三端界面：教师控制窗口 · 投屏窗口 · 学生答题端
- [ ] 断线重连的「拉快照」路径

**活动二（课堂拼写竞赛）实施**

- [x] 表 + RLS + 实时发布：`db-migration-live-spell.sql`（2026-09-22 已执行；7 表，其中 4 张进实时发布；`live_round_secrets` 仅教师可读、`live_spell_answers` 仅本人 + 教师可读）
- [x] RPC：`live_open_round`（开轮）/ `live_submit_answer`（判定 + 1 秒宽限）/ `live_start_group`（回合初始化）/ `live_settle_round`（结算，含抢答并列窗）—— `db-migration-live-spell-rpc.sql` + `-rpc2.sql`；**端到端冒烟测试通过**（淘汰段存活/淘汰、抢答段并列与占顺位）
- [ ] 教师端抽题（难度递增：按「最短可接受写法的归一化字母数」排序，跨回合共享已出池，避免重复与抽干）
- [ ] 三端界面：教师控制窗口 · 投屏窗口 · 学生答题端（含淘汰者的「等待下一回合」屏）
- [ ] 答案可见性由服务端在轮次结束后放开

**其它**

- [ ] **若要 Broadcast**（高频/低延迟）：把本文档「关键发现」一节转给托管方——Broadcast 触发 1011、`realtime.messages` 从未写入、控制台 Replication 未实现、服务端迁移停在 2025-09-05
- [x] 确认 Presence / Postgres Changes 可用性（2026-09-11 已完成）
- [x] 确定首个活动形态与全部口径（2026-09-12 已完成，见第六节）
- [x] 确定第二个活动（拼写竞赛）形态与全部口径（2026-09-22 已完成，见第七节）

### 附：`vocab_answer_forms` 物化的具体流程（2026-09-22 已实施）

**为什么需要**：现有判定逻辑（`answers.ts` 的 `getAcceptableKeys`）是纯前端 JS，服务端拿不到；竞赛要在服务端判分，就必须把这套口径搬进数据库。

**三步**

1. **生成**：发布词库时（教师端 `publishVocab` 之后）顺手对 `vocab_releases.data` 的每条词条跑一次 `getAcceptableKeys(item)`，得到归一化键集合。**用前端这一份函数算**，不要在服务端重写 —— 这样与前端判定天然同一口径。
2. **落表**：全表重建（`truncate` + insert，约 2000 行，秒级）。
   ```sql
   create table if not exists public.vocab_answer_forms (
     term_id text not null,
     form_normalized text not null
   );
   ```
   **不放开学生 SELECT**（RLS 不给读权限，或 `revoke`），只由 `security definer` 的 RPC 读。学生本地本来就有词库与 aliases，泄漏不增加信息量，但收紧无害。
3. **判定**：RPC 接收学生输入的**原文**，在服务端归一化后比对。**不能让客户端先归一化再提交** —— 学生本地有整套词库与前端代码，能自己算出正确答案的归一化键直接提交，等于送分。

**真正的难点：服务端也要有一份 `normalizeKey`**

SQL 侧需要与 JS 严格等价的函数（已实施）：

```sql
create or replace function public.normalize_answer(p text) returns text
language sql immutable as $$
  select regexp_replace(
           regexp_replace(
             regexp_replace(lower(coalesce(p, '')), '&', 'and', 'g'),
             '([a-z]+)isation\y', '\1ization', 'g'),
           '[^a-z0-9]', '', 'g')
$$;
```

两个已知坑：

- **`\b` 在 PostgreSQL 里不是词边界**：JS 的 `normalizeKey` 用 `\b`，PostgreSQL 的 ARE 里 `\b` 是退格符、词边界是 `\y`。照抄会**静默失效**（不报错，结果不同）。
- **变音符号**：JS 用 `normalize('NFD')` 去重音，SQL 没有等价物（除非装 `unaccent`，托管实例不一定有）。学生极少输入带重音的名字，但要显式决定「支持还是放弃」。

**对拍验证（已跑通，并保留为长期体检）**：`app/scripts/answer-forms-check.mjs`

- 输入 = 词库全部真实写法（term / aliases / chinese / definition）+ 单复数变体 + 定向变体（`&`、`-isation`、`-ise`、引号、中文混排、超长）+ **自动纳入全部「JS 处理后非空」的预组合字符**
- 结果 **5508 / 5508 完全一致**；改判定规则或 `answer-aliases.json` 之后必须重跑 —— 不一致会以「某个词学生写对了却判错」的形式静默出现

**变音映射的最终做法**（记在这里免得下次重推）：不用手工列表，而是**按 JS 的真实行为生成** —— 对每个候选码点跑一次 JS 的 `normalizeKey`：结果为空 = 两边都会删（不映射）；结果为 1 个字符 = 进 `translate`；结果多于 1 个字符 = `translate` 做不到（实测 0 条，因为 `Ǉ Ǳ` 这类是兼容分解、NFD 不展开）。最终 **735 条**映射，覆盖 Latin-1 / Extended-A / Extended-B / Extended Additional 里全部「NFD 可分解」的字符。**不可分解的（ø ß æ œ đ þ 等）刻意不映射** —— JS 本来就把它们删掉，映射成 o / s / ae 反而会造成不一致。

**已实施的部件**

| 部件 | 位置 |
|---|---|
| 表 + `normalize_answer()` / `normalize_answer_batch()` + RLS（教师可读写、学生无任何策略） | `db-migration-answer-forms.sql`（已对云端执行） |
| 物化函数 `replaceAnswerForms()`：复用前端 `getAcceptableKeys`，**先插新批次再删旧批次**（中途失败不会把表清空） | `app/src/lib/cloud.ts` |
| 发布后自动重建 + 手动「重建判定表」按钮（改静态别名 / 判定规则后重跑用） | `app/src/components/ImportPanel.tsx` |
| 对拍体检脚本 | `app/scripts/answer-forms-check.mjs` |

---

## 附：本次排查用到的验证手法（可复用）

1. **DevPanel → Realtime 连通性自检**：三项独立判定（连接 / Presence / Broadcast），带时间戳日志
2. **psql 直连查服务端状态**：
   ```sql
   -- 分区是否齐全
   select c.relname from pg_inherits i
   join pg_class c on c.oid = i.inhrelid
   join pg_class p on p.oid = i.inhparent
   where p.relname = 'messages' order by c.relname desc limit 10;

   -- 发布里有哪些表（Postgres Changes 的前提）
   select schemaname, tablename from pg_publication_tables where pubname = 'supabase_realtime';

   -- Realtime 服务端迁移版本（判断服务端新旧）
   select version, inserted_at from realtime.schema_migrations order by version desc limit 5;
   ```
3. **端到端验证超简洁**：建一张带主键的临时表 → 加进发布 → 客户端订阅 → 插一行 → 看是否收到 → 用完 `drop`
