# 随堂测验和作业功能 · 定稿方案

> 本文档是「随堂测验 + 作业」功能的定稿方案，实施时以此为准。
> 状态：已定稿（未完全实现）。所有设计决策已与需求方对齐，可直接据此开发。
> 两种模式共用同一套底层机制（云端试卷 + 密码进入 + 防作弊），仅在各维度参数与交互上不同。

## 一、功能目标

教师生成一份随堂测验或作业，得到 4 位数密码；告知学生后，学生输入密码进入并完成；题型混合现有拼写、选择、匹配（未来增加语境填空）。期间配套防作弊措施。

- **随堂测验**：严格限时、当场完成。
- **作业模式**：放宽完成时间（截止时间），但同样限制回主页与记录切换页面次数。

## 二、核心流程

```
教师：创建测试/作业（标题/模式/选题/题量/时限/题型）→ 点「生成」→ 抽题固定快照 + 生成 4 位密码 → 写入 quizzes 表
学生：首页输入密码 → 校验（密码 + open_at）→ 进入 → 锁导航 + 时限 + 乱序 → 作答 → 交卷
教师：查看成绩列表（含切屏记录等可疑指标）
```

## 三、已定稿的设计决策

### 1. 存储方案（决策一）：云端存储（方案 B）
- 新增 `quizzes`（试卷主表）和 `quiz_submissions`（交卷表）两张 Supabase 表。
- 教师生成时把「题目快照」固定存进 `quizzes.questions`（jsonb），所有学生拿到的是同一份权威试卷。
- 不做纯本地生成（本地无法做真正的统一开考/收卷/防作弊）。

### 2. 选题方式（决策二）：两种模式都保留
- **随机抽题（random）**：教师选范围（Paper/单元）+ 题量，生成时系统随机抽并固定。
- **手动指定（manual）**：教师按 Paper/单元筛选后手动勾选具体词条（重要/难词）。
- 两种模式最终都在「生成」那一刻把题目快照固定存进 `quizzes.questions`，学生拿同一份题。

### 3. 题型分配（已定稿）：统一随机分配
- 抽出的词条，按教师勾选的 `question_types` 均匀随机分配题型。
- 第一版不做「按题型分组精确指定」（精细版留待将来）。

### 4. 防作弊（决策三）——分「硬拦截」与「记录」两类

**硬拦截（真正阻止，第一版必做）：**
| 措施 | 说明 |
|---|---|
| 统一开考时间 | `quizzes.open_at` 未到，学生输密码提示「未到考试时间」 |
| 限时自动交卷 | 前端倒计时归零自动交卷；交卷时服务端校验 `started_at + duration_minutes` 兜底 |
| 每人只能提交一次 | `quiz_submissions` 加 `unique (quiz_id, user_id)`，重复提交被拒 |
| 题目/选项乱序 | 学生进入时用 `order_seed` 打乱题目顺序与选择题选项，判分按 itemId 不受影响 |
| 考试中锁死应用内导航 | 组件挂载期间禁用顶部导航、拦截 Android 返回键，防止回首页查「词典」 |

**记录（事后可见，不硬拦截）：**
| 措施 | 说明 |
|---|---|
| 切屏/失焦记录 | `visibilitychange` + `blur` 累计 `leave_count` / `leave_seconds`，交卷上传，教师端可见 |

**技术可行性结论：**
- 网页端能检测到「离开考试页面 / 浏览器失焦」（含打开词典软件导致失焦），但只能事后记录、不能实时阻止。
- 真正的漏洞在应用内部（回首页查词典），用「锁导航」硬拦截比切屏检测更可靠。
- 人脸识别/摄像头监考、锁死设备（禁止切 App）在 WebView/Capacitor 下不可行，不做。

### 5. 切换页面检测（网页端，决策四）

「切换页面」分两个层面，技术可行性不同：

| 层面 | 内容 | 能否实现 |
|---|---|---|
| A. 应用内部切页面（回首页 / 切词典 tab） | 我们 React 应用内的导航（`App.tsx` 控制） | ✅ 100% 可靠，纯前端状态控制，想锁就锁 |
| B. 浏览器层面切标签页 / 失焦（切 tab、开新窗口、最小化、切到词典软件） | 浏览器行为 | ⚠️ 能「检测」不能「阻止」，Chrome/Edge 支持良好 |

**B 层检测机制（Chrome/Edge 桌面网页端确认可用）：**
1. `visibilitychange`（主）——`document.hidden` 反映可见性：切到其他标签页、最小化窗口时触发。
2. `window.blur` / `focus`（辅）——浏览器窗口失焦（点开其他软件、切到词典 App）时触发。

两者组合可覆盖「切标签页 + 窗口失焦」两类行为。这是成熟可靠的技术（在线考试 / 网课防切屏通用方案）。

**边界与容错（必须诚实处理的点）：**
- 只能「检测 + 记录」，不能「阻止」学生切出去（浏览器安全机制不允许网页控制浏览器本身）。
- 移动端 WebView（Capacitor App）的 `visibilitychange` 行为与桌面浏览器有差异，需单独验证。
- 可被规避：第二台设备查词、提前查好等无法检测，属「记录型防作弊」的固有上限。
- 误报容错：系统通知弹窗等极短失焦（如 < 1 秒）需做过滤，避免误判。

### 6. 测验/作业错题与本地错题本的关系（决策五，2026-08-30 定稿 → 方案 B）

**结论：测验/作业交卷后，错题完整录入本地错题本，并同步更新掌握度、打卡（方案 B）。**

- 原决策（2026-08-30 早）为「测验错题不进错题本」，用户复查后改定为**方案 B**：交卷成功后将每道题结果走 `recordItem`（掌握度按题型权重 + 当日打卡 + 错题本）。
- **掌握度封底 0 是既有行为**：`adjustMastery` 已 `Math.max(0, Math.min(100, next))`，不会为负，无需额外改动。
- **实现方式**：`QuizTaker.doSubmit` 在 `submitQuizSubmission` 成功后，逐题遍历 `questions`：
  - 拼写/选择：`isAnswerCorrect(q, answers[q.itemId])` → `recordItem(q.itemId, correct, q.type)`（`q.type` 即 `PracticeMode`，权重与日常练习一致）。
  - 匹配块：每对逐对判断 → `recordItem(pair.itemId, correct, 'matching')`。
  - 只在**交卷成功**时记录（失败重试不重复录入）。
- 对照：普通练习模式（选择/拼写/匹配/crossword）答错会进错题本并更新掌握度，行为与测验一致。
- 待办：`QuizTaker.tsx` 从 `useStore` 解构 `recordItem`，在交卷成功后调用（**待实现**）。

### 7. 教师错题分析/统计功能（已定稿，2026-08-30，待实现）

**目标**：给教师后台提供数据统计，分析高频错题等。

**范围**：
1. **单卷错题排行**：某份测验/作业里，哪些题错得最多（错率 = 答错人数 / 总人数）。
2. **跨卷全局错题榜**：某词条在所有历史测验/作业中的总错率（按 `itemId` 跨卷聚合）——最有教学价值，可指导下学期选题/重点讲解。

**粒度**：
- 词条级（`itemId`）为基础。
- 支持按**题型 / 单元 / 流派**切片归因。

**下钻**：
- 可展开某题看错的学生名单 + 他们的作答 vs 正确答案。
- 沿用现有邮箱脱敏规则（`maskEmail`）。

**技术评估（已确认）**：
- 数据基础已就绪：题目快照 `quizzes.questions`（含 `itemId`）+ 学生作答 `quiz_submissions.answers`（`{ itemId -> 答案 }`），判分复用现有 `isAnswerCorrect` / `matchingCorrectCount`。
- **纯前端**实现：遍历 submissions 用现有判分函数逐题判对错、聚合排序即可。数据量小（每卷 9-20 人），全量拉取几百 KB 级。
- **不需要改数据库、不需要新增表/列、不动学生端、不动判分核心**。

**UI**：QuizManager 新增「错题分析」视图（每卷一个，+ 全局榜入口）。

**状态**：已定稿，**待用户指令触发实现**（实现时机未定，用户明确「听指令」）。

### 8. crossword 加入作业可选题型（已定稿，2026-08-30，待实现）

**需求**：教师布置纸质作业时也布置过 crossword，故把 crossword 加为**作业（homework）可选题型**；**测验（quiz）不用**该形式。

**关键差异（与现有题型）**：
- crossword 是「整块」而非「单题」：一个网格 + 多个词条（类似 matching 块），需存整个 `GeneratedCrossword` 快照。
- 所有学生拿到**同一固定网格**（快照机制），学生端**不能**有"重新生成"按钮。

**数据模型**：
- `QuizQuestionType` 增 `'crossword'`。
- `QuizQuestion` 增可选字段：`crossword?: { grid; clues; height; width }`（复用 `crossword.ts` 的 `GeneratedCrossword`）。

**生成逻辑（quiz.ts）**：
- 题型含 crossword 时，从选中词条（一次作业通常 ≥20 词）中**自动挑选最可能排布成功的 6-8 个词**组成一个 crossword 块。
- 挑选策略：`generateCrossword(items)` 贪心交叉，非所有组合都能成功。生成失败时**自动换词重试**（从剩余候选池补词），尽量保证出一个块；仍失败则降级为其他题型。
- 词条过滤沿用现有规则（词长 3-16、去空格/连字符、多答案随机挑一）。

**计分**：每个 clue（词）1 分，与 `totalPoints` 语义一致。

**教师端题型勾选（QuizManager）**：
- 按 `kind` 区分：`homework` 显示 crossword 选项，`quiz` 不显示。
- 已创建的 quiz 不受影响。

**学生端（QuizTaker）**：
- 渲染 crossword 块时复用 Crossword 网格 UI，但**去掉** paper/unit 筛选、类型筛选、重新生成（快照固定）。
- 作答存储：`answers[clueId] = 填写的字母串`（或整个 grid），接入现有判分/草稿/恢复。

**待办备注**：
- 需把 Crossword 网格渲染抽成可复用组件（当前整体在 `Crossword.tsx`）。
- crossword 块大小 6-8 词，灵活处理，优先保证排布成功率。

**状态**：已定稿，**待用户指令触发实现**（用户明确「听指令」）。

### 9. 语境题型与词条覆盖率的校验交互（决策九，2026-08-31，语境填空 P3 前置设计）

**背景**：语境题库（第一批来自 `语境填空题目池.md`，约 130 道）**不可能覆盖当前词库所有条目**。教师创建测验/作业时若勾选「语境」题型，但随机生成 / 手动勾选的词条中**没有对应的语境题**，会导致该题型无题可出。

**方案**：创建 / 生成题目阶段做「语境题覆盖率校验」——检查勾选语境题型时，当前选中词条中能匹配到语境题的词条数量；若为 0（或低于阈值，待定，如 <1 题），拦截并弹出**提示窗口**，给教师三个出口：
1. **重新生成**：重新随机抽一批词条，尝试覆盖语境题；
2. **重选单词**：回到选题界面，教师手动增删/调整词条；
3. **取消勾选语境题型**：去掉语境，用其余题型正常创建。

**校验粒度**：词条 ↔ 语境题的匹配依据语境题挖空词条（`ContextPassage` 内 `{{术语}}` 对应词库 stableId）；同一语境题可覆盖多个词条（一篇范文含多个挖空词），匹配到 ≥1 个即算覆盖。

**状态**：设计要点已定，待语境填空接入（P3）时实现。

### 10. 语境题形态设计（决策十，2026-08-31）

**命名**：语境填空 → 统称**「语境题」**（或"上下文题"），因为不止挖空一种形态。

**三种形态**：

| 形态 | 呈现 | 作答 | 难度 |
|---|---|---|---|
| 选词填空 | 原文挖空 + 给选项选择 | 点选 | 简单（相当于选择题） |
| 拼写填空 | 原文挖空 | 打字填词（复用 `isCorrectAnswer` 判分/别名容错） | 较难 |
| 原文点击选词 | 原文**不挖空**，附一句描述（如"原文中哪个词描述了……现象"） | 在原文中点击最符合的词 | 考察阅读定位 + 词义理解 |

**数据/判定影响**：
- 三种形态共享同一批 `ContextPassage`（原文 + 目标词），仅作答与判定方式不同 → **教师一次挖空/标注，可复用于三种形态出题**。
- 点击选词形态需存目标词在原文中的位置（词索引或唯一词文本）用于判定；判定复用 `normalizeKey` 容错。
- 与决策九一致：勾选「语境题」时做覆盖率校验（所选词条在语境题库有对应目标词）。

**状态**：设计要点已定，待语境填空接入（P3）时实现。

## 四、两种模式差异（随堂测验 vs 作业）

| 维度 | 随堂测验 | 作业模式 |
|---|---|---|
| 时限 | 严格倒计时自动交卷 | 放宽（截止时间，如数小时/数天） |
| 回首页 / 词典 | 硬锁死 | 硬锁死（同一套锁导航逻辑） |
| 切屏检测 | 记录次数 + 时长 | 记录次数 + 时长（同一套逻辑） |
| 中途退出 | 不允许 | **允许「保存并退出」，稍后回来继续** |

**作业模式的「退出」语义（关键设计）：**
- **合法退出**：点「保存并退出」按钮 → 保存进度，可回来继续，不算违规。
- **违规切换**：点首页/词典 tab、切屏失焦 → 记录次数，教师端可见。
- 用「是否走正规保存退出通道」区分「正常休息」和「偷看答案」。

## 五、数据库设计（Supabase 新增两张表）

### `quizzes`（试卷主表）
| 字段 | 类型 | 说明 |
|---|---|---|
| id | uuid | 主键 |
| code | text | 4 位密码，unique |
| title | text | 测试/作业标题 |
| kind | text | 'quiz'（随堂测验）/ 'homework'（作业） |
| selection_mode | text | 'random' / 'manual' |
| papers | text[] | random 模式的考卷范围 |
| category | text | random 模式的次级主题（可空） |
| units | text[] | random 模式的单元范围 |
| question_count | int | 题量 |
| duration_minutes | int | 时限（分钟）；作业模式为宽松时限或截止时长 |
| question_types | text[] | 'spelling' / 'choice' / 'matching' |
| questions | jsonb | 生成时固定的题目快照 |
| open_at | timestamptz | 统一开考/开放时间（可空 = 随时可进） |
| due_at | timestamptz | 作业截止时间（随堂测验可空） |
| allow_resume | boolean | 是否允许「保存并退出、稍后继续」（作业模式 = true） |
| created_by | uuid | 创建教师 uid |
| created_at | timestamptz | 创建时间 |

### `quiz_submissions`（交卷表）
| 字段 | 类型 | 说明 |
|---|---|---|
| id | uuid | 主键 |
| quiz_id | uuid | 关联 quizzes，on delete cascade |
| user_id | uuid | 学生 uid |
| email | text | 学生邮箱 |
| name | text | 学生姓名 |
| answers | jsonb | 作答记录（含未完成草稿） |
| score | int | 得分 |
| status | text | 'in_progress'（进行中）/ 'submitted'（已交卷） |
| started_at | timestamptz | 开始答题时间 |
| submitted_at | timestamptz | 交卷时间（可空 = 未交） |
| leave_count | int | 切屏/失焦次数 |
| leave_seconds | int | 累计离开时长（秒） |
| order_seed | int | 该学生题目乱序种子 |

- 约束：`unique (quiz_id, user_id)`（每人一份）
- 作业模式：`allow_resume = true` 时，`status = 'in_progress'` 的记录允许「保存并退出」后再次进入继续。

### RLS 规则
- `quizzes`：教师可 insert/update/delete；所有已登录用户可 select（学生凭密码拉题）。
- `quiz_submissions`：学生可 insert/update（交自己的卷、保存草稿）；教师可 select（看成绩）。

## 六、前端改动清单

| 文件 | 改动 |
|---|---|
| `lib/types.ts` | 新增 `Quiz` / `QuizSubmission` / `QuizQuestion` / `QuizQuestionType` / `QuizKind` 类型 |
| `lib/quiz.ts`（新增） | 抽题逻辑、题目快照生成、判分、密码生成、题型随机分配 |
| `lib/cloud.ts` | 新增 `createQuiz` / `getQuizByCode` / `submitQuiz` / `saveQuizDraft` / `listQuizSubmissions` / `listQuizzes` |
| `components/QuizManager.tsx`（新增） | 教师端：创建测试/作业表单（模式 + 随机/手动选题 + 时限 + 题型）、生成密码、查看成绩列表 |
| `components/QuizTaker.tsx`（新增） | 学生端：输密码 → 答题界面（锁导航 + 倒计时 + 乱序 + 混合题型 + 切屏计数 + 作业模式保存退出）→ 交卷 |
| `components/Home.tsx` | 学生首页加「随堂测验/作业」入口（输密码框） |
| `components/AdminPanel.tsx` | 教师后台加「随堂测验/作业」tab |
| `App.tsx` | 答题中拦截导航/返回键（锁导航逻辑） |

## 七、关键实现细节

1. **密码生成**：4 位数字随机，查重后写入（`code` 唯一约束，冲突重生成）。
2. **题目快照**：生成时按 `selection_mode` 抽题，每道题存为结构化 `QuizQuestion`（type / itemId / prompt / answer / 选项），保证所有学生同一份题。
3. **乱序**：学生进入时用 `order_seed` 打乱题目与选择题选项，判分按 itemId 对答案，不影响正确性。
4. **限时 + 自动交卷**：前端倒计时归零自动交卷；服务端校验 `started_at + duration_minutes` 兜底。
5. **锁导航**：通过全局状态（如 store 的 `inQuiz` 标志）让 `App.tsx` 导航栏禁用 + 返回键拦截，提示「答题进行中」。
6. **切屏记录**：`visibilitychange` + `blur` 累计 `leave_count` / `leave_seconds`，交卷上传；极短失焦（< 1 秒）过滤容错。
7. **题型分配**：抽出的词条按勾选的 `question_types` 均匀随机分配；拼写用 `isCorrectAnswer`、选择复用 `MultipleChoice.buildQuestion`、匹配复用配对逻辑。
8. **作业模式保存退出**：`allow_resume = true` 时提供「保存并退出」按钮，写入 `status = 'in_progress'` 草稿；再次进入恢复进度；「保存退出」不计违规，点导航/切屏计违规。

## 八、分期交付

| 阶段 | 内容 | 状态 |
|---|---|---|
| P1 + P2 | 核心流程 + 防作弊（统一开考 / 每人一次 / 乱序 / 锁导航 / 切屏记录）一次性实现（含随堂测验 + 作业模式） | 待开发 |
| P3 | 语境填空接入（等 `ContextPassage` 练习完成后） | 暂缓 |

## 九、待办备注

- 手动指定模式（manual）第一版做「题型统一随机分配」，不做按题型分组精确指定。
- 切屏检测定位为「威慑 + 可疑指标」，不做硬性拦截；需过滤 < 1 秒的极短失焦。
- 语境填空依赖 `ContextPassage`（`lib/types.ts` 已定义、`storage.ts` 已有存储，但练习组件尚未实现）。
- 移动端 WebView 的切屏检测行为需单独验证（本方案主要针对桌面 Chrome/Edge 网页端）。
