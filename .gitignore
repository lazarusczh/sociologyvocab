# macOS
.DS_Store

# Windows
Thumbs.db
desktop.ini

# Editor / IDE
.vscode/
.idea/
.codebuddy/
.freebuff/

# 环境变量（含 Supabase 连接信息与密钥，不纳入公开备份）
.env
app/.env

# Excel 词库源文件（教师知识产权，不纳入公开备份）
*.xlsx

# 语境填空教学范文（含答案版，教师知识产权，不纳入公开备份）
A1*.docx

# 由 Excel 生成的内置词库数据（同属知识产权，不纳入公开备份）
app/public/vocab-data.json

# 预抓取的社会数据（结婚率/离婚率等，教师 IP，由 scripts/fetch-social-data.mjs 生成）
app/public/social-data.json

# 教师本地重置码工具与私钥（绝不纳入公开备份）
reset-tool/
teacher-private-key.json

# 教师参考资料（2026-08-31 起集中存放于 Materials/，含 PPT 打印版、9699 范文、作业 docx、
# Name sheet / 词汇表 xlsx 等全部教学素材与知识产权，不纳入公开备份）
Materials/

# 教师课件与素材（旧散落路径与提取文本目录，仍属教师知识产权，不纳入公开备份）
A1 Paper*.pdf
A2 Paper*.pdf
pdf-text/
ppt-text/
pdf-libs/
unit-tags-backup/

# 打包生成的安装包（内含内置词库数据，属于教师知识产权，不纳入公开备份）
*.apk

# 设备相关的生成产物（Capacitor 在平板等具体设备上 sync 生成，文件名带设备名后缀）
**/*TABLET-*
app/package-TABLET-*.json
app/package-lock-TABLET-*.json
app/android/app/src/main/assets/

# 教师工具脚本（含飞书 open_id 等个人身份信息，不纳入公开备份）
app/scripts/send-apk.ps1

# 自动化诊断日志（本地临时产物）
@AutomationLog.txt

# 本地开发服务器日志（Vite dev server 输出，本地临时产物）
dev-server.log

# 数据库备份文件（含业务数据，不纳入版本控制；已存于本地/OneDrive）
backups/

# 词库诊断/归并报告（含业务数据与归并建议，不纳入公开备份）
theory-diagnostic-report.md
theory-cleanup-list.md

# 流派/理论关系设计决策文件（教师教学设计与知识产权，不纳入公开备份）
流派设计决策.md

# UI 设计综合文件（教师产品设计想法与知识产权，不纳入公开备份）
UI设计.md

# 语境填空题目池与提取文本（含全部答案，教师教学 IP，不纳入公开备份）
语境填空题目池.md
cloze-extract.txt

# 语境题语料（高分答卷/范文整理，教师教学 IP，不纳入公开备份）
语境语料.md

# 语境题内置题库（由题目池生成，含答案，教师教学 IP，不纳入公开备份）
app/public/cloze-data.json

# 语境题中间源数据与试验草稿（答卷源数据/挖空试验记录，教师教学 IP，不纳入公开备份）
cloze-p4.json
挖空试验.md

# mark scheme 切段数据（CIE 官方 ms 派生，含题干原文与评分细则，教师教学 IP，不纳入公开备份；
# 由 pdf-text/extract_ms_all.py + parse_ms.py 生成）
ms-data.json
pdf-text/ms/

# UI 设计参考图（Ardot 画布导出 PNG，教师产品设计想法与知识产权，不纳入公开备份；
# 同目录的 README.md 索引与 tokens.css 设计规范入库，便于开发对照）
ui-refs/*.png

# Agent 技能包与本地工具缓存（第三方 skill 源码、浏览器/Worker 本地状态，非本项目产物）
.agents/
.workbuddy/
.playwright-cli/
app/.wrangler/
skills-lock.json
m1.yaml

# 临时诊断截图（一次性排查用）
14258f93-ade4-4e34-9ff7-54fca2daa11d.png
ScreenShot_2026-09-05_215554_347.png

# 知识站语料清单（含本机绝对路径）与导入中间产物
app/scripts/skill-sources.local.json
app/scripts/skill-content*.json

# 参考书目抽取的中间产物（含教材被引原文片段，属教师知识产权）
app/data/refs-candidates.json