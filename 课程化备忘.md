# 课程化（把练习资产串成线性课程）· 讨论备忘

> **状态**：长远规划备忘，**未立项，不动代码**。
> **记录日期**：2026-09-14
> **性质**：本文分两部分——① **现状盘点**（重新摸一遍成本很高，故存档）；② 讨论中定下的**原则**与**待定问题**。
> **相关**：`模拟调研方案.md`、`练级与奖励体系方案.md`、`随堂测验和作业功能.md`、`实时多人在线功能规划.md`

---

## 一、目标

现在 app 的导向是「练习」（练什么随你挑）。对**自学学生**而言，按主题一步步推进是必要的。

课程化 = 把已有资产（词条库、闪卡、各练习模式、真题、模拟调研、未来的写作练习）按单元串成**可推进的线性路径**。

---

## 二、现状盘点（2026-09-14）

### 已有：骨架其实在

- **29 个单元**，三级层级 `paper → category → unit`；权威清单 = `app/src/lib/unitMapping.ts` 的 `UNIT_ORDER` 常量（L17-26）
  - **Paper 1**（11）：社会理论 / 社会化和社会控制 / 社会认同基础 / 阶级认同 / 性别认同 / 族裔认同 / 年龄认同 / **研究方法 / 研究设计 / 方法论取向 / 研究议题**
  - **Paper 2｜Family**（5）、**Paper 3｜Education**（7）、**Paper 4｜Globalisation**（4）+ **Media**（2）
- **单元顺序**：常量默认 → localStorage `socio_vocab_unit_order` 覆盖（教师可增删 / 改名 / 上下移）→ 随发布写进 `vocab_releases.unit_order` 下发
- **词条挂单元**：`VocabItem.unit` 是 `string[]`，**可跨多个单元**
- **单元筛选已普及**：11 个组件接入了 `components/CategoryFilter.tsx`（`filterByPaperCat`）
- **单元级聚合能现算**：`ProgressView.tsx` 现算 `{total, mastered, unlearned}`——但**不落盘**

### 缺：三样

| 缺口 | 现状 |
|---|---|
| ① **单元级进度** | 只有**词条级 mastery** + **日级打卡**两套互不相连的度量；单元聚合不持久化 |
| ② **"活动完成"事件** | **最大的结构性缺口**。所有正式练习只经 `recordItem(itemId, correct, mode)` 汇流，只产出"这个词更熟了"，**没有"这个活动完成了"的记录**；闪卡 / Wordle 完全不结算 |
| ③ **节点编排** | 哪个单元做什么、什么顺序、什么条件进下一节——**零落地**（全仓搜 lesson / course / chapter / unlock 无命中） |

### 其它关键事实

- `quizzes.units` 是**唯一**能把"活动"绑定到"单元"的现成字段
- `syllabus` 只是 `vocab_releases` / `classes` 上的文本列（默认 `'9699'`），**前端从不读取**，是休眠预留
- `练级与奖励体系方案.md` 是唯一相邻的设计稿（XP 挂 `recordItem`），**未实现**
- 教材知识库子站的"章"只用于 AI 检索语料组织，**无顺序、无进度**

---

## 三、已定原则（讨论中定下，重要）

### 3.1 归属看"能力"，不看"素材"

**不是"所有新功能都挂 unit"，而是：属于现有单元体系的才挂。**

| 对象 | 归属 |
|---|---|
| **模拟调研（课题包）** | **挂方法论单元**（Paper 1｜研究方法 / 方法论取向 一类），**不挂主题单元**。理由：主题是"借"来的素材，内核是"定量 vs 定性的优缺点"，属方法论 |
| **写作练习**（尚未讨论） | **超出目前单元范畴，独立新建体系**，不塞进 `UNIT_ORDER` |

由此得到的通用原则：

1. 活动的主归属 = **它教的"能力"落在哪个单元**，不是它用的"素材"来自哪个主题（主题降级为**素材标签**）；
2. 不属于现有体系的（写作）→ 独立建，但**要登记**（是什么、将来课程层怎么引用它），否则将来课程引擎要同时认识 N 套互不相通的分类；
3. 目的只有一个：**所有可教学的东西，在课程层有一个统一的引用坐标。**

> 这条修正了先前"给所有新功能加 `paper/category/unit` 字段"的说法——模拟调研该挂方法论单元，写作则完全独立。

### 3.2 排列是展示层，不强制线性（2026-09-14 定）

**课程按线性排布显示，但不强制线性学习**——允许学生跳，实际上也允许教师跳。

起因：**写作训练这类内容不属于任何位置**——任何时候都可能要教要学，放进线性序列"放哪儿都不对"。

由此确定：

- **"顺序"是展示 / 建议层，不是门禁层**：只存"建议顺序"，**不存"前置条件"**；
- **不实现"解锁 / 通关"**（推论：课程数据结构里"顺序"只是显示属性）；
- 教师同样不被顺序绑住（备课 / 授课按需取用）。

> 这条把先前的"线性 vs 可跳"从**待决风险**升格为**已定原则**（见第六节 #1）。

---

## 四、活动异质性 → 决定课程引擎的形状

| 类型 | 例子 | 判分 |
|---|---|---|
| **判分型** | 拼写 / 选择 / 匹配 / 填空 / 接龙 | 有对错，走 mastery + 打卡 |
| **完成型** | 闪卡过一轮、**模拟调研做完**、写作交稿 | **只有"完成"，无对错** |

**结论**：课程节点需要一个新的**"完成"事件模型**，**不能复用 mastery**（与"模拟调研不进掌握度"的决定天然一致）。

---

## 五、三套进度的分工（防口径混战）

- **mastery** = "点"的度量（这个词我熟不熟）
- **打卡** = "日"的度量（今天我练了没有）
- **课程进度** = "线"的度量（我推进到哪了）
- 三者不冲突，但**不要互相代偿**。

---

## 六、风险与设计问题

1. ~~**线性 vs 可跳**~~ → **已定原则，见 3.2**：排列只是展示层，不强制线性（学生、教师都可跳）。仍要注意：这与测验的"锁导航"是**不同语义**，别共用机制。
2. **完成判定**：阈值低 → 刷完就走；高 → 卡住。主观活动（写作 / 模拟调研）无法判分 → 只能"完成型"。**第一版建议只做"完成型"，不做"必须达标才解锁"。**
3. **成本 90% 在内容不在技术**：编排表谁写是真成本 → 起点应是**让编排自动 / 增量**（每单元自动列出"本单元词条 + 可用活动"），而非先人工写完整课程表。
4. **别过早造引擎**：29 个单元 + 有限的活动类型，先做"单元学习页"即可，不要建通用 course engine。
5. **教师课 vs 自学课要分账**：`quizzes` 是教师主导（密码 / 统一开考 / 防作弊 / `units` 定向）；课程化服务自学。**进度数据要分开**，否则一次随堂测验会搅乱自学路径。测验可当课程的"检查点"，但数据隔离。

---

## 七、最小可行的第一步（若立项）

**单元学习页**——几乎零新表：把该单元的（词条 / 掌握度 / 可用练习类型 / 相关真题考点）聚合成一个页面，给出"建议先做这个再做那个"（**建议顺序，非门禁**）。

真正作用是**验证唯一未验证的假设：学生真的会顺着走吗？** 数据全现成（除编排建议），成本极低，成了再谈引擎。

---

## 八、待定问题

1. 模拟调研挂哪个方法单元：**研究方法** / **方法论取向** / **研究设计**（`unit` 本就是数组，可多挂）。
2. 写作练习的分类体系长什么样（维度、粒度），**完全未讨论**。
3. "活动完成"事件的落点：本地（`socio_vocab_*` 系列）还是云端？课堂活动不喂练习口径，那课程完成事件是否要上云（跨设备）——未定。
4. 单元学习页的"建议顺序"是人工写还是自动生成，未定。

---

## 附：与其它文档的交叉

- `模拟调研方案.md`：本文决定其课题包**挂方法论单元**——已于 2026-09-14 记入该文档"六、课题包"。
- `练级与奖励体系方案.md`：XP 与本"课程进度"是两套东西，需协调（若都挂 `recordItem` 会纠缠）。
