# 词库上云与同步 · 定稿方案

> 本文档是「词库上云 + 学生同步 + 教师词条管理」的定稿方案，实施时以此为准。
> 状态：已定稿（未实现）。实施时逐阶段进行，每阶段完成后勾选。

## 一、范围

**包含（本次要做的）**
1. 词库上云：教师改词库后「发布」一个版本
2. 学生同步：启动 + 回首页时拉取最新词库
3. 稳定 id 迁移：把随机 id 换成确定性 id，保证改词/重发布后学生进度不丢
4. 教师词条管理：新增「单加一个词」，现有 Excel 导入改名「批量导入」
5. 教师编辑单个词条的答案容错（可接受答案/别名，即 B 方案）

**不包含（暂缓）**
- Syllabus（科目）层级、教师动态增删 Category/Unit —— 见「九、未来方向」

## 二、目标

教师增删改词库后，能发布一个「新版本」，所有学生自动同步到最新词库，同时各自学习进度（进度/打卡/错题本）不丢失。

## 三、核心数据流

```
教师：改词库 → 点「发布」→ 整份词库 JSON 存进 Supabase（版本 +1）
学生：启动 / 回首页 → 查最新版本号 → 若比本地新 → 静默拉取 → 覆盖本地词库
        └─ 学习进度按「稳定词条标识」继续对得上
```

词库是「共享只读」数据，整体发布、整体拉取，不做逐条同步。

## 四、数据库设计（Supabase 新增一张表）

**`vocab_releases`（词库版本表）**

| 字段 | 类型 | 说明 |
|---|---|---|
| id | uuid | 主键 |
| version | int | 递增版本号 |
| data | jsonb | 整份 `VocabItem[]`（含 paper/category/unit/aliases） |
| note | text | 版本说明（可选，如"新增 N 词"） |
| published_by | uuid | 发布教师 uid |
| published_at | timestamptz | 发布时间 |

**RLS 规则**
- `teacher_roles` 里的教师 → 可 `insert`（发布）
- 所有已登录用户 → 可 `select`（拉最新版本）

> 现有 `student_data` 表继续存「学习数据」，与词库分开，互不干扰。

## 五、关键设计点

### 1. 稳定 id（最重要）
现状：教师导入时词条 `id` 随机生成，同一词每导入一次 id 就变，会导致学生进度关联断裂。
方案：改用**确定性 id** —— 用 `category + type + term`（归一化后）生成稳定 id。同一词永远同一个 id，学生进度永远对得上。内置词库的 `term_7` 这类也一并迁移成稳定 id。

### 2. 版本判定
学生本地缓存「当前词库版本号」，查云上 `max(version)`：一样则不拉，更新则拉。

### 3. 增删改迁移规则
- **加词** → 学生进度里没有它的记录，无冲突，新词直接出现。
- **改词**（改释义/unit）→ id 不变，进度原样保留。
- **删词** → 学生进度里出现「孤儿 id」，保留不删（不崩），进度页可忽略，或定时清理。

### 4. unit 标签的归宿
词库上云后，`unit` 跟着词条数据一起走（不再依赖编译进 App 的静态 `unit-mapping.json`）。教师维护 unit → 发布 → 学生拿到带 unit 的词条。

### 5. 答案别名（aliases）的归宿
答案容错分三层，判定时取并集：
1. **全局规则**（大小写/空格/连字符/标点/重音、-ise/-isation → -ize/-ization、单复数互认）：代码统一处理，永远自动生效，不存数据。
2. **自动拆别名**（运行时从 term 派生）：自动拆分 term 里「括号 / 斜杠 / 等号」分隔的多个写法，并识别「括号内缩写」（如 `Intelligence Quotient (IQ)` → 自动接受 `IQ`；`Anti-Globalisation Movement (AGM)` → 自动接受 `AGM`）。
3. **手动别名**（`aliases?: string[]` 字段）：教师手动补充自动拆不出的特殊写法（如去修饰词、机构说明提取），随词条 data 发布。

词库上云后，`aliases` 随 data 发布，教师编辑 → 发布 → 学生同步即时生效，无需 rebuild + 部署。`answer-aliases.json` 收敛后保留为内置兜底（离线/未同步 fallback）；其中能被「自动拆别名」覆盖的条目不再需要手动维护。

## 六、同步时机（共识：检查/拉取分离）

核心原则：**「检查」和「拉取」分开**，检查极轻量，可频繁；拉取只在版本变化时做。

| 动作 | 时机 | 成本 |
|---|---|---|
| 查版本号 `max(version)` | App 启动时 + 每次回首页时 | 极轻（一个数字） |
| 拉整份词库 | 仅当版本号 > 本地时 | 才发生一次（几十 KB） |
| 首页 banner | 拉取成功后顶部提示「词库已更新到 vX（+N 词）」 | 无 |

- **自动静默拉取**：词库小，后台静默拉，学生无需额外操作；banner 只做「告知」，不做「逼点」。
- **离线兜底**：拉取失败时用本地缓存 + 内置 `vocab-data.json`，不影响使用。
- 已知边界：App 一直开着不回首页的极端场景不会实时感知更新，可接受（不做实时推送）。

## 七、教师词条管理

### 1. 词条管理（浏览 + 增删改）
在教师后台新增「词条管理」页（或 tab），提供完整 CRUD：
- **浏览**：按「考卷 → 主题 → 单元」筛选，列出所有词条（术语 + 学者）
- **新增**：单加一个词
- **编辑**：点某个词条，改 term / 中文 / 释义 / paper / category / unit（多选）/ 可接受答案
- **删除**：删除某个词条

表单字段（新增/编辑共用）：
- 类型：术语 / 学者
- 术语名（英文）/ 中文（学者可空）/ 释义
- 考卷（Paper）、主题（Category）、单元（Unit，多选）
- 学者额外：理论流派、备注
- 可接受答案（aliases，答案容错）

需要一个**「unit 选择器」组件**：按「考卷 → 主题 → 单元」层级展开勾选，此组件也复用于「单加词」和「编辑词条」。

### 2. 批量导入（改名）
现有 ImportPanel 的「导入文件」区块标题改为「批量导入」，功能不变（Excel 多文件、追加/替换、特殊姓氏确认）。导入时自动识别 paper（按 sheet 名）/ unit（按映射表）/ 别名（按自动拆分规则），**不设校对预览步骤**；识别错或需要补充的，事后再到「单加一个词 / 编辑词条」里单独改。

### 3. 答案容错（自动拆分 + 手动别名）
答案容错分「自动」和「手动」两部分：
- **自动拆别名（P4）**：运行时自动从 term 拆「括号 / 斜杠 / 等号」+ 识别「括号内缩写」，无需教师维护。
- **手动别名（P3）**：给「单加一个词」和「编辑词条」提供「可接受答案」编辑框，教师增删自动拆不出的特殊写法（同义词、特殊拼写等），写进 `aliases?: string[]` 字段，随发布同步。

全局规则（大小写/单复数/-ise）仍自动生效，无需维护。

## 八、分阶段实施

| 阶段 | 内容 | 依赖 |
|---|---|---|
| P0 | 教师端「发布」按钮 + 词库存进 `vocab_releases` | 无 |
| P1 | 学生端启动/回首页查版本 + 拉取 + 离线兜底 + 首页 banner | P0 |
| P2 | 稳定 id 迁移 + 进度关联（一次性数据迁移） | 无（可与 P0 并行） |
| P3 | 教师端「词条管理 CRUD」（浏览 + 单加 + 编辑 + 删除）+ 答案容错编辑（VocabItem 加 `aliases` 字段 + 编辑 aliases + 判定读 aliases + 迁移 answer-aliases.json）+ unit 选择器 + 导入改名「批量导入」 | P0、P2 |
| P4 | 答案容错自动拆分：`answers.ts` 加「自动拆别名」逻辑（括号/斜杠/等号 + 括号内缩写），从 term 自动派生多答案，减少手动别名 | 无（独立） |

> P0/P1 应一起做（否则发布了没人能收到）；P2 是关键正确性；P3（词条管理 CRUD + 答案容错编辑）在发布（P0）和稳定 id（P2）就绪后做——实现后教师可在线改词条属性（paper/category/unit）和答案容错（aliases），不再依赖改静态文件重新构建；P4（自动拆别名）独立、可与任何阶段并行，实现后含括号/斜杠/等号的词条自动多答案，大幅减少手动别名。

## 九、未来方向（暂缓，不在本方案执行范围）

多科目支持 = 把「社会学专用」的课程结构改造成「每个老师可自定义」的通用结构。待办清单（按成本从低到高）：

1. **数据模型加「科目」层（便宜，增量）**：词条加 `syllabus` 字段；`paper`/`category`/`unit` 变成该科目下自由定义的层级，不再假设 Paper 1-4。
2. **课程结构「数据化」（核心，中高成本）**：现在写死的社会学结构（4 张考卷、主题、`UNIT_ORDER`、`unit-mapping.json`）改成存进数据库的「课程结构表」，每个科目一份，教师可增删改。
3. **筛选三层 → 四层（机械但面广）**：`CategoryFilter` + 7 个练习组件 + 词典，从「考卷→主题→单元」变「科目→考卷→主题→单元」。
4. **导入/单加词适配**：导入时先选「导入到哪个科目」，再按该科目结构自动识别 paper/unit。
5. **词库上云按科目隔离**：`vocab_releases` 加 `syllabus` 字段，按科目发布/同步。
6. **教师端「课程结构管理」界面**：教师自定义「科目有哪些考卷、每卷有哪些单元」的 CRUD。
7. **学习数据按科目隔离**：学生进度/打卡/错题本跟科目走。

核心：大头不是「加 syllabus 字段」（便宜），而是「课程结构从硬编码改成可配置数据 + 教师 CRUD + 四层筛选」。

前提：本方案 P0-P4 完成后，且确认有其他科目教师使用，再启动。
