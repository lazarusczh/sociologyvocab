# 组卷器 —— 数据模型初稿

> 状态：初稿（2026-09-04）。配套背景见 `组卷器方案.md`（换算算法 2.1、阈值推导 2.2/2.3、P0/P1 分数线、题型语义）。
> 原则：检索字段与题干分离；分值数值化可求和；兼属用数组；保留每题真实来源（系列+variant）以支撑 P1 分数线与 mark scheme 定位。

---

## 1. 题目库（由 5 份 md 结构化而来）

数据源：`Paper1真题-按考点.md`、`Paper2/Paper3/Paper4Globalisation/Paper4Media真题-历年切题(至S26).md`。

```ts
/** 原始出处：定位真实卷面 + 反向带出 mark scheme */
interface SourceRef {
  session: string;        // 'S26' | 'M26' | 'W24' | '2021 Specimen 1'
  paper: 1 | 2 | 3 | 4;   // Paper 几
  variant: 1 | 2 | 3 | 0; // variant；Specimen 用 0；中国考区惯例 3
  q: string;              // 大题号：'1'..'5'（P4 为 '1'..'6'）
  part?: 'a' | 'b';       // 拆分到小问时使用（Q2/Q3）
}

/** 一条可检索、可组卷的题目（≈ md 里一行） */
interface BankItem {
  qid: string;                       // 稳定 id：`${session}-${paper}${variant}-${q}[-${part}]`
  source: SourceRef;
  kind: 'plain' | 'statement-pair' | 'statement-side';
  /** 题干原文（展示用），已清洗成单行、规范化引号 */
  stem: string;
  /** Q3 类（statement-pair/side）的命题语句，独立存，供抽单侧时随带 */
  statement?: string;
  /** 题型=题干形态，供模板判断 */
  qtype: 'describe' | 'explain' | 'statement-explain' | 'evaluate';
  /** 原始分值结构（可求和用） */
  marks: { total: number; parts?: { part: 'a' | 'b'; marks: number; side?: 'for' | 'against' }[] };
  /** 考点路径（数组支持“兼属两类”） */
  topics: string[];
  /** 考点“标准表述”（对应换算表/推荐的 Paper 类型，如 26 题/35 题/小题），供阈值匹配 */
  examType?: 'paper1' | 'paper2' | 'paper3' | 'paper4';
}
```

要点：
- `qid` 要能应对「同一题干跨 QP11/13 重复出现」——两者是不同真实卷面的题，各留一条；界面可按题干聚合去重显示。
- md 里的合并写法（如 `(W23 QP11 & QP13 Q3)`）转 JSON 时应**展开为多条**（各带自己的 source），避免丢 variant。
- `topics` 直接来自 md 的“考点标题路径”（如 `['社会认同','②仅场所','peer group']` 或 P4 Media 的 `['Media (mis)representations','一般/多群体']`）。

**实况（2026-09-05 重建）**：根目录 `question-bank.json`，**680 条**（Paper1=232 / Paper2=192 / Paper3=132 / Paper4=124），由两条可复现脚本生成：
1. `app/scripts/generate-question-bank.mjs`：5 份切题 md → 题库（Specimen 与 variant4 遗留老卷一律不收）；
2. `app/scripts/enrich-bank-q-from-ms.mjs`：结合 `ms-data.json`（137 卷官方 ms 分节，由 `pdf-text/extract_ms_all.py` + `parse_ms.py` 生成）按题干反查补全题号。

实现差异/修正（vs 本页接口）：
- `topics` = md 章节路径中文标签，兼属/重复行取并集去重；P1 方法区加粗小标题（如 结构化/半结构化）不并入；
- `marks` 保留原文串 + `marksTotal` + `kind`；`10+6` 拆 `parts`（a[10]·for / b[6]·against）；
- 题号：P2/P3 原不带 Qn，现由 ms 反查补全（P2: 4→Q1、6/8→Q2、10+6→Q3、26→Q4/Q5；P3: 4→Q1、8→Q2、12→Q3、26→Q4）；同卷同题号多条不同题干时 qid 追加 stem 哈希后缀保唯一；
- qid 用下划线分隔（`${session}_${comp}_q${q}`），与上文连字符写法不同；`statement` 已修复弯撇号截断。

## 2. 组卷（作业 / 测验 / 模考）

```ts
type PaperMode = 'quiz' | 'homework' | 'mock';   // 随堂测验 / 作业 / 模考

/** 预设模板 = 对齐真题卷面（P1/P2: 60，P3: 50，P4: 70） */
interface PaperTemplate {
  id: 'paper1' | 'paper2' | 'paper3' | 'paper4';
  label: string;
  slots: SlotTemplate[];   // 描述“每槽分值/题数/是否 EITHER/OR 等”
}

interface SlotTemplate {
  key: string;               // 'q1[4]' | 'q2a[8]' | 'q2b[6]' | 'q3[10+6]' | 'q4/5[26]x2选1' …
  marks: number;
  count: number;             // 通常 1
  alternatives?: 'either-or'; // Q4/Q5、P4 两 section 各一对
  kindFilter?: BankItem['kind'];  // 模板对题型有约束
}

/** 一次组卷的草稿/已发布卷 */
interface PaperDraft {
  id: string;
  title: string;
  mode: PaperMode;
  templateId?: string;            // 用模板时记录
  targetRaw: number;              // 目标原始分（模板=60/50/70；自定=如 20）
  items: AssembledItem[];
  scoreConfig?: ScoreConfig;      // 判分换算参数（见 §4）
  status: 'draft' | 'published';
}

/** 卷内一条题 */
interface AssembledItem {
  itemRef: { qid: string; mode: 'whole' | 'side-a' | 'side-b' }; // side-* 仅 statement-pair 可选
  raw: number;                    // 计入总分的原始分（whole=total；side-a=10；side-b=6）
  text: string;                   // 生成到试卷上的文本（side 模式=statement+对应指令）
}
```

组卷约束（写入实现前要过的用例）：
- **凑分**：给定 `targetRaw`，从可用分值块（4/8/6/10/6/16/26/35…）选题凑齐；10/6 单侧与 16 成对是不同“分值块”，由教师意图决定（见 §5-1）。
- **模板模式**：`statement-pair` 的 (a)+(b) 强制成对（模考）；减量作业可抽单侧；`either-or` 槽提供两道候选（可跨卷拼配）。
- **重复率限制**：模考启用同考点命中上限、同卷多题连出上限；作业默认不设限或弱限制（见 §5-2）。

## 3. Mark scheme 配套

```ts
/** ms 数据 = 卷级文本 + 按题号切段（不做 topic 切分） */
interface MsPaper {
  session: string;
  qp: string;                     // 如 '23'、'SP1'
  sections: MsSection[];          // 按卷面题号
}
interface MsSection { q: string; part?: string; text: string }

/** 组卷结果自动配套导出（PDF；按“单题”切 ms 段再拼接） */
interface MsBundle {
  forPaper: string;                       // 组卷标题
  items: { text: string; ms: string }[];  // 每题题干 + 该题切出的 ms 段
}
// 实现说明：MsPaper.sections 已按题号切段，依每题 qid 取对应段即可；
// 未进卷的题不取；同卷多题各自取段拼接，无需“同卷合并”特判。输出 PDF。
```

## 4. 判分换算（score conversion）

```ts
/** 一张“等级带”表：A*..U，含 CIE 原始分阈值与学校分阈值 */
interface GradeRow { grade: string; cieRaw: number; schoolPct: number }

interface ScoreConfig {
  fullRaw: number;                // 本次作业/模考满分（26 / 35 / 20 / 60 …）
  rows: GradeRow[];               // 有序：A* > A > … > U
}

/** 换算函数：E = bandLinear(r, rows) */
declare function bandLinear(raw: number, cfg: ScoreConfig): number;
```

> 口径：成绩按 **100 分制上报**（见 §5-5）；不产出 Excel I 列那种 20 分制。

换算规则（已与 Excel 公式一致）：
- 等级带：对 grade g，原始分区间 `[cieRaw(g), cieRaw(上一级))`（A* 上界 = fullRaw），映射到学校分区间 `[schoolPct(g), schoolPct(上一级))`（A* 上界 = 100）；
- 带内线性插值：`E = S_lo + (S_hi − S_lo)/(R_hi − R_lo) × (raw − R_lo)`；越界（0 与满分）封顶/封底；
- `rows` 的默认来源：
  - A–E：gt component（中国考区 P0 默认 variant 3；P1 自动按来源聚合）；
  - A*：教师自定（可给推荐）；
  - F/G：与 E 相同（仅兼容 IGCSE 口径）；
  - schoolPct：固定（90/80/70/60/50/40/30/20/0）。

```ts
/** gt 模板库（数据文件，供 P0 与 P1 查询） */
interface GtSeries {
  series: string;                 // 'S26'
  components: { paper: 1|2|3|4; variant: 1|2|3; max: number;
                thresholds: { A: number; B: number; C: number; D: number; E: number } }[];
}
interface ThresholdSuggestion {
  fullRaw: number;
  p0: { rows: GradeRow[]; note: string };      // variant3 兜底
  p1?: { rows: GradeRow[]; note: string };     // 自动聚合（按题量/权重）
}
```

P1 聚合算法（草案）：每题来源 component 的 A–E 阈值 → 除以该 component 满分得百分比 → 按权重汇总（先等权/按题量）→ × 当次 fullRaw → 向下取整 → 生成 A–E 行；A* 另给。

## 5. 决策点（✅ = 已定，其余下一轮过）

1. ✅ **statement-pair 的抽题粒度分场景**：**减量/作业允许抽单侧**（side-a=10 或 side-b=6，随带 statement）；**模考卷（按模板）必须成对**（whole=16）。（2026-09-04 定）
2. ✅ **重复率与覆盖**：**模考**设“同考点命中上限”与“同卷多题连出上限”；**作业**默认不设限或仅弱限制（可配置）。（2026-09-04 定）
3. ✅ **EITHER/OR 与 P4 选 section**：模考“供选一”槽的候选对**允许跨卷拼配**，不要求同卷原配（2026-09-04 定）。
4. ✅ **P1 口径已全定**（2026-09-04）：聚合集合 = 被选题真实来源（题来自哪场取哪场 gt、实际 variant；无 gt 的题如 Specimen 跳过）；权重 = **按题量**（每题 1 票）；P0 variant3 仅兜底/对照；“最近优先”不采用。仍待定：P0/P1 并排后教师可否“锁定其一”。
5. ✅ **成绩口径 = 100 分制上报**（2026-09-04 定）；Excel 的 I 列 20 分制不使用（如个别场景要 20 分制展示再单独处理）。
6. ✅ **Mark scheme 导出 = PDF**（2026-09-04 定）：**按“单题”切出**该题评分标准再拼接，未进卷的题目一律丢弃；同卷多题也按单题切段拼接，无需特判。
7. ✅ **题目库可进学生端**（2026-09-04 定）：按考点切题文件可在「资料」下开新分区作**只读展示**；题库**版本与 vocab_releases 分开**——vocab 高频微调，真题低频（一年约三轮考试、官方卷公布后才增补），用独立 version。

## 6. 题库管理后台（P0，2026-09-05 开工）

目标：新卷题 / 勘误 / 补 mark scheme 不再经 agent 对话——教师后台维护 `question_bank` 云表，与词库「VocabManager + 发布」同构；后续组卷器直接读云题库。

P0 范围（数据层先行，迁移脚本 `db-migration-question-bank.sql`）：
- `question_bank` 表：一行 = BankItem（qid pk；session/paper/variant/comp/q/stem/statement/marks/marks_total/kind/parts/topics/note/updated_at），topics 用数组便于按考点检索；
- `qb_releases` 发布快照表（版本自增，组卷器按版本读取，与 vocab_releases 思路一致）；
- RLS：教师（user_roles.role='teacher'）可写；所有已登录用户可读（pattern 同 quizzes）；
- 初始填充：后台「题库」tab 上传 `question-bank.json` 批量 upsert（或 SQL/控制台导入）；
- 教师 CRUD：按 paper/session/kind/marks/topics 检索 + 查看 + 有限字段编辑（校验分值/题型/source 语义，防破坏题干与 ms 对应）+ 删除/备注。

P1（后续）：PDF → 题库自动解析（确定性拆题/切 ms + 教师确认入库）；P2：Workers AI 建议考点归类与变体合并。
