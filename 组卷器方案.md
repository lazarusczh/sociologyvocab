# 组卷器（Teacher Paper Builder）——方案备忘录

> 状态：💬 讨论中（2026-09-03，教师口述；**未开始写代码**）。
> 背景：教师后台要加「组卷器」：基于历年真题按考点切题数据（根目录 `Paper1真题-按考点.md`、`Paper2/Paper3/Paper4Globalisation/Paper4Media真题-历年切题(至S26).md`）组卷，支持测验（限时）与作业（日常/周末）。

## 需求要点（2026-09-03）

### 1. 按「分数总值」配题（核心）
- **预设模板**：对齐真题卷面结构，可直接套用——
  - Paper 1 / Paper 2：`[4] + ([8]+[6]) + [10+6] + 两道 [26] 供选一` = **60**
  - Paper 3：`[4] + [8] + [12] + [26]` = **50**
  - Paper 4：Globalisation 与 Media 两个 section 各「一对 [35] 二选一」 = **70**
- **自定减量**（日常作业）：教师给目标总分（例：20 分），组卷器帮助**凑齐**目标分值——从可用题型分值（4 / 8 / 6 / 10+6 / 12 / 26 / 35 …）中选配题目使总和 = 目标分。

### 2. 单题布置 + 满分换算
- 支持单独布置一道大论文题作为作业（如周末 26 分题）。
- 涉及 **26 → 100** 与 **35（Paper 4 大题）→ 100** 两类换算；换算**不是粗暴的百分比**，教师过去用 Excel 手工换算。
- ⚠ **开始处理代码前，务必提醒教师提供该换算 Excel**（26→100 与 35→100 都覆盖）。

### 2.1 换算算法（已按教师提供的 Excel《score transformation formula - Sample.xlsm》理解，2026-09-04）
- 公式（每行学生，D=原始分，E=换算后 0–100）：`E = (K−H)/(J−G) × (D−G) + H`
  - 步骤：① 按原始分对照 **CIE 阈值表**（如 26 分制 A*≥22 / A≥18 / B≥16 / C≥14 / D≥11 / E≥8，其余 U；35 分制 A*≥30 / A≥28 / B≥26 / C≥24 / D≥22 / E≥19…）定等级；
  - ② 取该等级档位：`J/K` = 本等级 CIE/School 下限；`G/H` = **上一档**的 CIE/School 阈值（A* 档取满分 26/35 与 100）；
  - ③ 在 `[J→K]` 与 `[G→H]` 之间对 D **线性插值**；A* 顶部到满分时封顶 100。
- 验证样例：26 分制 raw=20（A 档）→ `(80−90)/(18−22)×(20−22)+90 = 85`；35 分制 raw=28（A 档）→ `= 80`；raw=26（A* 顶）→ `= 100`。
- 阈值表（CIE 与 School 各一列，A*–U）**为老师可编辑输入**，不同满分（26/35）阈值不同，还可能按作业微调 → 组卷器需提供阈值配置/导入，而非写死。
- Excel 中另有一列 `I = 20×E/100`（100 分制再转 20 分制，学校记录口径用）；实际组卷器是否沿用 20 分制口径待确认。
- **适用范围（2026-09-04 补充）**：只要是**论文类作业**教师一律用此表换算判分（论文判分不适合直接百分比定等第）；非论文作业另行处理。

### 2.2 减量作业的 CIE 阈值自动推导（依据官方 grade threshold，2026-09-04）
- 减量作业（如满分 20）不是随便定阈值：找**某一年官方 grade threshold（gt）文件**，取对应试卷的 component 行阈值，按 `作业满分 / 真题满分` 等比例缩小后作为换算表参数。
  - 例：作业满分 20、真题 Paper 2 满分 60 → 官方 A/B/C/D/E 阈值 ×(20/60)=×⅓。
- gt 文件位置与结构（已在 past papers 文件夹读取验证）：
  - 文件命名含 `9699` 与 `gt`（如 `9699_s24_gt.pdf`、`761523-…-june-2026-grade-threshold-table.pdf`）；
  - **component 级阈值**：Paper 1/2（Component 11/12/13、21/22/23）满分 60；Paper 3（31/32/33）满分 50；Paper 4（41/42/43）满分 70；每行给 A–E 的“最低 raw”；
  - ⚠ **component 级没有 A***（A* 只在 syllabus 整体加权分层面给）；换算表里 A*/F/G/U 行的 CIE 阈值需教师自定口径或另定规则。
- **School 阈值列**（90/80/70/60/50/40/30/20，U=0）在各作业里不变，像是学校在 100 分上的固定等级带。

**已确认口径（2026-09-04）**：
- **variant 3（13/23/33/43）只作为「兜底/基线」**——校内作业与模考的题目并非只从中国考区 variant 卷出；取 variant 3 分数线的意义是给自动计算当次分数线提供**兜底或对照基准**。关系为：**“只取 variant 3 分数线”= P0（基线），“按被选题实际来源自动加权计算”= P1（进阶）**。
- 缩放后阈值**向下取整**。
- **A\***：由教师自定；未来组卷器可给推荐（算法待定）。
- **F/G**：A level 无此等第，表格是为兼容 IGCSE 换算才留的；人工操作通常把 F、G 填成与 E 相同或极接近 → 默认 `F = G = E`。
- gt 年份**每次可选/可换**（不建议写死为“最新”）→ 组卷器存多份 gt 模板并向教师**推荐 1–2 组分数线**。
- 题目数据必须保留**真实来源（系列 + variant）**，供 P1 按实际来源聚合；md 里的出处（如 S26 QP23）已含 variant 信息。

### 2.3 gt 模板库与「当次分数线」自动推导（2026-09-04 定稿口径）
- **数据准备（已齐）**：本地官方 gt 现覆盖题库引用的全部系列——S21–S26、M21–M26、W21–W25（2026-09-04 联网补齐 M21/M23/W21，已存 `past papers/9699/`：`9699_m21_gt.pdf`、`9699_m23_gt.pdf`、`9699_w21_gt.pdf`）。入库模板：`系列 → component → {满分, A, B, C, D, E}`（按实际 variant 逐行）。
  - 注：**M 系列（March）官方只有 variant 2**（12/22/32/42）；S/W 系列才有 variant 1–3。
  - Specimen 卷无官方 gt → 不参与 P1，P0 也无对应行（界面给“无官方线”提示）。
- **P0（兜底/基线）**：默认展示 **variant 3**（13/23/33/43）分数线，供直接选用或作对照；教师也可手动指定某年/某 variant。
- **P1（自动计算）**：聚合集合 = **被选题的真实来源**——每道题来自哪场考试，就取那场考试的 gt component（实际 variant），无需另选系列；权重 = **按题量**（每道题 1 票）。
  - 算法：把每题来源 component 的 A–E 阈值折算成**占满分百分比**（如 37/60≈61.7%）→ 按题量加权合成 → 套用当次作业/模考满分 → **向下取整** → CIE 阈值行；A\* 教师定（可另给推荐）。
  - 无 gt 的题（如 Specimen）不参与 P1 合成。
- 组卷器界面把「P0 variant3 基线」与「P1 自动结果」并排展示供对照，教师可任选/微调。

### 3. 按考点主题分类组题
- 与现有「词汇题生成」一致：按选定主题分类（Paper → 考点 → 细分）**自动抽选**题目；
- 同时保留**教师手动勾选**入口（自动结果可增删替换）。

### 题型语义（决定 JSON 结构，2026-09-03 补充）
- **Q3 类（10+6）是「同一 statement 的一正一反」**：`(a) [10] Explain this view`（正方）+ `(b) [6] give one argument against this view`（反方）针对**同一句 statement**。
  - **减量/日常作业单独抽题**：可以只抽 (a) 或只抽 (b)，但**题干必须保留 statement**（= statement + 所抽侧的指令），不能把 10 分题或 6 分题变成无 statement 的裸题。
  - **按真题模板出模拟卷**：`(a)[10]+(b)[6]` **必然成对出现**，不可拆。
  - JSON 里应建模为「一条题 = statement + parts（10 正方 / 6 反方）」，供“成对”与“抽单侧”两种用法；分值可分别按 10 或 6 参与凑分，但抽单侧时要能带上 statement。
- **Paper 4 的「一对 35」是两道独立题的二选一**（EITHER/OR），与 10+6 不同：每题自带完整 statement，可单独布置，模板模式才作为一对候选呈现。
- Paper 1/2 的 `(a)[8]+(b)[6]`（Q2）不是同一 statement 的正反，仅为同一方法主题下的两个独立提问，按普通单题处理即可。

### 4. Mark Scheme：数据库化 + 组卷自动配套导出（2026-09-03 补充）
- 需求：组卷器**自动组完卷后，同时生成所选题目对应的 mark scheme 文件**供教师下载，省去逐份翻 PDF。
- Mark scheme 与题目的对应是**「卷级 → 题号」**：ms 本身按整卷给，不需要按 topic 切分；只要能从（session + 卷号 + 题号）定位到对应评分细则段落即可。
- 数据化方式：ms 源 PDF 仍在 past papers 文件夹（工作区外），先按卷抽取成文本（已有 `extract_ms.py` 思路）；再按题号锚点切出 `1(a)/1(b)/2…` 各段，question 条目记 `paper_code + qn` 即可反向关联。
- ⚠ 注意点：
  - ms 文本量不大（单卷 10–30 KB），但属**教师端资料**，不进学生同步、不进 git（与词库素材同策略，放 gitignored 目录或教师云存储）；
  - 组卷器导出格式待定（md / docx / pdf），可下载即可；
  - Specimen 卷（2021 Specimen 等）也有 ms，需一并覆盖。

### 2.4 P1 聚合的加权方式差异（2026-09-04 说明）

P1 思路：被选题来自若干“来源卷”，每卷有自己的 gt component 阈值；先把每卷的 A–E 阈值折算成**占该卷满分的百分比**，再对多个来源合成“一套百分比”，最后乘当次满分得到建议阈值。差别只在**“合成时每卷算几票”**：

| 方式 | 规则 | 效果 / 举例（假设：S26 P2 卷被选 3 题、W24 P3 卷被选 1 题） |
|---|---|---|
| **等权（按卷）** | 每张“被引用卷”1 票，取算术平均 | S26 与 W24 各占 1/2。选 S26 卷 3 题只是“这卷好出题”，不放大它的影响；分数线偏稳 |
| **按题量加权** | 每道被选题 1 票 | S26 占 3/4、W24 占 1/4——被选题目多的卷主导分数线 |
| **最近优先（可叠加任一种）** | 越新的系列权重越高（如 W24=1、S25=2、S26=3，或指数衰减） | 弱化老卷难度口径，反映“近年难度趋势”；适合作为默认的“系列选择”策略，而不是独立的“卷内权重” |

补充一个偏门但常更合理的变体：**按分值加权**——每卷权重 = 该卷被选题的原始分之和 / 当次满分。理由：分数线作用于“整张卷总分”，每题实际贡献的原始分才代表它在总分里的分量；若作业里 4 分小题占多数而 26 分大题只有 1 道，按题量加权会高估小题来源卷的影响，按分值加权则更贴“总分构成”。

**已定（2026-09-04）**：P1 合成采用**按题量加权**（每道被选题 1 票）；聚合系列集合 = **被选题的真实来源**（题从哪场来就取哪场的 gt、实际 variant），无需教师选系列；“最近优先”不采用（P1 已覆盖来源）。P0 variant3 仅兜底/对照，P1 可用时影响很小。

## 待办 / 待确认
- [x] 换算 Excel 已收到并理解（26→100、35→100，档内线性插值法，见 2.1）——编码时按此实现，另需支持阈值可配置
- [x] 减量（论文型）作业判分走换算表；CIE 阈值由官方 gt 按满分比缩放（见 2.2）——variant3、向下取整、A* 教师定、F/G=E 已定
- [x] **gt 覆盖已齐**：本地缺的 M21/M23/W21 已于 2026-09-04 联网下载到 past papers/9699/（后续解析入库即可，见 2.3）
- [x] **gt 模板已解析**：2026-09-04 生成 `gt-templates.json`（根目录，17 系列，component 级 A–E；M 系列仅 variant2；含少量多余 component 如 50/60、S21 的 14/24/34 老卷行，后续按需过滤）。schema：`[{series, file, components:{code:{max,A,B,C,D,E}}}]`
- [x] **进阶自动分数线口径已定**（按题量加权；聚合系列=被选题真实来源；P0 variant3 仅兜底）
- [x] 上报口径 = 100 分制（已定，2026-09-04）
- [x] **md→JSON 已完成**（2026-09-04）：`question-bank.json`（根目录，653 条，字段见 `组卷器数据模型初稿.md` §1）
- [ ] 主题分类层级是否与词库 `unit`（考卷→主题→单元）共用一套筛选结构，待定
- [ ] 「凑分」的取题策略细节（同考点多题重复率控制、二选一选项如何处理等）待定
- [ ] **Mark scheme 数据化**：确认覆盖范围（历年各卷 + Specimen）、抽取与按题号切分脚本、存放位置（gitignored 本地 / 教师云存储）、组卷导出格式（md/docx/pdf）
