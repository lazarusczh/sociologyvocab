# 逻辑关系与应用

> 最后更新：2026-09-07

教师后台「逻辑管理」维护词条间逻辑关系，随词库发布到学生端。本文记录：① 关系的建模规则；② 基于关系网络的练习玩法设计；③ 提高网络覆盖率的编辑助手（AI 推荐）。

## 一、关系建模现状

### 关系类型（对称/方向）
| 类型 | 语义 | 方向 | 存储 |
|---|---|---|---|
| higher / lower | 高于 / 低于（上下位） | 有向 | 对称存储（A.higher ↔ B.lower） |
| peer | 并列（同级） | 无向 | 两端各自存 |
| contrast | 相反（对立） | 无向 | 两端各自存 |

### 概念组（概念级镜像）
- 归并规则：按 `type + term` 精确归并，跨 paper/单元同名 = 同一概念组。
- 建/删关系按「概念组 × 概念组」全镜像写入（A 组所有条目 × B 组所有条目两两建边），保证任何 paper 的条目关系完整。
- 特例：`CONCEPT_SPLIT_TERMS` 可配置「同 term 但语义分裂」的词按 unit 拆组（当前为空；曾用于 Cultural deprivation，已合并恢复默认归并）。
- 有幂等规范化 effect：清空重建全镜像，修复方向污染 / 孤儿引用 / 重复边。

### 网络规模（云端 vocab_releases v73，2026-09-03 查）
- 897 词条 / 843 概念组。
- **骨架视图**（只算 higher/lower/peer/contrast 存储边）：633 个有边概念组 / 830 边 / 平均度 2.62 / 直径 14；其中 **49% 是叶子（度=1）**，结构偏树状、链状。
- **全关系视图**（再加派生同级 + 传递同级）：633 节点 / **3065 边** / 平均度 9.68 / 直径 12；度>20 的有 99 个——这正是「显示派生同级」默认关闭的原因。
- 网络为**单一连通分量**（100% 节点互通），任意两个概念之间都有通路。
- 结论：规模已能支撑接龙等玩法；覆盖率随教师持续编辑提高（术语为主，学者按需要接入）。

## 二、练习应用设计

### 公共底座 `conceptGraph.ts`（已落地 2026-09-02）

**层级推导层**：存储层只保留 higher/lower 骨架，同级由结构推导，不落库（避免全连接 peer 的 O(n²) 爆炸，且历史数据加载即自动享受推导，无需回补）。

- `buildConceptGraph(vocab)` → ConceptGraph：
  - `nodes`：概念组节点（cid / parents / children / depth 层级深度 / inCycle）
  - `derivedPeers`：共享同一父概念的兄弟节点推导同级（查询时算；已显式标「相反」的对跳过）
  - `explicitPeers` / `contrasts`：手动并列 / 对立（仅用于无共同父但仍需标同级等场景）
  - `cycles`：环检测（higher/lower 应构成 DAG，环内节点深度不可靠）
  - `neighbors`：全邻居（dir + 显式 + 派生 peer），供接龙/连线直接消费
- 层级深度：从根（无父）节点最长路径拓扑推导（0 = 根，向下逐层）。
- 验证（云端 v47）：244 概念组有层级边；显式 peer 26 / 派生 peer 489 / contrast 31；环 0；深度 0~13 层（根 17 个）；Family diversity 11 个子概念派生同级 55 对（零存储）；连通分量加派生后最大 255 节点。
- 待办：图谱接入层级展示（按深度分层 / 派生同级可选显示）。

### 玩法一：接龙（邻接推进）
- 规则：系统给概念 A → 学生答一个 A 的相邻概念 B → 系统给 B 的邻居 C（避开刚走的 A 防回跳）→ 学生再答…交替推进，预设 8~12 步一轮。
- 作答形式：
  - 选择型（P0）：给 4 个候选选一个相邻的，干扰项从非邻居中抽。
  - 输入型（P1）：学生打出相邻概念，判分 = 输入属于当前节点任一邻居的可接受写法（开放多答案判定）。
- 进阶变体：方向约束（只许走「高于」/只许走「并列」）；已访问节点防重复。
- 出题限定在「有关系」的子图内，起点优先选分支点（度≥2）。

### 玩法二：连线（起点 → 终点）
- 出题：BFS 从随机起点展开，选深度 3~6 的节点做终点（保证有解、长度适中）。
- 作答：逐步输入/选择中间概念，每步验证「与上一个相邻」，到达终点即成功。
- 判定决策：不要求最短路径——绕远但每步相邻且最终到达即算对；可展示「学生路径 vs 最短路径」作反馈。

### 优先级
1. P0：公共底座 + 接龙（选择型）——最小闭环，先让学生玩起来。
2. P1：连线题。
3. P2：输入型接龙、方向约束变体。

## 三、图谱可视化形态评估（2D vs 3D，2026-09-03 评估，未落地）

> 立项时讨论过"要不要上三维展示"。基于 v73 实测数据评估如下，**结论是暂不落地 3D**。

### 3.1 结论
- **全图三维不划算**：3D 的唯一实质收益是"第三维表达层级"（网络深度 0~13 层、直径 14），但在 633 节点规模下，遮挡、文字标签可读性、移动端性能与手势冲突、新增 3D 库（约 0.6~1MB）与重做交互的成本会盖过这点收益；3065 边的密视图在 3D 里也只是从"平面毛线球"变成"立体毛线球"。
- **2D 现状已覆盖主要需求**：聚焦检索、1~3 度范围、路径查询、度数着色、派生同级开关——靠**过滤**解决"太大看不清"，不需要靠第三维。
- **若将来要做，推荐形态**：
  1. **局部三维**：只把某个根/路径/聚焦范围内的子图（几十到百余节点）做成分层 3D，depth 作为高度，用于课堂投屏演示；备课仍用 2D。
  2. **2D 按层级分层布局**（性价比最高）：把 depth 映射到 Y 轴（相当于分层 3D 的正投影），拿到"层级一目了然"的收益，不付 3D 的代价。

### 3.2 配套工具（已落地 2026-09-03）
- 图谱聚焦检索 + **1~3 度范围**（BFS 展开）
- **路径查询**卡片：输入起终点，列出 Yen 算法求出的 K 条最短简单路径，点概念即在图谱聚焦

## 四、覆盖率提升：编辑助手（AI 推荐）

### 目标
教师编辑某个概念的逻辑关系时，系统推荐「可能相关且尚未入网」的词条，提醒教师去建立关系，形成「编辑 → 推荐 → 编辑」的正循环，提高网络覆盖率。

### 方案 P0：纯启发式（零成本、零依赖、即时可用）
- 同单元候选：推荐与当前编辑概念「共享 unit 但自身未入网」的词条（unit 共现是强信号）。
- 桥接候选：推荐结构上位于两个已入网簇之间的未入网词条。
- 文本近似：term / 中文 / 释义的归一化相似（字符重叠 / 编辑距离）找近似词。
- 前端本地计算即可，无需任何服务端。

### 方案 P1：Cloudflare Workers AI 语义相似（免费额度内零成本）
- Worker 接 `AI` binding，用开源 embedding 模型（如 multilingual-e5 / bge 系）把「term + 中文 + 释义」转向量。
- 给当前编辑词条算 embedding，与全库（或未入网子集）算余弦相似度，返回 top-N。
- 前端调 Worker；教师个人使用量远在免费额度内。
- 依赖：Worker 加 AI binding（部署配置）；无直接 API 计费。

### 交互
- 逻辑管理编辑区侧栏/下方显示「可能相关（未入网）」候选，点击可直接载入词条 B 检索框。
- 标注推荐依据（同单元 / 语义相似 / 桥接）。

## 五、待办 / 决策记录
- [x] 练习玩法立项：接龙（仅起点 / 起点+终点，选择 + 默写输入两种作答）——2026-09-03 已实现（`chain.ts` + `LogicChain.tsx`）
- [x] AI 推荐采用 P0 纯启发式（同单元 / 名称相关 / 释义提及），已落地
- [x] 图谱聚焦检索 + 1~3 度范围 + 路径查询（Yen K 最短路）——2026-09-03 已落地
- [ ] 图谱可视化：2D 按层级分层布局（depth → Y 轴）——候选，性价比高于 3D
- [ ] 局部三维演示（子树/路径，几十~百余节点）——低优先，仅课堂演示用；全图 3D 已评估为不划算
- [x] 关系性质探讨（第六节）：2026-09-07 完成第一轮研判——contrast 三类语义 + 判别规则实测（明细见 `rival-oppose-report.md`）
- [ ] contrast 子类标签（rival / oppose / 前提否定）：逻辑管理可选标签，自动判别只作默认建议、允许教师覆盖——候选，待定
- [ ] 补上位类节点（如「国际组织」）以提高结构信号质量——候选，与网络扩展一起做
- [ ] 关系网络持续扩展（教师维护），本文数据需随云端版本更新

## 六、关系性质探讨（2026-09-03 记录，2026-09-07 第一轮研判）

接龙玩法目前**只读连接与否**（`neighbors` 无向折叠），暂不辨析连接性质。但关系网络里的「性质」语义有深层二义性，需慢慢研究，先记录如下，**不要急着改代码/数据结构**。
2026-09-07 已用云端 v84 数据对 5.1 的判别规则做实测（见 5.4，明细见 `rival-oppose-report.md`），结论仍是**不落库**，但 5.1 的二分被修正为三类语义。

### 5.1 contrast（相反）的语义混用（原记两种，2026-09-07 修正为三类，见 5.4.1）

教师编辑时「相反」实际在表达不同东西（下列两种为 2026-09-03 的原始记录）：

| 类型 | 语义 | 例 | 结构特征（拟） |
|---|---|---|---|
| 同级对立 rival | 同一层级上竞争/相对的解释框架 | 马克思主义 ⊥ 功能主义 | 无层级边；同级/共享父 |
| 跨级反对 oppose | 大理论对局部概念/政策的批判立场 | New Right 反对 welfare | 存在层级差 |

- 编辑时的多数「相反」是 rival；少数表达意见「反对」（oppose）。
- 自动判别思路：**结构信号优先**（两节点直接存在 higher/lower 边或 `depth` 差 ≥1 → oppose；否则默认 rival）；degree 差只作辅助参考——因 degree 是「编辑热度 + 真实地位」的混合，纯阈值会受编辑不均干扰（New Right 连通度高部分是因被编得多）。
- 落地形态：推导层自动分类（不落库），图谱用不同样式区分 rival/oppose，覆盖统计分开看。
- 关联现状：conceptGraph 中兄弟派生同级（derivedPeer）时**已排除 contrast**（contrast 优先）；即"同级对立"目前只以 contrast 一条边呈现，不会被同时推导为 peer。

### 5.2 盟友关系（敌人的敌人）

- 猜测：A⊥B 且 B⊥C ⇒ A、C 在「反对 B」这一点上是同一战线（论文写作中有呼应意义）。
- 警惕：沿 contrast 链做传递闭包会产生大量假盟友（例：马⊥功、功⊥互，马与互未必互认）。**只建议一阶共同对手**（仅经由同一个 B），不向远处传播。
- 落地形态（拟）：不作正式边落库；作为「编辑助手建议」（标 A⊥B 后提示还有谁同标 ⊥B，由教师决定是否建 peer），或将来写作辅助查询。

### 5.3 结论 / 后续
- 接龙先行（只读连通性，不受 5.1/5.2 影响）。
- 若做「性质敏感」玩法（按方向走 / 找反对者 / 盟友互证），conceptGraph 需先导出**带类型邻接**（内部有 `dir`/`explicitPeers`/`contrasts` 但未暴露）。
- 2026-09-03 讨论结论：5.1 与 5.2 **暂不实现**，建议每隔一段时间复盘一次（有周期提醒）。
- 2026-09-07 更新：第一轮研判已完成（见 5.4），**结论维持不落库**；但 5.1 的 rival/oppose 二分被修正为**三类语义**。

### 5.4 第一轮研判结果（2026-09-07，云端 v84 实测）

**方法（只读）**：拉取云端 `vocab_releases` v84（897 词条），复刻 `conceptGraph` 的概念归并 / 拓扑 / 最长路径 depth，对全部 contrast 边套用 5.1 的判别规则（有直接层级边 → oppose；否则 depth 差 ≥1 → oppose；否则 rival）。逐条明细见 `rival-oppose-report.md`。

**实测口径**：contrast 边 49 条；层级边 742；参与层级/对立关系的概念组 631；环 0；孤儿引用 0。
**规则输出**：oppose 28 / rival 21 / 存疑 0。

**与语义直觉对照（AI 预判 + 教师复核）**：一致率约 47%——rival 侧 17/21（81%），oppose 侧仅 6/28（21%）；oppose 28 条里 19 条直觉应为 rival。

**核心结论：裸 depth 差不可靠。** 它衡量的是「层级骨架的接入深度」，而骨架覆盖不均（49% 叶子、大量未接入的孤立概念 depth=0），因此 depth 差主要反映**编辑覆盖度**而非「是否跨级」——与 5.1 中对 degree 的担忧同源。典型反例：5.1 的原始举例 **Welfare state ⊥ New Right 两端 depth 均为 0，规则判成 rival**。

#### 5.4.1 contrast 实为三类语义（不再是两类）

| 类型 | 语义 | 例 | 可否自动判别 |
|---|---|---|---|
| rival 同级对立 | 同一层级上竞争/相对的解释框架 | Marxism ⊥ Functionalism；Interpol/EU/UN ⊥ NGO（分类互斥）；RSA ⊥ Social control（理论传统对立） | 可（结构 + 词形信号） |
| oppose 跨级反对 | 大理论对局部概念/政策的批判立场 | New Right ⊥ Welfare state；Functionalism ⊥ Family diversity | 部分可（需祖裔 + 类型信号） |
| **前提否定 / 解构型** | 一端否认另一端**成立的前提** | Meta-narrative ⊥ Deviance（无基准则无叛逆）；Paradigm ⊥ Objective（库恩：无绝对客观） | **不可**，建议教师手标 |

- **rival 的两个子类**：
  - *分类互斥*：共享**隐含上位类**的互斥子类（IGO vs NGO）。结构上看不出同级，是因为骨架缺该上位类节点（如「国际组织」）。
  - *理论传统对立*：同一研究对象的两种理论传统解释（马克思主义的 RSA vs 功能主义的 social control），与流派本身对立（Marxism ⊥ New Right）不是一回事。
- **前提否定型对结构信号完全不敏感**：两条同构的边（Meta-narrative ⊥ Deviance、Paradigm ⊥ Objective）被规则分别判为 oppose 与 rival，说明此类无法由结构推导。
- 待教师确认是否同属前提否定型：Meritocracy ⊥ Myth of meritocracy、Crisis of modernism ⊥ Modernisation theory、Eclecticism ⊥ Modernisation theory。

#### 5.4.2 若将来实现，推荐的判别信号（按可靠性排序）

1. **祖裔可达性**替代裸 depth 差：一端是另一端祖先/后代 → 跨级；仅深度不同但无祖裔关系 → 不判 oppose。
2. **词形对偶**（强 rival 证据）：共享核心词根 + 反义构词（non-/a-/asym-/im-/de- 前缀；poly↔mono、matri↔patri、con-↔diver-、optimism↔pessimism）。19 条误判为 oppose 的边中约 12 条属此类。
3. **类型/地位信号**（找 oppose）：理论流派（带 theories 标签或高 degree）vs 具体政策/现象（Welfare state、Family diversity）→ 跨级反对；此处 degree 作辅助信号正合适。
4. **补上位类节点**：比任何启发式都治本，直接消除分类互斥型误判。

#### 5.4.3 落地建议（结论）

- 维持 **不落库**：性质分类仍在推导层做（或暂不做），不改数据结构。
- 若要辨析性质，推荐形态是**给 contrast 加可选子类标签**（rival / oppose / 前提否定），自动判别只给默认值、教师可覆盖——避免出现「系统判错但教师改不了」。
- 接龙等现有玩法只读连通性，不受影响；「性质敏感」玩法（找反对者 / 盟友互证）仍以先导出**带类型邻接**为前提（见 5.3）。
- **5.2 盟友关系本轮未展开，下次复盘优先。**
