# 逻辑关系与应用

> 最后更新：2026-09-02

教师后台「逻辑管理」维护词条间逻辑关系，随词库发布到学生端。本文记录：① 关系的建模规则；② 基于关系网络的练习玩法设计；③ 提高网络覆盖率的编辑助手（AI 推荐）。

## 一、关系建模现状

### 关系类型（对称/方向）
| 类型 | 语义 | 方向 | 存储 |
|---|---|---|---|
| higher / lower | 高于 / 低于（上下位） | 有向 | 对称存储（A.higher ↔ B.lower） |
| peer | 并列（同级） | 无向 | 两端各自存 |
| contrast | 相反（对立） | 无向 | 两端各自存 |

### 概念组（概念级镜像）
- 归并规则：按 `type + term` 精确归并，跨 paper/单元同名 = 同一概念组。
- 建/删关系按「概念组 × 概念组」全镜像写入（A 组所有条目 × B 组所有条目两两建边），保证任何 paper 的条目关系完整。
- 特例：`CONCEPT_SPLIT_TERMS` 可配置「同 term 但语义分裂」的词按 unit 拆组（当前为空；曾用于 Cultural deprivation，已合并恢复默认归并）。
- 有幂等规范化 effect：清空重建全镜像，修复方向污染 / 孤儿引用 / 重复边。

### 网络规模（云端 vocab_releases v47，2026-09-02 查）
- 892 词条 / 845 概念组；有关系的词条 272；去重无向边 333。
- 连边概念组 253，全部在同一连通分量；84 个分支点（度≥3），169 个链状/末端（度≤2）。
- 结论：网络已具备支撑玩法的最小规模；覆盖率约 30%，随教师持续编辑扩展。

## 二、练习应用设计

### 公共底座 `conceptGraph.ts`（已落地 2026-09-02）

**层级推导层**：存储层只保留 higher/lower 骨架，同级由结构推导，不落库（避免全连接 peer 的 O(n²) 爆炸，且历史数据加载即自动享受推导，无需回补）。

- `buildConceptGraph(vocab)` → ConceptGraph：
  - `nodes`：概念组节点（cid / parents / children / depth 层级深度 / inCycle）
  - `derivedPeers`：共享同一父概念的兄弟节点推导同级（查询时算；已显式标「相反」的对跳过）
  - `explicitPeers` / `contrasts`：手动并列 / 对立（仅用于无共同父但仍需标同级等场景）
  - `cycles`：环检测（higher/lower 应构成 DAG，环内节点深度不可靠）
  - `neighbors`：全邻居（dir + 显式 + 派生 peer），供接龙/连线直接消费
- 层级深度：从根（无父）节点最长路径拓扑推导（0 = 根，向下逐层）。
- 验证（云端 v47）：244 概念组有层级边；显式 peer 26 / 派生 peer 489 / contrast 31；环 0；深度 0~13 层（根 17 个）；Family diversity 11 个子概念派生同级 55 对（零存储）；连通分量加派生后最大 255 节点。
- 待办：图谱接入层级展示（按深度分层 / 派生同级可选显示）。

### 玩法一：接龙（邻接推进）
- 规则：系统给概念 A → 学生答一个 A 的相邻概念 B → 系统给 B 的邻居 C（避开刚走的 A 防回跳）→ 学生再答…交替推进，预设 8~12 步一轮。
- 作答形式：
  - 选择型（P0）：给 4 个候选选一个相邻的，干扰项从非邻居中抽。
  - 输入型（P1）：学生打出相邻概念，判分 = 输入属于当前节点任一邻居的可接受写法（开放多答案判定）。
- 进阶变体：方向约束（只许走「高于」/只许走「并列」）；已访问节点防重复。
- 出题限定在「有关系」的子图内，起点优先选分支点（度≥2）。

### 玩法二：连线（起点 → 终点）
- 出题：BFS 从随机起点展开，选深度 3~6 的节点做终点（保证有解、长度适中）。
- 作答：逐步输入/选择中间概念，每步验证「与上一个相邻」，到达终点即成功。
- 判定决策：不要求最短路径——绕远但每步相邻且最终到达即算对；可展示「学生路径 vs 最短路径」作反馈。

### 优先级
1. P0：公共底座 + 接龙（选择型）——最小闭环，先让学生玩起来。
2. P1：连线题。
3. P2：输入型接龙、方向约束变体。

## 三、覆盖率提升：编辑助手（AI 推荐）

### 目标
教师编辑某个概念的逻辑关系时，系统推荐「可能相关且尚未入网」的词条，提醒教师去建立关系，形成「编辑 → 推荐 → 编辑」的正循环，提高网络覆盖率。

### 方案 P0：纯启发式（零成本、零依赖、即时可用）
- 同单元候选：推荐与当前编辑概念「共享 unit 但自身未入网」的词条（unit 共现是强信号）。
- 桥接候选：推荐结构上位于两个已入网簇之间的未入网词条。
- 文本近似：term / 中文 / 释义的归一化相似（字符重叠 / 编辑距离）找近似词。
- 前端本地计算即可，无需任何服务端。

### 方案 P1：Cloudflare Workers AI 语义相似（免费额度内零成本）
- Worker 接 `AI` binding，用开源 embedding 模型（如 multilingual-e5 / bge 系）把「term + 中文 + 释义」转向量。
- 给当前编辑词条算 embedding，与全库（或未入网子集）算余弦相似度，返回 top-N。
- 前端调 Worker；教师个人使用量远在免费额度内。
- 依赖：Worker 加 AI binding（部署配置）；无直接 API 计费。

### 交互
- 逻辑管理编辑区侧栏/下方显示「可能相关（未入网）」候选，点击可直接载入词条 B 检索框。
- 标注推荐依据（同单元 / 语义相似 / 桥接）。

## 四、待办 / 决策记录
- [ ] 决定练习玩法是否立项（接龙 P0 / 连线 P1）
- [ ] 决定 AI 推荐采用 P0 启发式还是 P1 embedding
- [ ] 关系网络持续扩展（教师维护），本文数据需随云端版本更新
