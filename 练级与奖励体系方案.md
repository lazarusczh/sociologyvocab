# 练级与奖励体系方案（研究稿，2026-09-03）

> 目标：给学生除「连续打卡」和「掌握度」之外的第三种直观可见的长期进度——**等级 + 经验值**，
> 并在其上挂一套**奖励体系**（从补签机会到线下小额物质奖励），激励学生持续练习。

---

## 一、现状盘点（可复用的地基）

| 已有机制 | 位置 | 说明 |
|---|---|---|
| 每日打卡 `DayStudy` | `checkin.ts` / localStorage | `{ seconds, questions, correct }` 按天累计；达标 = 10 分钟 + 20 题 |
| 掌握度 | `storage.ts` `MASTERY_WEIGHTS` | 每题按模式权重 ±；0–100 |
| 练习结算统一入口 | `store.tsx` `recordItem(itemId, ok, mode)` | **所有正式练习每题必经**，是挂 XP 的最佳切点 |
| 补签 `applyMakeup(dayKey)` | store / 打卡页 | 现为「每周达标得 1 次、**仅限当周漏签日**」（`canEarnMakeup` / `missedDaysInWeek`）。2026-09-20 教师定：改为**补签卡**，规则见 4.3 |
| 错题本 / 周统计 / 连击 | 已有 | 可在 UI 上复用展示风格 |
| 云端 | `student_data.data` JSON 整包 | 学生端每次上传整体覆盖 |

**结论**：XP 挂在 `recordItem` 上最省事；等级/累计 XP 数据可并入现有本地进度结构，随既有 JSON 上传云端（见第六节兼容性）。

---

## 二、XP 经验值设计

### 2.1 获得途径（单次作答）

以现有练习模式权重为基准，**答对得 XP、答错得少或为 0**：

| 模式 | 答对 XP | 答错 | 说明 |
|---|---|---|---|
| **定义题（全对）** | **10** | **2** | **输出型，难度最高**（掌握度 gain 15，全表最高） |
| **定义题（部分正确）** | **5** | — | 与既有 `score: 0.5` 语义一致，给一半 |
| 拼写默写 | 8 | 2 | 回忆难度最高 |
| 语境填空 | 7 | 2 | |
| 纵横填字（结算题） | 6 | 2 | |
| 匹配块每对 | 5 | 1 | |
| 选择题 | 4 | 1 | |

- **逻辑接龙不在此表**——它**按「完成」一次性结算**，见 **2.1.1**。
- **学习时间**：每满 60s 学习 +2 XP（鼓励"坐下来练"，与打卡时间同源）。
- **连续打卡加成**：streak ≥3 天起每日首次练习 +10 XP（封顶 +30/日），奖励坚持。
- 闪卡 / Wordle 不算（沿用现有「不计掌握度」的口径，避免白嫖）。

#### 2.1.1 逻辑接龙：按「完成」结算（2026-09-21 教师定，原「每步 5 XP」已废止）

**为什么不能按步给**：

| 漏洞 | 说明 |
|---|---|
| **可绕远刷分** | `target` 模式（起点→终点）**不设步数上限**（`LogicChain.tsx` 注释：「无上限自由探索」），而**默写输入模式只要输入的概念「相邻」就算走成功**（`tryMove` 中 `nodesWithin` 判定通过即 `recordItem(..., true)`）。于是每步输入一个"相邻但离终点更远"的概念，就能无限拿 XP |
| **可回退刷分** | `undoStep`（`LogicChain.tsx:222`）**只回退界面状态、不撤销已发出的 XP**。走一步 +5 → 回退 → 再走 → 再 +5，可无限循环，且**同时虚增打卡题数** |

⇒ 改为**走完整条线才结算一次**；**中途放弃不给**（`giveUp` 已有明确入口）。
**附带好处**：XP 只在完成时发一次，上面两个漏洞**自动失效**（回退重走不再产生新 XP）。

**难度排序（2026-09-21 教师定，代码已按此改造完成）**：

```
仅起点+选择  <  起点→终点+选择  <  仅起点+默写  <  起点→终点+默写
```

**「有无选项」是比「有无方向目标」更大的难度分水岭** —— 自己回忆并拼出一个概念，
远比在四个选项里排除三个难。所以两个默写档整体高于两个选择档。

| 路线 | 作答方式 | **完成 XP** |
|---|---|---|
| **仅起点**（固定 8 步） | 四选一 | **20** |
| **起点→终点**（3~6 跳） | 四选一 | **25** |
| **仅起点** | 默写输入 | **30** |
| **起点→终点** | 默写输入 | **35** |

- **「完成」的判定**沿用现有 `choiceDone` / `irunDone`（`LogicChain.tsx:68-73`）：open = 走满目标步数，target = 抵达终点。
- **默写输入给更高**：自己打出概念名远难于四选一，与「选择题 4 / 拼写 8」同一梯度逻辑。
- **答错的代价不放在 XP 上**：选择模式答错仍可继续走完，不影响完成 XP；但**掌握度照扣（chain loss 16）、错题本照记**——惩罚已在别处体现，不再叠加。
- **效率校验**：open 一局约 1.5 分钟、target 约 1 分钟，折算后与选择题（约 16 XP/分钟）、拼写（约 12 XP/分钟）**量级协调**。
- **⚠ 实现要点**：每步**仍要调 `recordItem`**（否则掌握度、打卡题数、错题本会全断），只是**不要在每步发 XP**；完成时再单独发一次。即 `recordItem` 需支持 `opts.skipXp`，另加一个**只加 XP、不加题数**的 `awardXp(n)` 入口。

#### 2.1.2 选项生成已改造：让「起点→终点」真的需要找路径（2026-09-21 已实现）

**改造前的实况**：`buildOptions` 把当前概念的**所有邻居**都排除在干扰项之外（`!nb.has(c)`），
于是四个选项里**只有一个与当前概念相邻**——而"相邻"正是唯一判别特征，
**终点信息完全用不上** ⇒ target 与 open 难度相同，等于白给。

实测（真实词库，300 局 / 1300 步）：**旧口径下 100.0% 的题目只有 1 个相邻选项**，铁证。

**改造内容**（`chain.ts` 的 `buildOptions` 新增 `opts.target`）：

| 口径 | 用于 | 干扰项构成 |
|---|---|---|
| **不传 target** | open 模式 | 全部**非**邻居（维持原样） |
| **传 target** | target 模式 | **额外混入 1 个「相邻但方向错误」的邻居** ⇒ 选项里出现 2 个相邻概念，学生**必须判断哪个朝向终点** |

- ⚠ **open 模式不能这么改**：它的规则是"相邻即可前进"，选项里出现多个相邻概念会变成**多解**（全都合法）。
- 相邻干扰项只取 `dist(c, target) > dist(correct, target)` 的**确实绕远**的邻居；
  刻意排除"另一条同样最短的路"（距离相等），**避免两个都合理的选项造成判分歧义**。
- **每一步都要判断**，不是只在开头选一次方向；邻居不足时自动回退旧口径（不强凑）。

**改造后实测（同一数据集）**：

| 指标 | 改造前 | 改造后 |
|---|---|---|
| 只有 1 个相邻选项（"白给"） | **100.0%** | **2.2%**（仅邻居不足时回退） |
| ≥2 个相邻选项（需判方向） | 0% | **97.8%** |
| 正确答案缺失 | 0 | **0** |
| 相邻干扰"确实绕远" | — | **100%** |
| open 模式行为 | 单相邻 100% | **单相邻 100%（未受影响）** |

### 2.2 防刷上限（2026-09-22 由 120 上调为 400）

- **每日硬上限：400 XP/天**。到达后 XP 不再累计，但打卡题数照常计。
- **为什么从 120 提到 400**（基于真实打卡数据的实测）：
  - 全体学生 XP 中位数 **113/天** ⇒ **120 的上限意味着一半的练习日会被截断**；
  - 最努力的学生（日均练习 73 题、正确率 90%）实测 XP 中位 **370/天** —— 若上限设 200，会砍掉她 **46%** 的努力；
  - 上限**对普通学生毫无影响**（他们中位 113，够不到 400），它**只对尖子生起作用** ⇒
    压得越低，越像是"专门针对最努力的学生设的一道墙"。
- **上限的真实职责只是"防异常刷分"**，不是平均主义，**也不该用来控制升级节奏**——
  升级节奏由 3.1 的递增曲线负责。
- ⚠ **更正一条旧理由**：旧版写"上限让教师兑奖成本可控"**并不成立**——
  实物奖励只有**月度之星**（排名）与**全勤奖**（打卡天数），**两者都与 XP 上限无关**。
- 上限可配置（教师端常量即可）。

### 2.3 扣 XP？

**不建议扣**。扣分制造挫败、且与"掌握度扣分"语义重叠；等级应当只涨不跌（反映累计投入）。

---

## 三、等级体系

### 3.1 曲线（累计经验）—— 每级递增、**等级不设上限**（2026-09-22 重设）

**旧曲线的问题**（实测后废弃）：只有前 3 级增量小（80/100/140），从 LV4 起迅速变钝
（280 → 500 → 1200 → 1400），普通学生到后期**半个月才升一级**；且 **LV12 就是天花板**，
尖子生约 18 个练习日就到顶、之后毫无目标。

**新规则**：`LV(n) → LV(n+1)` 需要 **`100 + 5×(n-1)` XP**，**等级数不设上限**
（省去将来"有人练满级了要不要再加级"的更新负担）。

累计公式：**`XP(LV k) = 100(k-1) + 5(k-1)(k-2)/2`**

**设计意图（教师 2026-09-22 明确，别改错）**：
普通学生实测日均 **113 XP**。曲线要分两段**刻意制造不同手感**：

| 阶段 | 单级需求 | 与日均 113 的关系 | 效果 |
|---|---|---|---|
| **LV1 → LV4**（前 3 天） | 100 / 105 / 110 | **都 < 113 ⇒ 刚好够** | 每天升一级，**建立正反馈与节奏** |
| **LV4 之后** | 115 / 120 / 125 … | **略 > 113 ⇒ 差一点点** | 触发「**就差这么一点，不如刷上去**」的心理 |

⇒ 学生为补上那 2~7 XP 而多做几题，日均从 113 抬到 120+，便能**连升两三级**；
但单级需求随即追上来，**再次差一点** —— 形成
「**加练 → 升级 → 又差一点 → 再加练**」的螺旋。
**作用是在不提高门槛的前提下，间接、自愿地推高练习量**（呼应"门槛只做兜底"的定位，见 2.2 与第八节）。

| 等级 | 累计 XP | 本级增量 | 与 113 的差 | 普通学生(113/天) | 尖子生(370/天) |
|---|---|---|---|---|---|
| LV2 | 100 | 100 | **够** | 0.9 天 | 0.3 天 |
| LV4 | 315 | 110 | **够** | 2.8 天 | 0.9 天 |
| **LV5** | 430 | **115** | **差 2** | 3.8 天 | 1.2 天 |
| LV6 | 550 | 120 | 差 7 | 4.9 天 | 1.5 天 |
| LV10 | 1080 | 140 | 差 27 | 9.6 天 | 2.9 天 |
| LV20 | 2755 | 190 | 差 77 | 24 天 | 7.4 天 |
| LV30 | 4930 | 240 | 差 127 | 44 天 | 13.3 天 |
| LV40 | 7605 | 290 | — | 67 天 | 20.6 天 |
| LV50 | 10780 | 340 | — | 95 天 | 29.1 天 |
| LV60 | 14455 | 390 | — | 128 天 | 39.1 天 |

- **两个手感常量**（改它们只影响这两处）：
  - **起始 `100`**：调高 ⇒ 前几级也变成"差一点"，但**会失去"先建立信心"的阶段**（不推荐）；
  - **递增 `5`**：调大 ⇒ "差一点"的窗口缩短、更快变钝（**建议不超过 10**）。
- **⚠ 关于"差一点"的幅度**：LV5 只差 2、LV6 差 7、LV10 差 27。
  **幅度小才有"不如刷上去"的效果**；若某天只做了 80 XP（低于常规），差额会到 30+，
  这时**升不了级是正常的** —— 不是"卡死"，第二天按常规量做就能补回。
- **锚点**：普通学生一学期（约 60 个练习日）≈ **LV38**；尖子生 ≈ **LV77**。
  数字看起来快，是因为**频率高**正是设计目标（天天有反馈）；等级本身只是"投入刻度"，不必稀缺。
- 经验条显示：当前等级 + 距下一级百分比。
- ⚠ **等级无上限 + 数字增长快 ⇒ 徽章必须做成「档位 + 数字」**，不能一个等级一个图标（见第九节）。

### 3.2 等级显示：直接用 LV（2026-09-21 教师定）

**不做等级命名**（不要"冒烟实验→田野调查"那套学科梗）。一律显示 `LV1` / `LV2` / … / `LV12`。

理由：命名需要反复打磨且容易变成负担；而等级本身只是「长期投入的刻度」，`LV` 已经足够直观。
省下的注意力放在奖励设计上（见第四、五节）。

> 若将来想加称号，作为**里程碑奖励品**单独发即可（见 4.2），不必绑死在等级名上。

### 3.3 展示位置

- 首页今日目标卡下方/个人中心头部：**等级徽章 + 经验条 + 今日已获 XP**。
- 结算页（"再来一轮"旁）显示 +XP 的即时反馈（`+12 XP` 小字飘一下）。
- 完成页与打卡庆祝可连动（升级时弹「升级了！」）。

---

## 四、奖励体系

### 4.1 设计原则（2026-09-21 重定）

1. **奖励分三层，实物只从「月度」那一层出**：
   - **① 等级里程碑（长期）** → **纯虚拟**，零成本，可放心给、给得勤
   - **② 月度进度（每月）** → **实物的唯一出口**，并入既有的「月度之星」机制（见 4.5）
   - **③ 连续打卡（即时）** → 补签卡等虚拟奖励（见 4.3）
2. **等级里程碑不再挂实物**。教师明确不希望常态化动用实物奖励；
   且实物若在等级上发放，会与既有「月度之星」重复、还容易变成"熬到级别就有"的被动等待。
   ⇒ 等级负责**长期陪伴与即时反馈**，月度负责**稀缺的实物激励**。
3. **实物档必须人工核验**，杜绝纯线上自动兑换（XP 是本地算的，见第六节防作弊分级）。
4. 不按"每日表现"发实物（避免每天都要盯）。

### 4.2 等级里程碑奖励清单（**全虚拟**，2026-09-22 重排）

等级改为**无上限**后，里程碑不再绑定具体级别，改为**按规律分布**：

| 节点 | 奖励 | 说明 |
|---|---|---|
| **每 10 级**（LV10/20/30/…） | **补签卡 ×1 与加分卡 ×1 交替** | LV10 补签卡 → LV20 加分卡 → LV30 补签卡 … |
| **每 25 级**（LV25/50/75/…） | **加分卡 ×1**（**叠加**在上一行之上） | 即 LV50 同时触发两条 ⇒ 拿 **2 张加分卡** |
| **大节点 LV50 / LV100 / LV200** | **特殊徽章** + 加分卡 ×2 | 纯展示型，零成本 |
| 补签卡 | 与 4.3 的卡片余额**同一池子** | 不过期、可累积 |

- 原清单挂在 LV2/3/5/7/9/11/12 上，**等级延长后密度太稀**，故重排。
- ⚠ **频率是按 3.1 的曲线反推的**：普通学生一学期到 LV38，故"每 10 级"≈ 一学期 3~4 次奖励；
  若曲线参数调整（尤其递增 `5` 改动），**这一节的间隔要同步重算**，否则奖励会过密或过疏。
- **全部零成本**，可长期发放而不增加任何开销。
- 实物奖励**不在此表**，见 4.5。
- ⚠ **前置依赖**：特殊徽章需要**图标设计**，见**第九节**。
- 称号：原 LV5 的"自定义称号"保留为**可选**（想加就挂在 LV20，避免与加分卡抢节奏）。

#### 4.2.1 加分卡（2026-09-21 教师提出）

**作用**：学生在**作业记录**里选择一份已完成的作业/测验，**申请再次加分** ——
**允许在订正加分的基础上叠加**，但**仍然满分封顶**。

- **适用范围**：**仅限通过本站完成的作业与测验**（即 `quiz_submissions` 里已有记录的那些）。
  **线下纸质发放的作业不生效** —— 这条要在 UI 上写明，避免学生误用。
- **封顶**：`final = clamp(score − penalty + bonus + card_bonus, 0, M)`，**M = 真实满分**。
  现有结算函数 `applyGradingRules`（`quiz.ts`）已经是这个形状，只是需要多一个加成来源。
- **与订正加分的关系**：两者**独立并存、可叠加**。所以**不能复用现有 `bonus` 字段**
  （它是单一值），需并列新增 **`card_bonus`**；这样 UI 能分别显示「订正 +X / 加分卡 +Y」，
  也便于日后统计加分卡的实际使用率。
  **`quiz_submissions.grading` 是 jsonb，加字段无需改表结构。**

**已定（2026-09-21，教师拍板）**：

| 项 | 决定 |
|---|---|
| **加分量** | **满分的 10%** —— 与现有订正加分的 `percent: 10` **同一口径**，学生不必理解两套算法 |
| **叠加** | **可叠加于订正加分之上**，最终**满分封顶** |
| **每份限制** | **限用 1 张**（不可多张叠用，避免一次性冲顶，保持"额外激励"的分寸） |
| **适用范围** | **作业与测验都可** |
| **适用前提** | **仅限通过本站完成的**（`quiz_submissions` 里有记录的）；**线下纸质作业不生效** —— 这条要在 UI 上写明 |

**⚠ 实现前提：「测验」需要额外改造**（实装时容易漏）：

`types.ts` 里明确写着「评分规则快照（创建作业时固定；**测验为 null**）」——
即**测验目前完全没有 `grading` 机制**（`grading_rules` 与 `grading` 都只服务作业）。
所以要让加分卡作用于测验，必须先把这套结算扩展到测验：创建测验时也固化 `grading_rules`、
提交后也生成 `grading` 记录。**这不是加分卡本身的工作量，而是它的前置条件。**

**算法**：`card_bonus = round(M × 10%)`（`M` = 真实满分），最终
`final = clamp(score − penalty + bonus + card_bonus, 0, M)`。
与订正加分并列而非合并，便于分别显示「订正 +X / 加分卡 +Y」，也便于统计使用率。

- 补签卡：**与现有 `applyMakeup` 接合**——把"剩余补签卡张数"做成字段，`applyMakeup` 调用前先扣卡；无卡不可补签。细则见 **4.3**。
- 实物档金额建议控制在每次 5~30 元，兑换需学生在教师面前展示等级页面/或教师后台查该生云端数据核验。

### 4.3 补签卡细则（2026-09-20 教师定稿）

补签卡有**两个来源**，都进同一个余额池：

| 来源 | 频率 | 说明 |
|---|---|---|
| **每周达标** | 每周 1 张 | 沿用现有门槛：一周 ≥100 题且正确率 ≥80%（`MAKEUP_WEEK_QUESTIONS` / `MAKEUP_WEEK_ACCURACY`）。**取代**原来「当周赚当周用」的机制 |
| **等级里程碑** | LV2 / LV8 各 1 张 | 见 4.2 奖励清单 |

规则：

- **不过期**：卡片永久有效，跨周、跨月都不作废。
- **不设上限**：可以累积，攒多少用多少（教师 2026-09-20 决定——激励最足；接受「攒 20 张一次补一个月」这种极端情况）。
- **可补范围：最近 30 天内的漏签日**。补更久远的没有意义（那段时间的连续记录早已断掉），30 天既给足「随时可用」的感觉，又避免一次补太多。
- **补签后** `computeStreak` 自动重算：补上中间的洞，连续天数自然变长；不连续的补签不影响当前 streak（无副作用）。
- **扣卡时机**：`applyMakeup` 成功时扣 1 张；无卡则不可补。

**为什么要改**：原机制「当周赚当周用」有个明显漏洞——学生**本周全勤**（无漏签日可补）时，达标赚到的机会直接作废（`earnedMakeupWeeks` 下周清空）。努力拿不到实际好处，激励被浪费。改成卡片后奖励「存得住」，行为强化更强。

**存储与迁移**：

- 权威余额存在 `socio_vocab_xp.cards`（见 5.1）：每周达标 / 等级发奖时 `cards++`，`applyMakeup` 成功时 `cards--`。
- 「本周是否已发过」仍用现有 `earnedMakeupWeeks` 判定，防止同一周重复发放。
- ⚠ **迁移注意**：`cards` 是新增字段，老数据从 **0** 起算，**不要反推**。因为现有实现里 `earnedMakeupWeeks` 只在 `applyMakeup` 成功时才写入（见 `checkin.ts`），其长度**恒等于已用次数**，反推出来永远是 0 或负数，没有意义。

#### 4.3.1 上线时的「老同学」初始发放（2026-09-21 教师提出）

**动机**：在奖励体系引入**之前**就一直在打卡的同学，理应得到承认 —— 他们当时赚的补签机会
要么没用上、要么被「当周赚当周用」的规则作废了。所以上线时按**已有打卡记录**补发一批卡。

**可行性（已核对代码）**：打卡记录是**完整保留**的 —— `checkin.study` 按天存
`{ seconds, questions, correct }`，所以：
- **累计打卡天数**：遍历 `isDayChecked(checkin, dayKey)` 计数；
- **历史最长连续**：`checkin.bestStreak` 现成字段。

> ⚠ 这与 4.3 里「不能反推补签历史」**并不矛盾**：补签历史没留痕（`earnedMakeupWeeks` 只在补签成功时才写入），
> 但**打卡历史留了**。两者是不同来源，别混为一谈。

**建议标准（按里程碑累加，上限 7 张）**：

> **阈值依据实测分布定（2026-09-21，教师取向：从严）**。
> 当时 21 名学生的真实分布：零打卡 6 人；累计 1–3 天 12 人（其中 **≥3 天仅 4 人**）；4–7 天 0 人；
> 8–14 天 2 人；15–21 天 1 人；最多 20 天，**中位数仅 1 天**。历史最长连续：0 有 6 人、1–7 有 14 人、15–21 仅 1 人（最大 19）。
> 各档精确覆盖：**≥1 天 15 人 / ≥2 天 9 人 / ≥3 天 4 人 / ≥7 天 3 人 / ≥14 天 1 人**；连续 ≥3 天 3 人、≥7 天 1 人。
>
> **为什么门槛设在 ≥3 天而不是 ≥1 天**：打卡口径经历过一次放宽（见 `QuizTaker.tsx` —
> 「作业时间与正式练习同口径」），而单日门槛只有「10 分钟 + 20 题」，**做一次作业即达标**。
> 所以「累计 1–2 天」里有相当比例只是顺手做过一次作业，不代表真的在坚持。
> 分布上 15→9→**4** 的骤降也印证了这点：**≥3 天才是有意识连续打卡的那批人**。

| 条件 | 追加 | 实际覆盖（按 2026-09-21 分布） |
|---|---|---|
| 累计打卡 ≥ 3 天 | +1 | 4 人 |
| 累计打卡 ≥ 7 天 | +1 | 3 人 |
| 累计打卡 ≥ 14 天 | +1 | 1 人 |
| 历史最长连续 ≥ 3 天 | +1 | 3 人 |
| 历史最长连续 ≥ 7 天 | +1 | 1 人 |
| 历史最长连续 ≥ 14 天 | +1 | 1 人 |
| 曾补签过 ≥ 1 天 | +1 | **2 人** |

⇒ 该分布下：**约 16 人 0 张**（6 个零打卡 + 累计 ≤2 天者）、**2 人各 +1**（补签过）、
3 人 3 张左右、最活跃的 1 人可达 6–7 张。
**从严意味着覆盖面小，但这正是取向** —— 目标是认可「体系引入前就在坚持」的那批人，而不是普发。

> **「曾补签过」为什么会计入**（2026-09-21 教师决定）：实测 21 人中**仅 2 人用过补签、且最多 2 天**，
> 计入的实际影响极小；而那 2 人「漏了又把洞补上」本身就是投入的体现，值 1 张。
> 这与前面「已补签天数不适合当努力证明」的顾虑并不冲突 —— 顾虑是怕它喧宾夺主，
> **所以只给一档（+1），不分档累加**。
> 计数方式：`Object.keys(checkin.makeup).length`（即 `makeup` 里的日期数）。

- **以累计为主、连续为辅**：累计反映「总投入」，对「中途断过但一直在做」的同学更公平；
  连续作为额外加成。**只按连续发会让断过一次的同学几乎颗粒无收**，那反而打击了最努力的那批人。
- **上限 7 张**（累计 3 档 + 连续 3 档 + 补签 1 档）：不是抠门，而是初始发放**不该盖过后续每周赚卡的节奏** ——
  上线第一天就拿 20 张，后面每周 1 张就毫无吸引力了。按当前分布最活跃的 1 人刚好触顶。
- **「已补签天数」（`Object.keys(checkin.makeup).length`）不计入**：它代表的是「曾经漏过再补上」，
  不适合当作努力程度的奖励项。
- **阈值全部做成常量表**：学生积累会随时间增长，半年后同一套门槛覆盖的人群会完全不同 ——
  届时按当时分布再调一次即可。

**⚠ 实现要点（按重要性排序）**：

1. **只能发一次**。必须在 `socio_vocab_xp` 上加 `legacyGranted: true` 之类的一次性标记；
   否则每次登录重算都会再加一遍 —— 这是最容易踩的坑。
2. **只发给学生**。教师账号通常也有打卡数据，不该参与发放。
3. **以「首次登录即体系上线那一刻」的 `checkin` 为准**，之后的新打卡不再重复计入初始发放。
4. **阈值与张数做成常量表**，便于上线后按实际分布调整（先看数据再定更稳）。

**状态（2026-09-21）**：

- ✅ **发放依据已定**：累计打卡为主 + 历史最长连续加成。
- ✅ **阈值已按实测分布定为「从严」版**（见上表）：累计 3 / 7 / 14 天，连续 3 / 7 / 14 天。
  教师取向明确是**从严** —— 理由是打卡口径放宽过（作业与正式练习同口径），
  「累计 1–2 天」不足以证明真的在坚持；分布上 ≥3 天的骤降点也支持这个切分。
- ✅ **「曾补签过 ≥1 天」已计入**（+1，不分档）。实测 21 人中仅 2 人用过补签、最多 2 天，
  影响面极小；而「漏了又把洞补上」本身就是投入的体现。
- ✅ **上限 7 张**（累计 3 档 + 连续 3 档 + 补签 1 档）。
- ⏳ **随时间复查**：学生积累会增长，半年后同一套门槛的覆盖面会完全不同，
  届时按当时分布再调一次即可（阈值与张数都在常量表里，改动成本很低）。

### 4.4 兑付流程（实物档，人工核验）

1. 学生达标 → 在打卡页点「申请兑换 LV5 奖励」生成一句申请（含等级、时间）。
2. 教师课余口头/后台核对 → 兑付 → 后台勾选「已发放」防重复。

> 该流程不依赖强防作弊（教师目视核验），是最小成本落地路径。

### 4.5 月度奖励体系（2026-09-21 定，**实物的唯一出口**）

**与「月度之星」的关系**：月度之星是校内**既有机制**、本来就有实物奖励。
本方案**不新建一套发奖流程**，而是给它一个**数据化的评选依据** ——
把「凭印象评」变成「看数据评」，顺带省掉教师翻记录的工作量。

#### 4.5.1 两个奖项

| 奖项 | 名额 | 条件 | 奖励 | 单价（2026-09-21 教师给） |
|---|---|---|---|---|
| **月度之星** | **1 名** | **本月 XP 增长最高** | 实物（沿用现有月度之星奖品） | **30~50 元**（过往实操区间，按 40 估算） |
| **全勤奖**（二等奖） | **不限**（达标即得） | **本月打卡 ≥ 28 天** | 实物（奶茶级别） | **10~15 元**（按 12 估算） |

**成本模型（按上述单价估算）**：

| 项 | 月度 | 一学期（约 4 个月） |
|---|---|---|
| 月度之星 × 1 | 约 40 元 | 约 160 元 |
| 全勤奖 × N | 12 × N 元 | 48 × N 元 |
| 等级里程碑 | **0 元**（纯虚拟） | **0 元** |
| 补签卡 / 加分卡 | **0 元**（纯虚拟） | **0 元** |

- 按 2026-09-21 的分布，**本月打卡 ≥28 天为 0 人**，全勤奖暂时不产生支出；
  但随着打卡习惯养成会增长，按 3~5 人估算即 **36~60 元/月**。
- ⇒ **合计约 80~100 元/月，一学期约 320~400 元** —— 与现有「月度之星」的支出规模相当，
  **没有因为引入练级体系而增加实物开销**（这也是把实物只留在月度层的意义）。

- **月度之星**反映「投入强度」，每月仅 1 人 ⇒ 稀缺感与既有机制一致。
- **全勤奖**反映「坚持」，达标即得、不限名额 ⇒ 是**可达成的目标**而非零和竞争。
  两者一竞争一达标，覆盖不同的激励心理，也让多数人有奔头。
- **并列处理**：月度之星若 XP 增长并列，以**本月打卡天数多者**优先；仍并列则**都发**（人数少，成本可控）。

#### 4.5.2 指标口径

**本月 XP 增长** `= xp − monthStartXp`：

- `monthStartXp` 在**每个自然月的首次记录时**写入，并用 `monthKey`（如 `'2026-09'`）标记归属月份；
- 跨月首次调用时，先把当前 `xp` 写入 `monthStartXp`、更新 `monthKey`，再累计当次 XP；
- ⇒ 月度增长是**相对月初基线**，与历史总量无关。**老同学不会因为"底子厚"而占优**。

**全勤奖**：本月 `isDayChecked` 为真的天数 **≥ 28**。
固定 28 而非「当月天数」，是为了口径简单、学生好理解；代价是 2 月必须全勤。**阈值可调**。

#### 4.5.3 新同学的参评规则（教师定：次月才开始）

**本月加入的同学，本月只记录不参评**。

- 判据：新增一个 `firstSeenAt`（首次开始使用的日期键）；
  **`firstSeenAt` 的年月 ≠ 当前年月 ⇒ 可参评**，相等 ⇒ 本月加入、仅记录。
- 理由：月中加入者 XP 天然少于老同学，**按总量比必然吃亏**；
  而「按日均折算」又会让新同学的短期热情更容易冲到榜首，**反而对老同学不公**。
  ⇒ **"次月参评"最简单、最公平，也不需要额外解释**。
- 副作用可接受：首月只是拿不到**月度奖**，**等级、补签卡、连续打卡全照常发放**（它们不依赖月度评选）。

#### 4.5.4 结算与核验

1. **每月 1 号之后**，教师在后台查看上月榜单（按 XP 增长降序，附打卡天数与「本月新加入」标记）。
2. 系统给的是**候选数据**；XP 由本地累计（A/B 档防作弊，见第六节），
   **发放实物前必须核验** —— 与 4.4 同一路径：看云端 `student_data.data.xp`，或当面确认。
3. 核验后勾选「已发放」防重复。
4. **不展示"未达标"名单**，只列获奖者与榜单前几名，避免打击后进。

**⚠ 前置需求：打卡核验页需要「月度视图」（2026-09-22 教师提出）**

现有 `TeacherCheckPanel` 只显示**累计**统计（`checkinDays` / `totalQuestions` / `accuracy` 等），
而月度体系（4.5）**必须按自然月核验**，否则无法判断「本月打卡 ≥28 天」。

**要加的能力**：

- **月份选择器**：从学生数据里提取出现过的月份（`checkin.study` 的 dateKey 前缀 `YYYY-MM`），倒序；
- 选中某月后，各行显示**该月**的：打卡天数、练习题数、正确率、**是否达全勤（≥28 天）**；
- **每行附一个月度日历格**（该月每天一格，**三态**）—— **目视核验最省力**：
  - **达标**（正常色）：该日 `seconds ≥ 600 && questions ≥ 20`；
  - **有练习但未达标**（警示色）：有记录但没够线 —— **这一态最关键**，
    能区分「完全没做」与「做了但差一点」，直接对应实测中 **44.8% 的未达标天数**（见第八节）；
  - **无记录**（灰）：当天没有任何练习。

**为什么不依赖 XP 体系**：打卡数据（`checkin.study`）**早就存在**，
故这个视图**可以独立于 P0 先做**；等 XP 上线后再在表里补一列「本月 XP 增长」即可。

#### 4.5.5 展示位置

- 首页 / 个人中心：显示**本月 XP 增长**与**距全勤还差几天** —— 这两个是本月的可操作目标。
- 打卡页（`StreakCard`）：月末几天提示「再坚持 X 天即达全勤」。
- 月度榜单：教师端可见完整榜；**学生端只显示自己是否达标 / 是否获奖**，不公示排名。

---

## 五、数据与兼容

### 5.1 本地存储

沿用 scoped 命名空间（`user:<id>`），新增 key：

```
socio_vocab_xp: {
  xp: number,               // 历史累计
  todayXp: number,          // 当日已获（按 todayKey 重置）
  todayKey: string,
  level: number,            // 可由 xp 现算，仅作缓存
  cards: number,            // 补签卡余额（4.3）
  redeemed: string[],       // 已兑换的里程碑（防重复发放）
  legacyGranted: boolean,   // 老同学初始补签卡是否已发（4.3.1，必须，否则每次登录重复发）
  // ---- 月度体系（4.5）----
  monthKey: string,         // 当前统计归属月份，如 '2026-09'
  monthStartXp: number,     // 该月月初的 xp 基线 ⇒ 本月增长 = xp - monthStartXp
  firstSeenAt: string,      // 首次开始使用的日期键；用于判定"本月是否新加入"（4.5.3）
  // ---- 加分卡（4.2.1）----
  cardCount: number,        // 加分卡余额（等级里程碑发放，用一张减一）
}
```

**加分卡的「已用」记录放在作业一侧，而不是本地**：

- 在 `quiz_submissions.grading` 里加 `card_bonus?: number` 与 `card_used?: boolean`；
- 「这份作业用过卡没有」直接看该提交记录的 `card_used` —— **云端权威、换设备也不会重复用**，
  比在本地记"哪份作业用过"可靠（本地记录会随清缓存丢失，从而被重复使用）。
- 结算时 `final = clamp(score − penalty + bonus + card_bonus, 0, M)`，
  现有 `applyGradingRules`（`quiz.ts`）只需多接一个加成来源。

- `todayXp` 按天重置（比对 `todayKey`）；`xp` 为历史累计。
- `level` 可由 `xp` 查表推导（**只存 xp，等级始终由曲线函数现算**，避免两处维护）。
- `monthKey` / `monthStartXp` 在**每月首次记录时**翻转（见 4.5.2）；`firstSeenAt` 首次写入后不再改动。
- `legacyGranted` 与 `redeemed` 都是**一次性发放的防重标记**，缺了就会重复发奖 —— 这是最容易踩的坑。

### 5.2 云端兼容

- `student_data.data` 目前整包上传；把 xp 字段并入 data JSON 即可（学生端自带）。
- **教师要核验奖励，看 student_data.data.xp**。
- 老数据无该字段 → 读默认 `{xp:0}`，天然向后兼容。

---

## 六、防作弊分级（承接此前"排行榜防作弊三档"探讨）

| 级别 | 做法 | 能挡住 | 成本 |
|---|---|---|---|
| A | 纯本地 XP + 展示 + 虚拟奖励 | 不防改；纯娱乐 | 几乎零 |
| B | A + 实物档**教师人工核验**（看云端/当面） | 防"改数据骗兑奖"（靠目视/抽查） | 低 |
| C | 服务端权威计数（学生只上报"做了一题"事件，XP/等级服务端累计，学生无写总入口；`vocab_releases` 同款 RLS/RPC 套路） | 防直接改数据 | 中（需新表 + RPC + 前端事件上报改造） |

**建议：首期上 B 档**（XP 本地算、虚拟奖励即时、实物教师核验）。
**C 档留作**：若将来奖励价值上升或出现"学生互相帮刷"，再按此前防作弊方案（数据库触发器合理性约束 + 服务端累计）升级；届时 XP 结构设计（key 即 `xp/todayXp/todayKey`）能平滑迁移。

---

## 七、落地分期

| 阶段 | 内容 | 涉及 |
|---|---|---|
| P0（展示型，先让学生有感觉） | XP 累计 + 每日上限 + 等级/经验条 UI + 结算页 +XP 反馈；`recordItem` 挂 XP | 无新题型，纯前端 + storage key |
| P1（奖励规则） | 补签卡张数与 `applyMakeup` 扣卡联动（细则见 **4.3**：每周达标 + 等级里程碑两个来源、不过期、可累积、可补最近 30 天）、等级里程碑发放、兑换申请 UI；教师端「已发放」核验页。**改 4.3 时需同时改掉现有「仅限当周漏签日」的限制**（`checkin.ts` 的 `missedDaysInWeek`） | 前端 + 打卡页改造 |
| P1.5（月度体系） | **月度之星 / 全勤奖**（4.5）：`monthStartXp` / `monthKey` / `firstSeenAt` 三个字段、月末榜单、教师核验页、「距全勤还差 N 天」提示。**这是实物奖励的唯一出口**，也是与既有「月度之星」机制的接合点 | 前端 + 教师端榜单 |
| P2（可选加固） | 服务端权威 XP/等级（C 档）、月度榜单服务端统计 | 新表 + RPC + 事件上报 |

---

## 八、待教师拍板的点

### 已定（2026-09-21，不再待议）

- **等级显示**：不做命名，直接 `LV1 … LV12`（3.2）。
- **奖励结构**：三层 —— **等级里程碑纯虚拟 / 月度层是实物唯一出口 / 连续打卡给补签卡**（4.1）。
- **等级里程碑不含实物**（4.2 已重写为全虚拟清单）。
- **月度体系**：**月度之星 1 名**（本月 XP 增长最高）+ **全勤奖不限名额**（本月打卡 ≥28 天），两者都给实物（4.5）。
- **新同学次月才参评**（4.5.3）。
- **补签卡**：不过期、不设上限、可补最近 30 天、来源为每周达标 + 等级里程碑（4.3）。
- **老同学初始发放**：按已有打卡记录，**从严版阈值**（累计 3/7/14 天、连续 3/7/14 天、曾补签 ≥1 天，上限 7 张）（4.3.1）。
- **新增虚拟奖励「加分卡」**（4.2.1）：**加满分的 10%**（与订正加分同口径）、可叠加于订正加分之上、满分封顶、**每份限用 1 张**、**作业与测验都可**、仅限平台内完成者。
- **定义题 XP 已定**（2.1）：全对 **10**、部分正确 **5**、错 **2**。
- **逻辑接龙改为「完成结算」**（2.1.1，原「每步 5 XP」废止）：仅起点四选一 **20** / 输入 **30**；起点→终点四选一 **25** / 输入 **35**；**中途放弃不给**。
- **起点→终点的选项生成已改造**（2.1.2，代码已实现）：干扰项混入"相邻但方向错误"的概念，实测"只有 1 个相邻选项"的比例从 **100% → 2.2%**。
- **每日上限 400**（2.2，由 120 上调；依据：全体中位 113 XP/天、尖子生 370 XP/天）。
- **等级不设上限**，曲线为每级递增 `100 + 5×(n-1)`（3.1，原「LV12 封顶」已废止）：
  前 3 级增量（100/105/110）**低于普通学生日均 113 ⇒ 每天升一级**；之后略高于日均 ⇒ **差一点点**，
  靠 near-miss 心理驱动**自愿加练**（**不提高门槛**，呼应"门槛只做兜底"）。
- **等级里程碑重排**为「每 10 级 / 每 25 级 / 大节点（LV50·100·200）」三档（4.2）。
- **上线目标：2026-09-30（国庆假期前）**。动机：**利用国庆假期开始布置打卡任务** ——
  假期学生在家、时间自由，是养成打卡习惯的窗口期，故 XP 体系**必须在假期开始前可用**
  （这不是"顺便赶在假期前"，而是**假期就是第一次实战**）。
- **奖品单价**：月度之星 **30~50 元**（按 40 估）、全勤奖（二等奖）**10~15 元**（按 12 估）。
  ⇒ 一学期约 320~400 元，**与现有月度之星支出规模相当，未因引入体系而增加实物开销**。

### 仍待定

1. **打卡门槛是否微调**（**教师 2026-09-22 决定暂不改逻辑**）：实测 44.8% 的练习日未达标，
   主因是「20 题 + 10 分钟」**内部不协调** —— 按实测中位速度 2.6 题/分钟，做完 20 题约 **7.7 分钟**，
   到不了 10 分钟门槛。教师判断：**作业量本身就是 20 题，门槛以兜底为主**，有学习意愿的学生超额是自发行为，
   不该用门槛去驱动。**建议的「时间门槛 10 → 8 分钟」尚未采纳**，留待观察实际体验。
2. **等级里程碑的称号位**（4.2）：是否保留"自定义称号"，挂哪一级。
3. **加分卡：使用窗口**（建议作业结束后 N 天内，或本学期内；太晚用会失去意义）。
4. **加分卡的前置改造**：**测验目前没有 `grading` 机制**（`types.ts` 注明"测验为 null"），
   要让加分卡作用于测验，需先把结算扩展到测验 —— 这是加分卡实装的前置条件（4.2.1）。
5. **全勤奖阈值 28 天**是否合适：
   - **2 月会退化成「必须全勤」**（当月仅 28 天）。
   - ~~假期会让全勤失效~~ —— **此判断已被否定（2026-09-22）**：本站的用途就是
     「让大家相对随时随地地练习词汇」，**放假不等于上不了网**；恰恰相反，
     **国庆假期正是布置打卡任务的黄金期**（时间自由、人在家里），故**全勤与假期不冲突**。
   - 本次审计导出的数据未含单月日期分布，无法据此判断阈值是否合理；
     **下次跑审计时应在脚本里带上逐日日期**（现脚本已导出 `daily[].date`）。
6. **视觉资产**（第九节）：等级徽章/补签卡/加分卡图标，教师已计划交由其他 agent 设计。
7. **是否现在立项 P0**（预计半天内可完成首版）。

> **2026-09-20 教师决定**：
> - **补签卡规则已定**（不过期、不设上限、可补最近 30 天、每周达标与等级里程碑两个来源）→ 见 **4.3**，不再待议。
> - **补签卡不单独实现**，与等级奖励体系**一并落地**（即归入 P1，不提前插队做）。
> - 其余各点（XP 数值、等级曲线、命名风格、实物档）仍待拍板。
>
> **2026-09-22 教师决定**：
> - **每日上限 400**（2.2）；**等级不设上限**、曲线每级递增 `120 + 10×(n-1)`（3.1）；**里程碑重排**（4.2）。
> - **打卡门槛逻辑暂不改** —— 作业量本身就是 20 题，门槛以兜底为主，有学习意愿的学生超额是自发行为。
> - **视觉资产**交由其他 agent 设计（见第九节）。

---

## 九、上线前需要准备的视觉资产（供设计方）

代码侧先用纯 CSS / emoji 也能跑通，但下列图标**直接影响学生感知**，建议上线前补齐。

| # | 资产 | 用途 | 优先级 |
|---|---|---|---|
| 1 | **等级徽章** | 首页今日目标卡、个人中心头部的 `LVn` 标识 | **高**（天天看到） |
| 2 | **补签卡** | 打卡页的卡片余额、补签按钮 | **高**（学生会点） |
| 3 | **加分卡** | 作业记录页的申请入口 | **高**（学生会点） |
| 4 | **特殊徽章**（LV30 / LV50 / LV100） | 个人中心的展示墙 | 中（稀有，少数人解锁） |
| 5 | **升级动效**（可选） | 升级瞬间的弹窗/特效 | 低（锦上添花） |

**设计约束（重要，请设计方务必遵守）**：

1. **等级无上限** ⇒ 徽章**绝不能"一级一个图标"**，必须做成「**固定档位 + 数字**」的形式
   （例如 5 个色环档位，等级数字由文字渲染叠加）。否则 100 级以后无法维护。
2. **补签卡与加分卡要用同一套卡片语言**，靠配色 / 符号区分即可 ——
   它们在 UI 里经常同时出现（余额并排展示）。
3. **卡片图标需在小尺寸（约 24px）下可辨认** —— 会出现在列表行与按钮里，不只是大图。
4. **深浅主题都要清晰** —— 站点支持主题切换（见 `src/index.css` 的 CSS 变量）。
5. 交付格式建议 **SVG**（可缩放、可换色），另附 PNG 备用。

**可以先不管的**：等级徽章"每一档具体长什么样"**不是阻塞项** ——
P0 阶段用文字 `LV3` 就能上线，图标到位后再替换即可。
