# 分数同步到 ManageBac 方案

> 目标：把「试卷成绩」（`grouper_runs`）里录好的分数，**一键写进 ManageBac 成绩册**，
> 替代现有「复制成绩（邮箱⇥分数）→ 油猴脚本在 ManageBac 页面粘贴」流程。
>
> 状态：**设计中（未实现）** · 最后更新 2026-09-15

---

## 〇、决策与进度

| 议题 | 结论 | 日期 |
|---|---|---|
| 短码方案 | **采用**，格式 `[A1-0712]`（年级位 + 创建日月日，见 §2） | 2026-09-15 |
| 短码中间位用什么 | **年级位（A1/A2）** —— 教师判断：paper 位无意义（短期不会同天布置不同 paper，混搭总复习也不分两个 task 建），年级才有意义 | 2026-09-15 |
| 反向情形（task 先建好、名字里没短码 → 列出来让教师点选 + 顺手补短码改名） | **不做** —— 教师判断该情形几乎不存在。只保留「匹配不到 → 报错停下」 | 2026-09-15 |
| 班级绑定 | **做进前端**（教师后台 → 班级管理 → 贴 ManageBac 成绩册 URL） | 2026-09-15 |
| 学生定位键 | **名单 xlsx 桥接**（邮箱 → ManageBac 姓名 → 按姓名定位行）——行内无稳定学生 id（见 §6） | 2026-09-15 |
| 名单导入频率 | **不强制每学期重导**；发现有人匹配不到时**提示重导** | 2026-09-15 |
| 匹配范围 | 短码匹配在**该班级的 task 列表内**进行 ⇒ 同一作业发给两个班（两个 task 同名）也不歧义 | 2026-09-15 |
| **随堂测验 / 作业也要同步** | 教师 2026-09-15 提出 ⇒ `mb_task_links` 改**双源**（`run_id` / `quiz_id` 二选一）、`quizzes` 增 `mb_short_code` | 2026-09-15 |
| **「不登分」例外学生** | 教师 2026-09-15：AS 班有一名学生不进 ManageBac 名单（业余自学、不需要登分）⇒ `student_data.mb_exempt`：勾上后不计入名单缺口、同步时跳过 | 2026-09-15 |

---

## 一、与现有流程的关系

**现有（零凭证、零服务器，可继续用）**：

- `PaperResults.tsx` / `QuizManager.tsx` →「复制成绩（ManageBac）」→ 每行 `邮箱⇥分数`
- 油猴脚本在 ManageBac 页面读剪贴板 → 匹配学生 → 填分

**本方案**：把「复制 → 切页面 → 粘贴」自动化（服务端用 Cloudflare Browser Rendering 驱动），
**沿用油猴已验证的两件事**：① 匹配键用邮箱；② 显式约定（短码/名单）优于靠猜。

---

## 二、短码约定

**格式**：`[A1-0712]` / `[A2-0712]`

| 段 | 含义 |
|---|---|
| `A1` / `A2` | **年级位**：A1 = AS（Paper 1–2），A2 = Paper 3–4 |
| `MMDD` | **创建日**的月日（不是布置日、不是截止日） |
| 冲突 | 同一天第二次 → `[A1-0712b]`、第三次 `[A1-0712c]` |

要点：

- **我方生成、创建记录那一刻固定，永不改变**（教师直接复制，不需自己造）
- 年级位沿用现有推断规则（`PaperResults.tsx` 的 `inferClassId`：卷名含 `AS`/`A1` → `A1`；`A2`/`P3`/`P4` → `A2`），创建时可手改
- **随堂测验 / 作业**（`quizzes`）也发短码：年级位由 `quizzes.papers` 推（P1/P2 → A1、P3/P4 → A2）；这份卷没选 paper 时，在成绩页的下拉里选一次
- 若一次作业同时发给两届、无法唯一推断 ⇒ 创建时由教师选一下
- 放在**方括号**里 ⇒ 可用精确正则提取，不怕教师在名字前后加字
- 用法示例：`[A1-0712] AS Homework #4 Socialisation`
- **已建好的 task 也可以手动补短码**（教师 2026-09-15 确认）：匹配只看 task 名里有没有 `[短码]`，谁写的、什么时候补的都不影响 ⇒ 原先讨论的「反向情形」其实**不需要额外功能**，手动补一下名字即可 ✓
- 已知小限制：跨年后 `MMDD` 可能与上一年重复。因匹配只在**当前 term 的班级范围**内进行，判定可接受
- 若将来多科目共用同一班成绩册，可在前面加学科字母（`[S-A1-0712]`）—— 暂不需要

---

## 三、班级绑定（每班一次）

`classes` 增 `mb_class_url` + `mb_class_id`（前端从 URL 当场解析并回显，避免贴错班）。

教师贴的形态（实测）：

```
https://dtd.managebac.cn/teacher/classes/11496547/gradebook/core_tasks
                                             └ class id ┘
```

- 校验：必须是 `https://<school>.managebac.cn/teacher/classes/<数字>/gradebook/...`
- 存**整条 URL**、不写死形态 ⇒ 教师贴 `core_tasks` 视图或 `term/<id>` 视图都可以

---

## 四、流程

### 主路径

| 步 | 教师 | 系统 |
|---|---|---|
| 0 | 学期初，每班贴一次成绩册 URL | 解析出 class id 并回显「已绑定」 |
| 1 | 在我们这边创建本次练习（组卷器保存 / 新建试卷记录） | **生成短码** `[A1-0712]` |
| 2 | — | 「试卷成绩」详情页出现 ManageBac 区块：短码 + `复制短码` + 状态「未绑定」 |
| 3 | 去 ManageBac 建 task，名字里粘上短码 | — |
| 4 | 回来点「绑定」 | 抓该班 task 列表 → 按短码精确匹配 → 唯一命中即绑定并回显 task 全名<br>匹配不到/多条 → **停下报告**（不写任何东西） |
| 5 | 正常阅卷录分（现有流程） | — |
| 6 | 点「同步到 ManageBac」 | 差异预览 → 教师确认 → 行内写分 → 回读校验 → 报告 + 截图 |

**第 7 步以后不用再管**：绑定存的是 `core_tasks/<id>`，**task 改名不影响**。

**界面分工（2026-09-15 定，教师要求）**：两套机制放在同一张「ManageBac 同步」卡片里，用文案与分区区分 ——

- 上半区标 **「线上自动同步」**（短码 / 班级绑定 / 名单 / task 绑定）
- 下半区标 **「本地脚本同步」**（兜底）：原「复制成绩（ManageBac）」按钮移到此处并改名，含义与输出格式（`邮箱<Tab>分数`）**不变**

### 失败与安全

- 短码匹配失败 ⇒ 报错提示「请检查该 task 名里是否粘了 `[A1-0712]`」，**绝不猜、绝不写**
- **写前差异预览**是真正的安全网：显示「即将写入：`[A1-0712] AS Homework #4` · 12 行」及逐行 78 → 82
- 写入后**回读校验**：读回该行分数与字母等级是否随之变化

---

## 五、匹配层级

| 层级 | 依据 | 可靠性 |
|---|---|---|
| 首选 | 已存绑定 `mb_task_id`（URL 里的数字 id，改名不变） | 确定 |
| 次选 | 该**班级** task 列表内按 `[短码]` 精确匹配 | 确定 |
| 兜底 | 无（匹配不到即停下） | —— |
| **永不采用** | 名字模糊匹配、按创建时间猜 | 会错填 |

---

## 六、学生定位：为什么用邮箱 + 姓名

**实测结论（2026-09-15，`app/_ocrlab_out/mb-rows-*.json`）**：

- 成绩册不是 `<table>`，是 CSS grid：`div.grid-table-row.student-grade`
- 分数输入框：`input#grade-score-<数字>`，`name="core_task[grades][score]"`，每生唯一
- 行内 `<a>` 带 `/xxx/:n/xxx` 形态属性，但**那个数字不是学生 id** —— 教师编油猴脚本时已核对：与「学生个人信息主页 URL」里的数字对不上
  ⇒ `grade-score-<n>` 是**成绩记录 id**（随 task 变），不能作跨 task 的学生标识

**因此沿用邮箱桥接**：

```
ManageBac 导出的班级名单 xlsx（姓名 + 邮箱）
        ↓  导入一次（前端已有 xlsx 解析能力：xlsx@0.18.5 + src/lib/excelImport.ts）
邮箱 → ManageBac 显示名  映射表
        ↓  与站内学生邮箱对上（学生须用学校邮箱注册）
按显示名定位成绩册行 → 填分
```

- 学生**必须继续要求用学校邮箱注册** —— 这是整条链路的唯一可信键
- 复核机制：名单 xlsx 里同一邮箱的显示名 = 填分时用的行名
- 匹配不到的人（新转学等）⇒ 预览里列出，提示「请重新导出名单」

---

## 七、界面事实（教师实测 2026-09-15）

| 事实 | 影响 |
|---|---|
| 「全部任务」视图默认列 current term，有下拉可切 term | 抓取只抓当前 term；短码未命中时提示「可能在别的学期，请切换 term 后重试」 |
| 不分页 | 一次 DOM 解析即可拿到该 term 全部 task |
| 表格横向滑动 | 待实测：是真 overflow（DOM 全在，纯解析即可）还是虚拟渲染（需滚动触发） |
| 单 task 页 URL 形态 | `.../gradebook/term/109300/core_tasks/27568440`（term + task 同在 `gradebook/` 前缀下） |

---

## 八、数据模型（迁移草稿：`db-migration-mb-sync.sql`）

| 对象 | 内容 |
|---|---|
| `grouper_runs.mb_short_code` / `quizzes.mb_short_code` | 短码（普通索引；唯一性由应用层**跨两表统一查重**保证） |
| `classes.mb_class_url` / `mb_class_id` | 班级 ↔ ManageBac 班级绑定 |
| `student_data.mb_exempt` | 「不登 ManageBac 分」标记（TRUE = 不在 ManageBac 名单、不需要登分） |
| `mb_rosters` | 名单映射：`class_id` + `email` + `mb_name`（`class_id + email` 不重复，由应用层「先清空该班再整批写入」保证） |
| `mb_task_links` | task 绑定：**双源** `run_id` / `quiz_id`（CHECK 二选一）+ `class_id` + `mb_class_id` + `mb_task_id` + `mb_task_name`；一条作业每班最多一个 task，**一对多**以支持多班 |

迁移文件：`db-migration-mb-sync.sql`（第一批）+ `db-migration-mb-sync-2.sql`（第二批：测验支持与例外学生）。

RLS 一律「仅 teacher / developer」（沿用 `db-migration-grouper-runs.sql` 的写法）。

> 已定（2026-09-15）：名单表 `mb_rosters` **两边共用** —— 按班级存一份，试卷与测验都用它。

---

## 九、隐私与安全

- ManageBac 登录态：走官方 `features/live-view` + `human-in-the-loop` 让教师**手动登录一次**，cookie 只存在本地 `app/_ocrlab_out/mb-cookies.json`（已被 `.gitignore` 的 `app/_ocrlab_*` 覆盖，**不入库**）
  - **2026-09-15 起推荐改用「本机登录」**（教师反馈 CF 版 live-view 登录"加载特别卡" —— 本质是远端投屏：浏览器在 CF 机房，键鼠往返延迟高，且登录过程**消耗浏览器额度**）：用 `app/scripts/mb-login-local.mjs` 调**本机已装的 Chrome/Edge**，正常速度、**不消耗任何 CF 额度**；给它一个**独立且持久**的 profile（`%LOCALAPPDATA%\mb-login-profile`，刻意不放工作区以避开 OneDrive 同步；Chrome 136+ 只允许"非默认 profile"开启调试端口）⇒ profile 复用后**通常免登录**。输出文件格式与 CF 版完全一致，`mb-tasks.mjs` 等下游脚本零改动；脚本只打印 cookie 条数与名字，**不打印值**
- `mb-rows-*.json` 勘探产物曾含真实学生姓名（中文掩码是之后才补上的）⇒ 建议同步功能做完后**清理该目录**
- 分数写入属于**破坏性操作** ⇒ 必须「写前预览 + 教师确认 + 写后回读校验」三步齐全
- 浏览器额度：免费版 **10 分钟/天**，一轮（建会话+开页+读结构+关闭）实测 **8.4 秒** ⇒ 每天约 70 次，够用

---

## 十、待办

- [x] ~~迁移执行~~ → `db-migration-mb-sync.sql` + `db-migration-mb-sync-2.sql` **均已执行**（2026-09-15，本机 psql 直连，不是控制台手工）
- [x] ~~短码生成（创建记录时）+ 「试卷成绩」详情页 ManageBac 区块（复制短码 / 绑定状态）~~ → 已完成（新记录自动生成；旧记录详情页一键生成）
- [x] ~~班级管理 → 「绑定 ManageBac 成绩册」输入框 + URL 解析校验~~ → 已完成（当场解析并回显 ManageBac 班级号）
- [x] ~~名单 xlsx 导入（姓名 + 邮箱）→ `mb_rosters`~~ → 已完成（自动认姓名/邮箱列；与站内邮箱比对并提示缺口）
- [x] ~~随堂测验 / 作业也接入~~ → 已完成（`quizzes.mb_short_code` + 测验成绩页同款区块）
- [x] ~~「不登分」例外学生~~ → 已完成（班级管理「学生分班」勾选框；名单缺口提示与同步都会跳过）
- [ ] Worker：抓班级 task 列表 → 短码匹配 → 绑定（`/app-api/*`）
- [ ] Worker：差异预览 → 确认写入 → 回读校验
- [ ] **第 4 步进度（2026-09-15）**：原型脚本 `app/scripts/mb-tasks.mjs` 已写好（只读：抓某班 task 列表 → 按 `[短码]` 精确匹配 → 报告命中/未命中/多条，并顺带报告 term 下拉与横向滚动 DOM 情况）；**当天浏览器额度已用尽（429，按天重置、不扣费）** ⇒ 次日先跑
      `node scripts/mb-tasks.mjs --class 11420931`（A2）/ `--class 11496547`（AS）验证读取与匹配，通过后再把这段逻辑搬进 Worker + UI 的「绑定」按钮
- [ ] 班级号备查：**AS = `11496547`**（站内班名 `AS Sociology`）、**A2 = `11420931`**（`A2 Sociology`）
- [ ] 若次日 cookie 过期：脚本会提示重跑 `node scripts/cf-managebac-login.mjs` 手动登录一次（cookie 存 `app/_ocrlab_out/mb-cookies.json`，不入库）
- [ ] 实测：`core_tasks` 视图是否含全部 term 的 task、横向滑动下 DOM 是否完整
- [ ] 实测：managebac 登录 cookie 的有效期（决定教师多久需重新登录一次）
- [x] ~~决定：随堂测验（`QuizManager`）是否也接入同一套同步~~ → **已定：要接入**（教师 2026-09-15）
