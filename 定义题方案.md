# 定义题：给出术语 → 学生写定义（💬 讨论中，2026-09-01）

> 记录一次关于「反写题型」判定方案的讨论。长期规划，未实现，先存方案。
> 相关现状：词条已有 `term`（术语）、`definition`、`chinese`（中文释义）字段；现有拼写题为「给释义→写术语」，本方案为反向题型。

---

## 一、需求背景

- 把「拼写默写」反过来：**给出术语，要求学生写出定义**（中文释义或英文定义）。
- 难点不在题型本身，而在**判定**：定义不是唯一答案，无法和词条释义要求一字不差。

## 二、为什么难（判定本质）

- 开放答案空间无限：同义改写 / 语序颠倒 / 主被动互换 / 不完整 vs 冗余 / 口语化 vs 学术化。
- 判定从「字符串匹配」变成「**语义判定**」——容错空间无法用相似度枚举。

## 三、方案阶梯

| 方案 | 原理 | 成本 | 能否处理同义改写 |
|---|---|---|---|
| 纯规则 | 归一化 + 提取释义核心概念词，看覆盖个数 | 零（纯前端可做） | ❌ |
| 字符串相似度 | 编辑距离 / difflib | 零 | ❌ |
| 语义嵌入 | `sentence-transformers` / `bge-m3`，向量余弦相似度 | 免费开源，但模型需跑在某处（本地/自建服务） | ✅ |
| LLM 判定 | 提示词让模型做「人工判卷」并输出分档 | 按调用计费，多数有免费层 | ✅✅ 最接近人工 |
| 混合流水线 | 规则粗筛 → 边界样本才交 AI | 中间 | ✅ 省调用量 |

## 四、结合本项目现状的免费落点（关键）

- 项目**已有 Cloudflare Worker** → **Workers AI 有免费额度**（每天约 1 万次推理，内置文本生成 + 嵌入模型）。wrangler 配 `ai` binding 即可用，零新增服务器、零成本。
- 其他免费选项：**Gemini Flash 免费层**、**Groq**（免费且快）、**DeepSeek API**（极便宜）、国内**智谱 GLM / 通义千问**（免费额度）。
- 本地：**Ollama**（qwen 等）免费，但需要教师侧设备。

## 五、推荐判定策略（分档 + 省调用）

1. **归一化预检**：去标点/空白/大小写后完全一致 → 直接满分，不调 AI。
2. **核心概念覆盖**：从标准释义挑 2–3 个「不可替代核心词」（术语本身、关键限定语），学生答案全覆盖 → 判对。
3. **语义兜底**：前两步不中的边界样本，才送 embedding 相似度或 LLM。
4. **落档**：`correct` 满分 / `partial` 半分 / `wrong` 零分；顺带存一句话理由，作为学生反馈（产品价值）。

## 六、落地路径

- **P0 纯规则版**：前端离线判定，先做题型跑起来，判定精度后换。
- **P1 接 Workers AI**：语义判定一步到位；需处理额度与隐私（学生答案会发给 Cloudflare）。

## 七、待讨论

- 阈值定档（「部分对」怎么给分、核心词权重）。
- 学生隐私 / 数据出口（答案送第三方 AI 的合规与知情）。
- 是否分两步（先 P0 后 P1），以及题型入口与权限（是否同样仅开发者可见）。

---

## 八、调研更新（2026-09-13）：AI 已接入后的落地可行性

> 背景：主站 AI 后端（Cloudflare Worker）已上线（教材问答 `/skill-api/ask` + 术语翻译 `/skill-api/terms`），P1「AI 语义判定」具备条件。本节为代码级调研结论。

### 8.1 现成可复用件

| 件 | 位置 | 用途 |
|---|---|---|
| `normalizeKey()` | `app/src/lib/answers.ts` | 归一化预检（归一化后完全一致 → 直接满分，零成本、不调 AI） |
| `maskAnswer()` / `collectMaskForms()` | 同上 | 可复用于「学生答案是否直接抄了术语」的泄露检测 |
| 结算式题型范式 | `app/src/components/Crossword.tsx`（`settledRef` + 批量 `recordItem` + 未结算离开确认弹窗） | 整轮提交后一次性判分 |
| `recordItem(itemId, correct, mode)` | `app/src/lib/store.tsx` | 一次写入掌握度 / 打卡 / 错题本；权重集中在 `storage.ts` 的 `MASTERY_WEIGHTS` |
| `verifyUser()` / `aiGateForbidden()` / `corsHeaders()` / `msAsk()` | `app/worker.ts` | 鉴权、门禁、CORS、多源降级调用（可抽公共函数复用） |

数据现状：`definition` 基本无空值（内置词库探测 `"definition":""` 为 0 条，教师端保存时也有非空校验）；`chinese` 存在空值（学者条目恒为空）。

### 8.2 最小改动集

| 层 | 改动 |
|---|---|
| 新组件 | `app/src/components/Definition.tsx` —— 照 `Spelling.tsx` 骨架（无 props、`useStore()`、`filterByPaperCat` 筛选、抽 10 题、`useStudySession`），但**题干渲染 `term`、不渲染 `definition`**；若走整轮提交判分则照 `Crossword.tsx` 的结算模式 |
| 入口 | `App.tsx` 四处（View 联合类型、练习组导航项、渲染分支、import）+ `Home.tsx` 模式卡片 + 新增 `app/public/mode-icons/definition.svg` |
| 类型 | `PracticeMode` 加 `'definition'`、`MASTERY_WEIGHTS` 补权重（两者都是穷尽 Record，编译器会强制补齐） |
| 后端 | 新增 `POST /practice-api/grade`（**JSON 非流式**；复用鉴权/门禁/CORS；返回 `correct/partial/wrong` + 一句话理由）。不复用 `/skill-api/ask`：它是 SSE 流式且入参语义是"长材料+提问+多轮历史" |
| 前端封装 | 主 App 目前**没有** AI 封装（只有子站 `skill-site/src/ask.ts` 可参考）；注意 `supabaseFetch` 的同源代理**不覆盖**新端点，需自行处理域名回退（APK 内 origin 是 `https://localhost`） |

### 8.3 必须解决的四个风险（按优先级）

1. **成本/配额**：判分是"每题一次调用"，ModelScope 免费额度仅约 **250 魔粒/日** —— 一个 30 人班 × 10 题即打爆。必须：规则粗筛（归一化 + 核心词覆盖）只把**边界样本**送 AI；并在 worker 侧加 **per-user 日配额**（当前 worker 无任何限流，`ai_gate` 是唯一闸门）。
2. **门禁语义冲突**：`ai_gate` 是全局单行的「防作弊」开关（考试/论文期间关学生 AI）。若判分复用同一门禁，会**连带打死定义题练习**——而练习恰恰是考试期间最该保留的。建议：门禁关闭时**降级为纯规则判定**，而不是拒绝学生；或为练习判分单开一个开关位。
3. **判分档位与记录体系不匹配**：现有 `recordItem(itemId, correct, mode)` 是 boolean + 权重加减，本方案要三档。引入半分需同时改 `recordItem` 签名、`adjustMastery`、`ItemProgress`、打卡正确率口径 —— 比"加一个组件"大得多。**建议 P1 先把三档映射为 boolean 落库**（partial 按宽松判对），P2 再扩半分。
4. **数据口径**：`definition` 质量不齐 —— 样本中已见拼写错误（`institutitons`）；学者条目走 `desc || notes || lastTheory` 三级兜底，可能只剩一个流派名而无法判分。建议：默认只对 `typeFilter='term'` 出题；出题前用脚本筛掉过短/过长/异常定义；并与词库"发布/快照"机制对齐（题目快照不会随词库发布自动更新，参考 `quiz.ts` 的 `refreshQuestionAliases` 所解决的同类问题）。

### 8.4 建议分期

- **P0（零 AI）**：纯规则版（归一化精确 + 核心词覆盖）先把题型与记录链路跑通，同时积累"规则判不了的答案"真实样本。
- **P1（AI 兜底）**：规则不中的边界样本才送 AI；三档映射 boolean 落库；返回一句话理由作为学生反馈。**低档位降级（Workers 8B）判主观定义不稳定**，建议此时只输出"机器判定不确定 / 建议自评"，**不写掌握度**。
- **P2**：支持半分（改记录体系）、低置信度样本进**教师复核队列**（教师判定结果反哺规则与提示词）。

### 8.5 仍待拍板

- 判分档位：P1 是否接受"内部三档、落库 boolean"的折中？
- 门禁：练习判分是否单开开关；门禁关闭时降级规则判定是否可接受？
- 额度：是否给每个学生设每日判分次数上限（如 30 次/天）？
- 隐私：学生自由文本答案会出境（魔搭 / OpenRouter / Cloudflare），是否需要在首次使用时明示并征得同意？
- 入口与权限：定义题是否与其它练习一样对所有学生可见，还是先仅开发者试跑？

---

## 九、材料调研与判分设计（2026-09-13）

### 9.1 三个定义来源（都可自动获取，均无需手工录入）

| 来源 | 规模 | 性质 | 位置 |
|---|---|---|---|
| 0495 / 2251 官方 Glossary（2025 起考） | **307 条**可用 | **考试标准答案**（简洁权威，IGCSE 口径） | `Materials/04952251 Glossary (for examination from 2025).pdf` |
| 9699 教材 Key terms（两本） | 估计 **400–600 条** | **准官方定义**；节级粒度、贴合 9699 深度 | 教材 PDF，已在 `skill_pages` 页文本内（tb1 约 100 页含、tb2 每章末尾） |
| 主站术语库 `definition` | 889 条 | 教师手写的教学口径（含中文） | `app/public/vocab-data.json` |

### 9.2 关键数据：为什么必须 AI 判定、且要按"要素"判

- 0495（307 条）与主站词库（889 条）：**术语重合 89 条，其中释义完全相同的只有 1 条**，88 条措辞不同但语义接近：
  - `Achieved status` — 官方 "A position in society that an individual acquires through their own effort." ／ 主站 "The social statuses that are earned by individuals through efforts and hardwork"
  - `Birth rate` — 官方 "The number of live births per 1000 people in the population in one year." ／ 主站 "The number of live births per 1,000 of the population per year"
- 结论：这是"同一概念、多种可接受表述"的典型场景 —— 字符串/相似度判定必然误判；**多源标准答案并存是常态**，因此判分要按「要素覆盖」，而不是"像不像某一句"。

### 9.3 教材 Key terms 可全自动提取（已验证）

OCR 版保留了加粗与字号，Key terms 小节结构规整：

```
[12.4 加粗] Key terms
[11.1 加粗] Social solidarity This involves a commitment
[11.1 常规] to society, a sense of belonging, ...
```

提取规则（零人工）：
1. 以加粗 `Key terms` 为锚点取小节范围；
2. 加粗 span = 条目起点（术语 + 释义首句），其后常规 span 续入释义；
3. 术语切分用**三份现成术语表反查**（主站词库 / 9699 蒸馏 glossary / 0495 glossary）：命中前缀即术语，其余归释义；未命中时退回「Title Case 短语（≤5 词）」启发式。

产出可直接挂在现成链路（`skill-pages-import.mjs` → `skill_pages`），**不需要新表**。

### 9.4 判分 rubric：直接参考 ms 里的 4 分题口径

`data/ms-skill/papers/chapters/*.md` 里 P1–P4 反复出现的给分写法：
- **"识别 + 为什么"两段式**（identify + explain why）
- **2 分/条**（4 分题 = 两条，各 2 分）
- **AO1（知识本身）/ AO2（带理论名与年份、对题）**
- **"任意两条/两点"**（不要求穷尽）
- 扣分项：**离题式背书**（问什么答什么）

映射为三档：

| 档 | 判据（建议） | 分值 |
|---|---|---|
| correct | 覆盖 ≥2 个核心要素 **且**有解释（不只是罗列名词） | 满分 |
| partial | 覆盖 1 个核心要素；或要素齐但只有罗列、无解释 | 半分 |
| wrong | 要素错误 / 答非所问 / 直接抄题干术语 | 0 |

**核心是「要素化判分」**：先把标准定义拆成 2–4 个"必需要素"（可离线预生成并缓存），AI 只判"覆盖了几个要素 + 是否给出解释"。这比直接问"像不像标准答案"稳定得多 —— 相似度对答案长短不敏感，要素覆盖更接近人工判卷。

### 9.5 判分模型选型（按 2026-09-13 决定）

- **主力：OpenRouter `nemotron-super-120b:free`**（免费额度约 1000 次/天，足够判分），**不占用 ModelScope 魔粒**（留给问答）；
- 降级：Workers AI 8B 仅用于低风险粗判；**低档位降级时不写掌握度**，只输出"机器判定不确定，建议自评/教师复核"。

### 9.6 判分语料积累（一致性靠它）

新建判分日志表（如 `definition_grading`）：`itemId, 学生答案, 覆盖要素, AI 档位, 置信度, 教师复核档位(可选), 时间`。三个用途：
1. 取**教师复核过的样本**当 few-shot，提升后续判分一致性；
2. 新模型/新提示词上线前，用冻结样本做回归；
3. 反过来校准「要素拆分」——若某要素学生从未命中，多半是拆错了。

### 9.7 待拍板（新增）

- 标准答案以哪份为主？（建议：**要素以 9699 教材 Key terms 为准**，0495 与主站表述作为"可接受变体"）
- 教材 Key terms 抽出来是"节级定义"，同一术语可能出现多个版本 —— 判分是否接受"任一版本要素被覆盖"即可？
- 判分日志是否给教师看（建议先只存不展示，稳定后再做复核界面）？

---

## 十、多来源权重与「踩分点」自动归纳（2026-09-13 追加）

### 10.1 为什么权重不能是一个数字：两个环节要分开

同一份材料在两个环节里扮演的角色不同，权重必须分开算：

- **要素归纳**（决定"哪些点算必踩"）→ 看**权威性 + 覆盖密度**
- **答案可接受性**（决定"学生这么写算不算对"）→ 看**学生实际 exposure + 表述习惯**

还有一点很关键：各来源的**覆盖稀疏度**差别极大 ——
- mark scheme 只覆盖少数术语，但对覆盖到的术语**极其权威**；
- 主站词库覆盖最广（889 条）但属**校本手写**，权威性中等；
- 0495 glossary 官方但**与词库重叠低**（307 条里仅 89 条重合）。

所以不能给"全局平均分"式权重，应用 **底座覆盖 + 权威加成** 的双层模型：
**底座**由"有多少个来源都强调了这一点"决定（广度优先），**加成**由权威来源（ms、官方大纲）是否命中决定。

### 10.2 建议权重（区间 0.6–1.2，均衡但略有差异）

| 来源 | 性质 | 要素归纳权重 | 答案可接受性权重 | 覆盖密度 |
|---|---|---|---|---|
| **ms / mark scheme 给分点** | 官方批阅口径 | **1.2** | 0.9 | 稀疏（少数术语）|
| **9699 教材 Key terms** | 准官方（考纲同级） | **1.1** | **1.1** | 密（每节末尾）|
| **0495 / 2251 官方 glossary** | 官方大纲 | **1.0** | 1.0 | 中（307 条）|
| **主站词库（手写）** | 教学校本 | **0.8** | **1.2** | 最广（889 条）|
| 教材正文案例 / 研究名 | 细节佐证 | 0.6 | 0.7 | 散 |
| 学生作答（**教师复核后**） | 真实判例 | **1.3**（复核）／0.5（未复核） | — | 动态增长 |

读法：
- **要素归纳**里 ms 最高（1.2）但它是**稀疏源** —— 不靠"全局高权重"，而靠 **命中即提升该要素为必踩**（见 10.3 Step 4）；主站最低（0.8）不是看不起它，而是它的角色是"覆盖面"，在"答案可接受性"里才是最高（1.2）。
- 全部落在 0.6–1.2，符合"均衡但略有差异"。

### 10.3 踩分点自动归纳（5 步，全自动 + 抽检）

**Step 1 术语对齐聚类** — 术语名归一化（小写、去标点、单复数、同义映射表，如 `anti-school subculture` / `anti-school sub-culture` / `反学校亚文化`），把 5 个来源里同一概念的记录聚成一个 cluster。

**Step 2 单源要素切分** — 对 cluster 内每份定义，用模型拆成原子要素，严格 JSON 输出：
```json
{"term": "Achieved status",
 "elements": [
   {"text": "通过个人努力/成就获得", "type": "core"},
   {"text": "是一种社会位置/地位", "type": "core"},
   {"text": "与先赋地位（ascribed）相对", "type": "qualifier"}]}
```
一次性生成、按术语缓存，可版本化复用（术语定义基本不变）。

**Step 3 跨源要素合并** — 先用词面/语义相似度粗聚，再让模型确认"这两条是否为同一要素"，得到**要素簇**，记录：支持来源集合 + 各来源权重。

**Step 4 排序定档** — 每个要素簇计算：

```
加权票 votes   = Σ_j (w_j × hit_ij)          # w_j 见 10.2 要素归纳权重
支持广度 breadth = Σ_j hit_ij                 # 有几家来源强调过

必踩(core)    : breadth ≥ 3  或  ms 命中
次要(minor)   : breadth = 2
变体(variant) : breadth = 1 且非 ms（不计分，仅作为"可接受表述"归入并集）
排序           rank = votes + 0.5 × breadth
```
最后每术语取 **top 2–4 个 core** 作为踩分点（与三档判分的粒度对齐；多了学生踩不全、少了区分度不足）。

**Step 5 抽检 + 闭环** — 自动产出后**抽 5–10% 人工核对**（教师几分钟量级）；上线后由判分日志反哺：若某"必踩点"学生几乎从没踩到、而教师复核认为应该踩到 → 提示它可能拆错或该降档。

### 10.4 判分口径：并集原则（配合 10.2 的"可接受性权重"）

- **命中即算**：学生的表述只要能对上**任一来源**的等价要素，就算踩到该点（不必匹配某一家的原话）—— 这直接解决 9.2 里"88 条释义不同但语义相同"的问题。
- 档位仍按 9.4 三档；`partial` 的边界是"踩到的 core 数 ≥1 但未齐，或只有罗列无解释"。
- `wrong` 需 AI 明确说出"错在哪"（要素错位 / 答非所问 / 抄题干），便于日志里积累负样本。

### 10.5 落地顺序（建议）

1. 抽两本教材 Key terms → 结构化定义库（纯脚本，可验证）；
2. 跑 Step 1–4 生成**踩分点基线**，先给教师看 20–30 个术语的抽检结果；
3. 抽检通过后再接练习组件与判分端点（OpenRouter nemotron）；
4. 判分日志表随练习一起上，攒 few-shot 与回归样本。

---

## 十一、第一步产出：教材 Key terms 结构化定义库（2026-09-13）

### 11.1 新增脚本

`app/scripts/skill-keyterms.py` —— 教材 PDF → `{术语, 释义, 章, 节, 页, 抽取方法}`

- **锚点**：加粗 `Key terms` 标题行（或用独立短行 `KEY TERMS`，个别章不设粗体）；
- **两种版面**：tb1 = 「加粗术语 + 释义续行」（无分隔符）；tb2 = 「术语: 释义」（冒号）；
- **术语/释义切分**：术语表最长前缀反查（主站 889 + 两本蒸馏 glossary + 0495 官方 glossary）× 大小写边界 × 尾部小写词回并（`Belief | system A set of ideas` → `Belief system`）；
- **噪声过滤**：术语 ≤ 6 词、排除编号列表 / 版式标题（`THINK LIKE A SOCIOLOGIST`）/ 导论页整句说明；
- **全程无 LLM**，教材换版重跑即可。

### 11.2 抽取结果

| 来源 | 条目 | 说明 |
|---|---|---|
| **tb1 Haralambos** | **755** | 8 章均有：Globalisation 165／Family 126／Research methods 106／Socialisation 90／Introduction 81／Education 76／Religion 74／Media 37；噪声仅 4 条 |
| **tb2 Livesey** | 5 | 该书风格是「正文首次出现处定义」，章末 Key terms 仅 3 处 —— 需另走「正文加粗术语」抽取 |
| **0495 / 2251 官方 glossary** | 303 | 已解析为结构化（`Terms/Definition` 两列） |
| 合计 | **~1060** | 主站 889 条之外的定义增量来源 |

### 11.3 关键指标：多来源交集（决定踩分点是否立得住）

以主站 650 个术语为基准做对齐：

| 对齐方式 | 0 来源 | 1 来源 | **≥2 来源** |
|---|---|---|---|
| 严格字符串 | 423（65%） | 174 | **53** |
| 宽松词集 | 326（50%） | 202 | **122** |

两条结论：
1. **术语对齐必须语义化** —— 仅换成词集宽松匹配，多来源样本就从 53 → 122（+130%）；模型语义对齐会更多。这印证了 Step 1（术语对齐聚类）必须用模型，不能靠字符串。
2. **仅 3 个来源不够** —— 主站仍有 **50% 术语零命中**（教材与 0495 都没收）。要把踩分点做厚，必须扩充来源（见 11.4 B）。

### 11.4 下一步（待定）

- **A** 把三份定义入库（结构化定义库表，挂现成 `skill_pages` 链路）；
- **B 扩充来源**：教材正文「定义句」抽取（`X is defined as…` ／ 加粗术语 + 紧随释义；可覆盖 tb2 与 tb1 正文共 ~1000 页）+ ms 语料里的定义口径 —— 这是把多源交集从 122 抬上去的关键；
- **C** 用现有 122 条多源样本先跑一次要素归纳，给 20–30 条术语的踩分点抽检。
