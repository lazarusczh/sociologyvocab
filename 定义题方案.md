# 定义题：给出术语 → 学生写定义（💬 讨论中，2026-09-01）

> 记录一次关于「反写题型」判定方案的讨论。长期规划，未实现，先存方案。
> 相关现状：词条已有 `term`（术语）、`definition`、`chinese`（中文释义）字段；现有拼写题为「给释义→写术语」，本方案为反向题型。

---

## 一、需求背景

- 把「拼写默写」反过来：**给出术语，要求学生写出定义**（中文释义或英文定义）。
- 难点不在题型本身，而在**判定**：定义不是唯一答案，无法和词条释义要求一字不差。

## 二、为什么难（判定本质）

- 开放答案空间无限：同义改写 / 语序颠倒 / 主被动互换 / 不完整 vs 冗余 / 口语化 vs 学术化。
- 判定从「字符串匹配」变成「**语义判定**」——容错空间无法用相似度枚举。

## 三、方案阶梯

| 方案 | 原理 | 成本 | 能否处理同义改写 |
|---|---|---|---|
| 纯规则 | 归一化 + 提取释义核心概念词，看覆盖个数 | 零（纯前端可做） | ❌ |
| 字符串相似度 | 编辑距离 / difflib | 零 | ❌ |
| 语义嵌入 | `sentence-transformers` / `bge-m3`，向量余弦相似度 | 免费开源，但模型需跑在某处（本地/自建服务） | ✅ |
| LLM 判定 | 提示词让模型做「人工判卷」并输出分档 | 按调用计费，多数有免费层 | ✅✅ 最接近人工 |
| 混合流水线 | 规则粗筛 → 边界样本才交 AI | 中间 | ✅ 省调用量 |

## 四、结合本项目现状的免费落点（关键）

- 项目**已有 Cloudflare Worker** → **Workers AI 有免费额度**（每天约 1 万次推理，内置文本生成 + 嵌入模型）。wrangler 配 `ai` binding 即可用，零新增服务器、零成本。
- 其他免费选项：**Gemini Flash 免费层**、**Groq**（免费且快）、**DeepSeek API**（极便宜）、国内**智谱 GLM / 通义千问**（免费额度）。
- 本地：**Ollama**（qwen 等）免费，但需要教师侧设备。

## 五、推荐判定策略（分档 + 省调用）

1. **归一化预检**：去标点/空白/大小写后完全一致 → 直接满分，不调 AI。
2. **核心概念覆盖**：从标准释义挑 2–3 个「不可替代核心词」（术语本身、关键限定语），学生答案全覆盖 → 判对。
3. **语义兜底**：前两步不中的边界样本，才送 embedding 相似度或 LLM。
4. **落档**：`correct` 满分 / `partial` 半分 / `wrong` 零分；顺带存一句话理由，作为学生反馈（产品价值）。

## 六、落地路径

- **P0 纯规则版**：前端离线判定，先做题型跑起来，判定精度后换。
- **P1 接 Workers AI**：语义判定一步到位；需处理额度与隐私（学生答案会发给 Cloudflare）。

## 七、待讨论

- 阈值定档（「部分对」怎么给分、核心词权重）。
- 学生隐私 / 数据出口（答案送第三方 AI 的合规与知情）。
- 是否分两步（先 P0 后 P1），以及题型入口与权限（是否同样仅开发者可见）。

---

## 八、调研更新（2026-09-13）：AI 已接入后的落地可行性

> 背景：主站 AI 后端（Cloudflare Worker）已上线（教材问答 `/skill-api/ask` + 术语翻译 `/skill-api/terms`），P1「AI 语义判定」具备条件。本节为代码级调研结论。

### 8.1 现成可复用件

| 件 | 位置 | 用途 |
|---|---|---|
| `normalizeKey()` | `app/src/lib/answers.ts` | 归一化预检（归一化后完全一致 → 直接满分，零成本、不调 AI） |
| `maskAnswer()` / `collectMaskForms()` | 同上 | 可复用于「学生答案是否直接抄了术语」的泄露检测 |
| 结算式题型范式 | `app/src/components/Crossword.tsx`（`settledRef` + 批量 `recordItem` + 未结算离开确认弹窗） | 整轮提交后一次性判分 |
| `recordItem(itemId, correct, mode)` | `app/src/lib/store.tsx` | 一次写入掌握度 / 打卡 / 错题本；权重集中在 `storage.ts` 的 `MASTERY_WEIGHTS` |
| `verifyUser()` / `aiGateForbidden()` / `corsHeaders()` / `msAsk()` | `app/worker.ts` | 鉴权、门禁、CORS、多源降级调用（可抽公共函数复用） |

数据现状：`definition` 基本无空值（内置词库探测 `"definition":""` 为 0 条，教师端保存时也有非空校验）；`chinese` 存在空值（学者条目恒为空）。

### 8.2 最小改动集

| 层 | 改动 |
|---|---|
| 新组件 | `app/src/components/Definition.tsx` —— 照 `Spelling.tsx` 骨架（无 props、`useStore()`、`filterByPaperCat` 筛选、抽 10 题、`useStudySession`），但**题干渲染 `term`、不渲染 `definition`**；若走整轮提交判分则照 `Crossword.tsx` 的结算模式 |
| 入口 | `App.tsx` 四处（View 联合类型、练习组导航项、渲染分支、import）+ `Home.tsx` 模式卡片 + 新增 `app/public/mode-icons/definition.svg` |
| 类型 | `PracticeMode` 加 `'definition'`、`MASTERY_WEIGHTS` 补权重（两者都是穷尽 Record，编译器会强制补齐） |
| 后端 | 新增 `POST /practice-api/grade`（**JSON 非流式**；复用鉴权/门禁/CORS；返回 `correct/partial/wrong` + 一句话理由）。不复用 `/skill-api/ask`：它是 SSE 流式且入参语义是"长材料+提问+多轮历史" |
| 前端封装 | 主 App 目前**没有** AI 封装（只有子站 `skill-site/src/ask.ts` 可参考）；注意 `supabaseFetch` 的同源代理**不覆盖**新端点，需自行处理域名回退（APK 内 origin 是 `https://localhost`） |

### 8.3 必须解决的四个风险（按优先级）

1. **成本/配额**：判分是"每题一次调用"，ModelScope 免费额度仅约 **250 魔粒/日** —— 一个 30 人班 × 10 题即打爆。必须：规则粗筛（归一化 + 核心词覆盖）只把**边界样本**送 AI；并在 worker 侧加 **per-user 日配额**（当前 worker 无任何限流，`ai_gate` 是唯一闸门）。
2. **门禁语义冲突**：`ai_gate` 是全局单行的「防作弊」开关（考试/论文期间关学生 AI）。若判分复用同一门禁，会**连带打死定义题练习**——而练习恰恰是考试期间最该保留的。建议：门禁关闭时**降级为纯规则判定**，而不是拒绝学生；或为练习判分单开一个开关位。
3. **判分档位与记录体系不匹配**：现有 `recordItem(itemId, correct, mode)` 是 boolean + 权重加减，本方案要三档。引入半分需同时改 `recordItem` 签名、`adjustMastery`、`ItemProgress`、打卡正确率口径 —— 比"加一个组件"大得多。**建议 P1 先把三档映射为 boolean 落库**（partial 按宽松判对），P2 再扩半分。
4. **数据口径**：`definition` 质量不齐 —— 样本中已见拼写错误（`institutitons`）；学者条目走 `desc || notes || lastTheory` 三级兜底，可能只剩一个流派名而无法判分。建议：默认只对 `typeFilter='term'` 出题；出题前用脚本筛掉过短/过长/异常定义；并与词库"发布/快照"机制对齐（题目快照不会随词库发布自动更新，参考 `quiz.ts` 的 `refreshQuestionAliases` 所解决的同类问题）。

### 8.4 建议分期

- **P0（零 AI）**：纯规则版（归一化精确 + 核心词覆盖）先把题型与记录链路跑通，同时积累"规则判不了的答案"真实样本。
- **P1（AI 兜底）**：规则不中的边界样本才送 AI；三档映射 boolean 落库；返回一句话理由作为学生反馈。**低档位降级（Workers 8B）判主观定义不稳定**，建议此时只输出"机器判定不确定 / 建议自评"，**不写掌握度**。
- **P2**：支持半分（改记录体系）、低置信度样本进**教师复核队列**（教师判定结果反哺规则与提示词）。

### 8.5 仍待拍板

- 判分档位：P1 是否接受"内部三档、落库 boolean"的折中？
- 门禁：练习判分是否单开开关；门禁关闭时降级规则判定是否可接受？
- 额度：是否给每个学生设每日判分次数上限（如 30 次/天）？
- 隐私：学生自由文本答案会出境（魔搭 / OpenRouter / Cloudflare），是否需要在首次使用时明示并征得同意？
- 入口与权限：定义题是否与其它练习一样对所有学生可见，还是先仅开发者试跑？

---

## 九、材料调研与判分设计（2026-09-13）

### 9.1 三个定义来源（都可自动获取，均无需手工录入）

| 来源 | 规模 | 性质 | 位置 |
|---|---|---|---|
| 0495 / 2251 官方 Glossary（2025 起考） | **307 条**可用 | **考试标准答案**（简洁权威，IGCSE 口径） | `Materials/04952251 Glossary (for examination from 2025).pdf` |
| 9699 教材 Key terms（两本） | 估计 **400–600 条** | **准官方定义**；节级粒度、贴合 9699 深度 | 教材 PDF，已在 `skill_pages` 页文本内（tb1 约 100 页含、tb2 每章末尾） |
| 主站术语库 `definition` | 889 条 | 教师手写的教学口径（含中文） | `app/public/vocab-data.json` |

### 9.2 关键数据：为什么必须 AI 判定、且要按"要素"判

- 0495（307 条）与主站词库（889 条）：**术语重合 89 条，其中释义完全相同的只有 1 条**，88 条措辞不同但语义接近：
  - `Achieved status` — 官方 "A position in society that an individual acquires through their own effort." ／ 主站 "The social statuses that are earned by individuals through efforts and hardwork"
  - `Birth rate` — 官方 "The number of live births per 1000 people in the population in one year." ／ 主站 "The number of live births per 1,000 of the population per year"
- 结论：这是"同一概念、多种可接受表述"的典型场景 —— 字符串/相似度判定必然误判；**多源标准答案并存是常态**，因此判分要按「要素覆盖」，而不是"像不像某一句"。

### 9.3 教材 Key terms 可全自动提取（已验证）

OCR 版保留了加粗与字号，Key terms 小节结构规整：

```
[12.4 加粗] Key terms
[11.1 加粗] Social solidarity This involves a commitment
[11.1 常规] to society, a sense of belonging, ...
```

提取规则（零人工）：
1. 以加粗 `Key terms` 为锚点取小节范围；
2. 加粗 span = 条目起点（术语 + 释义首句），其后常规 span 续入释义；
3. 术语切分用**三份现成术语表反查**（主站词库 / 9699 蒸馏 glossary / 0495 glossary）：命中前缀即术语，其余归释义；未命中时退回「Title Case 短语（≤5 词）」启发式。

产出可直接挂在现成链路（`skill-pages-import.mjs` → `skill_pages`），**不需要新表**。

### 9.4 判分 rubric：直接参考 ms 里的 4 分题口径

`data/ms-skill/papers/chapters/*.md` 里 P1–P4 反复出现的给分写法：
- **"识别 + 为什么"两段式**（identify + explain why）
- **2 分/条**（4 分题 = 两条，各 2 分）
- **AO1（知识本身）/ AO2（带理论名与年份、对题）**
- **"任意两条/两点"**（不要求穷尽）
- 扣分项：**离题式背书**（问什么答什么）

映射为三档：

| 档 | 判据（建议） | 分值 |
|---|---|---|
| correct | 覆盖 ≥2 个核心要素 **且**有解释（不只是罗列名词） | 满分 |
| partial | 覆盖 1 个核心要素；或要素齐但只有罗列、无解释 | 半分 |
| wrong | 要素错误 / 答非所问 / 直接抄题干术语 | 0 |

**核心是「要素化判分」**：先把标准定义拆成 2–4 个"必需要素"（可离线预生成并缓存），AI 只判"覆盖了几个要素 + 是否给出解释"。这比直接问"像不像标准答案"稳定得多 —— 相似度对答案长短不敏感，要素覆盖更接近人工判卷。

### 9.5 判分模型选型（按 2026-09-13 决定）

- **主力：OpenRouter `nemotron-super-120b:free`**（免费额度约 1000 次/天，足够判分），**不占用 ModelScope 魔粒**（留给问答）；
- 降级：Workers AI 8B 仅用于低风险粗判；**低档位降级时不写掌握度**，只输出"机器判定不确定，建议自评/教师复核"。

### 9.6 判分语料积累（一致性靠它）

新建判分日志表（如 `definition_grading`）：`itemId, 学生答案, 覆盖要素, AI 档位, 置信度, 教师复核档位(可选), 时间`。三个用途：
1. 取**教师复核过的样本**当 few-shot，提升后续判分一致性；
2. 新模型/新提示词上线前，用冻结样本做回归；
3. 反过来校准「要素拆分」——若某要素学生从未命中，多半是拆错了。

### 9.7 待拍板（新增）

- 标准答案以哪份为主？（建议：**要素以 9699 教材 Key terms 为准**，0495 与主站表述作为"可接受变体"）
- 教材 Key terms 抽出来是"节级定义"，同一术语可能出现多个版本 —— 判分是否接受"任一版本要素被覆盖"即可？
- 判分日志是否给教师看（建议先只存不展示，稳定后再做复核界面）？

---

## 十、多来源权重与「踩分点」自动归纳（2026-09-13 追加）

### 10.1 为什么权重不能是一个数字：两个环节要分开

同一份材料在两个环节里扮演的角色不同，权重必须分开算：

- **要素归纳**（决定"哪些点算必踩"）→ 看**权威性 + 覆盖密度**
- **答案可接受性**（决定"学生这么写算不算对"）→ 看**学生实际 exposure + 表述习惯**

还有一点很关键：各来源的**覆盖稀疏度**差别极大 ——
- mark scheme 只覆盖少数术语，但对覆盖到的术语**极其权威**；
- 主站词库覆盖最广（889 条）但属**校本手写**，权威性中等；
- 0495 glossary 官方但**与词库重叠低**（307 条里仅 89 条重合）。

所以不能给"全局平均分"式权重，应用 **底座覆盖 + 权威加成** 的双层模型：
**底座**由"有多少个来源都强调了这一点"决定（广度优先），**加成**由权威来源（ms、官方大纲）是否命中决定。

### 10.2 建议权重（区间 0.6–1.2，均衡但略有差异）

| 来源 | 性质 | 要素归纳权重 | 答案可接受性权重 | 覆盖密度 |
|---|---|---|---|---|
| **ms / mark scheme 给分点** | 官方批阅口径 | **1.2** | 0.9 | 稀疏（少数术语）|
| **9699 教材 Key terms** | 准官方（考纲同级） | **1.1** | **1.1** | 密（每节末尾）|
| **0495 / 2251 官方 glossary** | 官方大纲 | **1.0** | 1.0 | 中（307 条）|
| **主站词库（手写）** | 教学校本 | **0.8** | **1.2** | 最广（889 条）|
| 教材正文案例 / 研究名 | 细节佐证 | 0.6 | 0.7 | 散 |
| 学生作答（**教师复核后**） | 真实判例 | **1.3**（复核）／0.5（未复核） | — | 动态增长 |

读法：
- **要素归纳**里 ms 最高（1.2）但它是**稀疏源** —— 不靠"全局高权重"，而靠 **命中即提升该要素为必踩**（见 10.3 Step 4）；主站最低（0.8）不是看不起它，而是它的角色是"覆盖面"，在"答案可接受性"里才是最高（1.2）。
- 全部落在 0.6–1.2，符合"均衡但略有差异"。

### 10.3 踩分点自动归纳（5 步，全自动 + 抽检）

**Step 1 术语对齐聚类** — 术语名归一化（小写、去标点、单复数、同义映射表，如 `anti-school subculture` / `anti-school sub-culture` / `反学校亚文化`），把 5 个来源里同一概念的记录聚成一个 cluster。

**Step 2 单源要素切分** — 对 cluster 内每份定义，用模型拆成原子要素，严格 JSON 输出：
```json
{"term": "Achieved status",
 "elements": [
   {"text": "通过个人努力/成就获得", "type": "core"},
   {"text": "是一种社会位置/地位", "type": "core"},
   {"text": "与先赋地位（ascribed）相对", "type": "qualifier"}]}
```
一次性生成、按术语缓存，可版本化复用（术语定义基本不变）。

**Step 3 跨源要素合并** — 先用词面/语义相似度粗聚，再让模型确认"这两条是否为同一要素"，得到**要素簇**，记录：支持来源集合 + 各来源权重。

**Step 4 排序定档** — 每个要素簇计算：

```
加权票 votes   = Σ_j (w_j × hit_ij)          # w_j 见 10.2 要素归纳权重
支持广度 breadth = Σ_j hit_ij                 # 有几家来源强调过

必踩(core)    : breadth ≥ 3  或  ms 命中
次要(minor)   : breadth = 2
变体(variant) : breadth = 1 且非 ms（不计分，仅作为"可接受表述"归入并集）
排序           rank = votes + 0.5 × breadth
```
最后每术语取 **top 2–4 个 core** 作为踩分点（与三档判分的粒度对齐；多了学生踩不全、少了区分度不足）。

**Step 5 抽检 + 闭环** — 自动产出后**抽 5–10% 人工核对**（教师几分钟量级）；上线后由判分日志反哺：若某"必踩点"学生几乎从没踩到、而教师复核认为应该踩到 → 提示它可能拆错或该降档。

### 10.4 判分口径：并集原则（配合 10.2 的"可接受性权重"）

- **命中即算**：学生的表述只要能对上**任一来源**的等价要素，就算踩到该点（不必匹配某一家的原话）—— 这直接解决 9.2 里"88 条释义不同但语义相同"的问题。
- 档位仍按 9.4 三档；`partial` 的边界是"踩到的 core 数 ≥1 但未齐，或只有罗列无解释"。
- `wrong` 需 AI 明确说出"错在哪"（要素错位 / 答非所问 / 抄题干），便于日志里积累负样本。

### 10.5 落地顺序（建议）

1. 抽两本教材 Key terms → 结构化定义库（纯脚本，可验证）；
2. 跑 Step 1–4 生成**踩分点基线**，先给教师看 20–30 个术语的抽检结果；
3. 抽检通过后再接练习组件与判分端点（OpenRouter nemotron）；
4. 判分日志表随练习一起上，攒 few-shot 与回归样本。

---

## 十一、第一步产出：教材 Key terms 结构化定义库（2026-09-13）

### 11.1 新增脚本

`app/scripts/skill-keyterms.py` —— 教材 PDF → `{术语, 释义, 章, 节, 页, 抽取方法}`

- **锚点**：加粗 `Key terms` 标题行（或用独立短行 `KEY TERMS`，个别章不设粗体）；
- **两种版面**：tb1 = 「加粗术语 + 释义续行」（无分隔符）；tb2 = 「术语: 释义」（冒号）；
- **术语/释义切分**：术语表最长前缀反查（主站 889 + 两本蒸馏 glossary + 0495 官方 glossary）× 大小写边界 × 尾部小写词回并（`Belief | system A set of ideas` → `Belief system`）；
- **噪声过滤**：术语 ≤ 6 词、排除编号列表 / 版式标题（`THINK LIKE A SOCIOLOGIST`）/ 导论页整句说明；
- **全程无 LLM**，教材换版重跑即可。

### 11.2 抽取结果

| 来源 | 条目 | 说明 |
|---|---|---|
| **tb1 Haralambos** | **755** | 8 章均有：Globalisation 165／Family 126／Research methods 106／Socialisation 90／Introduction 81／Education 76／Religion 74／Media 37；噪声仅 4 条 |
| **tb2 Livesey** | 5 | 该书风格是「正文首次出现处定义」，章末 Key terms 仅 3 处 —— 需另走「正文加粗术语」抽取 |
| **0495 / 2251 官方 glossary** | 303 | 已解析为结构化（`Terms/Definition` 两列） |
| 合计 | **~1060** | 主站 889 条之外的定义增量来源 |

### 11.3 关键指标：额外来源覆盖（决定踩分点是否立得住）

**口径**：主站 889 条词条中，`type=term` 的 **650 条**是定义题对象；**239 条 `type=scholar`（学者）已排除**，不纳入定义题（学者属人物简介，不是术语默写）。

以主站 650 条术语为基准，看"额外来源"（教材 Key terms + 0495 官方 glossary）能覆盖多少：

| 对齐方式 | 仅主站 | +1 个外部来源 | +≥2 个外部来源 |
|---|---|---|---|
| 严格字符串 | 423 | 174 | 53 |
| 宽松词集 | 326 | 202 | 122 |
| **词形归一（最终采用）** | **300（46%）** | **221（34%）** | **129（20%）** |

（"词形归一"= 去停用词 + 单复数/词干归一 + 词集包含判定。最终口径下：129 条有 ≥2 个外部来源可互相印证。）

### 11.4 实测结论：B（无靶向扩充来源）**不值得做**

按"对照主站空白术语去做才有意义"的思路做了靶向实测 —— 抽 40 条"仅主站"（无任何外部来源）术语，回教材正文（1015 页）找定义：

| 结果 | 条数 |
|---|---|
| 教材正文能定位到该词 | 22 / 40 |
| **其中是真定义句** | **0 / 40** |

原因是这批术语的性质：`Free will`、`Reward`、`Workplace`、`Privacy`、`Funding`、`Formal rule`、`Hidden rules`、`Public ridicule`… 它们是**主站自己的教学化颗粒度**，教材与 0495 都只在行文中顺带提及、从不给出定义。

→ 所以 **B 的产出会接近于零**，不应作为独立工作项。真正该做的是**按来源数分层处理**（见 11.5）。

### 11.5 建议路线：按来源数分层，而不是硬凑来源

| 组 | 条数 | 判分依据 | 置信度处理 |
|---|---|---|---|
| **A 组**：主站 + ≥1 外部来源 | 129 | 跨源要素归纳（多来源互相印证）→ 踩分点最稳 | 高：可自动判分，抽检 5–10% |
| **B 组**：主站 + 1 外部来源 | 221 | 双源对照抽要素 | 中：抽检 20% |
| **C 组**：仅主站 | 300 | 以主站定义为准 + 模型补要素（标注"校本口径"） | 低：抽检比例提高，靠判分日志 + 教师复核积累 |

C 组要变稳，靠的不是教材，而是 **9.6 的判分日志 + 教师复核** —— 也就是用真实学生作答与教师判定来沉淀"校本口径"，这正好符合"与主站人工标注互补"的初衷。

### 11.6 下一步（待定）

- **A'** 把三份定义入库（结构化定义库表，挂现成 `skill_pages` 链路）；
- **C'** 先用 A 组 129 条跑要素归纳，给 20–30 条术语的踩分点抽检（推荐先做这个）。

---

## 十二、C′ 首轮：要素归纳跑通（2026-09-14）

### 12.1 新增脚本

`app/scripts/skill-keypoints.py` —— 对应方案 10.3 的 Step 2–4（单源要素切分 → 跨源合并 → 加权排序），由**一次 LLM 调用**完成。

- 模型：ModelScope `Qwen/Qwen3-235B-A22B`（`enable_thinking=false`），每条术语一次调用；
- 结果落 JSONL，支持 `--resume`（断点续跑）/ `--retry-failed`（重跑失败）/ `--stratify`（按 Paper·Unit 分层抽样，避免样本全落一个单元）；
- 对齐规则修正：Jaccard ≥ 0.6 **或**「单侧包含且较短方 ≥2 词、词数差 ≤1」—— 既放宽复数/定语差异，又挡住 `Brain power` 被单条泛词 `Power` 命中的错配。

### 12.2 A 组跑完：93 条术语的踩分点

**A 组共 93 个 unique 术语**（98 = 含同一术语挂多个 unit 的重复位置），**93 条全部归纳成功（100%）**。产出见 `定义题踩分点抽检.md`。

- 每条 core 数：1 个 ×5、**2 个 ×84**、3 个 ×4（粒度符合"2–4 个"的设计）
- 要素被支持来源数：1 源 ×47、**2 源 ×97、3 源 ×41** → 合计 185 个要素，其中 **75% 有 ≥2 个来源互证**
- 覆盖 Paper 1–4 全部主题单元（分层抽样生效）

另有 **221 条 B 组**（仅 1 个外部来源）与 **300 条 C 组**（仅主站）尚未归纳 —— 待定是否纳入。

样例（`Patriarchy`，来源 main+tb1+igcse0495）：
```
core  男性在社会中占据主导地位，女性处于从属地位      ⟵ main,tb1,igcse0495
core  男性通过社会制度和组织结构维持其支配地位        ⟵ tb1,igcse0495
```

### 12.3 过程中修掉的两个数据问题

1. **宽松对齐「单侧包含」过松** → 出现 `Brain power` 命中 `Power`、`Hybrid identity` 命中 `Identity` 的错配（已按 12.1 的规则收紧）；
2. **0495 glossary 旧解析约 17% 串行错位**（`Vocationalism :: particular jobs…`）→ 新增 `app/scripts/skill-igcse-glossary.py`，按**列切分**（术语列 x0≈62、释义列 x0≈201，按 y 配对）重解析：条目 307 → **348**，抽查正确。

**已知问题**：0495 解析的「不完整条目」已从 18 条（5.2%）降到 **1 条（0.3%）**（过滤页脚 `www.cambridgeinternational.org` + 合并跨行术语）；`skill-keypoints.py` 首轮的失败是脚本 bug（分层抽样里变量名 `key` 覆盖了 API key），已修。

### 12.4 下一步

- 把 A 组剩余条目（129 − 40）跑完，再决定 B 组（+1 外部来源，221 条）与 C 组（仅主站，300 条）的处理方式；
- 用第 9.4 节的三档，拿这些踩分点做一次"判分一致性"自测（同一答案重复判分是否稳定）。

---

## 十三、判分链路自测（2026-09-15）

> 注：定义题属**主站**功能，数据随之走主站链路（`vocab-data.json` / 主站 Supabase 表）；
> 第 12 节里提到的 `skill_pages` 是知识库子站存**教材原文页**用的，与本题型无关（此前表述有误，勘正）。

### 13.1 方法与两轮迭代

脚本 `app/scripts/definition-grading-selftest.py`。样本由**真实语料**构造，预期档位确定，可直接算准确率：

| 样本 | 构造方式 | 预期 |
|---|---|---|
| `full` | 权威来源的完整定义原文 | correct |
| `partial` | 同一定义的第一个分句 | partial |
| `wrong` | 同一 Paper 下**另一个术语**的定义 | wrong |

判分设计两轮迭代：
1. **v1（失败）**：让模型直接给档位 → `partial` 5/5 被误判 correct（模型太宽容）；
2. **v2（采用）**：**模型只判"要素覆盖度"（0 / 0.5 / 1），档位由代码按规则算** → 可复现、可审计，且对"只有 1 个核心要素"的简单术语同样适用。

### 13.2 结果（18 样本，ModelScope `Qwen3-235B`）

| 指标 | 结果 |
|---|---|
| **防放水** wrong → wrong | **6/6** ✓ |
| **防误杀** full → 非 wrong | **6/6** ✓ |
| **档位精度** partial → partial | **6/6** ✓ |
| full → correct | **0/6** ✗ |

零调用复算得分分布：`wrong = 0.00`、`full = 0.50`、`partial = 0.50` → **wrong 与"部分正确"完全可分；full 与 partial 完全重叠**；阈值扫描（0.4–0.8）无法兼顾。

### 13.3 关键结论：错在"必踩点"的定义方式，不在阈值

`full` 与 `partial` 是**同一段定义原文的不同截断长度**，而那段定义只覆盖清单里的 1 个要素 —— 另一要素来自**另一个来源**。也就是说：

> **拿"多来源并集"当必踩点，会让"照某本教材答全"的学生被判 partial** —— 教学上不可接受。

修正方向（下一轮）：
1. **以"参考来源"为中心**：每个术语选定一个参考来源（教材 Key terms ＞ 官方 glossary），把**该来源定义自身的要素**作为判分清单；其他来源的独有内容降为"可接受变体 / 加分点"；
2. 为此需要在生成阶段让模型**分来源输出要素**，或离线算"每个来源定义的要素覆盖向量"（约 93 × 3 次调用）；
3. 样本改造：`full` 改为"覆盖清单全部要素的合成/改写答案"，`partial` 改为"只覆盖 1 个要素"。

### 13.4 已确认可用的部分

- **防放水**（答错必判 wrong）与**防误杀**（答到要点不判 wrong）两项都已验证通过 —— 这是教学场景里最不能出错的两种误判；
- 连续覆盖度（0/0.5/1）比布尔覆盖更稳；
- `tier` 分层已落地：93 条中 **high 46 / low 47**（low 的核心要素不足 2 个，判分需更宽容或走教师复核），见 `app/data/definition-keypoints.json`。

### 13.5 判分通路改为走主站 API（2026-09-15）

为避免消耗本地魔搭额度，判分/抽取这类**教师侧批处理**改走主站通路：

- **新增端点** `POST /app-api/ai/complete`（`worker/ai/text.ts` + `worker/appApi.ts`，教师/开发者鉴权）：
  ```
  body: { prompt, tier?: auto|nemotron|agnes|ms, maxTokens?, temperature?, system?, fallback? }
  resp: { text, model, tier, ms, fellBack }
  ```
  - `tier` 默认 `auto`（nemotron → agnes → ms）；`fallback:false` 可只跑单档，便于实测各档表现；
  - key 全部来自**线上 secret**，脚本侧不再需要本地 key；
  - `agnes` 档在 worker 里默认关闭（CF 出口曾被 WAF 1015 限流），此处**显式指定时才启用**，正好用于实测。
- **探针脚本** `app/scripts/app-api-probe.py`：可达性探测 + 三档对比。
  注意：**必须带浏览器 UA**，否则 Cloudflare 直接 403 `error code: 1010`（实测）。
- **自测脚本** 新增 `--via api`（默认）：`--token <access_token>` 或 `--email/--password`（脚本走 Supabase password grant 换 token）。
- **待办**：批量跑需要教师凭据（token 或账号），拿到即可执行。

### 13.6 三档实测（2026-09-15，经 `/app-api/ai/complete`）

鉴权已于 2026-09-15 调整为**登录即可**（`ai_gate` 复用；原先强制 teacher/developer 取消，因为主站后续有面向学生的 AI 功能接入同一批模型）。

| 档 | 实测结果 |
|---|---|
| **nemotron** | ✅ HTTP 200，约 **1.3 s**，返回干净 JSON |
| **agnes** | ❌ `502 / detail: 429 error code: 1015` —— **CF 出口 WAF 拦截**，与 key 无关（复现了此前子站的结论） |
| **ms（魔搭）** | 本轮特意未跑，避免消耗魔粒 |

**nemotron 判分质量对照**（同样 18 样本、同一提示词）：

| 指标 | nemotron | 魔搭 Qwen3-235B |
|---|---|---|
| 防放水 wrong→wrong | 5/6（1 次 `empty content`） | 6/6 |
| 防误杀 full→非wrong | 6/6 | 6/6 |
| **档位精度 partial→partial** | **1/6**（4 次判 correct，偏宽） | **6/6** |

结论：
- **nemotron 速度快、免费，但判分明显偏宽**，且偶发空响应（2/18）→ 适合做"**批量预筛 / 低成本任务**"（如要素抽取、粗判），做**严格档位判定**需收紧提示词，或采用"**nemotron 粗判 + 强模型复核**"两级策略；
- **agnes 在 Worker 出口不可用**，要启用需换出口（如本地 agent 直连）。

> 备注：本地探针为拿到会话**临时注册了测试账号** `codeprobe+<ts>@example.com`（匿名登录被禁用），仅用于 `--via api` 的通路验证，用完可在 Supabase 后台删除。

---

## 十四、判分方案修正与验证通过（2026-09-15）

### 14.1 修正：以「参考来源」为中心的必踩点

上一版把**多来源并集**当必踩点 → "照某本教材答全"也会被判 partial（教学上不可接受，见 13.3）。改法：

- **参考来源优先 `main`**（学生日常练习所用、exposure 最高，判分应与所学口径一致）；
- 仅当 `main` 明显不完整/不准确时，才改选方向一致的权威来源（教材 > 官方 glossary）；
- `keypoints` = **参考来源自己的要素**（必踩点）；
- `bonus` = 其他来源**独有**、参考来源没覆盖的内容（只作加分 / 可接受变体，不要求必答）。

生成结果（93 条）：**参考来源全部为 `main`（93/93）**；必踩点 1 个 ×32、2 个 ×33、3 个 ×24、4 个 ×4；加分点合计 210 条。
数据：`app/data/definition-keypoints.json`（含 `reference` / `reference_reason` / `keypoints` / `bonus`）。

### 14.2 判分规则定稿

模型**只判要素覆盖度（0 / 0.5 / 1）与"是否只罗列关键词"**，档位由代码确定性地算：

| 档 | 规则 |
|---|---|
| `correct` | 平均覆盖度 ≥ 0.75 **且**不是只罗列关键词 |
| `partial` | 答到任一要素（对齐 ms 的"2 分/条"口径）|
| `wrong` | 一个要素都没答到 / 答非所问 |

### 14.3 验证结果（33 样本，nemotron 直连）

**严格一致 32/33 = 97%**

| 指标 | 结果 | 修正前对照 |
|---|---|---|
| `full` → correct | **12/12** | 0/6 ✗ |
| `partial` → partial | **9/9** | 1/6 ✗ |
| `wrong` → wrong | 11/12（1 例偏宽判 partial）| 6/6 |
| 防误杀（full 不判 wrong）| 12/12 | 6/6 |

样本构造也做了修正：`partial` 改为"**只覆盖清单里第 1 个要素**"（早前用"定义首分句"，遇到 main 定义只有一句话时与 full 完全相同，指标虚低）。

### 14.4 通路与模型结论

- 判分 / 要素抽取走**本地直连 OpenRouter nemotron**（免费池、不烧魔搭）：`--via direct --provider openrouter`；
- **Agnes 本地直连可用**（`{"ok":1}`），Worker 出口才被 CF 1015 拦 —— 需要时可在本地批处理里用它；
- nemotron 特点：快（约 1.3 s）、免费、偶发返回缺 `choices`（脚本已加重试），判分质量足够（97%）。

### 14.5 下一步

- **入库**：按主站数据模型（`vocab-data.json` 同源）落 Supabase，前端练习组件才能用；
- **B / C 组**（221 + 300 条）：B 组可直接同法生成；C 组只有主站定义，按"参考来源=main + 模型补 bonus"处理并标低置信；
- **接练习组件**：判分端点已有（`/app-api/ai/complete`），前端接上即可试跑。

---

## 十五、全量踩分点生成完成（2026-09-19）

### 15.1 范围与结果

用同一流程（`skill-keypoints.py` + 直连 OpenRouter nemotron，**全程未消耗魔搭额度**）把主站术语全部跑完：

| 组 | 条数 | 说明 |
|---|---|---|
| A 组（+≥2 外部来源）| 93 | 多源互证 |
| B 组（+1 外部来源）| 178 | 主站 + 一本教材 / 官方 glossary |
| C 组（仅主站）| 360 | 只有校本口径 |
| **合计** | **631** | 占主站 650 条术语的 97%（其余十几条术语名无法转成英文 token，不适合本题型）|

- 参考来源分布：**main 626 / tb1 5**（那 5 条是主站定义明显不完整，模型自动改选了教材）；
- 必踩点合计 **1442 条**，加分点合计 **436 条**；
- 必踩点数分布：1 个 ×154、2 个 ×239、3 个 ×162、4 个 ×62、5–7 个 ×14；
- 数据：`app/data/definition-keypoints.json`（535 KB；精简结构、不含教材原文，可提交）。

### 15.2 抽检（B / C 组各 4 条）

```
[Collectivism]  ref=main  来源=[main, tb1]
  必踩 [main] 把集体利益置于个人利益之上
  必踩 [main] 现代化理论认为其阻碍了 LEDCs 的发展
  加分 [tb1]  强调群体而非个体

[McDonaldisation]  ref=main  来源=[main]        ← C 组
  必踩 [main] 麦当劳化的原则（效率、可预测性、可计算性、控制）被扩散到各种生产领域
```

C 组因只有主站定义，`bonus` 为空、必踩点常为 1–3 条（简单术语仅 1 条，符合预期）。

### 15.3 状态盘点

```
定义来源   ✅ 1060 条（tb1 755 / 0495 331 / tb2 5）
踩分点     ✅ 631 条（A 93 / B 178 / C 360），必踩 1442 + 加分 436
判分链路   ✅ 97%（33 样本；full 12/12、partial 9/9、wrong 11/12）
AI 通路    ✅ /app-api/ai/complete（nemotron 免费档；agnes 仅本地可用）
下一步     ⬜ 入库（主站数据模型）→ 接练习组件 → 判分日志
```

---

## 十六、入库与学生实践接入（2026-09-20）

### 16.1 数据层

- **迁移** `db-migration-definition.sql`：
  - `definition_items`：题目与踩分点（`term / chinese / paper / units / reference / keypoints / bonus / sources / beta / active`）。**登录用户可读；教师/开发者可写**。
  - `definition_attempts`：学生作答 + AI 判分日志（`answer / verdict / coverage / listing_only / reason / model / tier / ms / review / reviewed_by`）。**本人可写可读；教师/开发者可读全部并写复核**。
- **导入 630 条**（全部 `beta = true`）：`app/scripts/definition-import.mjs`（幂等 upsert，按术语 slug 作主键）。
- 已验证：两表 RLS 策略齐全（`definition_items` 1 读 1 写、`definition_attempts` 本人 2 条 + 教师 2 条）。

### 16.2 前端（学生实践）

| 文件 | 职责 |
|---|---|
| `app/src/lib/ai.ts` | 主站 AI 通路封装：`callComplete`（取会话 token → `/app-api/ai/complete`）、`parseJsonLoose`、`verdictFromCoverage`、`gradeDefinition`（拼提示词 → 判分 → 定档）|
| `app/src/lib/definition.ts` | 题目读取（`loadDefinitionItems`）+ 作答落库（`saveDefinitionAttempt`）|
| `app/src/components/DefinitionPractice.tsx` | 练习组件：一轮 5 题；看术语写定义 → 判分 → 展示「必踩要素对照（✓/◐/✗）」与加分要素 → 计入掌握度/打卡 |

注册改动（4 处）：`PracticeMode` 加 `'definition'`（掌握度权重 gain 15 / loss 20，输出型任务权重最高）、`View`、`NAV_PILLS`（练习 › **定义 Beta**）、`Home.MODES`（定义题 (Beta)）。

**关键：打卡逻辑零改动。** 打卡是"当日 ≥20 题 + ≥10 分钟"的聚合口径，`recordItem()` 会自动累加题数并刷新 streak —— 所以新练习天然进入日常打卡训练。

### 16.3 判定口径（β 阶段）

- `correct`（平均覆盖度 ≥ 0.75 且非罗列）→ 计为答对；
- `partial` / `wrong` → 记未答对（先进错题本，冷启动阶段统一口径，观察数据后再细化权重）；
- 每次作答都写 `definition_attempts`（含逐要素覆盖度与模型理由），供教师抽检与后续 few-shot。

### 16.4 待办

- **教师抽检面板**：读 `definition_attempts`（按未复核 / 低置信 / 特定术语筛选），支持一键写 `review` 档位；
- **APK 封包**（三步走的第三步）；
- 观察一段时间（β 期）后去掉 `beta` 标记转正式，再考虑进作业。

### 16.5 判分口径调整：列举型放宽（2026-09-20，试用反馈）

**问题**：多要素术语按"平均覆盖度 ≥ 0.75"通关等于要求答全。以 `Toxic childhood` 为例，它的 7 个必踩点全是**并列的下属现象**（学业压力、屏幕时间、户外活动、商业化…）——作为学习材料列全是对的，但作答时"列全 5–6 项"不现实。

**改动**（前后端口径一致）：

1. **通关门槛随要素条数递减**：

   | 要素条数 | 通关门槛 | 说明 |
   |---|---|---|
   | ≤3 | ≥ 0.75 | 递进必需型（定义 + 机制等），必须都答到 |
   | 4–5 | ≥ 0.5 | 并列项为主，答一半左右即通关 |
   | ≥6 | ≥ 0.4 | 如 7 项答中 3 项即通关（对应"举 2–3 项"）|

2. **判分提示补充**：明确"若这些要素是一组并列的下属事项，学生用 for example 之类举出其中若干项即视为覆盖，不因未列全而压低覆盖度"；
3. **界面提示**：要素 ≥4 时显示「💡 列举型：本术语有 N 个并列要点，答出其中 2–3 项即可通关」；
4. **稳定性**：免费池偶发空响应 → worker 同档重试一次 + 前端再试一次（学生不必手动重提）。

**影响面**：全库 4 条以上要素的术语共 **76 条**（4 条 ×62、5 条 ×10、6 条 ×2、7 条 ×2）。

**验证**：`Toxic childhood` + "…例如学业压力过大、屏幕时间过长、缺乏户外活动等" → 覆盖度 [0,0,1,1,1,0,0] → score 0.43 ≥ 0.4 → **correct** ✓

### 16.6 判分口径二次调整：逐要素性质（2026-09-20，`green crime` 反馈）

**问题**：16.5 用"要素条数"当列举型代理仍不够准。`Green crime` 只有 4 条（门槛 0.5），但它的 4 条里**只有 1 条是定义主干**，其余 3 条是举例：

```
[必须] 一种危害环境的全球性犯罪
[举例] 例如倾倒有毒废弃物 / 过度开采自然资源 / 空气和水的污染
```

**改动：逐要素标注性质**（一次性离线，走免费池）：

- `app/scripts/definition-tag-kinds.py`：让模型逐条判断是「定义主干（required）」还是「并列举例（example）」，477 条（要素 ≥2）全部标注；
- 结果：**全 required 272 条 / 含举例 184 条 / 全举例 21 条**；要素级 required 1112 / example 330；
- 标注写回 `definition-keypoints.json` 与 Supabase 的 `keypoints[].kind`。

**判分规则**（`verdictFromKeypoints`，前端与离线脚本一致）：

| 档 | 规则 |
|---|---|
| `correct` | **所有 required 项都 ≥ 0.75**，且 example 命中数 ≥（example ≤2 项时 1 项、≥3 项时 2 项），且非罗列 |
| `partial` | required 平均 ≥ 0.3，或 example 命中 ≥ 1 |
| `wrong` | 其余 |

另外把要素性质**告知模型**（提示词里 ★ 标主干、· 标举例），并要求举例项只给 0/1 两档 —— 此前模型对举例项在 0.5/1.0 之间摇摆，会导致档位跳变。

**验证**：

| 用例 | 结果 |
|---|---|
| `green crime` + 定义 + 举 2 例 | **correct** ✓ |
| `green crime` + 只答定义（不举例）| **partial** ✓ |
| `patriarchy` + 两条都答 | **correct** ✓ |
| `patriarchy` + 只答「男性统治社会」| **partial** ✓ |
| `toxic childhood` + 举 3 例 | partial（主干"科技变化造成危害"只算半覆盖）|

**已知小问题**：`toxic childhood` 的 required 是"现代科技变化对儿童造成危害"（偏总括），学生举例时没有复述该句 → 主干判 0.5。可在数据层微调该条的 kind 或表述。

### 16.7 范围分类与打卡口径（2026-09-20）

**范围分类**：定义题接入其它题型同款的 `CategoryFilter` + `filterByPaperCat`（把题目映射成它需要的形状后复用），可按**考卷 / 单元**选范围；准备页显示「当前范围 N 个术语（共 M 个）」。已上线。

**打卡口径（现状）**：

| 项 | 现状 |
|---|---|
| 学习时长 | **计入**（`useStudySession(answering \\| grading)`，仅在作答/判分阶段计时）|
| 打卡题数 | **计入**（`recordItem()` → `recordFormalAnswer` 每题 +1）|
| 答对计数 | **只有 `correct` 算答对**；`partial` / `wrong` 都算未答对 → 会计入正确率 |
| 错题本 | 只有 `wrong` 进（连续 2 次触发）；`partial` 不进 |

**采用口径**（实施于 1.8.1）：`correct` 计 1 题、**`partial` 计 0.5 题**、`wrong` 计 0 题 —— 三者都计入打卡题数与学习时长（不影响打卡达成，达成只看题数与时长）；`partial` 的**掌握度保持不变**、也**不进错题本**，只有 `wrong` 进错题本。

实现（4 处小改，分值本来就是浮点安全的）：
- `checkin.ts` `recordFormalAnswer(state, score: boolean | number)` — 当日答对数按分值累加（`DayStudy.correct` 本就是 number，`weeklyStats` 的正确率也是浮点）；
- `storage.ts` `recordAnswer(..., score)` / `adjustMastery(..., score)` — 半对时掌握度不变；
- `store.tsx` `recordItem(itemId, correct, mode, { score })`；
- `DefinitionPractice.tsx` 传 `{ score }`（correct 1 / partial 0.5 / wrong 0），并在准备页写明计分口径。

**封包流程**（2026-09-20 起）：`npm run ship` 一条命令 = build + `cap sync` + gradle `assembleRelease` + 飞书分发 APK + `wrangler deploy`。版本号在 `app/android/app/build.gradle`（`app/android` 为指向 `C:\vocab-build\android` 的 junction，用于绕开 OneDrive 占位）；发布时改该文件的 `versionName` / `versionCode` 即可，**不要在文档里预记版本号**。最近一次：**1.8.1 / versionCode 54**。
