# XP 体系：从 B 档（本地累计）转 C 档（服务端权威）—— 改造方案

> 2026-09-22。本文只输出**方案、迁移步骤与清单**，具体改动由各会话分别执行。
> **不修改任何 `definition_*` 相关代码与文件**。
> 相关背景见《练级与奖励体系方案.md》§2（XP 数值）、§六（防作弊分级）、§七（落地分期）。

---

## 〇、先纠正两处前提（影响后面所有结论）

### 0.1 `student_data.data.xp` **目前并不存在**

方案原本描述「云端已存在 `student_data.data.xp`（本地累计的整包快照）」，但实测：

- `CloudStudentData` 接口只有 `{ name, checkin, progress, wrongBook }`；
- 全仓库（排除 `node_modules/dist/android`）搜 `socio_vocab_xp` / `data.xp` / `xp_baseline`，
  **只命中 `definition.ts:109` 的一句纪律注释**，无任何存储 key；
- `app/src/lib/cloud.ts` 无未提交改动。

⇒ **P0（本地 XP 累计）尚未实装，没有任何历史 XP 需要迁移。**

**这是个好消息**：只要**不先上 P0 的本地 XP**，转 C 档就是**零迁移成本**。
（若先上 P0 再转 C，则 §三 的基线迁移必须做。）

### 0.2 打卡数据**有真实历史**，不能一并跳过

`checkin.study` 虽在 localStorage，但**已经产生了几个月的真实记录**（20 名学生 / 105 个练习日，见
2026-09-21 审计）。转 C 档**必须处理它** —— 详见 §8，那是本方案里最需要裁决的一环。

---

## 一、C 档的能力边界

### 1. 学生上报什么、服务端怎么累计

**事件粒度：逐题 + 批量上报**（不是"每组一个汇总"）。

理由：只有逐题才可能做「每题用时下限」「同题去重」这类合理性校验；汇总包无法校验。
量级可承受：一个学生一天 20~70 题，全校 20 人 × 30 天 ≈ **3.6 万行/月**，对 Postgres 微不足道。

**新表 `public.xp_events`**：

```sql
create table if not exists public.xp_events (
  id          bigint generated by default as identity primary key,
  user_id     uuid not null,
  event_id    uuid not null,                 -- 客户端生成，**幂等键**
  kind        text not null check (kind in ('answer', 'chain_complete')),
  item_id     text not null,                 -- 词条 id（chain_complete 时记起点）
  mode        text not null,                 -- PracticeMode 七种，逐条列明（勿简写，曾因此被误读为不含 definition）：
                                             --   choice / spelling / matching / crossword / cloze / chain / definition
  correct     boolean not null,
  score       numeric(3,2) not null check (score >= 0 and score <= 1),
  elapsed_ms  integer,                       -- 该题用时（客户端声明）。可 NULL：缺失时「时长不计」但事件仍收
                                             -- 低于下限 ⇒ 按 500ms 截断，**不拒收**（见 §2 #4）
  answered_at timestamptz not null,          -- 客户端声明（归日 + 补报窗口）
  received_at timestamptz not null default now(),
  day_key     date not null,                 -- 服务端按 answered_at 折算（见下）
  session_id  uuid,                          -- 一次练习会话；chain_complete 靠它防重复
  chain_mode  text,                          -- 仅 chain_complete：open | target
  chain_kind  text,                          -- 仅 chain_complete：choice | input
                                             -- ↑ 接龙的完成 XP 取决于这两者（20/25/30/35），
                                             --   服务端据此查表；客户端**不得**直接指定 XP
  xp          integer not null default 0,    -- **服务端**算出的分值（客户端不可指定）
  suspicious  boolean not null default false,
  unique (user_id, event_id)                 -- ★ 幂等
);
create index if not exists xp_events_user_day_idx on public.xp_events (user_id, day_key);
```

**⚠ `day_key` 必须按中国时区折算**：

```sql
day_key := (p_answered_at at time zone 'Asia/Shanghai')::date;
```

本地 `dateKeyOf()` 用的是**本机时区**。若服务端用 UTC 折算，**晚上 8 点之后（UTC 仍在前一天）
的练习会被记到错误的日期**，直接毁掉打卡与月度统计。这是最容易埋雷的一处。

**XP 分值由服务端按 §2.1 / §2.1.1 的表计算**，客户端**只报"做了什么"，不报"值多少 XP"**。

**⚠ 接龙是特例，必须两种 kind 并存**：

- 接龙**按完成结算**（§2.1.1），不是每题给分；
- 但每步仍然要计入**掌握度与打卡题数**，所以每步照报 `answer`，**只是 `xp = 0`**；
- 一局走完时再报一条 `chain_complete`，携带该模式的完成 XP（20 / 25 / 30 / 35）。

⇒ `xp = 0` 是合法值，不要用 `check (xp > 0)` 把它挡掉。

**上报 RPC**：

```sql
create or replace function public.submit_xp_events(p_events jsonb)
returns jsonb   -- { accepted: n, duplicated: m, rejected: [{event_id, reason}] }
language plpgsql security definer
set search_path = ''
as $$ ... $$;
```

一次最多接受 **100 条**（超出拒绝），内部逐条校验（见下），`on conflict do nothing` 处理重复。

---

### 2. 能挡什么、**挡不了什么**

**服务端防线清单**（全部在 `submit_xp_events` 内）：

| # | 校验 | 规则 | 挡什么 |
|---|---|---|---|
| 1 | **幂等** | `unique(user_id, event_id)` + `on conflict do nothing` | 重复上报 |
| 2 | **补报窗口** | `answered_at` 距 `now()` **≤ 72 小时**，且 `<= now() + 5min`，且**不落在已结算月份** | 请求失败重试的正常兜底；异常回溯（**非防作弊**，见 §3） |
| 3 | **模式白名单** | `mode in (七种)` | 伪造题型 |
| 4 | **每题用时下限** | `< 500ms` ⇒ **按 500ms 计**（截断，**不拒收**）；完全缺失 ⇒ 记 NULL（时长不计，**事件仍收**） | 非人类节奏（**只做数据整形，绝不丢事件**） |
| 5 | **同题去重** | 同一 `item_id` 在 **60 秒内**只计一次 | 同题反复刷 |
| 6 | **`chain_complete` 唯一** | `unique(user_id, session_id)`（`kind='chain_complete'`）+ 要求该 session 有 ≥3 条 chain 步事件 | 反复发"完成" |
| 7 | ~~词条存在性~~ **本版暂缓** | XP 计算只依赖 `mode/score/kind`，**完全不依赖 `item_id`** ⇒ 伪造它拿不到额外 XP，只影响「同题去重」精度；而校验要查 `vocab_releases` 的大 jsonb（慢），收益不抵成本 | — |
| 8 | **每日裁剪** | 按 `day_key` 聚合，**练习 XP 超过 400 的部分 `xp` 记 0**（事件仍保留） | 一天刷爆 |
| 9 | **异常标记** | 一次批量里 `elapsed_ms` **方差为 0**（或全相同）⇒ `suspicious = true` | 脚本批量（**只标记、不拒绝**） |

**⚠ 刻意「不」做单题时长上限**（2026-09-22 教师裁定，见 §8）：它拦不住想作弊的人，
却会截掉诚实学生在一道难题上的真实投入。

**⚠ 关于 #4「每题用时下限 500ms」的定位（2026-09-22 澄清）**

**500ms 是暂定值，没有实测依据**（现有审计数据只有日汇总、无逐题用时；
上线后应据真实分布再定）。须明确它的**能力边界**：

- **能拦**：同一秒提交多题这类"明显非人类"的节奏；
- **拦不住**：脚本按 600ms/题刷，以及**按 30 秒/题刷** ——
  因为要同时满足「20 题 **且** 10 分钟」，脚本**必须**把均值做到 30 秒/题，
  **远在 500ms 之上** ⇒ **它在"刷时长"上基本不起作用**。

⇒ 它是**便宜的第一层过滤**，主力防线是：幂等、每日 400 裁剪、异常标记（方差为 0），
以及**题数与时长的且关系**。

**刻意「不」按题型细分下限**：已记住的拼写 1 秒内打完是正常的，设高会误伤诚实学生；
设低不带来风险。**定义题无需特殊处理**（它要写一段话，耗时长天然远超任何下限）。

**⚠ 2026-09-22 定为标准：只截断，不拒收**（教师提出"担心下限误伤答题快的学生"后定案）

原设计是「低于下限 ⇒ 拒收整条事件」。**这有问题**：万一真被误伤，学生丢的不是"一点时长"，
而是**整条事件 —— 连 XP 与题数一起丢**。而拒收对防刷**没有任何额外效果**
（能伪造的人直接报 600ms 即可，照样通过）。

⇒ 改为：

| 情况 | 处理 |
|---|---|
| `elapsed_ms < 500` | **按 500ms 计入**（截断）—— 真误伤时只少算一点时长，**不丢事件** |
| `elapsed_ms` 完全缺失 | **记 NULL，时长不计**，但**事件仍收、XP 与题数照给** |

**原则与 §8「不加单题上限」一致：宁可少算，不要丢事件。**

**500ms 的取值依据**：人类**简单视觉反应**极限约 200~250ms，而
「看到题目 → 做出选择」至少约 500ms。所以 500ms **基本贴着人类极限**，
「秒选已记住的词」（1~2 秒）完全在安全区，**正常不会触发截断**。

**#9 刻意只标记不拒绝**：真实场景里学生连做 5 道选择题，用时也可能接近到秒；自动拒绝会误伤。
标记交给教师核验时判断。

### ⚠ 挡不了的（必须写清楚，避免误以为 C 档 = 防住一切）

| 挡不了 | 说明 |
|---|---|
| **伪造事件内容** | 前端在学生手里。他可以改客户端，直接发 `{correct: true, elapsed_ms: 3000}`。**C 档无法验证这次作答是否真实发生过**。 |
| **共用账号** | 两人一账号，无法检测。 |
| **人工慢速乱选** | 每题老老实实等 3 秒、但全部瞎选。只能靠"正确率极高 + 用时极短"这类统计标记，**而真学霸会被误伤**，所以不能自动判罚。 |
| **本地计时口径** | `elapsed_ms` 是客户端声明的，同样可伪造（与现状的 `addStudySeconds` 等价）。 |

**⇒ 核心结论（请勿省略）**：

> **C 档把作弊门槛从"改一个 JSON 字段"提高到"写客户端脚本"，但没有消除伪造的可能。**
> **因此实物奖励仍然必须保留教师人工核验（B 档那一层不能撤）。**
> C 档真正的收益是：**教师核验的输入从"可被随意编辑的整包 JSON"变成"不可篡改的事件流水 + 可疑标记"**，
> 且**学生无法通过清缓存/改 localStorage 来凭空造出历史**。

---

### 3. 断网补报与幂等键

**幂等键 = 客户端生成的 `event_id`（uuid v4）**。

为什么**不能**用"服务端单调 seq"：那需要**先向服务端要号再上报**（两次往返），
**断网时根本拿不到号** —— 而断网补报正是必须支持的场景。

客户端 uuid 的额外好处：**恶意复用同一 `event_id` 只会被去重，不会多计**，滥用方向是安全的。

**补报流程**：

1. 学生离线作答 → 事件写入 IndexedDB / localStorage 队列（含 `event_id`、`answered_at`）；
2. 联网后（含下次打开 App）批量调 `submit_xp_events`；
3. 服务端逐条校验，返回 `{accepted, duplicated, rejected}`；
4. 客户端**只删除 accepted + duplicated 的本地队列项**（rejected 保留并提示原因，避免静默丢分）。

**⚠ 补报窗口：72 小时 +「不落在已结算月份」兜底**（2026-09-22 修正两轮）

**第一轮修正**（撤回原"7 天窗口"的理由）：原写"防止攒一个月再一次性上报"，**不成立**——
攒着本身无害（学生真做了 1000 题就该给分），且窗口**也挡不住伪造者**
（他直接声称"这是我前 7 天做的"即可，7 天和 30 天对他没区别）。

**第二轮修正**（教师指出真实使用场景，整个概念随之收窄）：

> 本站是**网站**，**登录本身就要联网** —— 所以"打开时就没网"根本进不来，
> 也就不存在"未上报的数据"。**长期离线不是真实场景。**

⇒ 补报的必要性**不在"离线几天"，而在"请求失败"**，而这类失败是**必现**的：

| 场景 | 会发生 |
|---|---|
| 移动网络下请求超时（地铁、电梯、弱信号） | **是** |
| 服务端部署时的短暂 5xx —— 我们自己 `npm run ship` 就会有那几秒 | **是** |
| 练习中途 WiFi 断开（页面已打开，可继续做） | **是** |
| 标签页关闭时仍有未发事件 | **是** |

**⚠ 现有 `resilientFetch` 只管「直连失败 → 换代理」，不重试业务请求** ⇒ 失败就是真失败，
必须自己排队。

⇒ **窗口收窄为 72 小时**：足够覆盖"关页面 / 断网一晚 / 次日补上"。
**"补报"由此从"跨月机制"降级为"本地队列 + 失败重试"**，实现复杂度大幅下降。

**仍保留「已结算月份冻结」作兜底**：72 小时内也可能跨月（9/30 晚断网 → 10/1 重试），
该规则成本几乎为零，而它保护的是「**教师核验过的数字不再变化**」。

**⚠ 性质必须说清**：**这不是防作弊**（想作弊的人直接报当月的假数据即可绕过），
而是**保证结算稳定**与**不丢事件**。"伪造日期"属于"**C 档无法消除伪造**"的一部分（同 §2），
**故人工核验仍然必须保留**。

超过窗口的**拒收并在客户端明示**（"这条记录超过 72 小时，无法补报"），**不要静默丢弃**。

**项目内已有同类先例可参照**：`db-migration-live-spell-rpc.sql` 的 `live_submit_answer`
已实现「`already=true` 表示重复提交、返回首次结果、不改写」的幂等语义。

---

## 二、策略裁决（四问）

### 4. 教师奖励 XP **不计入**每日 400 上限 —— **同意贵方主张**

**理由**：

1. 上限的**唯一目的是防刷**，而教师签发**天然不可刷**（教师手动点击，且 `attempt_id` 唯一索引挡重复）；
2. **若计入**：学生当天练习已满 400，教师随后签发的 20 XP **当场被吞掉** ⇒ 激励在"最该被鼓励的时刻"失效；
3. 语义上本应分开：**400 是「自主练习」的上限**，教师奖励是**外部授予**，不是同一类东西。

**⇒ 计算口径**：

```
每日可用 XP = min(当日练习 XP, 400) + 当日教师奖励 XP
```

**教师侧不建议设硬限额**（签发本身就是低频、可见的人工动作），但建议教师端显示
**"今日已签发 N XP / M 人次"**，便于自查异常。

### 5. **计入**月度 XP 增长与榜单 —— 但需要用"量级"而非"规则"来防操纵

**先给量级**：

| 项 | 量级 |
|---|---|
| 普通学生月度练习 XP | 约 **3000~4500**（日均 113 × 约 30 天） |
| 尖子生月度练习 XP | 约 **11000**（日均 370 × 30 天） |
| 教师奖励条数 | 每人每月**约 0~3 次**（教师手动签发） |
| 教师奖励合计 | 约 **0~60 XP** |

**⇒ 教师奖励在月度总分里**不足 2%**，**在结构上不可能操纵排名**。

**⇒ 裁决：计入，不设额外权重上限**（规则越简单越不容易出错）。

**⚠ 但必须留一个触发条件**：**若将来提高 `XP_FOR_DISPUTE` 的单价**（例如提到 200），
上述量级论证失效，**必须回头重新评估"是否计入排名"**。这条写进 §7 未决事项。

**为什么仍然选择计入**：若不计入，则"提出有价值质疑"这件事**在月度榜上完全不可见**，
而月度榜恰恰是学生唯一能感知到的公开反馈 —— 那会让这个正反馈断在最后一环。

### 6. 总 XP 在**展示层现算相加**，永不落盘

**三层，全部现算**：

```
练习 XP   = 服务端聚合 sum(xp_events.xp)，按 day_key 应用 400 裁剪
奖励 XP   = 服务端聚合 sum(student_xp_bonus.amount)
显示总 XP = 练习 XP + 奖励 XP
```

**⇒ 关键约束（沿用贵方纪律）**：

- **绝不写进 `student_data.data`**（整包覆盖 ⇒ 会被学生下一次同步冲掉，且会重复累加）；
- **绝不写进任何"本地累计"字段**；
- **教师奖励绝不并进练习 XP**（否则月度裁剪会把它吞掉，与 §4 自相矛盾）。

**⇒ 统一出口：一个 RPC，学生端与教师端共用**

```sql
create or replace function public.get_xp_summary(p_user_id uuid default null)
returns jsonb
-- 返回 { practice_xp, bonus_xp, total_xp, daily: [{day_key, practice_xp, capped, bonus_xp}] }
```

- **学生调用**：`p_user_id` 必须为空或等于 `auth.uid()`，否则抛异常；
- **教师/开发者调用**：可传任意 `p_user_id`（内部按 `user_roles` 判角色，与
  `student_xp_bonus_read_staff` 同款写法）；
- **两端共用同一函数**是刻意的：**防止"学生看到的总 XP"与"教师核验用的总 XP"口径分叉**
  —— 那正是最容易出争议的地方。

---

## 三、老数据与迁移

### 7. 历史 XP：**不存在，无需迁移**（前提是跳过 P0 的本地 XP）

见 §0.1。**两条路径，请择一**：

| 路径 | 做法 | 迁移成本 |
|---|---|---|
| **A（推荐）** | **直接上 C 档**，P0 的"本地 XP 累计"**不落地**（或只保留等级 UI 的即时反馈，不持久化 XP） | **零** |
| **B** | 先上 P0（本地方案已写好），再转 C | 需 `xp_baselines` 表：把每个学生 `student_data.data.xp` 快照成一次性基线，之后该字段**只读废弃** |

**若选 B，基线必须进独立表，不能留在 `student_data` 里**：

```sql
create table if not exists public.xp_baselines (
  user_id     uuid primary key,
  amount      integer not null,
  note        text,
  captured_at timestamptz not null default now()
);
```

**理由**：`student_data.data` 是**整包覆盖**。把基线放在里面，学生一清缓存再同步
（上传一个不含基线的包）就会**把基线抹掉**。**任何"权威"数据都不能放在整包里。**

**⚠ 无论哪条路径**：**绝不能把 `student_xp_bonus` 的金额并进基线**（会与实时相加重复计算）。

### 8. ⚠️ 打卡数据的分裂 —— **结论：应当一并服务端化**

**这是本方案最重要的裁决。**

**问题复述**：若只把 XP 服务端化、打卡不动，会出现「**XP 权威在服务端、打卡权威在本地**」的新分裂。
而月度**全勤奖是实物奖励的唯一出口之一**，其判据正是打卡天数。

**现状风险清单**：

| 风险 | 严重度 | 说明 |
|---|---|---|
| **教师看到的不是最新** | **高** | 打卡只随 `student_data` 整包上传（800ms 防抖）。学生**一周不登录**，教师看到的就是**一周前**的打卡，却要据此发实物奖。 |
| **可被本地编辑** | **高** | `checkin.study` 在 localStorage，改一行就是个"全勤"。这与 XP 的防作弊目标**直接冲突**。 |
| **跨设备合并不可靠** | 中 | `mergeStudentData` 按日取 `max`。多设备交替使用时会**丢掉较小的一侧**（同一天在两台设备各练一部分 ⇒ 只留大的那天）。 |
| 清缓存丢数据 | 低 | 有云端 merge 兜底，通常能恢复。 |

**⇒ 结论：一并服务端化。而且边际成本很低 —— 因为 XP 和打卡本来就是同一次练习的两个侧面。**

**具体做法**：打卡不再单独上报，而是**从 `xp_events` 聚合出来**：

```sql
-- ⚠️ 改用 RPC 而非视图：Postgres 的视图默认以**视图所有者**权限执行，
--    会**绕过基表 RLS**；要安全需 `security_invoker`（PG 15+），
--    而本库是 AnalyticDB for PostgreSQL 兼容版、版本不保证
--    ⇒ 改用 security definer 的 RPC，内部显式做归属校验，行为确定。
create or replace function public.get_daily_study(
  p_user_id uuid default null, p_from date default null, p_to date default null
) returns jsonb
language plpgsql security definer set search_path = ''
as $$
  -- 学生只能查自己；教师/开发者可查任意。
  -- 聚合口径：
  --   questions = count(*) filter (where kind = 'answer')
  --   ms        = sum(elapsed_ms) filter (where kind = 'answer')  ← 只算作答，不含 chain_complete
  --   correct   = count(*) filter (where kind = 'answer' and correct)
$$;
```

**打卡达标 = 服务端判定**：

```
questions >= 20  and  ms >= 7 * 60 * 1000
```

**⚠ 时长门槛：保持 10 分钟不动**（2026-09-22 教师裁定 —— 此处撤回我原先"应降到 7 分钟"的建议）

原判断有两处错误：

1. **误把两个门槛看成互相打架**。实际它们**各管一件事**、是互补关系：
   **10 分钟防「一道题挂 10 分钟」，20 题保真实练习量**。
   「做完作业只要 7.7 分钟」并不构成冲突。
2. **7.7 分钟的数据只来自 2 个学生**（n=2），不足以据此调门槛；
   实测里有学生是 1.0~1.9 题/分钟，调低会**误伤做得慢的人**。

**⚠ 「不需要」单题时长上限**（2026-09-22 教师裁定 —— 此处再度修正我的建议）

我原以为新口径新增了"一题挂很久"的攻击面、需要加单题上限。**这个推理是错的**，
因为误判了两个门槛的关系 —— **门槛是「且」，不是「或」**：

| 攻击 | 时长 | 题数 | 结果 |
|---|---|---|---|
| 一题挂 20 分钟（`elapsed_ms = 1200000`） | 1200s ✓ 满足 | **1 题 ✗ 不满足** | **打不了卡** |
| 快速乱点凑 20 题（每题 3 秒） | **60s ✗ 不满足** | 20 题 ✓ 满足 | **打不了卡** |

**两个门槛从两头夹住「真实练习」**：挂机凑时长则题数不够，乱点凑题数则时长不够。
**任何一头投机都会被另一头挡住，不需要额外裁剪。**

**更关键：那个上限根本拦不住想作弊的人。** 他既然能伪造事件，直接报
20 条 `elapsed_ms = 30000` 即可，**完全落在任何合理上限之内** ⇒
上限唯一的作用是**截掉诚实学生在一道难题上的真实投入**，属于**纯粹的误伤**。

**⇒ 因此：门槛保持 10 分钟，防线清单回到 9 条，不做任何单题裁剪。**

**两个门槛的分工（记录下来，避免以后再误判）**：

| 门槛 | 真正的职责 |
|---|---|
| **10 分钟** | 防「**题数凑够但没投入时间**」（快速乱点凑数） |
| **20 题** | 防「**时间够但没有题数**」（挂机） |

**互补而非冗余** —— 这正是必须用「且」的原因。

**⚠ 达标率影响：比预想的小得多**（2026-09-22 修正 —— 我此前高估了它）

原先写"挂机不再产生时长 ⇒ 达标难度必然上升"。**这个判断是错的**，因为它把
"旧口径下可以挂机补到 10 分钟"当成了正常使用 —— **而"正常使用"的定义本来就是不靠挂机**。

**实测印证**（Jennifer，4 天全部未达标）：

```
08-31  20 题  停留 0.0 分  未达标   ← 早期作业未纳入计时制度的历史遗留
09-06  20 题  停留 2.5 分  8.0 题/分  未达标
09-12  20 题  停留 3.3 分  6.0 题/分  未达标
09-19  25 题  停留 4.3 分  5.8 题/分  未达标
```

她**最多做过 25 题，从来不是靠挂机凑时长** —— 而是**做得快、做得少**。
新口径下她的处境**完全不变**（两种口径都未达标）。

⇒ 对**真的在练**的学生，两个口径的差异**只来自「反馈展示 + 切换题目的间隙」**，
量级约 **10~20%**，**不是"变严很多"**：

| 学生类型 | 新旧口径差异 |
|---|---|
| **慢**（20 题就要 12+ 分钟） | **≈ 0**（时长本来就超线） |
| **中位**（2.6 题/分钟） | 约 **10~15%**（多做几题即补上） |
| **快**（6 题/分钟） | **本来就未达标**（旧口径也未达标）⇒ 差异同样为 0 |

⇒ **结论：既不预先调门槛**（预先调会放宽慢速学生、同时仍卡住快速学生），
**也不预设"会大面积不达标"**。**上线后看真实分布**再定，那时才有依据。

**历史打卡的迁移**：已有一份 2026-09-21 的全量审计导出（逐日 `questions/seconds/correct`）。
照 §7 的教训，**历史打卡也必须进独立表**，不能留在 `student_data` 里：

```sql
create table if not exists public.checkin_baselines (
  user_id   uuid not null,
  day_key   date not null,
  questions integer not null,
  seconds   integer not null,
  correct   numeric(6,2) not null,
  primary key (user_id, day_key)
);
```

**迁移 SQL 可以由审计 JSON 自动生成**（我手上就有那份文件），**一次性导入、之后只读**。
服务端最终口径 = `baselines` + `v_daily_study`，**按 `day_key` 合并取和**（不是取 max）。

---

## 四、接口约定

### 9. `student_xp_bonus`：**沿用，无需任何结构变更** ✅

现有结构（`db-migration-definition-dispute.sql` §3）已完全满足需求：

- `amount` / `reason` / `attempt_id`（唯一索引防重复）/ `created_by` / `created_at` —— 字段齐备；
- **`created_at` 已够用**：月度统计要按"奖励发生在哪个月"归集，直接用它即可，无需新增列；
- **RLS 三策略齐备**（学生读自己、教师读全校、教师写、教师删）；
- **`check (amount > 0)` 保持**（本体系不含扣分，见方案 §2.3）。

**⇒ 给"定义题会话"的结构变更清单：空（无变更）。**

这与方案原本的设想完全一致（"独立表由教师写、学生只读；将来 XP 上线时只需 `总XP = 本地XP + sum(amount)`"），
**说明当初的接口设计是对的**。

### 9.1 一并裁决：`XP_FOR_DISPUTE = 20` —— **保留 20** ✅

`DefinitionReviewPanel.tsx` 里标注了"数值待 XP 体系会话裁决"，现裁决如下：

**保留 20**，理由：

| 参照 | XP |
|---|---|
| 定义题全对 | 10 |
| 拼写默写 | 8 |
| 选择 | 4 |
| 逻辑接龙一局（约 1~1.5 分钟） | 20~35 |
| **质疑被认定有价值（教师手动签发）** | **20** |

- 20 = **2 道定义题全对**，对"**主动挑战判分、推动判分改进**"这种**稀有且高价值**的行为，
  是合适的量级；
- 它**不计入每日 400 上限**（§4），所以不会挤占学生的日常练习收益；
- 量级上**不足以影响月度排名**（§5），所以不会引入操纵风险。

**允许的调整区间：15~20。** 若实际操作中教师发现签发过于频繁（例如一人一月 5 次以上），
降到 15；若发现学生因此更愿意提出高质量质疑，保持 20。

### 9.2 本方案需要新增的对象（**由 XP 体系会话负责，非定义题会话**）

| 对象 | 类型 | 说明 |
|---|---|---|
| `xp_events` | 表 | 事件流水（§1） |
| `checkin_baselines` | 表 | 历史打卡一次性迁移（§8） |
| `submit_xp_events(jsonb)` | RPC | 批量上报 + 九道防线（§2） |
| `get_xp_summary(uuid)` | RPC | 两端共用的总 XP 出口（§6） |
| `xp_of()` | 函数 | 服务端算分（XP 表的唯一实现，见 §2.1 / §2.1.1） |
| `get_daily_study(uuid, date, date)` | RPC | 打卡从事件聚合（§8）。**用 RPC 而非视图**，避免视图绕过 RLS |
| `xp_baselines` | 表 | **仅路径 B 需要**（§7） |

---

## 五、迁移步骤（按路径 A —— 推荐路线）

1. **建表与 RPC**（§9.2 四个对象），**幂等脚本，可重复执行**；
2. **导入历史打卡基线**：由 2026-09-21 审计 JSON 生成 `checkin_baselines` 的 INSERT；
3. **前端改造**：
   - `recordItem` 产出事件（含 `event_id`、`elapsed_ms`、`answered_at`、`session_id`）；
   - 离线队列 + 联网补报；
   - 等级/经验条改为读 `get_xp_summary`（不再读本地 XP）；
4. **打卡判定切到服务端**：等级 UI 与打卡页读 `get_daily_study()`（**RPC，不是视图** —— 理由见 §9.2）
   + `checkin_baselines`（历史基线），两者**按 `day_key` 求和**（不是取 max）；
   ⚠ 此处原先写的 `v_daily_study` 是早期版本的说法，**已废弃**，实际实现是 RPC。
5. **同步调门槛**：时长 10 → 7 分钟（§8）；
6. **双跑验证期**（建议 3~5 天）：本地算一份、服务端算一份，**对比差异**，
   差异大的学生逐个看是被哪道防线挡掉还是算错；
7. **撤掉本地 XP 持久化**，`student_data` 中不再出现任何 XP 字段；
8. `student_data.data` 里**保留 `checkin`**（作为离线兜底与审计留痕），但**不再是权威**。

**⚠ 并行会话注意事项**：`store.tsx` / `home.tsx` 是热点文件。建议把事件上报与读取封装进
**新文件**（如 `lib/xp.ts`、`lib/xpQueue.ts`），对热点文件的改动收敛到 `recordItem` 一处。

---

## 六、需要其他会话执行的清单

| 会话 | 需要做的事 |
|---|---|
| **定义题会话** | **无。** `student_xp_bonus` 结构不变（§9）；`loadMyXpBonus()` 保持不变，**继续遵守"现算不落盘"**；`XP_FOR_DISPUTE = 20` 已裁决保留（§9.1），可把代码注释里的"待裁决"改为已定。 |
| **XP 体系会话** | §9.2 的四个对象 + §五 的 8 步。 |
| **实时活动会话** | 无（不涉及）。 |

**若定义题会话希望"质疑奖励"在教师端显示为已并入总 XP**，请**调用 `get_xp_summary`**，
不要自行 `sum(amount)` 后写进任何地方 —— 避免出现第二个口径。

---

## 六之二、定义题接入裁决（2026-09-22，回应定义题专项会话）

**先纠一处误读**：`definition` **本来就在 `PracticeMode` 七种之内**
（`types.ts`：`choice | spelling | matching | crossword | cloze | chain | definition`）。
§1 原先把 mode 写成行内注释「PracticeMode 七种」没列全，导致看起来像"不在范围内"。
**⇒ 定义题本就纳入 `xp_events`，不需要另外"加白名单"。**

### 四问四答

**1. 是否纳入？—— 纳入，且必须纳入。**
`definition_attempts` **没有作答用时字段**（它的 `ms` 是**判分耗时**），
若定义题不入流水，**§8 的打卡时长就会缺一块**。

**2. XP 由谁算？—— 只从事件流算，不与 `definition_attempts` 双算。**

| 对象 | 用途 |
|---|---|
| `xp_events`（`mode='definition'`） | **唯一的 XP 来源** |
| `definition_attempts` | **业务记录 + 教师复核**（现有面板在用），**不参与计分** |

分值由**服务端**按事件里的 `score` 映射：**1.0 → 10、0.5 → 5、0 → 2**。
**⚠ 绝不双算** —— 两个来源必然分叉（一个用 `score`、一个用 `verdict`）。

**3. `elapsed_ms` 需要前端新增采集，且有一个关键细节：**
**只覆盖「答案输入」阶段（`answering`），不含 AI 判分等待。**
判分等待是**服务端响应时间**，不是学生投入。
⚠ 现有实现把两者一起计时了（`DefinitionPractice.tsx`：
`useStudySession(phase === 'answering' || phase === 'grading')`），**新口径下要拆开**。

**4. 10 / 5 / 2 是最终值。** 但定义题会改变分布：

| 例子 | 题数 | 时长 |
|---|---|---|
| 15 道选择题（~4 分钟）+ 3 道定义题（~9 分钟） | **18 题** ✗ | 13 分钟 ✓ |

⇒ **定义题耗时长、单次题数少，会让「20 题」更常成为约束**。
但净影响小，**§8 的结论不变：不预调门槛，上线后看真实分布**。

**观察项（不改）**：定义题 XP 效率偏低 —— 选择题约 16 XP/分钟，
定义题约 4 XP/分钟。**但不必调整**：它在掌握度上权重最高（gain 15），
且已从"打卡时长"一侧获得偏待。上线后看学生是否愿意做再议。

### 需要定义题会话执行的清单

| # | 改动 |
|---|---|
| 1 | 前端采集 `elapsed_ms`：题目呈现 → 点击提交，**仅 `answering` 阶段**，不含判分等待 |
| 2 | 上报时 `mode: 'definition'`，`score` 取 pass=1 / partial=0.5 / fail=0 |
| 3 | **不要**从 `definition_attempts` 派生 XP |
| 4 | `definition_attempts` **全部字段保持不动**（`ms` 仍是判分耗时） |

**⇒ 无 DB 变更，无 `definition_*` 结构改动。**

### ⚠ 施工顺序约束（2026-09-22 定义题会话提出 —— 我原先的清单漏了这条）

这两件事**必须分批，不能一起做**：

| 步骤 | 内容 | 时机 |
|---|---|---|
| **①** | 前端**独立采集 `elapsed_ms`** | **不动 `useStudySession`**，可与其它改动并行 |
| **②** | **摘掉 `grading` 阶段的计时**（改为只算 `answering`） | **必须与「打卡切服务端」同批上线** |

**为什么 ② 不能提前**：现有写法是
`useStudySession(phase === 'answering' || phase === 'grading')`，
而 `grading` 是 **AI 判分等待**（数秒级）。若在打卡切服务端之前就摘掉它：

- 学生的**本地打卡时长会立刻减少** ⇒ **达标率当场下降**；
- **更麻烦的是污染观察窗口** —— §8「上线后看真实分布」是想观察**口径变化**的效应，
  提前摘掉 `grading` 后，届时看到的下降**分不清是"口径变了"还是"提前摘了 grading"**，
  两个变量混在一起。

**⇒ 故 ② 必须与 §8 的切换同批**，这样观察到的差异才只有一个原因。

**其余相关确认**（同日收到定义题会话回复）：

- 清单第 2 条**已天然满足** —— `DefinitionPractice.tsx` 早就传了 `mode='definition'` 与
  `VERDICT_SCORE = {correct: 1, partial: 0.5, wrong: 0}`，正是 pass/partial/fail 口径，**无需改动**；
- **`recordItem` 需增加 `elapsedMs?: number`**（`lib/store.tsx`），由定义题侧负责传值；
- **`elapsed_ms` 每题一次**（与每题的 `recordItem` 一一对应），非整轮一次；
- **判分失败后重答的时间计入**（累计本题所有 `answering` 段），理由：
  学生确实在投入，且**判分失败是系统问题、不该让学生承担时长损失**。

---

## 六之三、落地方式：走「鉴权可见」（2026-09-22 教师定）

**不做分支、不新开 Worker。** XP 的新功能（尤其 UI）**仅对 `developer` 账号可见**；
打卡切服务端与历史迁移**同样先只对 developer 生效**，学生继续走旧路径。

| 要点 | 说明 |
|---|---|
| **读写路径都要分流** | **不能只藏 UI** —— 打卡判定是**读路径**，必须 `isDeveloper ? 服务端 : 本地`，否则全体学生的打卡判定都被改了 |
| **Worker 改动保持极小** | XP 的计算全在 Supabase 侧，**几乎不需要改 Worker**（最多 `/sb` 白名单一行） |
| **必须接受的风险** | 生产会跑着**未对学生验证过**的代码；**Worker 顶层抛错会整站挂**（先例 `d1d770a`）⇒ 改动前先 `wrangler dev` 验证 |
| **双跑验证** | developer 账号**同时显示「本地算 / 服务端算」两个数字**，口径差异会立刻暴露 |
| **放量 / 回滚** | 改判断条件后重新部署；也可把 flag 存在 Supabase 小表里，做到**不部署即开关** |

**放弃的方案**：分支（要反复 rebase main，最后合并成本高）、
新 Worker（要维护两套部署，且数据仍是同一个库）。

---

## 六之四、执行记录（2026-09-23）

**已在生产库执行 `db-migration-xp-c.sql`**（`--single-transaction -v ON_ERROR_STOP=1`，exit 0）。
五个对象全部创建成功，并跑了一次**覆盖全部 9 道防线的冒烟测试**（15 项全通过；
测试数据用事务回滚，**未留痕**——复核 `xp_events` 仍为 0 行）。

### ⚠ 测试中发现并修复 1 个真 bug

> **幂等检查必须排在所有其它检查之前。**

原实现把「同题去重」放在幂等之前，于是重复上报同一条事件时会被记为
`rejected: same_item_too_soon` 而**不是** `duplicated`。
后果：**客户端无法区分「这条我已经收到了」与「这条被永久拒绝」**，
补报队列会把**已成功**的事件当成失败反复重试。
⇒ 已把幂等检查提到所有检查最前（`insert` 里的 `unique_violation` 捕获保留作并发兜底）。

### 验证到的关键数值

| 项 | 结果 |
|---|---|
| 正常上报 choice / spelling / definition | XP = **4 / 8 / 5** ✅ |
| 重复上报同一条 | **`duplicated: 1`**，不新增行 ✅ |
| `elapsed_ms = 100` | 截断为 **500**，**仍收** ✅ |
| `elapsed_ms` 缺失 | 记 **NULL**，**事件仍收** ✅ |
| 接龙 `answer` 步 / `chain_complete` | XP = **0 / 30**（open + input）✅ |
| 同 session 重复结算接龙 | **`duplicated: 1`** ✅ |
| 一批 4 条 `elapsed_ms` 全相同 | **`suspicious = true`** ✅ |
| 灌 110 题（原始 440 XP） | 当日练习 XP **停在 400** ✅ |
| 另一个学生读他人事件 | **0 行**（RLS 生效）✅ |
| 学生查他人总 XP | **被拒**：`not allowed to read other users` ✅ |

### 对象现状

- `xp_events` 表（0 行）、`xp_of()`、`submit_xp_events()`、`get_xp_summary()`、`get_daily_study()` 均已就位；
- `xp_events` 上**只有两条 SELECT 策略**（`read_own` / `read_staff`），
  **没有任何写策略** ⇒ **学生只能经 RPC 写入**，无法直接 INSERT/UPDATE/DELETE ✅
- 服务端为 **PostgreSQL 15.8**（`filter (where …)`、`jsonb_to_recordset`、部分唯一索引均支持）。

### 产出物

| 文件 | 说明 |
|---|---|
| `db-migration-xp-c.sql` | 迁移脚本（**已在生产库执行**；幂等，可重跑） |
| `app/scripts/xp-smoke-test.sql` | 9 道防线的冒烟测试（事务回滚，可重复跑） |
| `app/scripts/checkin-xp-audit.js` | 打卡数据审计（教师账号在浏览器控制台跑，导出 JSON） |

### 下一步

`lib/xp.ts`（事件构造）+ `lib/xpQueue.ts`（本地队列 + 失败重试）
⇒ `recordItem` 增加 `elapsedMs?: number` ⇒ 接 UI（**先只对 developer 可见**，见 §六之三）。

---

## 六之五、客户端落地（2026-09-23）

**三个新文件**（对热点文件 `store.tsx` 的改动收敛到最必要处）：

| 文件 | 职责 |
|---|---|
| `app/src/lib/xpLevel.ts` | **等级曲线纯计算** —— 无网络依赖，可独立测试、可被其他模块复用 |
| `app/src/lib/xp.ts` | 事件构造 + RPC 上报/读取出口（`export * from './xpLevel'`）|
| `app/src/lib/xpQueue.ts` | 本地队列 + 离线补报 + 节流 + 心跳 |

**`store.tsx` 只改三处**：
① 两行 import；
② `recordItem` 增加 `opts.elapsedMs` / `opts.sessionId`，并在末尾把事件入队
   （`useCallback` 的依赖由 `[]` 改为 `[authUser]`）；
③ 一行 `useEffect(() => startXpSync(), [])`（补报心跳，与登录状态解耦）。

### 关键设计决策

| 决策 | 理由 |
|---|---|
| **客户端绝不算分** | 只上报「事实」（哪题、对否、耗时），分值由服务端 `xp_of()` 查表。改分值只改一个 SQL 常量，**不必发前端版本**，也不会出现新旧客户端口径分叉 |
| **`event_id` 由客户端生成** | 幂等键。重试 / 离线补报 / 重复提交全靠它去重 |
| **不设「重试次数上限」，只按 72 小时清理** | 与服务端补报窗口一致。断网较久时**不会白白丢掉学生已积累的事件**（这是最伤信任的一类 bug）|
| **15 秒节流 + 三重心跳** | 学生一分钟能做 4 题以上，不能每答一题打一次请求；`online`（断网恢复）/ `visibilitychange`（手机锁屏解锁不触发 online）/ 定时（偶发失败）各自覆盖不同场景 |
| **队列条目自带 uid** | 补报前校验归属，**换账号后不替别人补报**（不符则丢弃）|
| **上报失败绝不抛给练习流程** | 练习体验优先；事件留在队列等下次 |

### 验证（均为真跑，非静态检查）

**① 等级换算**

| 检查 | 结果 |
|---|---|
| 与方案表格对账（LV2=120 / LV5=540 / LV10=1440 / LV20=3990 / LV50=17640）| ✅ 全部吻合 |
| 每级增量 = `120 + 10×(n−1)` | ✅ 扫 1~60 级 |
| 边界（门槛值本身 / 门槛−1 各落哪级）| ✅ 扫到 400 级 |
| `levelOf` 与 `xpForLevel` 互为逆运算 | ✅ **0 ~ 30 万逐点验证**，无矛盾 |

**顺带验证到设计意图**：`119 XP ⇒ LV1（99.2%）` —— 普通学生（实测日均 113）
恰好卡在「差一点升级」上；尖子生 LV1→LV2 只要 0.3 天（新手期爽感）。

**② 端到端**（前端构造函数 → 生产库 RPC）

用 `lib/xp.ts` 自己的 `buildAnswerEvent` / `buildChainCompleteEvent` 造 5 条事件，
直接提交给生产库 `submit_xp_events()`（**事务回滚，无残留**，复核 `xp_events` 仍为 0 行）：

| 事件 | 期望 | 实得 |
|---|---|---|
| `choice` 答对 | 4 | **4** ✅ |
| `definition` 部分正确（score 0.5）| 5 | **5** ✅ |
| `spelling` 答错 | 2 | **2** ✅ |
| `cloze` 答对（**不给用时**）| 7 | **7** ✅ `elapsed_ms` 记 NULL，**事件仍收** |
| `chain_complete`（target + input）| 35 | **35** ✅ |
| **合计** | 53 | **53**，`day_key = 2026-09-23` ✅ |

⇒ **字段名与服务端 `jsonb_to_recordset` 逐字对齐，全链路打通。**

### 下一步

把各题型接上（传 `elapsedMs` / 发 `chain_complete`）：
`DefinitionPractice.tsx` → `Choice` / `Spelling` / `Cloze` / `Crossword` / `Matching` → `LogicChain.tsx`；
然后接 UI（经验条、等级、待同步条数）—— **先只对 developer 可见**（§六之三）。

---

## 六之六、各题型接入（2026-09-23）

**接入原则**：只新增一个 hook（`useElapsedTimer`）与 `recordItem` 的一个参数，
所有练习组件按**同一模式**接入，避免每种题型各写一套计时逻辑。

### 计时：`useElapsedTimer()` 一个 hook 覆盖两种场景

| 方法 | 语义 | 谁用 |
|---|---|---|
| `lap()` | **结算并重置** | 逐题题型（选择 / 错题练习 / 拼写 / 匹配 / 接龙每步）**以及定义题的每一段** |
| `peek()` | 只读不重置 | 目前无人使用（保留备用）|
| `reset()` | 归零 | 开始新一轮 / 切到下一题 / 回退 / 丢弃「判分等待」段 |

**定义题为什么要单独处理**：它每次提交都可能判分失败、退回重答，而约定要求
**累计本题所有 answering 段**（判分失败是系统问题，不该让学生损失时长）。

⚠ **初版实现是错的**（2026-09-23 由定义题会话复核后修正，commit `f14b2fa`）：
初版用「失败时不 `reset()` + 下次 `peek()`」，以为这样能把各段加起来 ——
但 `peek()` 读的是**从上次重置到此刻的全部时间**，于是第二次提交拿到的是
`A + G + B`，其中 **`G` 是那次判分等待**（Agnes 档实测约 7.5s，超时可达 30s+），
**被错算成了学生的作答用时**，与约定不符。

**正确做法：每段都用 `lap()` 结算，段长并入 `carryMs`**：

```ts
const elapsedMs = carryMs.current + timer.lap();  // lap 取走本段并立刻重置
carryMs.current = elapsedMs;
setPhase('grading');
try {
  // …上报…
  carryMs.current = 0;      // 成功 ⇒ 清空结转
} catch {
  timer.reset();            // 失败 ⇒ 只丢弃「判分等待」那一段，已累计的段保留
}
```

`lap()` 在进入判分前就把计时器重置了，判分等待因此天然落在下一次结算之外；
`catch` 里的 `reset()` 只是让那段等待不残留。**各段准确相加，判分等待一次都不计入。**

### 两类题型的用时策略

| 类型 | 文件 | 策略 |
|---|---|---|
| **逐题** | `MultipleChoice` / `WrongPractice` / `Spelling` / `Matching` / `LogicChain` | 每题 `lap()`；起点在「开始」与 `next()` / `advance()` |
| **批量** | `Cloze`（一段多空）/ `Crossword`（整盘线索）/ `QuizTaker`（整套作业）/ `CorrectionPractice`（整套订正）| 提交时 `lap()` 取整段用时，**均摊**到每道题 |

**为什么批量用均摊**：这些题型本来就是「一起提交」，无法分辨哪一题花了多久。
均摊保证**总时长不失真**（服务端累加后即真实投入），代价是单题用时不准 ——
但「一起提交」本就无从精确，均摊是唯一合理的近似。

### ⚠ 均摊逼出的一处服务端修正（重要）

原「防线 #9」判据是**「一批 >3 条且 `elapsed_ms` 全相同 ⇒ 可疑」**。
接上均摊后，批量题型的一批事件**用时天然全相同** ⇒ 会被**一律误标为刷分**，
真正的异常反而淹没在噪声里。

**已收紧为「全同 **且** 贴着下限（≤500ms）」**：
服务端会把 `<500ms` 截断成 500，所以脚本刷出来的批次清一色是 500；
而均摊出的真实用时（整段 ÷ 空数）基本在数秒以上。

冒烟测试新增一对正反例（`app/scripts/xp-smoke-test.sql`）：

| 用例 | 输入 | 期望 | 实测 |
|---|---|---|---|
| **10a** | 4 条，用时全同且 = 500 | 标记为可疑 | `suspicious_rows_should_be_4` = **4** ✅ |
| **10b** | 5 条，用时全同且 = 3000（均摊值）| **不**标记 | `suspicious_must_still_be_4` = **4**（不增）✅ |

### ✅ 一并更正方案 §2.1.1 的一处实现要点

原文说接龙要「`recordItem` 支持 `opts.skipXp`」。实装时确认**服务端早已处理**：
`xp_of()` 里写着 `when p_mode = 'chain' then 0` —— 接龙每步的 `answer` 事件计 **0 XP**，
但**照常计入题数与时长**。

⇒ 客户端**不需要 `skipXp`**，每步照常上报即可。
**反倒是若用 `skipXp` 把事件整个跳过，会断掉打卡题数与当日时长** —— 与初衷相反。

**接龙的完成结算**：走完整条线后发一条 `kind='chain_complete'`
（带 `session_id` / `chain_mode` / `chain_kind`），服务端按路线与作答方式查表给分，
并靠 `unique(user_id, session_id)` 的部分唯一索引保证**一局只结算一次**。
中途放弃（`giveUp`）不发 ⇒ **自然一分不得，无需额外判罚**。

### 接入覆盖清单（11 个调用点 → 8 个文件）

| 文件 | 题型 | 用时 |
|---|---|---|
| `DefinitionPractice.tsx` | definition | 每段 `lap()` 并入 `carryMs`（判分等待不计；见上）|
| `MultipleChoice.tsx` / `WrongPractice.tsx` | choice | 逐题 `lap` |
| `Spelling.tsx` | spelling | 逐题 `lap`（含「不会，看答案」分支）|
| `Matching.tsx` | matching | 每对 `lap`（判定在 `useEffect` 里）|
| `LogicChain.tsx` | chain | 每步 `lap`，另发 `chain_complete` |
| `Cloze.tsx` / `Crossword.tsx` / `QuizTaker.tsx` / `CorrectionPractice.tsx` | 批量 | 整段 `lap` 后均摊 |

---

## 六之七、第④步的范围与一处缺口裁决（2026-09-23 与定义题会话）

### 裁决 1：教师端用 **staff-only RPC**，不建视图（口径唯一优先）

**问题**：`get_daily_study(p_user_id)` 是**单用户**函数
（`v_target := coalesce(p_user_id, v_uid)`），而 `TeacherCheckPanel` 需要**一次拿全班**
—— 走现有 RPC 就是 20 人 20 次往返。

**结论**：新增 **`get_daily_study_all(p_from, p_to)`**，仅 `teacher` / `developer` 可调，
一次返回全班；**不建视图**。

**理由**：视图会把「基线 + 事件 + 补签」的合并口径**复制成第二份实现**，
而学生端走的是 RPC ⇒ **两套口径迟早分叉**。
教师端的读者是 React 面板（本来就走 RPC），视图的便利只在「SQL 直查 / 导出」；
真有那种需求时再加，且**那时也应基于同一个内部函数**。

**实现约束（采纳定义题会话提出的要点）**：

- 三个来源 `union all` 后**按 `day_key` 求和**（基线 + 事件），**不取 max**；
- ⚠ **`correct` 类型必须统一**：`checkin_baselines.correct` 是 `numeric(6,2)`，
  而 `xp_events` 侧是 `count(*)`（整数）⇒ 合并时统一到 `numeric`；
- **`p_user_id` 的语义保持不变**（`null` ⇒ 读自己），学生端在用；
  全班版本**另开函数**，不改现有行为。

### 裁决 2：⚠ **补签必须一并服务端化**（原方案漏了）

**问题**（定义题会话提出，成立）：教师月度核验的「达标天数」**必须把补签算进去**
（`TeacherCheckPanel.summarizeMonth` 专门处理，并注明「补签日可能完全没有 `study` 记录」）。
但服务端只有 `xp_events`（纯练习事件）与 `checkin_baselines`（历史），
**新的补签没有落点**。

**为什么是实质问题**：补签直接改变「达标天数」⇒ 若不服务端化，
第④步之后**仍有一条本地可改的路径影响全勤奖** —— 而全勤奖是**实物出口**。

**⚠ 更要紧的一点**：本地 `makeup` 只是一个 `{ dayKey: true }` 映射（`types.ts:75`），
**插一条就多一天达标**，比伪造 `study` 记录**容易得多**。

**一处措辞更正**：定义题会话提到「补签卡余额 `cards`」—— 现库中**没有** `cards` 字段，
`CheckInState` 只有 `study / makeup / earnedMakeupWeeks / bestStreak`（`types.ts:73-78`）。
当前机制是「**本周练习 ≥100 题且正确率 ≥80% ⇒ 自动获得一次补签机会**」（`canEarnMakeup`）；
「补签卡」是《练级与奖励体系方案》里**尚未实装**的等级奖励。
**但这不影响结论** —— 无论形态是「每周机会」还是「卡」，补签都必须服务端化。

**方案**：

**① 新表 `checkin_makeups`**

```sql
create table if not exists public.checkin_makeups (
  user_id    uuid        not null,
  day_key    date        not null,      -- 被补签的那一天
  week_start date        not null,      -- 该天所在周的周一
  created_at timestamptz not null default now(),
  primary key (user_id, day_key),
  unique (user_id, week_start)          -- 一周只能补一次（替代 earnedMakeupWeeks）
);
```

**② RPC `apply_makeup(p_day_key date)`**（`security definer`，**服务端校验**）

把 `applyMakeup` 的两道判断搬到服务端，**客户端不再自行判定**：

1. `p_day_key` **早于今天**，且落在**本周**内（沿用「只补本周漏签日」的现有规则）；
2. 该天**尚未达标**（由 `xp_events` 现算 + 不在 `checkin_makeups`）；
3. 本周**尚未补过**（`unique(user_id, week_start)`）；
4. 本周练习 **≥100 题且正确率 ≥80%**（由 `xp_events` 现算，即 `canEarnMakeup` 的服务端口径）。

**③ `get_daily_study` 扩展为合并三源**：
`checkin_baselines ∪ xp_events ∪ checkin_makeups`，返回的每天带 `makeup` 标记
（教师端日历格的「补签 / 已达标 / 有练习未达标 / 无记录」四态要用）。

**④ `bestStreak` 与 `earnedMakeupWeeks` 不必落盘**：

- `earnedMakeupWeeks` ≡ `checkin_makeups` 里出现过的 `week_start`；
- `bestStreak` 由 `get_daily_study` 的日序列现算。

⇒ 与「**总量现算、永不落盘**」的既有哲学一致。

### 裁决 3：若第④步不做补签，必须显式记录这条分裂

**默认不忽略。** 若最终决定暂不做，方案里要写明：

> **练习服务端权威；补签仍本地权威。**
> ⇒ 全勤奖的核验因此仍存在一条客户端可篡改的路径。

**但建议做** —— 它本来就落在第④步范围内（「打卡切服务端」天然包含「达标天数的判定」），
把它拆出去只会让口径变成两半。

### 第④步的完整范围（据此更新）

| # | 事项 | 状态 |
|---|---|---|
| 1 | 导入 `checkin-xp-audit-2026-09-21.json` → `checkin_baselines` | ⏳ 表已建，待导入 |
| 2 | `checkin_makeups` 表 + `apply_makeup()` RPC | ⏳ 待做（本裁决新增） |
| 3 | `get_daily_study` 合并三源（基线 / 事件 / 补签） | ⏳ 待做 |
| 4 | `get_daily_study_all()`（staff 全班） | ⏳ 待做（本裁决新增） |
| 5 | 打卡页与等级 UI 改读服务端 | ⏳ 待做 |
| 6 | 门槛同步调整（时长 10 → 7 分钟） | ⏳ 待定，见 §8 |
| 7 | **与定义题侧「摘掉 `grading` 计时」同批** | ⚠ 顺序约束，不可提前 |
| 8 | 双跑验证期 → 撤掉本地 XP 持久化 | ⏳ 待做 |

---

## 六之八、第④步的日期口径与导入细则（2026-09-23 与定义题会话核实）

### 月度核验口径：**自然月**

- 判据：`day_key` 属于该月（`summarizeMonth` 用 `startsWith('YYYY-MM')`）；
- 达标天 = 该月内 `isDayChecked` 为真（**含补签**）；
- 全勤线 = 当月达标 **≥28 天**；
- ⚠ **核验可回看任意历史月份 ⇒ 基线必须覆盖全部历史，不是最近 N 个月。**

### ⚠ 硬规则：**基线只到「上线日前一天」，上线当天 00:00 起只信事件**

**原因**：新版前端**仍会写本地 `checkin`**（`recordFormalAnswer` 未移除；方案 §五第 8 条
即「保留 `checkin` 作兜底与留痕、不再是权威」）。于是**上线之后，同一天会同时存在
本地 `checkin` 与 `xp_events` 两份记录** —— 若基线含上线当天，服务端合并时就是**双倍**，
全勤天数失真。

**代价**：仅「上线当天用旧版前端练的那部分不计」。
⇒ **建议把上线时点选在当天较早、学生尚未开始练习的时候。**

### 历史补签的导入：**以 `makeup` 自身为准**

**⚠ 审计 JSON 不足以还原补签**：它的 `daily` 字段是
`{ date, questions, seconds, correct, checked }`，**没有 `makeup` 标记**；
`makeupDays` 只是汇总数字，无法得知**具体哪几天**。

**⇒ 直接从生产库取**：

```sql
select user_id, data->'checkin'->'makeup' as makeup
  from public.student_data
 where data->'checkin'->'makeup' not in ('{}'::jsonb, 'null'::jsonb);
```

**实测（2026-09-23）**：全库仅两条非空 ——

| 账号 | `checkin.makeup` |
|---|---|
| `chenzh@dtd-edu.cn`（教师，**导入时排除**）| `{"2026-08-19": true, "2026-09-02": true}` |
| `jinmy@student.dtd-edu.cn` | `{"2026-09-02": true, "2026-09-09": true}` |

⇒ **真实学生 2 条**（均属 `jinmy`）。

⚠ **关键**：`chenzh` 的 `08-19` **早于最早的 `study` 记录（08-20）** ——
说明 `makeup` 里存在**独立于 `study` 的日子**。
⇒ **导入必须以 `makeup` 自身为准，不要用 `study` 的日期范围去框，否则会漏。**

**`week_start` 由被补那天所在周反推**（机制是「只补本周漏签日」⇒ **动作周 == 被补天所在周**，无歧义）：

| 被补日 | 所在周（周一）|
|---|---|
| 2026-08-19（周三）| 2026-08-17 |
| 2026-09-02（周三）| 2026-08-31 |
| 2026-09-09（周三）| 2026-09-07 |

⇒ 两组各落不同周，**不违反 `unique(user_id, week_start)`**（已核）。

### 导入的技术细节

- ⚠ **`dayKey` 是浏览器本地时区字符串，与 `Asia/Shanghai` 一致 ⇒ 原样使用、勿二次换算**，
  否则整体偏移一天。
- ⚠ **`student_data.user_id` 是 `text`（不是 `uuid`）**，而 `checkin_baselines` /
  `checkin_makeups` 用 `uuid` ⇒ 导入时需 `user_id::uuid`。
  实测 **21/21 全部符合 uuid 格式**，转换安全。

### 实测规模（2026-09-23）

| 项 | 值 |
|---|---|
| `checkin.study` 逐日行合计 | **125** |
| 日期范围 | **2026-08-20 ~ 2026-09-22** |
| 按月 | 2026-08：28 行 / 2026-09：97 行 |
| 非空 `makeup` | **2 名学生 + 1 名教师**（共 4 条）|

⇒ **量很小**，导入是一次性轻量操作，**可在导入后立即复算校验**。

⚠ 注意：库里的数据比审计 JSON **新一天**（JSON 导出于 09-21，库到 09-22）
⇒ **导入应以库为准，不要用那份 JSON。**

### ⚠ 导入的时序要求：**幂等 + 上线当天最后跑一次**（2026-09-23 定义题会话补充）

基线源是**活数据** —— 学生在上线前会继续同步 `checkin`。所以两种写法都不安全：

| 写法 | 问题 |
|---|---|
| 只在上线**前一天**导一次 | 之后（直到上线为止）的练习仍 `< 上线日`，会**漏掉** |
| 只在**上线当天**跑、但语句无日期上限 | 上线当天**已被事件记录**的部分会**双计** |

**⇒ 两个条件必须同时满足**：语句自带 `day_key < 上线日` 的硬上限 **+** `upsert` 幂等
**+** 在上线当天跑最后一遍。

```sql
-- 幂等导入（可反复执行，结果一致）。:launch_day 由教师确定的上线日期传入。
insert into public.checkin_baselines (user_id, day_key, questions, seconds, correct)
select s.user_id::uuid,
       kv.key::date,
       coalesce((kv.value->>'questions')::int, 0),
       coalesce((kv.value->'seconds')::int, 0),        -- 注意：integer 字段用 -> 即可
       coalesce((kv.value->>'correct')::numeric, 0)
  from public.student_data s
  cross join lateral jsonb_each(coalesce(s.data->'checkin'->'study', '{}'::jsonb)) kv
 where kv.key < :launch_day                            -- ★ 硬上限：绝不吃到上线当天及以后
   and not exists (
     select 1 from public.user_roles r
      where r.user_id = s.user_id::uuid
        and r.role in ('teacher', 'developer'))        -- ★ 排除教职工与测试号
on conflict (user_id, day_key) do update               -- ★ 幂等：重跑覆盖而非累加
  set questions = excluded.questions,
      seconds   = excluded.seconds,
      correct   = excluded.correct;
```

**实测（2026-09-23，只跑 select 不写入）**：`jsonb_each` 在本库**可用**；
不设排除时 **125 行 / 20 人**，排除 staff 后 **101 行 / 18 人**。
目标表 `checkin_baselines` 当前 **0 行**。

### 排除判据要统一（一处小一致性隐患）

**现状**：`TeacherCheckPanel` 只按 `role = 'developer'` 排除（`TeacherCheckPanel.tsx:189`）。

**实测 `user_roles`（2026-09-23）共 5 条**：

| 账号 | 角色 |
|---|---|
| `test.student@example.com` | `developer` |
| `abc@example.com` | `developer` + `student` |
| `chenzh@dtd-edu.cn` | `developer` + `teacher` |
| *（`jinmy@student.dtd-edu.cn` 等真实学生）* | **无角色记录** |

⇒ **唯一的 `teacher` 同时也是 `developer`**，故「按 `developer`」与「按 `teacher|developer`」
**去重后都是同一批 3 个人，现状完全等价**。

**但判据应当统一**，否则将来出现「只有 `teacher`、没有 `developer`」的账号时会分叉。

- **本方案的导入与 RPC 一律按 `role in ('teacher','developer')`** —— 语义更正确：排除一切教职工；
- **建议 `TeacherCheckPanel` 的排除也收紧为同一判据**（现状等价、不急，
  随第④步的前端改动一起做即可）。

**被补签两人的归属**（导入时对照）：`chenzh` = `developer + teacher` ⇒ **排除**；
`jinmy` = **无角色记录** ⇒ **保留**。

### ⚠⚠ `correct` 口径不一致（不修的话，`apply_makeup` 与三源合并都会算错）

**问题**（2026-09-23 定义题会话发现，成立，已逐条核实）：

**本地的 `DayStudy.correct` 不是「答对数」，而是「score 累加」**：

```ts
// checkin.ts:79,84  —— recordFormalAnswer
const add = typeof score === 'number' ? score : (score ? 1 : 0);
// …correct: cur.correct + add
```

定义题「部分正确」记 **0.5**；`weeklyStats` 用它算出**浮点**正确率，
喂给 `canEarnMakeup` 的 **80%** 门槛（`checkin.ts:123`）。

**而 `get_daily_study` 现在返回的是 `count(*) filter (where kind='answer' and correct)`
—— 布尔版**（`xp_events.correct` 是 `boolean`，定义题 partial 记 `false`、
真正的分值只在 `score = 0.5` 里）。两个后果：

| # | 后果 |
|---|---|
| ① | **补签判定比本地更严**：一周 10 题全 partial ⇒ 本地 **50%**，布尔版 **0%** |
| ② | **三源相加出错**：`checkin_baselines.correct` 是 `numeric(6,2)`（含 0.5），与布尔版 `union all` 后按 `day_key` 求和 ⇒ **两种口径直接相加，数值无意义** |

**裁决：事件侧改用 `sum(score) filter (where kind = 'answer')`**，理由三条：

1. 与本地 `DayStudy.correct` **同口径**（都是 score 累加）⇒ **切服务端时数值不跳变**；
2. 与 `checkin_baselines.correct` **同类型**（`numeric`）⇒ 相加有意义，`numeric(6,2)` 也容得下；
3. `sum(score)` 天然不含 `chain_complete`（它 `score = 1` 但不是「作答」），
   但仍**显式加 `filter (where kind='answer')`** —— 与 `questions` / `ms` 保持**同一个过滤条件**，
   免得日后有人改一处忘一处。

**另外保留一个布尔计数**（新增字段，不替换主字段）—— 教师端要区分
「真答对」与「部分正确」时会用到：

| 字段 | 类型 | 含义 |
|---|---|---|
| `correct` | `numeric` | **`sum(score)`** —— 与本地、基线同口径，**正确率的分子** |
| `correct_full` | `integer` | `count(*) filter (where kind='answer' and correct)` —— 全对题数 |

⚠ **`apply_makeup` 的「≥100 题且 ≥80% 正确率」必须用同一口径**（分子取 `sum(score)`）。
否则会出现「本地判得过、服务端判不过」，学生的体感就是
**「我明明够条件了，却补不了签」** —— 这类不一致最难排查。

### `week_start` 的「周一」必须与 `day_key` 同源（`Asia/Shanghai`）

`day_key` 本身已按 `Asia/Shanghai` 折算（见 `submit_xp_events`），
所以取周一只需在 `day_key` 上直接截断：

```sql
date_trunc('week', p_day_key)::date   -- 周一；p_day_key 已是上海日期，不要再做时区转换
```

⚠ **不要**用「服务器当前时间」或带时区转换的写法去推周一：
跨周边界的补签会落到**错误的周**，`unique(user_id, week_start)` 因此可能误拒或误放。

### ⚠ 单位坑：基线的 `seconds` 是「秒」，而事件侧是「毫秒」

设计迁移时发现：**`checkin_baselines.seconds` 存的是「秒」**（来源即本地
`DayStudy.seconds`），而 **`xp_events.elapsed_ms` 与 `get_daily_study()` 返回的 `ms`
都是「毫秒」**。

⇒ 三源合并时**必须** `seconds * 1000`。否则基线的时长会被**低估 1000 倍**，
表现为「历史打卡时长全部趋近 0」，进而**把原本已达标的历史日误判为未达标** ——
而历史日正是`checkin_baselines`唯一的用途，这个错误会让整张表失去意义。

⚠ **同理，基线不含「布尔全对计数」**：`checkin_baselines.correct` 只存了
`numeric` 的 score 累加，没有对等的 `correct_full`。合并时**只能对基线部分记 `NULL`**，
**不要用 `floor(correct)` 近似** —— 有 partial 的日子会算错
（如 `correct = 8.5` 时 `floor` 给 8，而真实全对数可能是 7）。

⇒ **`correct_full` 是「仅事件侧可得」的指标**；教师端若展示它，需注明基线部分不含。

### 上线判据清单（教师定日期时逐条核对）

| # | 判据 |
|---|---|
| 1 | 上线时点选在**当天较早**、学生尚未开始练习（把「旧版练习不计」的损失压到最小）|
| 2 | **无并行封包**（避免版本号冲突）|
| 3 | **版本号未被用过**（比对 APK 产物与 `build.gradle` 的修改时间，见 `project-memory`）|
| 4 | 走完整 **`npm run ship`**（改动落在 `app/src/**`，不能只 `wrangler deploy`）|
| 5 | 上线前跑**最后一遍导入**（幂等，可重复执行）|
| 6 | 上线后验证：① 线上 `assets/index-*.js` 出现新特征串；② `xp_events` **开始出现新行** |

### ⚠ 该库缺失的函数（环境坑）

`jsonb_object_length(jsonb)` **不存在**（尽管 `version()` 报 PostgreSQL 15.8，
实为 **AnalyticDB for PostgreSQL 兼容版**）—— 需用
`(select count(*) from jsonb_object_keys(x))` 替代。

其余常用函数（`jsonb_to_recordset` / `jsonb_array_elements` / `jsonb_object_keys` /
`jsonb_agg` / `jsonb_build_object` / `jsonb_pretty`）**均可用**，
**现有 XP RPC 未受影响**（冒烟测试 15 项全通过即为证）。

---

## 七、未决事项

**⇒ 四组问题已全部裁决完毕，无遗留待定项。**
以下改为**保留的触发条件** —— 将来条件变化时需要回头重评：

| # | 触发条件 | 要重评什么 |
|---|---|---|
| 1 | **教师奖励单价提高到 >50** | §5：是否仍「计入月度排名」（现成立的前提是奖励量级 <2%） |
| 2 | **上线后达标率明显偏低** | §8：是否调整门槛 —— ⚠ **只有真实分布能作依据**，不要用不足 3 人的样本调参 |
| 3 | **出现「学生互相帮刷」** | 是否升级校验强度（心跳、服务端下发题目等） |
| 4 | **学生反馈「不愿做定义题」** | §六之二观察项：是否上调 10 / 5 / 2 |

### 已裁决（不再待议）

| 项 | 结论 |
|---|---|
| **路径** | **直接 C 档** —— 零迁移，**不需要 `xp_baselines`** |
| **打卡门槛** | **完全不动**（10 分钟 **且** 20 题；**不加单题上限**） |
| **补报窗口** | **72 小时 + 不落在已结算月份**（概念降级为「本地队列 + 失败重试」） |
| **`XP_FOR_DISPUTE`** | **保留 20**（允许区间 15~20） |
| **落地方式** | **鉴权可见**（不分分支、不新开 Worker） |
| **定义题接入** | **纳入事件流；XP 只从事件流算**，不与 `definition_attempts` 双算 |
| **心跳事件** | **不做**（成本高，且新口径更贴近真实投入） |
