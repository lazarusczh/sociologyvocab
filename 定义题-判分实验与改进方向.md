# 定义题 · 判分质量实验与改进方向

> 2026-09-20。触发问题：用户实测 `Precariat` 的要素「低工作保障」（原文 `low job security`），
> 学生写 **`low-security jobs`** 被判**未覆盖** —— 语义等价却被误杀。
> 用户质疑：「容错词序变化」听着像是**归一化匹配**，而不是语义理解；到底是模型能力不行，还是 prompt 该下功夫？
>
> 实验脚本：`app/scripts/definition-grading-lab.py`（案例集内置在脚本里，可随时回归）

---

## 一、先把问题定性：这是「识别」问题还是「评分粒度」问题

实验刻意把任务**拆到单要素级**（比端到端更容易看清能力边界），用 16 个人工标注案例：

| 类型 | 条数 | 例 |
|---|---|---|
| 同义改写 / 词序复合词化 | 8 | `low job security` ← `low-security jobs`；`low pay` ← `badly paid` |
| 跨语言（中文作答） | 2 | 要素「低工作保障」← 「工作不稳定、随时可能失业」 |
| **不应覆盖**：方向相反 | 2 | 要素「低工作保障」← `They enjoy high job security` |
| **不应覆盖**：无关 | 2 | 要素「科技危害儿童」← `Children spend more time outdoors` |
| **不应覆盖**：只有上位词 | 1 | 要素含"边缘化机制" ← `A new type of masculinity.` |

四种判分策略（同一批案例、同一批模型）：

- **A baseline**：现状口径 —— 给 0 / 0.5 / 1 连续覆盖度（≥0.75 视为覆盖）
- **B binary**：改成二值问答「是否表达了该含义」并要求**引用对应片段**
- **C paraphrase**：先让模型自己列出 5 种**等价表述**，再判断学生答案是否命中
- **D restate**：先**无评价地重述**学生答案，再据此判断

## 二、结果：prompt 不是瓶颈，模型差异才是

| 模型（通道） | 策略 A | 策略 B | 策略 C | 策略 D | 误杀 | 放水 |
|---|---|---|---|---|---|---|
| **Qwen3.5-122B**（魔搭，本地直连） | **16/16** | **16/16** | **16/16** | **16/16** | 0 | 0 |
| **Agnes 2.5-flash**（本地直连） | 13/16 | — | — | — | 3 | 0 |
| nemotron-super-120b（OpenRouter） | 当日额度用尽，**待补测** | — | — | — | — | — |

**三条结论**：

1. **任务对合格大模型不难**：Qwen3.5 **连"容错规则"都没写的策略 A 也 100%** ——
   包括用户报的那个 `low-security jobs` 案例。
   → 所以「容错词序变化」这类规则**不是关键**；把它写进 prompt 只是"给弱模型加的拐杖"。
2. **四种策略在强模型上无差异**（全 100%），说明**策略改造的收益上限很低**；
   它们更可能只在弱模型上起作用 —— 这需要 nemotron 的对照数据来确认。
3. **Agnes 的 3 个误杀不属于"识别能力"问题，而是"评分粒度"问题**：

```
要素：因长期失业、家庭主要养家者角色逆转而被边缘化的男性气质
答案：Men who feel pushed to the margins because they have lost their role as the main earner.
判分：coverage 0.5 ——「提到了养家者角色丧失和边缘化，但未提及长期失业」   ← 逐子点核对

要素：累积的金钱及其他财产
答案：房屋、汽车、珠宝等积累起来的财富。          ← 中文作答
判分：coverage 0.5 ——「提到积累和财产，但未提金钱」                      ← 逐子点核对
```

模型把**一条要素内部的多个子点**当成"都要答到"的清单；学生答出实质、少列一个并列原因就掉到 0.5，
而 0.5 会直接打断 `reqAllOk`（主干必须 ≥0.75）→ 整题判 `partial`。

## 三、改进方向（按证据强度排序）

### 方向 1：拆开"打包要素"（**证据最强、纯数据、零运行成本**）

一条要素里塞了两个以上子点时（`A、B 和 C` / `因 X 和 Y 导致 Z`），模型倾向于逐子点核对，
造成系统性过严。修法是**把要素拆细**或**显式标注"命中要点即可"**。

可脚本化检测的候选特征：要素文本里出现 `、`／`和`／`及`／`such as`／`i.e.`／多个逗号分句，
或长度超过 ~25 字。→ 先产出一份「打包要素清单」，人工过一遍再拆。

### 方向 2：明确 0.5 的边界（**低成本、防过严**）

提示词里把 0.5 定义收窄为「**方向或程度明显不对**」，并明确：

> 学生答出要素的实质、只是**少列了其中一个并列子点**（如只说了"失去养家者角色"、
> 没提"长期失业"），**不算只沾到一部分，应给 1.0**。

这条正是 Agnes 那 3 个误杀的直接解药。

### 方向 3：nemotron 推理 —— **已实测，结论是「判分必须保持关推理」**（2026-09-21 更新）

**历史（用户补充）**：关推理**不是**"推理无用"，而是当年做 AI 问答时 nemotron **思考溢出**
（思考混进回答）时的绕行方案。OpenRouter 后来提供了 `reasoning.exclude`
（[文档](https://openrouter.ai/docs/use-cases/reasoning-tokens)）：

```json
"reasoning": { "enabled": true, "exclude": true }
```

`exclude: true` = **照常推理但不返回思考内容**（官方标注"所有模型支持"），正是当年想要的隔离。

**实测（16 个人工标注案例 × 各 3 轮）**：

| 配置 | 轮 1 | 轮 2 | 轮 3 | 误杀特征 |
|---|---|---|---|---|
| **关推理**（现状） | **16/16** | **16/16** | **16/16** | — |
| 开推理 + `exclude` | 14/16 | 15/16 | 14/16 | 「提到养家者角色逆转，**未提长期失业**」→ 0.5 |

**结论：对判分任务，关推理反而更准，且差异可重复。**
推理让模型更严格、更爱**逐子点核对**，而本任务最需要的恰恰是「宽容的语义等价判断」——
这与 Agnes 的误杀模式**完全一致**（见方向 1）。

据此定案：

- **判分（`/app-api/ai/complete`）保持关推理**，并在 `worker/ai/text.ts` 注释里写明这是**实测结论**
  （防止后人"顺手打开"）；关推理本身也能规避当年的思考溢出；
- **子站问答（`/skill-api/ask`）改用 `exclude`**：长答案、需要深度推理，且不存在"逐字核对"的失分场景；
  前端 `ssePiece` 只取 `content`、不读 `reasoning_content`，是第二道保险；
- 配套：`msAsk` 默认 `maxTokens` 1500 → **3600**（推理与可见输出共享预算）；
  `completeText` 把**空文本按失败处理**继续降级（防"推理吃光预算 → 空内容"被当成成功）。

### 方向 4：判分模型的选择与仲裁（**结构性问题**）

| 选项 | 优点 | 代价 |
|---|---|---|
| 换 Qwen3.5-122B（魔搭） | 实测 100% | 烧魔粒（目前约定留给问答）|
| OpenRouter 上找其它**推理型**免费档 | 不占魔粒 | 需实测；免费档限流 |
| **双档仲裁**：nemotron 判 `partial` 时用第二模型复核 | 只对边界情况多调一次，额度可控 | 实现稍复杂 |

> ⚠️ **Agnes 不能作为线上判分档**：从 Cloudflare 出口直连会被 WAF 拒（1015，早已实测过），
> 它只能用于**本地脚本对照**（本次实验即如此）。因此上表不含 Agnes。

### 方向 5：策略 B/C/D（**低优先**）

在强模型上无差异；若 nemotron 开推理后仍有残留误杀，再把 **B（二值 + 引用片段）** 作为默认形态 ——
它输出的 `quote` 字段还便于教师复核与日志分析。

### 方向 6：判分日志 + few-shot 闭环（**长期**）

`definition_attempts` 已在记录 `verdict / coverage / reason`。待积累到一定量（尤其教师复核过的样本），
把它们作为 few-shot 放进提示词，或用冻结样本做上线前回归。

## 四、复现与回归

```powershell
cd app
python scripts/definition-grading-lab.py --strategy A,B,C,D --provider ms     # 魔搭（当前最可靠）
python scripts/definition-grading-lab.py --strategy A,B --provider openrouter # 额度允许时
```

- 案例集 = 脚本内 `CASES`（16 条，人工标注 `expect`）；
- 指标 = 逐策略准确率 + **误杀/放水**分列（教学上"误杀"比"放水"更伤学生）；
- **建议**：把这个脚本当作判分改动的**上线前回归**——任何 prompt / 模型 / 要素调整都先跑一遍。

---

## 五、实验的局限与下一步（重要）

**本次实验没有复现用户报的那次漏判**：关推理的 nemotron 在三轮里，对含 `low-security jobs` 的案例
**全部判对**。这说明线上漏判的成因**不在模型配置、也不在单要素识别能力**，而更可能在**线上条件**：

| | 本次实验 | 线上真实条件 |
|---|---|---|
| 要素数 | 1 个 | **4–5 个**（含 ★/· 标注） |
| 附带信息 | 仅要素与原文 | **各来源英文原文 + 大段判定规则** |
| prompt 规模 | ~200 字 | **~1500 字** |

前者可能让模型注意力分散，并诱发"逐条打勾"的核对模式 —— 而方向 1 已经证明，
**"逐子点核对"正是误杀（把 1.0 压成 0.5）的主要机制**。

**下一步（待做）**：给实验台加一个「贴近线上」的模式 —— 从 `definition-keypoints.json` 取真实条目，
拼**与线上完全一致**的 prompt，答案针对其中一个要素做同义改写，专门观察多要素场景下的漏判。

> ⚠️ 在补上这个实验之前，"**用户案例仍未解释**"这一点要一直挂着：
> 不能用单要素实验的 100% 去证明线上判分没问题。

---

## 六、短作答退化：用户案例的复现与解法（2026-09-21 补）

第五节列出的"下一步"已做，并**成功复现了用户报的漏判**，机制也清楚了。

### 6.1 长作答没问题，短作答才出事

用「覆盖全部要素的完整长句」（模型生成的学生风格改写）测**多要素**场景：

| 判分形态 | 要素级 | 题级 |
|---|---|---|
| M0 线上现状 | 44/44 | **10/10** |
| M1 强化「独立判断」 | 44/44 | 10/10 |
| M2 先重述再判 | 44/44 | 10/10 |
| M3 拆成单要素逐条判 | 40/44 ✗ | 6/10 ✗ |

**多要素 + 长作答下判分完全没问题，"逐条确认"也没有发生**；
而且**拆成单要素反而更差**（缺上下文、更容易抠字眼）—— 这条路不要走。

> 顺带修掉实验台一个 bug：无 `example` 要素时 `ex_need` 应为 **0**（线上 `ai.ts` 是对的），
> 首版写成 1，造成一批"全覆盖却判 partial"的假阳性；复算时抓到，未据此误判线上。

### 6.2 换成短作答（贴近学生真实写法）就崩了

构造 7 个「只写一个要点的极简作答」（10~20 词、同义改写、不照抄原文），用同一个多要素 prompt 判分：

| 判分形态 | 命中率 |
|---|---|
| J0 线上现状 | **71%**（另一轮 57%，**不稳定**） |
| J1 强化「同义/上下位词算覆盖」+ 给示例 | 86% |
| J2 先重述学生答案再判 | 86% |
| **J3 = 先重述 + 同义规则 + 示例** | **100%** |

典型失败：

```
要素「低工资」〔原文 low pay〕        ← 学生写 "Low wages"          → J0 判 0.0 ✗（wages = pay，纯同义词）
要素「低工作保障」〔low job security〕 ← "Low job security"        → J0 判 1.0 ✓（照抄原文措辞反而判对）
要素「历史时期为 2010-2012 年」      ← "between 2010 and 2012."   → J0 判 0.0 ✗
```

**照抄原文的判对、同义替换的判错** —— 这正是"词面匹配"的指纹：
短作答上下文不足，模型退化成"在答案里找与要素/原文相同或相近的词打勾"。
之前的长作答实验全对，是因为上下文够丰富，模型自然做了语义判断。

### 6.3 解法：先重述 + 同义规则（已上线 1.8.5）

线上判分提示词（`app/src/lib/ai.ts`）改为：

1. **先重述**：第一步用 1~3 条列出「学生这段话表达了哪些含义」（只描述学生意思，不许改写成要素措辞）；
   第二步再与要素逐条对照 —— 强迫模型**先理解、后判断**；
2. **同义规则带具体示例**：明确"同义词与上下位词一律算覆盖"，并把
   `low wages = low pay`、`working class = proletariat`、`low-security jobs = low job security` 写成正面示例；
3. 明确"不要在答案里搜与要素/原文相同或相近的词去打勾"；
4. `maxTokens` 600 → 1000（多输出一个 `restate` 字段）。

**上线前回归**：

| 场景 | 现状 M0 | 新规则 M4 |
|---|---|---|
| 长作答（44 要素 / 10 题） | 44/44、10/10 | **44/44、10/10**（无退化）|
| 短作答（7 探针） | 71% | **100%** |

上线：**1.8.5（versionCode 58）**。

### 6.4 仍待确认

- 请用真实账号**复测 `Precariat`**（就写 "low-security jobs" 那种短句），确认线上不再漏判；
- `restate` 字段目前只参与判分推理、**未在界面展示** —— 后续可考虑显示给教师（复核判分依据时很有用）。

---

## 七、判分质量是「模型能力」问题（2026-09-21 补）

用户第二次实测（术语 `Impersonal`）把问题推到了更根本的层面：

```
要素 2「认为男性同情心较少」〔believes men have less sympathy〕
学生写 "men are less able to empathize with others"        → 判 0（未识别）
同时**误判**学生答了 "therefore more objective"（学生根本没写）
```

### 7.1 两个方向相反的错误，成因不同

| 现象 | 成因 | 状态 |
|---|---|---|
| **假阳性**（识别了没写的 "more objective"） | `source_defs` 参考原文里恰好含这句 → 模型把它当成了学生写的内容 | ✅ 已修：prompt 明令「判定依据只有学生答案本身，绝不能把参考原文当成学生说过的话」|
| **漏判**（`empathize ≈ sympathy` 未识别） | **模型能力**（见 7.3） | ✅ 已通过换模型解决 |

### 7.2 数据缺陷：要素英文的「跨要素夹带」

`Impersonal` 的数据长这样：

```
要素 2 [example ] 认为男性同情心较少  en='believes men have less sympathy AND are therefore more objective'  ← 夹带了要素3
要素 3 [required] 因此更客观         en=None                                                              ← 主干却没有英文
```

于是学生只答「同情心少」时，模型按"整句都要答到"判 0。全池审计：**94 处跨要素重叠** + **46 个 required 要素缺英文**。

修法：重跑 `definition-align-en.py`，提示词新增「每个要素只摘它自己那一部分；同一句里按 and / therefore 切分」
（并给了 `believes men have less sympathy and are therefore more objective` 这个切分示例）。
结果：required 缺英文 **46 → 30**，`Impersonal` 变为 `要素2 = believes men has less sympathy`、
`要素3 = are therefore more objective` ✅

### 7.3 关键：同 prompt 下模型间差异巨大

即便要素英文已切干净、prompt 已明确"近义词一律算覆盖"，`nemotron-super` 仍判 0。
于是用**同一 prompt、同一数据**横向测模型：

| 模型 | `less able to empathize with others` | |
|---|---|---|
| **nemotron-super-120b**（原判分模型） | `[0, 0.0~0.5, 0]` | ✗ 不达标 |
| **nemotron-ultra-550b** | `[0, 1.0, 0]` | ✅ |
| **qwen3.8-27b** | `[0, 1.0, 0]` | ✅ |
| Qwen3.5-122B（魔搭，对照） | `[0, 1.0, 0]` | ✅ |
| Agnes-2.5-flash（本地对照） | `[0, 1.0, 0]` | ✅ |

**这是模型能力差异，不是 prompt 措辞问题** —— 用户最初的判断成立：
`nemotron-super-120b` 在细粒度同义判断上明显偏弱，而同类任务上 ultra / qwen 都判得对。

**处置**：判分模型换成同门 **`nvidia/nemotron-3-ultra-550b-a55b:free`**
（`app/worker/ai/text.ts` 的 `OR_MODEL`），降级链不变，上线 **1.8.6（versionCode 59）**。

### 7.4 关于「剔除中英转译」

用户诉求：判分应直接读学生英文答案的语义，不应经由中文转译。本轮已做的：

- **判分要素行改为英文在前、作为判定依据**，中文降为「仅供参考」注释
  （无英文的 30 个 required 要素退回中文并显式标注）；
- 数据侧把要素英文的跨要素夹带清理掉（7.2）。

**仍未消除的**：30 个 required 要素暂无英文表述（多为原文中确实没有对应表述的引申类要求）→ 待人工或再跑一轮补齐。

### 7.5 待补（额度恢复后）

- **ultra 的完整回归**：16 单要素案例 + 短作答 7 探针 + 长作答 10 题。
  今日 OpenRouter 免费额度已耗尽（429），现有证据仅为单案例对照 + 同门升级的合理性；
- 若 ultra 也限流严重，备选 `qwen/qwen3.8-27b:free`（同批实测同样判对）；
- 可考虑把 `restate` 字段展示给教师，便于复核判分依据。

### 7.6 Agnes vs ultra 判分对照（2026-09-21）

同一脚本、同抽样（`definition-grading-selftest.py --n 12 --seed 11`，33 样本），两模型各跑一轮：

| 指标 | **Agnes-2.5-flash**（本地直连 .cn） | nemotron-3-ultra-550b（OpenRouter `:free`） |
|---|---|---|
| 完整跑通 | **33/33** | **仅 9/33**（其余全部 429） |
| 严格一致 | **29/33 = 88%** | 样本不足，无意义 |
| 防误杀（full → 非 wrong） | **12/12** | — |
| 防放水（wrong → wrong） | 11/12 | — |
| 档位精度（partial → partial） | **9/9** | — |
| 格式遵循 | **2/33 PARSE_FAIL**（未输出合法 JSON） | — |

**ultra 跑不完的原因**：`429 Rate limit exceeded: free-models-per-day-high-balance`
（OpenRouter 免费池上限 `X-RateLimit-Limit: 1000`，**当日已耗尽**；本轮之前也曾在 09-21 出现过一次）。

#### 7.6.1 结论：判分主力的选择应以「额度」为首要约束

1. **质量上无法定论**：ultra 的完整回归**两次都没拿到**（额度总是先耗尽）。
   现有唯一单案例证据（`less able to empathize with others`）中，ultra 与 Agnes **打平**（都判 1.0）。
2. **可用性上 Agnes 压倒性占优**：OpenRouter 免费池 1000 次/日，而判分是**高频调用**
   （含同档重试、每个术语多要素），**必然打爆** —— 今天已实证。
   好在降级链（ultra → agnes → 魔搭）会自动兜住：线上表现是"ultra 先 429 失败一次、立刻落到 agnes"，
   **不会导致判分失败，但每次多一次失败往返的延迟**。
3. **Agnes 的 88% 出自「旧 prompt」**：它的两个判偏（`Manipulation`、`Culture`）都是
   "要素子点核对"型误判 —— `Culture` 甚至 `reason` 写着"答案与核心要素完全一致，意思表达到位"
   却给了 `partial`（即 coverage 里被压到 0.5）。而线上 prompt 已针对这类倾向收窄 0.5 的定义
   （"少列一个并列子点不算只沾到一部分"）→ **新 prompt 下预期更好，待复测**。
4. **Agnes 的真实弱点在「格式遵循」**：2/33（6%）未输出合法 JSON。判分依赖 JSON 解析，
   这会触发该次失败（线上有重试 + 降级链兜底，但多一次往返）。
   **对策**：给 Agnes 的判分调用加 `response_format: {"type":"json_object"}`（OpenAI 兼容接口），
   或把 `parseJsonLoose` 再放宽。

#### 7.6.2 定案（教师 2026-09-21）

> **判分口径：在「次数充足」的前提下，质量优先；OpenRouter 与 Agnes 是几乎并列的两条免费源。**

因此：

- **判分链保持 `ultra → agnes → 魔搭` 不变**（`worker/ai/text.ts`）——
  不为了省掉一次失败往返而把 Agnes 提前；
- **今天的对比不作数**：ultra 只跑完 9/33 样本（其余 429），属**额度干扰**，
  不能作为质量依据 → **待 OpenRouter 额度充裕时重跑同抽样对照**，再判定两者质量；
- **1000 次/日对判分是够用的**：本次耗尽源于同一天内连续多轮实验。
  真出现连续 429 时降级链会落到 Agnes，**判分可用性不受影响**；
- 若要降低"注定失败那次往返"的开销，后续可考虑给 worker 加**档位短时熔断**
  （记住某档近期 429 就跳过），但不是当前优先级。

**Agnes 侧仍待修一点**：2/33 未输出合法 JSON → 加 `response_format: {"type":"json_object"}`
或在 `parseJsonLoose` 放宽，可与明天的对比实验一起做。

#### 7.6.3 附：手动档位新增 Agnes（2026-09-21）

`AI_TIER_CATALOG`（`worker.ts`，经 `GET /skill-api/models` 下发给前端）新增
`{ code: 'agnes', label: 'Agnes', model: 'agnes-2.5-flash' }` —— 子站问答的「模型」下拉
**自动出现该项**（前端是动态渲染，无需改前端代码），便于人工横向对比各档回答质量。

**另：子站问答的 Nemo 档已同步升级（2026-09-21 教师决定）** ——
`worker.ts` 的 `OR_MODEL` 由 `nemotron-3-super-120b` 改为 `nemotron-3-ultra-550b`。
原注释把 super-120b 记为「上游池宽松且稳定 200」，那只是**可用性**观察，不构成质量优势；
同门弱档没有保留理由，故两处（判分档 / 问答档）统一为 ultra。
线上目录实测：`{"code":"nemotron","model":"nvidia/nemotron-3-ultra-550b-a55b:free"}`。
注意 ultra 的上游池稳定性未经长期观察：若子站问答频繁出现 429，优先调整**降级链位置**而非退回弱档。

### 7.7 Agnes 格式问题修复（2026-09-21，已上线并验证）

#### 7.7.1 诊断：不是「不懂 JSON」，而是双重成因

用同一判分 prompt 本地直连 Agnes 做 4 组对照：

| 组 | finish_reason | JSON 合法 | 说明 |
|---|---|---|---|
| 基线 | stop | 是 | 但 JSON 被 ```json 围栏包裹 |
| **+ `response_format: {"type":"json_object"}`** | stop | **是** | **干净 JSON，无围栏、无前言** |
| + `enable_thinking: false` | stop | 是 | 推理仍在（`reasoning_tokens` 140）→ **该参数被 Agnes 忽略** |
| + `response_format`、max_tokens 2400 | stop | 是 | 同样干净 |

两条关键事实：

1. **`response_format` 被 Agnes 支持**（不报 400），能去掉 markdown 围栏；
2. **Agnes 的推理关不掉**：`reasoning_content` 与 `content` **共享 `max_tokens`**，
   实测单次推理占 140~255 token —— 截断风险来自"推理吃掉预算"，**不是**模型不懂 JSON。

#### 7.7.2 为什么不能靠「截断补全」兜底（重要）

`verdictFromKeypoints` 对缺失索引是按 0 计的（`vals[i] ?? 0`）。
若把截断的 JSON 补齐（`[1.0,1.0,` → `[1.0,1.0]`），后面几个要素会被**静默按 0 计** →
学生被判成"答得少"，而且**全程没有任何报错**。这比"解析失败"更糟（失败至少能重试）。

因此采用的策略是**严格长度校验 + 整轮重判**，而不是容错补全。

#### 7.7.3 改动

| 位置 | 改动 |
|---|---|
| `app/worker/ai/text.ts`（agnes 分支） | 加 `body.response_format = { type: 'json_object' }` |
| `app/src/lib/ai.ts`（`gradeDefinition`） | 校验"覆盖度条数 == 要素数，且逐项为有效数字"，不符则**整轮重判一次**，两次都不符才抛错 |
| `app/scripts/definition-grading-selftest.py` | 同步 `response_format`；`max_tokens` 默认 500 → **1200**（给推理留余量） |

#### 7.7.4 验证（同脚本、同抽样规则）

| 指标 | 修前（33 样本） | 修后（17 样本） |
|---|---|---|
| 严格一致 | 88% | **94%** |
| **PARSE_FAIL** | **2/33** | **0/17** |
| 防误杀 | 12/12 | 6/6 |
| 防放水 | 11/12 | **6/6** |
| 档位精度 | 9/9 | 5/5 |

**PARSE_FAIL 归零。** 剩余 1 例判偏（`Manipulation` full → partial）属**已知的"逐子点核对"倾向**，
正是线上 prompt 已针对修正的那一类。

#### 7.7.5 ✅ 脚本 prompt 已与线上同步（2026-09-21 当日完成）

原问题有两层：`definition-grading-selftest.py` 的 `PROMPT` 是旧版（缺"两步法 / ★·标记 / 参考原文 /
0.5 边界"等条款）；**而且档位规则口径也不同** —— 脚本用"全要素平均覆盖度 ≥ 递减门槛"，
线上 `verdictFromKeypoints` 是"**★ 主干要素每一项都必须达标** + · 举例举够数"。两头都会让脚本成绩失真。

已全部同步为线上同构版本：

| 项 | 同步内容 |
|---|---|
| `PROMPT` | 两步法（先 restate 再 coverage）、★/· 标记、参考原文块、0.5 边界条款、同义词条款、listing_only 细则 —— 与 `lib/ai.ts` 逐条对齐 |
| 要素行 | 英文表述在前（判定依据）、中文仅作参考说明；`kind=='example'` 渲染为 `·`，其余为 `★` |
| 参考原文 | 注入 `ref_definition()` 的英文原文，带 `SOURCE_LABEL` 来源标注 |
| 档位规则 | `verdict_from_coverage` → **`verdict_from_keypoints`**（与线上同构） |

同步后立即验证（`--n 4`，11 样本，Agnes）：**full 4/4 全中** —— 旧 prompt 下反复出现的
`Manipulation` 判偏（reason 写着"完整覆盖核心要素"却给 `partial`）**已消失**，
印证该判偏确实是旧 prompt 所致，而非模型能力问题。

⚠️ **prompt 仍是两份真源**（TS 一份、Python 一份）。改线上时必须同步这里，否则必然再次漂移。

> 注：7.7.4 的两轮 Agnes 数据（88% / 94%）与 7.6 的 ultra 数据均为**旧 prompt** 下取得，
> 彼此横向可比，但都**不代表线上水平**。以本节的线上同构版本为准。

#### 7.7.6 部署状态

- worker 侧**已上线**（`f15f117d`）→ **Agnes 的格式修复已对学生生效**；
- 前端 `lib/ai.ts` 的长度校验属**兜底**，需**下次封包**才进 APK 生效（不封包也不影响格式修复本身）。

### 7.8 子站问答 Agnes 长答被截断（2026-09-21 修复）

**现象**（教师反馈）：子站 AI 问答手动选 Agnes 回答「think 档」类问题时，回答读到结尾处被截断。

**原因：不是上下文（前后文）限制，而是输出预算不足。**

- Agnes 的**推理关不掉**（`enable_thinking:false` 被忽略，实测 `reasoning_tokens` 占 140~600）；
- 推理与正文**共享 `max_tokens` 预算**；
- 而 `worker.ts` 里 Agnes 档显式设的是 **1800**（其余档：魔搭 think 2400、魔搭 fast 与 OpenRouter 走默认 3600）
  → 扣掉推理后留给正文仅约 1200 token，遇到要求长论证的提问（2000 字以上）**必然写不完**。

**对照实验**（本地直连，长 context + 长答要求，三档 `max_tokens`）：

| max_tokens | finish_reason | 正文长度 | 结论 |
|---|---|---|---|
| 1800 | stop | ~1590 字 | 该题未写满、未复现；但预算已贴近上限 |
| 3600 | stop | ~1348 字 | 完整 |
| 6000 | stop | ~1171 字 | 完整 |

→ `finish_reason` 都是 `stop`，说明模型只是"按需停笔"；一旦提问要求的篇幅超过「1800 − 推理」的余量，
就会在中途被硬截断。

**修复**：Agnes 的两处调用（自动链 4b + 手动档）`maxTokens` **1800 → 3600**，与其它档一致。
线上已部署（`898ba8de`）。

**另注**：其它档无此问题 —— 魔搭（`enable_thinking:false`）与 OpenRouter（`reasoning:{enabled:false}`）
都能真正关闭推理，输出预算即正文预算。**只有 Agnes 需要为推理额外留额度**，
这也是「Agnes 与 OpenRouter/魔搭行为不同」的关键差异点（判分侧同理，见 §7.7.1）。

### 7.9 教师复核入口已上线（2026-09-21）

**问题**：定义题上线后，教师**看不到学生在普通练习里的实际作答**，因此
「模型判分与教师口径是否一致」无从验证 —— 而这正是它能否作为**日常作业计分**的前提。
（首次核验只能靠 SQL 导出 `definition_attempts`；且当时 31 条作答里 **14 条是教师本人的测试数据**，
真正来自学生的只有 17 条 —— 这个偏差让当时的"真实准确率"估计不够可靠。）

**关键发现：后端早已就绪** —— `definition_attempts` 已含
`review` / `reviewed_by` / `reviewed_at` 三个字段，RLS 也已有
`definition_attempts_read_staff`（teacher/developer 可读全部）与
`definition_attempts_update_staff`（可更新）两条策略。
**因此本次不涉及任何数据库改动，只补了前端入口。**

**新增**：`app/src/components/DefinitionReviewPanel.tsx`，
挂在教师后台「任务管理 → 定义题复核」（与「OCR 阅卷」同类）。

| 功能 | 说明 |
|---|---|
| 逐条展示 | 术语（含中文）· 学生答案原文 · 模型判定 · 逐要素覆盖度 · 模型理由 · 档位与真实模型 id · 判分耗时 · 学生（邮箱掩码）· 时间 |
| 一键判定 | 正确 / 部分正确 / 未答对 + 可选备注；写入 `review`（JSON `{verdict,note}`）/ `reviewed_by` / `reviewed_at`，支持清除 |
| **一致率** | 顶部统计：作答总数 / 已复核 / 与模型一致 / **一致率**（分母只算已复核的） |
| 筛选 | 待复核（默认）/ 已复核 / 全部 · 模型判定 · 学生 · 关键词（术语或答案） |

**用法建议**：先按默认的「待复核」过一遍（当前几十条，约 20 分钟）。
重点核对已识别为「疑似过严」的那几条 —— `precariat`（学生原文明写 low-skill，模型却判"未点出低技能"）、
`quantitative-research-methods`（"collect numerical data"只给 0.5）、`patriarchy`（"male mainstream society"落 wrong）、
`material-deprivation`（"lack of money"判全 0）、`ostracism`（核心已答出仅判 partial）。
若教师判定确实更高，即为**模型误杀**。

**一致率的定位**：它是定义题能否进入「日常作业计分」的**核心判据**。
建议路径：先只计完成度（不计正确率）→ 一致率稳定后再转低权重计分 → 最后正常计分。

> 部署：前端已上线（`e4ff5c31`），**浏览器打开 9699vocab.cn 即可用**；
> APK 需下次封包才带上（教师用浏览器访问不受影响）。

### 7.10 ultra vs Agnes 判定（2026-09-22，额度充裕、同抽样、线上同构 prompt）

脚本：`definition-grading-selftest.py --via direct --n 10`（同一 `--seed 11`，29 个样本）。
**与 7.6 的关键区别**：脚本 prompt 已在 7.7.5 同步为**线上同构版本**，所以本轮数据**代表线上**。

| 指标 | **nemotron-ultra-550b** | **Agnes-2.5-flash** |
|---|---|---|
| 完整跑通 | 29/29 | 29/29 |
| 严格一致 | **26/29 = 90%** | 24/29 = 83% |
| **排除网络失败后** | **26/27 = 96%** | 24/28 = 86% |
| 防误杀（full → 非 wrong） | **10/10** | **10/10** |
| 防放水（wrong → wrong） | 9/10 | 9/10 |
| 档位精度（partial → partial） | 8/9 | 8/9 |

**结论：ultra 更强，但 Agnes 也够格当兜底档。**

- ultra 的 3 个失败中 **2 个是网络问题**（`SSL: UNEXPECTED_EOF_WHILE_READING`，与当天 git push 失败同源），
  并非模型判错 → 有效样本内 **26/27 ≈ 96%**，真实判偏只有 1 个；
- Agnes 的 5 个失败中 1 个 PARSE_FAIL + **3 个真实判偏**：
  `Adolescence wrong→partial`（放水）、`Homogenisation full→partial`（过严）、
  `Homogenisation partial→correct`（放水）—— 注意 **`Homogenisation` 上 Agnes 两个方向都判错，
  而 ultra 两个方向都对**，这是两者差距最清晰的案例（要素语义把握）；
- **两个模型的「防误杀」都是满分**：教学上最关键的"别把答对的学生判成没答"这一条，两者都守住了。

**判分链维持 `ultra → agnes → 魔搭`**（7.6.2 的定案）—— ultra 质量更高故优先，
Agnes 免费且达 86% 可作兜底，符合"次数充足前提下质量优先"的口径。

#### 7.10.1 顺带发现一个数据侧 bug（`Institutional racism`）

**两个模型在同一个 `full` 样本上都判 `partial`** —— 这是数据问题的典型信号。核对
`data/definition-keypoints.json`：

```
1. kind=required  en=The failure of an organisation to provide proper services to individuals due to
   ← 英文在 "due to" 处被截断，后半句（ethnic origin）丢失
2. kind=example   en=Racial prejudice or discrimination that has become established as normal behaviour...
   zh=原因是个人的民族/种族出身
   ← 标成了「举例」，但它其实是定义主干（种族偏见已常态化为制度）；且中英文含义不一致
```

后果：要素1 标为 ★（须达标），要素2 标为 `·`（须举出）—— 而 `full` 样本用的就是参考原文，
原文并不含那个"举例"，于是**这个术语永远判不出 correct**（两个模型都被"结构性地"卡住）。

**待办**：这类问题（英文被截断 / `kind` 标错 / 中英不一致）**很可能不止一个术语**，
建议做一次批量排查：例如筛出 `en` 以介词或 `that` 结尾的要素（疑似截断），
以及 `example` 项却含定义性表述的（疑似该为 `required`）。
