# 移动端系统栏与安全区方案（APK / edge-to-edge）

> 状态：已实施并真机验证（小米 HyperOS，2026-09-11，APK 1.7.6）
> 涉及：Android 原生入口、主站样式、知识库子站（/skill/）、Capacitor 配置

## 一、目标

1. **状态栏**：主站与顶栏同色无缝、顶栏保持原高度；知识库子站由深蓝顶栏自己铺满状态栏区域。
2. **导航栏**：不预留纯色带，页面内容一直铺到屏幕底边，手势条**悬浮在内容之上**。
3. **只有交互控件**才留安全区（否则会被系统栏压住点不到）：问答输入框、抽屉底部按钮、移动导航列表、版本横幅。

## 二、最终策略（全部显式控制，无黑盒）

应用整体 **edge-to-edge**：内容延伸到状态栏与导航栏下方，两处系统栏都由页面内容或 `windowBackground` 自绘。

| 区域 | 主站 | 知识库子站（URL 含 `/skill/`） |
|---|---|---|
| 顶部 | WebView 父容器 padding-top = 状态栏高度 → 顶栏保持原高，状态栏区域由原生 `windowBackground`（surfaceColor）填色 | padding-top = 0 → 深蓝顶栏自绘铺满状态栏；状态栏图标切浅色 |
| 底部 | **一律不处理** → 内容铺到屏幕底边，手势条悬浮在内容之上 | 同左 |

判定与时机（全部在原生侧，**不依赖 JS**）：
- 判定依据：当前 URL 是否包含 `/skill/`
- 执行时机：`onCreate` / `onPostCreate` / `onResume` / `onPageCommitVisible`（整页跳转后）/ `onConfigurationChanged`
- 双保险：`ViewCompat.setOnApplyWindowInsetsListener`（insets 变化时自动应用）+ 主动读取 `ViewCompat.getRootWindowInsets()` 应用一次；高度取真实 insets `getInsets(systemBars | displayCutout).top`

## 三、关键配置与代码位置

| 文件 | 作用 |
|---|---|
| `app/android/app/src/main/java/com/sda/vocabulary/MainActivity.java` | 核心逻辑：edge-to-edge、透明系统栏、关对比度遮罩、按 URL 决定顶部避让；日志 tag = `MainActivity` |
| `app/android/app/src/main/res/values/styles.xml`（+ `values-night/`） | `windowBackground` / `statusBarColor` = `surfaceColor`（浅 `#FFFFFF` / 深 `#161C24`）；`navigationBarColor` 透明、`enforceNavigationBarContrast=false`；图标明暗随主题 |
| `app/android/app/src/main/AndroidManifest.xml` | `android:windowSoftInputMode="adjustResize"`（关掉自动 insets 后保证键盘弹出时输入框能被顶上来） |
| `app/capacitor.config.ts` | `plugins.SystemBars.insetsHandling = 'disable'` —— 关闭 Capacitor 内置的自动安全区处理 |
| `app/index.html`、`app/skill-site/index.html` | `viewport-fit=cover` |
| `app/skill-site/src/index.css` | `html.native` 兜底：顶栏/抽屉/问答布局取 `max(env(safe-area-inset-top, 0px), 24px)` |
| `app/skill-site/src/main.tsx` | 加 `html.native` 类；`StatusBar.setStyle(Dark)`（深蓝顶栏配浅色图标）；`setBackgroundColor('#10243e')` 作为老系统兜底 |
| `app/src/main.tsx` | 加 `html.native` 类；`StatusBar.setStyle` 依系统主题（从子站返回时恢复图标明暗） |
| `app/src/index.css` | `.topbar` 不含顶部安全区（避让由原生完成）；底部不留白 |

## 四、踩过的坑（重点，避免重犯）

1. **Capacitor 8 内置 `SystemBars` 插件会替你做安全区处理**（`Bridge.java` 自动注册）。
   判定：`shouldPassthroughInsets = WebView 版本 >= 140 && 页面声明了 viewport-fit=cover`；
   不满足时它会给 **WebView 的父容器**设 `padding-bottom = 导航栏高度` → 底部出现一条纯色带（`windowBackground`），内容无法延伸。
   → 现在统一 `insetsHandling: 'disable'`，改为完全自绘。
2. **跨 Web/原生的初始化不要放在 JS 里**。曾用 JS 调自定义插件控制避让，调用发生在 bridge 就绪之前被 `catch` 静默吞掉，表现为"主站顶栏顶进状态栏"。→ 改为原生按 URL 判断，和页面加载时机天然同步。
3. **`android.R.dimen/status_bar_height` 在部分 ROM（小米）不可靠** → 必须用真实 `WindowInsets` 的 `bars.top`。
4. **`StatusBar.setBackgroundColor` 在 Android 15+ 已废弃**（强制 edge-to-edge），不能作为主路径，只能当老系统兜底。
5. **"内容留白 vs 手势条悬浮"的取舍**：内容级不留白（内容延伸、手势条悬浮其上），只有可点击控件留安全区。曾误加全局 `body { padding-bottom: env(safe-area-inset-bottom) }`，导致底部一条预留纯色带。
6. 已评估但放弃的方案：`windowOptOutEdgeToEdgeEnforcement`（会同时牺牲子站的顶栏效果）、纯 CSS 偏移（无法让状态栏区域显示顶栏颜色）、`setOverlaysWebView` 动态切换（与原生判定重复且时机不可控）。

## 五、验证清单

- 主站：顶栏高度与原版一致、不与状态栏重叠；状态栏颜色 = 顶栏色；**底部无纯色带**，滚动时内容从手势条下滑过
- 子站：状态栏区域深蓝，与顶栏连成一片；底部同样延伸
- 子站问答：输入框不被手势条/导航栏压住，可正常点击
- 深色模式：两站配色正确，状态栏图标明暗正确
- 子站 → 返回主站：状态栏图标恢复正常（不残留浅色图标）
- 三键导航与手势导航各测一次

## 六、升级 Capacitor / 换机时的注意事项

- 升级 Capacitor 后，**先确认 `SystemBars` 的 `insetsHandling` 选项是否仍存在**（上游源码有 TODO：Cap 9 计划新增 `"full"` 选项），否则"底部纯色带"可能复发。
- 相关上游修复：Chromium safe-area fix（WebView 140+，issue 40699457）、键盘 safa-area fix（144+，issue 457682332）。
- 出现新问题时，先用 `adb logcat -s MainActivity` 看日志：
  - `top inset applied: top=<px> url=<url>` —— 检查高度是否非 0、URL 判定是否正确
  - `top != 0` 但仍重叠 → 布局未消费 padding（改用 inset 消费方式）
  - `top = 0` → 该窗口没有状态栏 inset（ROM 特殊行为，需另取高度来源）
  - 完全没有日志 → 上述生命周期回调未触发（改挂 `onWindowFocusChanged` 或 Bridge 钩子）
