# 定义题 · 主干「丢句型」现状与改动清单

> **第二版（2026-09-20 修订）**。第一版把这个现象叫「总括式主干」，并按中文表象分成 A/B/C 三类，
> **判定方法是错的**（详见附录二）。本版改为以**英文原文**为准重新判定。
>
> 数据源：`app/data/definition-keypoints.json`（池 623 条）· `app/public/vocab-data.json`（英文原文）
> 相关代码：`app/src/lib/ai.ts`（`verdictFromKeypoints`）、`app/scripts/definition-tag-kinds.py`

---

## 一、判分口径与筛选条件

每个术语的踩分点被 `definition-tag-kinds.py` 标成两类：

| kind | 含义 | 判分口径（`verdictFromKeypoints`） |
|---|---|---|
| `required`（★ 主干） | 概念本身的核心含义 | **每一条都要覆盖 ≥0.75**（`reqAllOk`） |
| `example`（· 举例） | `such as / i.e. / e.g.` 式的并列举例 | ≤2 项时答 1 项、≥3 项时答 2 项即可 |

**筛选条件（纯结构）**：`required == 1 且 example >= 2` → 共 **47 条**（占池子 7.4%）。

但**这个结构条件本身不区分两种完全不同的情况**——第一版正是把它们混为一谈：

| | 英文原文形态 | `example` 里装的是 | 判定 |
|---|---|---|---|
| **(a) 真·并列列举型** | `A X ...; featuring / such as / e.g. <枚举>` | 真正的举例 | **主干完整，无问题** |
| **(b) 丢句型** | `A X that / who / on how <限定>` | **被切掉的定语从句内容** | **主干缺信息，需改写** |

## 二、现状：47 条里，11 条是「丢句型」

把每条的**英文原文**与中文主干并列，规律非常整齐：英文都是 `A X that <限定>` 结构，
中文归纳把 `that / who / on how` 引导的限定成分**丢掉了**，还把它标成了 `example`。

### 2.1 丢句型（11 条，需改写主干）

| # | 术语 | 英文原文（**加粗** = 被丢掉的部分） | 现主干 | 建议主干（只补原文里有的内容） |
|---|---|---|---|---|
| 1 | Diabetes | A disease **that is increasingly seen on children**, which is a counter example against the view of march of progress | 糖尿病是一种疾病 | 一种在儿童中日益常见的疾病 |
| 2 | Education | The formal education, **the knowledge learned and experiences encountered in schools** | 正规教育 | 正规教育：在学校中有组织地获得的知识与经历 |
| 3 | Equality | A way of defining human rights **that focus on the equal right in realms such as political beliefs, religious beliefs, voting (suffrage) and all other parts of civil life** | 定义人权的一种方式 | 以"政治信仰、宗教信仰、投票权及公民生活各个方面的平等权"为核心的人权观 |
| 4 | Islamophobia | A type of moral panics; **the public fears for the ethnic minority groups and immigrants that have religious belief of Islam…; sometimes lead to hate speeches and crimes** | 一种道德恐慌 | 针对穆斯林／来自伊斯兰国家的移民的公众恐惧（一种道德恐慌），有时导致仇恨言论与犯罪 |
| 5 | Marginalised masculinity | A new type of masculinity **that men are marginalised in society and their families due to long-term unemployment and a reversal of the major provider role in their families** | 一种新型的男性气质 | 因长期失业、家庭主要养家者角色逆转，而在社会与家庭中被边缘化的男性气质 |
| 6 | 'New Man' / 'New Father' | A new type of men and fathers **who would share the housework with their partners and treat their partners equally and with respect** | 新型男性和父亲 | 愿与伴侣分担家务、平等且尊重地对待伴侣的新型男性与父亲 |
| 7 | "Pick and Mix" | A postmodernist metaphor **on how individual identities are personalised and varied between every individual** | 后现代主义隐喻 | 关于"个体身份是个人化的、且因人而异"的后现代主义隐喻 |
| 8 | Pentecostal Church | A sect of Christianity **that is often shared by African American / Black Carribean ethnic group, contributing to the cultural defence as well as serving as the supportive network for new migrants** | 基督教的一个宗派 | 基督教的一个宗派（常为非裔美国人／加勒比黑人群体信仰，起文化防卫与新移民支持网络作用） |
| 9 | Silent generation | The generation after the Greatest generation in the US **that tends to work within 'the system' and is afraid of being seen as against the authority, leading them to take up traditional family views** | 沉默的一代是美国'最伟大的一代'之后的一代 | 美国"最伟大的一代"之后的一代：倾向在体制内工作、惧怕被视为反权威，因而持传统家庭观 |
| 10 | Multifunctional | **Pre-industrial age extended families** need to fulfill multiple needs of members at the same time, i.e. … | 同时满足成员多种需求 | （前工业时代的大家庭）同时满足成员的多种需求 |
| 11 | Wealth | The accumulated money **and other properties**, such as houses, cars, jewelry, art collections | 累积的金钱 | 累积的金钱及其他财产 |

> **改写原则：只把英文原文中被丢掉的部分补回主干，不得用外部知识替换来源表述。**
> 例如 `Pentecostal Church` 主站原文里并没有"说方言／圣灵体验"的内容，就不能补进去。
>
> 同时要把已并入主干的 `example` 项删掉（否则会把同一点重复计分）。

### 2.2 主干完整（其余 36 条，不动）

这些术语的英文主干本身完整，`example` 是 `such as / i.e. / e.g. / featuring` 之后的真列举：

```
Toxic childhood      : Modern technology changes have harmed children; featuring <6 项>
Gendered curriculum  : The contents of curriculum that are biased towards one gender, e.g. <2 项>
Single-parent family : A family that has only one father or mother and his/her dependent child(ren); <补充>
Accent               : A certain way of pronouncing words ... that shows one's geographical, ethnic or class origin
Entitlement          : A way of defining human rights that focus on the rights to life and living quality, i.e. <5 项>
```

（第一版把其中的 `Cash crop`、`Dark side of family life` 等误判为"主干无信息"——实际上它们的主干
完整保留了英文的限定从句，"举例"是真正的举例。还有 `Horizontally extended family`、`Hidden curriculum`、
`Home factor`、`In-school factor` 等都是完整的主干 + 真列举。）

## 三、改动方案

### 3.1 数据侧（✅ 已实施 2026-09-20）

脚本 `app/scripts/definition-apply-fixes.py --write`：按 2.1 表格改写 11 条主干，
并删除已并入主干的 `example` 项（`Multifunctional`／`Wealth` 只补主干——它们的举例是原文的 `i.e./such as` 枚举，保留）
→ `definition-import.mjs` 生成幂等 SQL → psql upsert。

结果：**11 条全部改写入库**，库内 `active` **622** 条（见附录一）。

> ⚠️ 这 11 条是**人工修正**：重跑 `skill-keypoints.py` / `definition-tag-kinds.py` 会覆盖它们，
> 重跑归纳后需再次执行本脚本。

### 3.2 归纳侧（根因）：修 `definition-tag-kinds.py` 的提示词

该脚本让模型按"**去掉它概念是否成立**"判断 A（主干）/ B（举例）。对 `A X that ...` 这种结构，
模型很自然地把限定从句判成"去掉也成立" → 归 B → 于是从句内容被切进 `example`。
**这是提示词缺陷，新归纳的术语还会复发**，所以要修：

> 明确规则：`that / who / which / on how` 引导的**限定成分属于 A（主干）**；
> 只有 `such as / i.e. / e.g. / including / featuring` 之后的**枚举**才归 B（举例）。

可选：改完提示词后，只对这 11 条重跑一次做验证（量小、额度友好），确认能自动得到正确切分。

### 3.3 撤回：不做"判分放宽"

第一版曾建议给"总括式"加一条替代通过路径（`example` 命中 ≥2 即算达标）——**本版撤回**。
理由：这些主干在英文原文里就是**完整定义的核心句**，学生本就该答出来；举例是附加要求。
放宽会让主干不被要求，等于降低标准。

`ai.ts` 里现有的缓解条款（"若学生举出的例子已经体现了某个 ★ 主干要素，该主干要素可给 0.5 以上"）
**保留不动**——它只影响部分覆盖度的给分，不改变 0.75 的门槛。

### 3.4 判分基准补英文原文（A 兜底，✅ 已实施）

**问题**：判分提示词原本是「中文要素 ↔ 学生英文答案」的**跨语言比对**，
而中文要素经两次转译后信息有损 → 学生答对了英文原文里的内容却可能只判 `partial`；
反之主干笼统时又容易"沾到就满分"。

**实施**：

1. `definition_items` 加列 `source_defs jsonb`（迁移 `db-migration-definition-source-defs.sql`，幂等）；
2. `definition-apply-fixes.py` 顺带写入各来源**英文原文**（云端词库 `main` + 教材 `tb1`/`tb2` + `igcse0495`）——
   **622 / 622 条全部有原文**，无一条缺失；
3. `ai.ts` → `gradeDefinition(term, keypoints, answer, sourceDefs)`：prompt 增加「参考原文（英文）」段落，
   并明确**成档仍只看「核心要素」清单**，原文只用于判断语义等价（避免模型改成"必须复述原文"）；
4. `definition.ts` / `DefinitionPractice.tsx` 传递该字段。

**上线**：**1.8.2（versionCode 55）**，`npm run ship`（build + APK 分发 + worker 部署）。

## 四、验证方法

`definition_attempts` 已在记 `verdict` / `coverage` / `reason`：

- 改写前 vs 改写后，这 11 条的 **`partial` 占比应下降、`correct` 占比应上升**（因为主干现在可以被真正答到）；
- 若某条术语 `wrong` 占比异常高，检查它的主干是否仍有丢失（用英文原文对照）；
- 新归纳的术语上线前，抽检方式固定为：**先看英文原文，再比中文主干**（不要只看中文）。

---

## 附录一：定义模式为何比主站术语数少

**结论：不是"漏抽"，而是出题单位是"术语"，而主站同一术语会有多行。**

| 口径 | 条数 |
|---|---|
| 主站 `type=term` 行数 | 650 |
| 主站**唯一术语**数 | **631** |
| 定义池（同步前 → 同步后） | 631 → **623** |
| 库内 `active` | **622**（623 条 JSON 中有 1 组 slug 冲突 → 唯一 id 622） |

差额来自四处：

1. **16 组重名术语**（同一术语挂在多个 Paper/单元下各存一行）→ 少 19 条：
   3 组 ×3 条（`Fatalism`／`Institutional racism`／`Risk society` 各少 2）+ 13 组 ×2 条（各少 1）。
   按 Paper：Paper 1 少 8、Paper 4 少 8、Paper 3 少 2、Paper 2 少 1。
2. **1 组 slug 冲突**：`Deferred (delayed) gratification` 与 `Deferred / Delayed gratification` 归一后同 id → 少 1 条。
3. **2026-09-20 元数据同步**：8 条孤儿（云端已并入别处／已删）停用 → 631 → 623。
4. **2026-09-20 修正导入**：改名条目 `Cultural factor (deprivation)` → `Cultural factor` 会生成**新 slug 行**，
   旧 slug 行必须单独停用 —— 即「**改名 = 新 id = 旧行残留**」（本次已手工补一条 `active = false`）。

> 这是**预期行为**：定义题以"术语"为出题单位，同一术语不该出两次。

---

## 附录二：修订说明（第一版错在哪）

第一版把 47 条统一叫「总括式主干」，理由是"主干是一句总括、真正的知识点都在 `example` 里"，
进而分出 A（主干无信息，11 条）/ B（总括+举例，16 条判分放宽）/ C（20 条不动）。**这个判定不成立**：

1. **筛子是纯结构的**：`required == 1 且 example >= 2` 不区分"主干完整 + 真列举"与"主干丢句"两种情况，
   却被当成了"总括式"的定义。
2. **归类靠中文表象**："糖尿病是一种疾病"读起来像同义反复 → 判"无信息"。
   但对照英文原文，"A disease **that is increasingly seen on children**" 的信息都在那个从句里，
   **是归纳把它切掉了**，不是概念本身没有内容。
3. **结论方向因此反了**：A 类说"学生不可能踩到"，实际是"主干丢失导致**学生的正确答案被判 partial**"；
   B 类说"学生举例就该过关"，实际是"主干本来就该答出来"。

**正确做法（本版执行）**：判定任何与词条文本有关的问题时，**先取英文原文，再比中文**。
本次 11 条的成员也与第一版不同（第一版误含 `Cash crop`、`Dark side of family life`，
漏掉 `Multifunctional`、`Wealth`、`Education`）。
