# OCR 辅助阅卷（🚧 试点中，本地实验室已跑通，未上线）

> 目的：提升教师手写/纸质答卷的阅卷效率。**不追求自动判分**（对 AI 判分主观论述题的可信度持保留态度）。
> 最后更新：2026-09-13

---

## 〇、决策与进度（2026-09-13）

| 项 | 结论 |
|---|---|
| **主产物形态** | **原文内就地高亮**（不是命中清单）。理由：只看清单只能判断"用没用术语"（AO1），看不出"用得对不对"（AO2）；教师需要上下文。命中清单降级为另存的普通数据分析（`match_report.txt`）。 |
| **识别引擎** | 定档 **魔搭 `Qwen/Qwen3-VL-235B-A22B-Instruct`**（2 魔粒/次，实测 **13 秒/页**）。8B 档虽 6 秒/1 魔粒，但**会丢内容**（实测把 `Meritocracy is one of…` 读成 `Moreover, it is…`，术语整个丢失；`the way of work` 读成 `the unity of male`），故只作快速预览。 |
| **免费本地 OCR** | **已实测排除**：RapidOCR/PP-OCRv5 在同一真迹页上输出 `Eluulonn! system Saes tle Solty…`（786 字符几乎全错），术语/学者命中 **0**。印刷体模型对连笔手写不可用。 |
| **成本量级** | 30 人 × 3 页 = 90 页 × 2 魔粒 = **180 魔粒**，在魔搭每日 ~250 额度内（紧但够）。**不分额度池**（与学生问答共用，时间错开）。 |
| **key 位置** | 线上 = Worker secret `MODELSCOPE_API_KEY`（`wrangler.toml [vars]` 里没有）；本地 = `app/.dev.vars`（已 gitignore）。 |
| **路由与入口** | 进主站教师后台 → 归入 **`/app-api/*`**（**登录即可**，2026-09-15 起；沿用 `ai_gate` 门禁，教师/开发者始终放行）。**主站 AI 基础层（抽 `worker/ai/*` 共享模块）与入口一次性搭建**，当前不动 Worker。 |
| **入口状态** | 已建教师后台第十个 tab「**OCR 阅卷**」（`OcrMarkPanel.tsx`）：**相机拍题（getUserMedia 连拍）/ 选图 / 拖拽批量导入** → 逐页转写 → **原文内就地高亮 + 原图并排**；相机在 Surface+Edge 实测可用（2026-09-14） |
| **上传主体** | **教师统一收卷批量上传**（2026-09-13 定；学生端不做上传入口） |
| **通用词分级** | 按「核心 / 一般」两级：一般词清单在 `app/src/lib/ocr-term-levels.json`（初始 21 条，教师可直接增删）；一般词默认**淡化显示**、可一键收起。只影响阅卷观感，**不参与判分、不进词库发布字段**（与 `answer-aliases.json` 同模式） |
| **上线状态** | ✅ **已上线（2026-09-14）**：网页端 `https://9699vocab.cn` 教师后台可用（当前 **v1.7.17 / versionCode 39**，含录入记录），APK 同版已发飞书。线上 `POST /app-api/ai/transcribe` 已验证生效（无 token 返回 401）；线上/本地 dist/APK 内嵌 `version.json` 一致。**待真实作业试用后定稿** |
| **录入记录** | **已做（2026-09-14）**：识别成功即落库 `public.ocr_pages`（RLS 仅本人可读写，`grant` 只给 authenticated 的增删改查、已收掉 TRUNCATE；**只存文本不存图片**）。面板下方「录入记录」可刷新/载入/删除，每条带备注（如学生姓名）；载回后高亮在前端重算、**不再调用模型**。**导出功能不做**（教师手上有纸质原件） |
| **视图默认** | 默认 **「只看文字」开**（拍照录入时手上有纸质原件）；需要对照时关掉开关显示原图 |

### 管线（已跑通）

```
【前端正式路径】（进教师后台，随主站构建）
app/src/components/OcrMarkPanel.tsx   入口：拍题/选图/批量/高亮对照
app/src/lib/ocrHighlight.ts           容错匹配（TS 移植；四档 + 核心/一般分级）
app/src/lib/ocr-term-levels.json      通用词分级表（教师可编辑）
app/worker/appApi.ts                  /app-api/* 主站路由（教师向）+ /app-api/ai/transcribe
app/worker/ai/{auth,models,vision}.ts 共享鉴权 / 模型目录 / 视觉转写（含 235B→8B 降级）

【实验室脚本】（不部署，用于批量与调参）
app/_ocrlab_vlm.py      图片 → 魔搭视觉模型 → 忠实转写（读 app/.dev.vars 的 key）
app/_ocrlab_match.py    词库零改动的容错匹配（四档）
app/_ocrlab_highlight.py  原图 + 原文并排的 HTML（就地高亮、悬浮定义、可打印）
app/_ocrlab_make_levels.py 生成/重建通用词分级表
app/_ocrlab_ocr_local.py  本地方案对照（已证不可用，留作证据）
```

实测样本（真实手写答卷 1 页）：235B 转写 2065 字符，命中术语 15 个/22 处、学者 3 位/5 处；教师对效果**确认满意**。

---

## 一、功能定位（已明确）

- 学生以 **纸质手写** 作答社会学 A-Level 论述题（写论文卷），交付形态为拍照或扫描 PDF。
- 期望：把字迹转成文本，并在文本上**自动高亮学生使用的术语与学者名**——这些「应用点」正是按要点给分（AO）的关键依据，帮助老师快速扫描一份答卷覆盖了哪些给分点。
- 阅卷仍由教师人工进行（对照原图给原始分 → 现有换算表换算），AI 不参与判分。
- 属于「提升阅卷效率的辅助工具」。

## 二、端到端管线（设想形态）

```
学生答卷（拍照 / 扫描 PDF）
   ↓ 上传（APK 相机 或 网页上传）
   ↓ 若 PDF：前端用 pdfjs-dist 逐页渲染为图片（Capacitor WebView 支持 canvas）
图片 → 云视觉模型转写为结构化文本
   ↓ 存回 试卷成绩（grouper_runs）对应学生的阅卷记录
文本 → 词库（术语 + 学者名）拼写容错匹配 → 自动高亮（含词库定义悬浮卡）
教师阅卷页：原图 + 高亮文本并排 → 人工给分
```

## 三、识别引擎选型与成本

| 方案 | 适合度 | 说明 |
|---|---|---|
| **视觉大模型**（魔搭 Qwen3-VL-235B 为主力；Workers AI 视觉档备选） | **已采用** | 理解布局与语义：分段、涂改后小箭头插入、题号跳到别处作答；转写 essay 效果好；13 秒/页 |
| 魔搭 Qwen3-VL-8B（快速档） | 备选（预览用） | 6 秒/1 魔粒，但实测**会丢内容**（丢术语），不作正式阅卷 |
| Azure Document Intelligence「Read」手写模式 | 备选 | 便宜、结构化，但只吐字不懂语义，涂改/乱序会丢 |
| Tesseract / 本地免费 OCR（RapidOCR 等） | **已实测排除** | 手写效果差（同页命中 0 术语）；APK 也放不下 |
| Google / AWS 传统 OCR | 一般 | 主打印刷体 |

**经济成本量级**（按 30 人班 × 2–3 页 ≈ 60–90 张图 / 次，价格随时变动，此处仅为量级参考）：

| 引擎 | 60 张图 / 次 | 每月 4 次 |
|---|---|---|
| 魔搭 Qwen3-VL-235B（2 魔粒/次） | 120 魔粒（免费额度内 ~250/日） | — |
| Claude Sonnet 级 | ~$1.0 | ~$4 |
| GPT-4o-mini 级 | ~$0.05 | ~$0.2 |
| Gemini Flash 级 | ≈ $0.02–0.05 | < $0.2 |
| Azure Doc Intelligence | ~$0.06–0.2 | ~$0.3–0.8 |

结论：**成本可忽略**。优先用已有的魔搭通道（免费额度 + 视觉档质量已实测达标）。

**隐私提醒**：学生答卷含个人字迹与答题内容，走第三方视觉 API 等于数据出域。缓解口径：**识别完即弃图只留文本**；若需留档走 Supabase **私有 bucket + 仅 teacher 可读**；上传处明示知悉。

## 四、术语 / 学者名高亮

- 转写文本后用词库 `term` + 学者实体做**拼写容错匹配**（词干归一、大小写、编辑距离、音近形近）标出命中词；命中词旁/悬浮显示词库定义。
- 学生拼错（如 `Durkheim` 写成 `Durkhiem`）也能挂上。
- 命中率上限受 OCR 质量约束 → **原图 + 高亮文本并排，以图为准**，文字仅辅助扫描。
- **四档标记**（2026-09-13 定）：精确命中（实心）/ 同词根变形（淡绿点线，属正常用法**不打问号**）/ 疑似笔误（浅红虚线 + `?`，交教师确认）/ 学者（蓝色系）。

## 五、拼写容错：独立机制（不与词库 aliases 耦合）【讨论结论】

### 为什么单独成立
| | 现有 `aliases`（判分用） | 拟新增拼写容错（阅卷高亮用） |
|---|---|---|
| 语义 | 该写法**算正确** | 疑似某术语的**笔误** |
| 确定性 | 高（答对即给分） | 概率性（可能真是别的词） |
| 后果 | 误容=白拿分；误拒=丢分 | 误标无代价（人工复核） |
| 维护 | 逐词人工维护 | 零维护、运行时计算 |

- 在线作业判分保持严格（拼写错不给分）说得通；**手写 + OCR 场景更易产生拼写偏差**，因此高亮层单独做模糊匹配。
- 两者语义不同，不应把模糊规则塞进词库 aliases，词库保持干净、判分逻辑不被污染。

### 运行时匹配器层级（纯函数，词库零改动）
1. **归一化**：大小写、去重音符号（`Émile`→`Emile`）、连字符/撇号差异、英式美式（`behaviour/behavior`）
2. **屈折**：单复数、常见词尾（`-ing/-ed/-ion`）
3. **编辑距离分级**：按词长给阈值（短词 ≤5 容 1；中词容 1–2；长学者名容 2，如 `Durkhiem`→`Durkheim`）
4. **多词短语对齐**：`labelling theory`、`Karl Marx` 按 token 逐词容错再整体判定
5. **OCR 混淆对**（手写特有，非拼写错）：`r/n`、`m/rn`、`l/I/1`、`o/a` 等，维护运行时小表
6. **同词根变形单列**（2026-09-13 修）：`education/educational`、`functionalism/functionalist` 共享词干 ≥5 字符且互为前缀 → 归入"正常用法"，**不打问号**（此前误标为"疑似笔误"，反而污染判断）

### 防误报的 UI 分级（关键）
- 精确命中 → 实心高亮 + 词库定义
- 同词根变形 → 淡底 + 点线（仅提示"这是哪个词条的变体"）
- 疑似笔误 → **虚线/浅色高亮 + 「？」**，悬停显示「疑似 Durkheim（学生写作 Durkhiem）」，供教师快速确认

### 现实约束
- 模糊匹配只能救**轻微拼写偏差**；OCR 认成完全不相干词时救不回（需靠混淆表或更强转写模型）。
- 容错参数必须用**真实手写样本**校准（20 份左右跑一遍 OCR 统计错误类型），否则「容多少」是拍脑袋。

## 六、遗留问题

- [x] ~~是否有可用 AI API key~~ → **已解决**：魔搭 key 已在 Worker secret（`env.MODELSCOPE_API_KEY`）；本地开发用 `app/.dev.vars`
- [x] ~~先做小样验证端到端可行性~~ → **已完成（2026-09-13）**：真实手写页转写 + 高亮报告，教师确认效果满意
- [x] ~~上传主体~~ → **已定（2026-09-13）：教师统一收卷批量上传**（学生端不做上传入口；提示词与 UI 均按"教师视角"设计）
- [x] ~~通用词刷屏~~ → **已定（2026-09-14）：按「核心 / 一般」两级**（`app/src/lib/ocr-term-levels.json`）。只影响观感、不参与判分
- [ ] 高亮展示位置与「试卷成绩」（`grouper_runs`）详情页的整合方式（可选；当前是独立 tab）
- [x] ~~转写文本可否手工订正~~ → **暂不做（2026-09-15 教师确认）**：如有需要再由教师提出；届时改法是文本可编辑 + 改动后前端重算高亮（不影响已入库记录，除非另存）
- [ ] （可选）`/app-api/*` 目前是「登录即可 + `ai_gate` 门禁」；若将来出现滥用，再加按功能/按角色的细粒度配额
- [ ] 别名表瑕疵：`Social identity` 收了 `identity` 做别名，学生写 `national identity` 时会命中它但悬浮显示"Social identity"（属词库别名问题，非匹配器问题）

## 七、可复用现状
- 词库（术语 + 定义）已存在，且现有作业判分已支持单复数 + 人工维护的语义容错 —— 可复用作高亮匹配的词典来源。
- 「试卷成绩」（grouper_runs）已具备：试卷记录、学生名单、原始分→百分制换算。OCR 记录可挂载其上，形成「上传答卷 → 转写 → 高亮 → 给分」闭环。
- Worker 侧已有可复用的多级模型链与 OpenAI 兼容调用器（`msAsk`）、Supabase JWT 鉴权（`verifyUser`）、AI 门禁（`aiGateForbidden`）、CORS 白名单 —— 入口化时抽为 `worker/ai/*` 共享层，路由归入 `/app-api/*`。
