# 用户分类管理方案

> 本文档是「账号角色分类 + 班级 + 统计筛选」的定稿方案，实施时以此为准。
> 状态：已定稿（未实现）。按优先级分阶段实施（部分近期就用，部分长期）。

## 一、目标

1. 解决测试数据「污染」统计：教师/测试账号的打卡、进度与真实学生分开统计
2. 区分班级/年级：A1 年级学 Paper 1-2，A2 年级学 Paper 3-4
3. 账号从「教师/学生」二分类，演进为四分类 + 多归属
4. 为未来多教师、多科目铺路

## 二、现状

- 角色：`teacher_roles` 表（单一布尔 `isTeacher`），"非教师 = 学生"，跳过登录 = 离线游客
- 数据：`student_data` 表（`user_id, email, data` JSONB），无班级字段
- 统计：教师后台 `TeacherCheckPanel` 读全部 `student_data` 行，**无任何筛选/排除**

## 三、角色系统（四分类 + 多归属）

| 角色 | 权限/用途 |
|---|---|
| developer | 标记角色：统计默认排除 + 调试；**不自动授予业务功能** |
| teacher | 教师后台（导入/发布/统计/班级管理） |
| student | 学生功能（练习/进度） |
| guest（注册游客） | 登录但非校内学生，学生功能 + 统计单独归类/排除 |

- **多归属**：`user_roles` 表一行一个角色（一个用户多行）。你自己的账号 = `teacher + developer`；测试账号 = `student + developer`。
- 判断从 `isTeacher` 布尔 → `roles.includes('teacher')`（角色集合）。

**权限叠加规则（关键）**：业务功能由「显式业务角色」决定，`developer` 只是标记、**不参与业务权限判断**。
- 教师后台 access = 只有 `teacher` 角色（`roles.includes('teacher')`）
- 学生功能 = `student` 角色（登录默认可用）
- `developer` = 统计排除 + 调试，不授予教师后台/学生功能的额外权限

这样：测试账号 `student + developer` 登录看到的是**学生视角**（无 teacher，看不到教师后台），正好验证「学生看不到教师后台」；你自己的 `teacher + developer` 看到教师视角。两者都因 `developer` 被统计排除。

**注册游客（guest）vs 离线游客**：
- 离线游客：跳过登录，数据在本地（guest 命名空间），不上云
- 注册游客（guest 角色）：登录了，数据上云，但统计单独归类/排除

## 四、班级功能

- `classes` 表：`id / name / teacher_id / syllabus（可选）/ papers（可选，该班学的考卷）`
- `student_data` 加 `class_id` 关联班级
- 教师后台按班级查看/统计

> `papers` 先做「该班学哪些考卷」的标签属性（如 A1 = ['Paper 1','Paper 2']），用于统计时按班级+考卷关联；将来可用于学生端默认筛选（暂不做，避免过度设计）。

## 五、统计筛选

- 教师后台统计**默认排除 `developer` 角色**（自己 + 测试账号），无需逐个手选账号
- 支持按班级、角色筛选

## 六、数据库设计（Supabase）

### `user_roles` 表（替代 `teacher_roles`）
| 字段 | 类型 | 说明 |
|---|---|---|
| user_id | uuid | 主键（Supabase uid） |
| role | text | 'developer'/'teacher'/'student'/'guest'（与 user_id 组成联合主键） |

> 迁移：`teacher_roles` 里的 `user_id` → `user_roles` 的 `role = 'teacher'` 行。

### `classes` 表
| 字段 | 类型 | 说明 |
|---|---|---|
| id | uuid | 主键 |
| name | text | 班级名（如 "A1 社会学"） |
| teacher_id | uuid | 归属教师 |
| syllabus | text | 关联科目（未来多科目用，可空） |
| papers | text[] | 该班学的考卷（如 ['Paper 1','Paper 2']，可空） |

### `student_data` 加字段
| 字段 | 类型 | 说明 |
|---|---|---|
| class_id | uuid | 学生归属班级（可空） |

## 七、分阶段实施（按优先级）

| 阶段 | 内容 | 优先级 |
|---|---|---|
| P0 | 统计排除测试数据：加 `developer` 角色 + 教师后台统计默认排除 developer 账号 | 近期（开学前） |
| P1 | 班级功能：`classes` 表 + `student_data.class_id` + 教师后台按班级筛选 | 近期（开学前） |
| P2 | 四分类角色：`teacher_roles` → `user_roles`（多角色）+ `isTeacher` → 角色集合判断 + `guest` 注册游客 | 中期 |
| P3 | 多教师/多科目关联班级（启用 `syllabus` 字段、教师-班级-科目归属） | 长期 |

> P0 优先：你的测试数据已在库里，开学后真实学生一加入就污染统计；把现有教师/测试账号标记为 `developer` 即可自动排除，无需删数据。P1 紧随（两个年级）。P2/P3 视「多科目」进度推进。
