# 项目交接索引 · md 文档导航

> 目的：让新接手的 agent 快速定位「项目是什么、各 md 讲什么、当前做到哪一步」。
> 生成：2026-09-07。状态：随项目演进而更新（最近：2026-09-22）。
> ⚠ 读取优先级：先读 `project-memory.md`（工程约束/教训全表）+ 本索引；方案细节再按需落到各 doc。

---

## 0. 必读入口（最重要的 2 份）

| 文件 | 内容 | 为什么必读 |
|---|---|---|
| `project-memory.md` | 全部 Hard Constraints / Engineering Conventions / Lessons Learned | 项目的"宪法"，含部署三步走、git 代理、psql 云端直连、RLS/RPC 坑、**Worker 路由命名空间约定**等铁律 |
| `KNOWN_ISSUES.md` | 已知隐患清单 | 词库同步刷新无条件覆盖未发布编辑（vocabDirty 未保护）——建议优先修 |

---

## 1. 产品与架构（定稿方案）

| 文件 | 状态 | 讲什么 |
|---|---|---|
| `词库上云与同步方案.md` | ✅ 已实现 P0-P3 | 词库发布到 Supabase `vocab_releases` + 稳定 id + 教师词条 CRUD + aliases 答案容错；P4 自动拆别名暂缓 |
| `随堂测验和作业功能.md` | ✅ 已实现，持续迭代 | 随堂测验/作业：云端试卷 + 密码 + 防作弊；决策 1-11（含错题入账、错题分析、crossword 作业化、语境题形态、逻辑链连线作业化候选二） |
| `用户分类管理方案.md`（**未入库**，仅本机/OneDrive，含内部付费化规划） | ✅ 已实现 P0/P1 | 四分类角色（developer/teacher/student/guest）+ 班级 + 统计排除 developer |
| `逻辑关系与应用.md` | ✅ 接龙/连线已实现 | 概念网络建模（897词条/843组）、接龙/连线玩法、图谱可视化评估、AI 推荐；§5.4 性质研判结论=不落库 |
| `组卷器方案.md` + `组卷器数据模型初稿.md` | 🚧 核心已实现 | 组卷器：按分值配题、判分换算（教师 Excel 公式 + CIE gt 阈值 P0/P1）、mark scheme 配套、题库后台 |
| `UI设计.md` | 部分实现 | 两级导航、夜间模式、数据看板、安卓系统栏配色 |
| `移动端系统栏与安全区方案.md` | ✅ 已实施并真机验证（1.7.6） | edge-to-edge 策略：主站顶栏避让状态栏 / 子站顶栏自绘深蓝 / 底部内容延伸（手势条悬浮）；Capacitor `SystemBars` 自动处理已关闭；含踩坑清单与升级注意事项 |
| `知识库AI问答方案.md` | ✅ 已上线（2026-09-11） | 教材 AI 问答：四层语料（蒸馏/原文页/页级索引/答题脚手架）、两级检索链路、模型链与 8B 降配、数据流水线、覆盖体检、渲染与踩坑 |
| `流派设计决策.md` | 数据清洗进行中 | 理论流派字段管理 + 概念网络/反义关系练习（暂缓） |

---

## 2. 研究稿 / 讨论稿（未立项，等"听指令"）

| 文件 | 状态 | 讲什么 |
|---|---|---|
| `练级与奖励体系方案.md` | 💬 讨论中 | XP/等级/奖励体系（P0 展示型待拍板） |
| `定义题方案.md` | 💬 讨论中 | 术语→写定义的反写题型，P0 纯规则 / P1 Workers AI 语义判定 |
| `OCR辅助阅卷.md` | ✅ **已上线试点（2026-09-14，v1.7.15）** | 手写答卷 → 视觉模型转写（定档魔搭 Qwen3-VL-235B，13 秒/页）→ **原文内就地高亮**（精确/同词根/疑似笔误/学者四档 + 核心/一般分级）辅助人工给分；主产物是**带高亮的原文**而非命中清单（AO2 需要上下文）；上传主体=**教师统一批量**；教师后台 tab「OCR 阅卷」（拍照/选图/拖拽批量）；路由 `/app-api/*`（`worker/appApi.ts` + `worker/ai/*` 共享鉴权与模型目录）。**待真实作业试用后定稿**：导出/落库、转写文本可否手工订正 |
| `模拟调研方案.md` | 💬 讨论稿（未立项） | AI 当受访者：全班协作设计一份问卷 → 投喂 3–5 个人格 → 揭晓"答卷时心理活动"。内核=展现定量的局限（validity vs reliability）；课堂三阶段，绑 `live_*` 实时基建 + 主站 AI 通路；**归属＝方法论单元**；附录含已否方案表与二期归并 |
| `实时多人在线功能规划.md` | 讨论稿（未实现） | 实时课堂活动的**能力边界实测**（Presence / Postgres Changes 可用，**Broadcast 已坏**）+ 两个已定稿活动：① 课堂猜词（抽人描述、其余抢答）② **课堂拼写竞赛 Spelling Bee**（全班同题同轮、答错出局、剩 3 人抢答定胜负）。含难度递增口径（按**最短可接受写法**的归一化字母数）与配套存量改造（拼写题中文优先 → 随机） |
| `课程化备忘.md` | 💭 长远规划（未立项） | 把练习资产串成线性课程。现状盘点（29 单元 / 词条级 mastery + 日级打卡 / **缺"活动完成"事件** / `quizzes.units` 是唯一活动↔单元绑定）+ 分类归属原则（模拟调研→方法论单元；写作练习独立新建）+ 风险与 MVP（单元学习页） |
| `语境填空题目池.md` | 题库已整理 | 约 130 道语境题（已 gitignore） |
| `语境语料.md` | 🔧 | 语境题语料源 |
| `挖空试验.md` | 💬 | 自动挖空试验记录 |
| `作业加分与罚分方案.md` | 方案 | 作业加减分规则（部分已并入随堂测验体系） |

---

## 3. 真题 / 题库数据（教师资料，gitignored）

| 文件 | 讲什么 |
|---|---|
| `Paper1真题-按考点.md` / `Paper1真题题库.md` | Paper 1 按考点切题 |
| `Paper2真题-历年切题(至S26).md` | Paper 2 历年切题 |
| `Paper3真题-历年切题(至S26).md` | Paper 3 历年切题 |
| `Paper4Globalisation真题-历年切题(至S26).md` | Paper 4 全球化 |
| `Paper4Media真题-历年切题(至S26).md` | Paper 4 媒体 |
| `question-bank.json` | 组卷器题库（680 条，由脚本生成，勿手改） |
| `rival-oppose-report.md` | contrast 性质实测明细（配套逻辑关系文档） |
| `theory-cleanup-list.md` / `theory-diagnostic-report.md` | 流派数据清洗 |

---

## 4. 关键工程事实速查（新 agent 必知）

- **仓库位置**：项目根（`...\Vocabulary Project`），非 app 内。app/ 是前端源码。
- **数据双源**：词库内容走云端 `vocab_releases`（改数据去云端，勿改本机数据源，否则分叉）；本机数据文件仅用于代码/结构改动。
- **部署约定（2026-09-13 修订为「一次构建」）**：一条命令 **`npm run ship`**（= `npm run release` + `npx wrangler deploy`）。步骤=① git commit+push（英文 message）② 改 `app/android/app/build.gradle` 的 `versionCode` +1 / `versionName` 递增 ③ `npm run ship`。**部署必须在 release 之后、且不得再 build**：`wrangler deploy` 只上传 `app/dist`、不构建；两次构建会让 `version.json` 分叉（APK 内嵌 ≠ 线上，2026-09-12 踩过）。
- **云端数据库直连**：psql `postgresql://postgres@spb-olltk79n0rjrawe5.supabase.opentrust.net:5432/postgres`（.pgpass），迁移文件=根目录 `db-migration-*.sql`。
- **概念网络规模**：897 词条 / 843 概念组；骨架 830 边 / 全关系 3065 边；单一连通分量。
- **题库规模**：question-bank.json 680 条（P1=232/P2=192/P3=132/P4=124）。
- **角色**：自身账号 = teacher+developer；测试账号 = student+developer（统计默认排除 developer）。
- **Worker 路由前缀（2026-09-13 定）**：`/skill-api/*` = 教材知识站（子站，学生向：`ask` 问答 / `terms` 检索词）；**`/app-api/*` = 主站（登录即可 + `ai_gate` 门禁；端点 `ai/transcribe` 视觉转写、`ai/complete` 文本补全）**；`/sb/*` = Supabase 同源代理；`/wb/*` = World Bank 代理。同批模型、两套命名空间、**魔搭每日 ~250 魔粒不分池**（子站复习期与主站批卷期时间错开）。详见 `project-memory.md` Engineering Conventions 首条。
- **模型 key 位置**：线上 = `wrangler secret put`（`wrangler.toml [vars]` 只有 Supabase）；本地 = `app/.dev.vars`（已 gitignore，模板 `app/.dev.vars.example`）。

---

## 5. 建议的后续交接动作

- [ ] 修 KNOWN_ISSUES #1（syncVocabFromCloud 加 vocabDirty 保护，改动小、防数据丢失）
- [ ] 语境题接通随堂测验（P3，前置：ContextPassage 练习组件）
- [ ] 图谱 2D 按层级分层布局（depth→Y，性价比最高的可视化项）
- [ ] 练级与奖励 P0（XP 挂 recordItem，预计半天）
- [ ] 组卷器：mark scheme 数据化范围与脚本、凑分策略细节、主题分类复用 unit 结构
- [ ] OCR 辅助阅卷**入口化**（届时一次性搭主站 AI 基础层：抽 `worker/ai/*` 共享 provider/鉴权/额度/开关 + 建 `/app-api/*` 路由；先定上传主体与展示位置）
