# 作业加分与罚分方案 · 定稿方案

> 本文档是「订正（错题重做）+ 订正加分 + 迟交罚分」的定稿方案，实施时以此为准。
> 状态：已定稿（未实现）。所有设计决策已与需求方对齐，可直接据此开发。
> 与《随堂测验和作业功能.md》配套：本方案在其「交卷」之后的环节追加「订正、加分、罚分」评分机制。

## 一、功能目标

1. **订正（错题重做）**：学生可针对某一次测验/作业提交记录里的错题，生成一份专门的错题重做。
2. **订正加分**：鼓励订正——订正一次全对的学生，在原始分基础上加分。
3. **迟交罚分**：作业允许迟交（不是过了截止时间就锁死），但对迟交施加累积百分比罚分。
4. **可配置评分规则**：为未来同事复用做准备，把「订正加分 / 迟交罚分」做成规则驱动，教师账号可编辑自己习惯的方案。

## 二、订正（错题重做）

### 入口
- 学生端「历史结果」→ 单份答卷详情（`QuizTaker.tsx` 的 `HistoryDetail`）。
- 该份记录存在错题时，显示「订正错题（N 题）」按钮。
- 订正入口天然受「结果公布时间」约束（`isResultPublished`：`due_at` 为空或已过才可见）。

### 错题提取
新增 `extractWrongItemIds(questions, answers)`：遍历 `quizzes.questions`，用现有判分函数筛错题词条 id（去重）：

- **拼写 / 选择**：`!isAnswerCorrect(q, answers[q.itemId])` → 该词条为错题。
- **匹配块**：遍历 `q.pairs`，`answers[p.itemId] !== p.itemId` 的配对为错，仅收集这些配对的词条（**只订正错的那几个词，不整块重做**）。

### 订正题型（已定稿：从选择 / 默写两种随机生成）
每个错题词条随机分配 `'choice'` 或 `'spelling'`，复用 `buildQuestion(item, type, pool, idx)` 生成题目快照：

- 选择题的干扰项仍走 `buildQuestion` 的「同 paper 优先」逻辑，避免一眼看出不相关。
- 匹配块的错词条降级为单个词条，同样参与 choice/spelling 随机分配。

### 订正规则
| 规则 | 结论 |
|---|---|
| 是否可重复订正 | **不可重复**。`correction` 字段写入后即视为已订正，入口锁死（显示「已订正」） |
| 是否计入打卡 | **计入**。订正时原样调用 `recordItem(id, correct, type)`，涨掌握度 + 当日打卡题数 + 触发错题本「连续答对 3 次移出」 |
| 是否影响原始分 | **不影响**。`score` 保持考试快照不变，加分另存，最终分单独结算 |
| 订正结果教师可见 | **可见**。订正结果写入云端，教师端答卷详情展示 |

## 三、订正加分

### 门槛（已定稿）
- **仅当「订正全对」才加分**（订正答对数 === 错题总数）。
- **仅作业（homework）加分，测验（quiz）不加**。

### 算法（已定稿：两种算法取更高者）
设 `S = 原始分`、`M = 满分`（真实满分 `totalPoints(questions)`）：

- **方法一**：加满分的 10% → `final₁ = min(S + 0.1 × M, M)`
- **方法二**：几何平均（百分制分数开根号 × 10 后的化简）→ `final₂ = √(S × M)`（天然 ≤ M，不需封顶）

```ts
const final1 = Math.min(S + 0.1 * M, M);
const final2 = Math.sqrt(S * M);
const finalScore = Math.round(Math.max(final1, final2)); // 取更高者 + 四舍五入取整
const bonus = finalScore - S;                             // 加分幅度（整数）
```

验证示例（M = 20）：

| 原始分 S | 方法一 | 方法二 | 最终分（取整） | 加分 |
|---|---|---|---|---|
| 10 | 12 | √200 ≈ 14.14 | 14 | +4 |
| 5 | 7 | √100 = 10 | 10 | +5 |
| 18 | 20（封顶） | √360 ≈ 18.97 | 20 | +2 |

### 取整（已定稿）
最终分**四舍五入到整数**（`Math.round`，例：14.14 → 14、14.6 → 15）。

## 四、迟交罚分

### 范围（已定稿）
- **仅作业（homework）生效**。
- 测验（quiz）是「统一开考 + 限时 + 自动交卷」，不存在迟交，不适用。
- 实现时需确保作业**过了 `due_at` 仍能交卷**（放开截止拦截），只记录迟交状态用于罚分。

### 迟交天数（已定稿：按 24 小时向上取整）
```ts
lateDays = Math.ceil((submitted_at - due_at) / 86400_000)  // 毫秒差 / 一天
```
- 纯时长计算，不依赖时区/「午夜」判断，无歧义。
- 只要迟交（> 0）至少算 1 天；满 24h 升 2 天，以此类推。

### 算法（已定稿：累积百分比罚分）
每日追加罚分百分比为数组 `daily_percents = [10, 20, 30, 40]`，第 i 天追加 `10% × i`：

```ts
let pct = 0;
for (let i = 1; i <= lateDays; i++) {
  pct += daily_percents[i - 1] ?? 0;
}
const penaltyPercent = Math.min(pct, 100); // 累积封顶 100%
const penalty = M * penaltyPercent / 100;  // 罚分
```

| 迟交天数 | 当日追加 | 累积罚分率 | 罚分（M=20） |
|---|---|---|---|
| 1 | 10% | 10% | 2 |
| 2 | 20% | 30% | 6 |
| 3 | 30% | 60% | 12 |
| 4 | 40% | 100% | 20（必然 0 分） |

- **封底 0 分**（已确认）：累积 100% 时原始分扣光为 0。

## 五、最终分结算公式（已定稿）

罚分先扣、加分后加、整体封底 0 / 封顶 M：

```ts
finalScore = clamp(S − penalty + bonus, 0, M)
// S=原始分, M=满分, penalty=迟交罚分, bonus=订正加分
```

结算时间线：

1. **交卷时**：确定 `submitted_at`、原始分 `score`；若作业且迟交，结算 `penalty`。
2. **订正时**：若订正全对且作业，按公式结算 `bonus`，更新 `finalScore`。

## 六、可配置评分规则（规则驱动架构，为未来准备）

把「订正加分 / 迟交罚分」统一成一个**规则对象**，取代硬编码逻辑。

### 规则结构
```ts
interface GradingRules {
  correction_bonus: {
    enabled: boolean;
    method: 'none' | 'percent' | 'geometric' | 'max_of_two'; // 预设方案
    percent: number;   // method 为 'percent' / 'max_of_two' 时用，默认 10
  };
  late_penalty: {
    enabled: boolean;
    daily_percents: number[]; // 每迟一天追加的百分比，默认 [10, 20, 30, 40]
  };
}
```

- **默认规则**（= 当前需求方习惯）：`correction_bonus: { enabled, method: 'max_of_two', percent: 10 }` + `late_penalty: { enabled, daily_percents: [10, 20, 30, 40] }`。

### 两层存储（与「题目快照」哲学一致）
| 层 | 表 | 作用 |
|---|---|---|
| 教师默认规则 | `teacher_grading_rules`（新表） | 影响**新创建**的作业 |
| 作业快照 | `quizzes.grading_rules`（jsonb） | 创建作业时把当时的规则**快照进去**，历史作业不受教师后续改默认规则影响 |

### 未来 UI（已实现部分）
- **迟交罚分编辑**（已实现，2026-08-30）：教师创建/编辑作业时，「启用迟交罚分」勾选 + 「每迟一天追加的百分比」编辑框（逗号分隔）+「恢复默认」按钮；默认规则存 localStorage（`socio_vocab_grading_rules`），创建作业时快照进 `quizzes.grading_rules`（规则快照，历史作业不受改默认规则影响）。
- **加分规则 UI**（暂不实现，仅预留）：预设方案下拉（不加分 / 满分百分比 / 几何平均 / 两者取高）。

## 七、数据模型（Supabase）

### SQL
```sql
-- 1. 交卷表：订正记录 + 评分结算
ALTER TABLE quiz_submissions
ADD COLUMN IF NOT EXISTS correction JSONB;   -- 订正答题明细（订正不可重复的判据）
ADD COLUMN IF NOT EXISTS grading JSONB;      -- 评分结算明细（penalty / bonus / final_score）

-- 2. 试卷表：评分规则快照（创建作业时写入）
ALTER TABLE quizzes
ADD COLUMN IF NOT EXISTS grading_rules JSONB;

-- 3. 教师默认评分规则表
CREATE TABLE IF NOT EXISTS teacher_grading_rules (
  user_id uuid PRIMARY KEY REFERENCES auth.users(id) ON DELETE CASCADE,
  rules JSONB NOT NULL,
  updated_at timestamptz DEFAULT now()
);
```

### `quiz_submissions.correction` 结构（订正记录）
```json
{
  "submitted_at": "2026-08-27T10:30:00Z",
  "score": 5,
  "total": 5,
  "all_correct": true,
  "details": [
    { "itemId": "xxx", "type": "choice", "correct": true },
    { "itemId": "yyy", "type": "spelling", "correct": false }
  ]
}
```

### `quiz_submissions.grading` 结构（评分结算明细）
```json
{
  "penalty": 2,
  "bonus": 4,
  "final_score": 12
}
```
- 交卷时写 `penalty`（作业且迟交才 > 0），`bonus: 0`、`final_score = score - penalty`。
- 订正后更新 `bonus`、`final_score`。
- 测验（quiz）无罚分/加分，教师端直接展示 `score`，`grading` 可为空。

### RLS 提醒
- 学生需有权 **update 自己的 `quiz_submissions` 记录**（写 `correction`、`grading`），注意此前遇到过的 `quiz_submissions` RLS 约束，需确保 update 权限覆盖这两个新列。

## 八、前端改动清单

| 文件 | 改动 |
|---|---|
| `lib/types.ts` | 新增 `CorrectionDetail` / `CorrectionResult` / `GradingDetail` / `GradingRules` 类型；`QuizSubmission` 加 `correction`、`grading`；`Quiz` 加 `grading_rules` |
| `lib/quiz.ts` | 新增 `extractWrongItemIds`、`buildCorrectionQuestions`、`correctionBonus`、`latePenaltyPercent`、`applyGradingRules`（统一评分结算函数） |
| `lib/cloud.ts` | 新增 `saveCorrection`；`createQuiz` 写入 `grading_rules` 快照 |
| `components/CorrectionPractice.tsx`（新增） | 订正组件：逐题重做（选择/拼写），完成后 `recordItem` + 计算加分 + `saveCorrection` |
| `components/QuizTaker.tsx` | `HistoryDetail` 加「订正错题」入口；订正后锁死；分开展示原始分/加分/最终分 |
| `components/QuizManager.tsx` | 答卷详情分开展示「原始分 · 迟交罚分 · 订正加分 · 最终分」；分母改真实满分 `totalPoints` |

## 九、关键实现细节

1. **错题提取**：`extractWrongItemIds` 复用 `isAnswerCorrect` / `matchingCorrectCount`，matching 块只取错配对词条，去重。
2. **订正题生成**：`buildCorrectionQuestions` 每个错词条随机分配 choice/spelling，复用 `buildQuestion`（同 paper 干扰项）。
3. **加分公式**：`correctionBonus(S, M)` = `round(max(min(S+0.1M, M), √(S·M))) - S`，仅订正全对且 homework 时生效。
4. **罚分公式**：`latePenaltyPercent(lateDays, daily_percents)` 累积封顶 100%；`lateDays = ceil((submitted_at - due_at)/86400000)`。
5. **统一结算**：`applyGradingRules(quiz, submission, rules)` → `{ penalty, bonus, finalScore }`，前后端口径一致；`finalScore = clamp(S - penalty + bonus, 0, M)`。
6. **规则快照**：`createQuiz` 时读教师默认规则（无则用内置默认），快照进 `quizzes.grading_rules`；历史作业规则不变。
7. **订正不可重复**：`correction` 非 null 即已订正，前端锁死入口；服务端可用 `correction IS NULL` 条件兜底。
8. **作业迟交放行**：确保作业过了 `due_at` 仍可交卷，`submitted_at` 记录实际交卷时间用于罚分。

## 十、待办备注

- 教师「自定义评分规则编辑 UI」暂缓，本次先落「规则驱动 + 默认规则」，UI 留到同事复用需求出现时再加。
- 迟交罚分 `daily_percents` 数组超出长度后的行为（超出末尾天数如何追加）待实现时确定；默认 `[10,20,30,40]` 第 4 天已封顶 100%，无需额外处理。
- 「取整」采用四舍五入 `Math.round`；如需求方后续要「向下取整」再改。
- 学生端与教师端「真实满分」展示：分母由 `question_count` 改为 `totalPoints(questions)`（匹配块一题算多分）。
